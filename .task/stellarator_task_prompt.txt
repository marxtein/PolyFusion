Autonomous development run on the PolyFusion 0-D fusion code. Repo: E:\work\digitalfusion-release (git, branch main). You run COLD - no memory of prior chats; everything you need is below or in the repo docs.

ENVIRONMENT: Windows. Python = the Microsoft Store Python 3.11; run code via bash with `cd /e/work/digitalfusion-release && PYTHONPATH=/e/work/digitalfusion-release python <script>`. Tests are standalone main() scripts run as `python polyfusion/tests/test_X.py` (exit 0 = pass), EXCEPT test_golden.py which uses pytest (`python -m pytest polyfusion/tests/test_golden.py -q`; pytest is installed). Use the superpowers skills: test-driven-development for new code, verification-before-completion before claiming done. Optionally use subagent-driven-development.

ORIENT FIRST (read these): docs/37_仿星器几何重构_SchemeD_实施记录.md, docs/superpowers/plans/2026-06-14-stellarator-scheme-d.md, docs/38_用户指南_加预设_喂实测值_自定义位形.md, polyfusion/nearaxis.py (first-order Garren-Boozer/Landreman-Sengupta near-axis solver, validated vs pyQSC), polyfusion/configs/stellarator.py (uses near-axis; section_outlines draws first-order ELLIPSE cross-sections + wall layer), polyfusion/presets/stellarator.json (presets are JSON data now; loaded by polyfusion/presets_io.py).

GIT: create a NEW branch nearaxis-r2-and-sw off main. Commit after each task (message ending with Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>). DO NOT merge to main and DO NOT push to any remote - leave the branch for the user to review.

=== TASK 1: Second-order near-axis geometry (bean / crescent flux-surface shapes) ===
WHY: the current first-order near-axis surfaces are pure ellipses (X1=(etabar/kappa)cos(theta), Y1=(kappa/etabar)(sigma cos(theta)+sin(theta))). Real stellarators (W7-X etc.) show bean/triangular/crescent cross-sections, which only appear at SECOND order (the r^2 terms). This adds the second-order shape so the UI flux-surface view looks like real machines.
REFERENCE: pyQSC is cloned at E:\work\digitalfusion-compare\pyQSC - study its second-order construction (qsc/calculate_r2.py / Qsc(..., order='r2'): X20/X2c/X2s, Y20/Y2c/Y2s, Z20/Z2c/Z2s, B2c/B2s, from Landreman and Sengupta, "Direct construction of optimized stellarator shapes. Part 2", J. Plasma Phys. 2019). Port the minimum needed to produce second-order surface cross-sections, numpy-only (no scipy/pyQSC dependency in the shipped code - pyQSC is reference only).
DO:
1. Extend polyfusion/nearaxis.py: add a second-order solve (e.g. solve_near_axis(..., order="r2") or a NearAxisResult.second_order payload) returning the r2 coefficients needed to reconstruct surfaces P(theta, r) = axis + r[X1 n + Y1 b] + r^2[X2 n + Y2 b + Z2 t] for given B2c (B2s=0 in stellarator symmetry). Keep first-order behavior the DEFAULT and unchanged.
2. TDD: polyfusion/tests/test_nearaxis_r2_benchmark.py - validate your r2 coefficients against pyQSC for at least one published config (e.g. Landreman-Sengupta Part 2 example, or run pyQSC locally if its env is importable to generate reference numbers; if pyQSC is not importable, hardcode reference values you computed and cite them). Reasonable tolerance for r2 (1e-6..1e-8 if you match the construction; looser only with justification).
3. Update polyfusion/configs/stellarator.py section_outlines to render second-order surfaces at finite minor radius a when r2 data is available (so cross-sections show bean/crescent), keeping first-order as fallback. The returned dict keeps its existing keys (mode/sections/axis/wall) so app/index.html drawShape still works; add the bean shape to the boundary/surface polygons.
4. IMPORTANT: do NOT change the 0-D power account. Volume/area stay as they are (first-order area-preserving pi*a^2); r2 is for the SHAPE view (and optionally a more accurate elongation diagnostic, clearly labeled). Every existing test must stay green.
5. If a full r2 port proves too large for one session, implement incrementally, keep everything green, and document HONESTLY what is done vs deferred - do NOT fake results or loosen physics anchors to force green.

=== TASK 2: Sw_override for real-machine stellarator presets ===
WHY: W7-X/HSX/CFQS presets set measured iota+Vp_override but leave Sw_override=0, so wall area comes from the near-axis elongation profile, which is extreme for these small machines (HSX elongation ~384) leading to distorted Pwall=(Pfus+Pheat)/Sw. Impact is currently negligible (Qfus~0, Pwall~0) but it is a modeling wart.
DO: in polyfusion/presets/stellarator.json, add a reasonable Sw_override [m^2] (plasma-facing surface area) to W7-X, LHD, HSX, CFQS using published/estimated values (e.g. W7-X plasma surface ~120-130 m^2, LHD ~120 m^2; HSX/CFQS are small - estimate from 2*pi*R0 * 2*pi*a * sqrt((1+kappa^2)/2) with a modest kappa~1.5 if no published number). Mark them "approx, confirm vs published" in a comment-equivalent (or a note in docs). Verify: run_preset for all 6 stellarator presets returns valid=1, finite sane Pwall. Keep test_stellarator_overrides.py and the rest of the suite green; add/adjust an assertion if useful.

=== TASK 3: Comprehensive report ===
Write docs/39_仿星器几何升级系列报告.md covering the ENTIRE series of stellarator changes: (a) Scheme D - single near-axis geometry, deletion of kappa_s + legacy rotating-ellipse, etabar as shaping knob with derived elongation, measured overrides iota/Vp_override/Sw_override, custom axis rc/zs, near-axis section outlines + wall layer, preset rebaseline (concepts pure near-axis, real machines measured); (b) Plan B - presets moved to JSON + loader with user-dir merge, UI "save my preset" (localStorage); (c) the three pre-existing test failures fixed (golden=pytest, mirror_sanity + physics_p2 = stale tests root-caused to the use_tauE=1 default gating self-consistent closures, fixed to use_tauE=0); (d) tonight's work - second-order near-axis bean shapes + real-machine Sw_override. For each: what/why/how, before->after, test evidence, and honest known limitations. Reference commit SHAs from git log.

=== FINISH ===
Run the FULL test suite (all python polyfusion/tests/test_*.py exit 0, plus python -m pytest polyfusion/tests/test_golden.py). Confirm no regressions. Leave the branch nearaxis-r2-and-sw committed but NOT merged. End with a concise written summary (in the final message) of: what you completed, what you deferred and why, the new branch name + commit SHAs, and any blockers needing the user's decision. Do not claim success for anything you did not actually run and verify.
