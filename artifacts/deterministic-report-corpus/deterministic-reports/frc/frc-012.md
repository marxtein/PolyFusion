# 1. 核心结论
- 本次案例为 `config=frc`（场反位形 FRC），`preset=C-2W`。
- 当前运行点：`valid=1`、`ignited=0`；`Pfus=3098`、`Qfus=0.2511`、`Pheat=1.234e+04`、`Pwall=1137`。
- 当前点是数值有效的 0-D 解。`Qfus` 低于 1，当前点表现为外部加热主导。
- POPCON 扫描为 `Ti × B_e`，命中 `1/64` 个 best 网格（`best_fraction=0.01562`），候选区在当前粗网格中非常稀疏；这不代表当前运行点必然位于 best 区。
- `use_tauE=1`，结果依赖固定约束时间假设。

# 2. 关键指标解读
- 功率与损失：`Pfus=3098`、`Qfus=0.2511`、`Pheat=1.234e+04`、`Pwall=1137`、`Ptrans=1.295e+04`、`Pbrem=4.516`、`Pcycl=2.409e-05`、`P_line=0`、`Pn=2478`。
- 约束、成分与几何：`tau_E=0.0015`、`Zeff=1`、`Vp=1.282`、`Sw=13.57`。
- 场反位形 FRC 专属指标：`beta=0.7778`、`s_param=88.23`、`s_over_E=23.53`、`tau_Bohm=0.003657`、`tau_classical=1.798`。
- 当前点对默认 best 准则的逐项核对：`Pfus=3098` 满足 `>=1`；`Qfus=0.2511` 不满足 `>=1`；`s_param=88.23` 满足 `>=2`。

# 3. 风险与不确定性
- `valid` 只描述单点数值有效性，不能替代 POPCON 工作窗证据。
- 扫描缺少完整 `x/y` 坐标证据，不能给出最佳区实际边界或敏感性方向。
- best 命中不超过 5%，粗网格下可能是孤立单元，需要局部加密确认连续性。
- 当前点未满足默认 best 准则中的：`Qfus`。
- 固定 `tauE` 会直接影响功率平衡、增益和点火判断，需做约束时间敏感性扫描。
- `P_line=0` 只代表当前成分和模型输出，不能外推为线辐射始终可忽略。

# 4. 下一步建议
1. 加密 `Ti × B_e` POPCON 网格，保存完整 `x/y/best/valid`；监控 `best_fraction`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
2. 扫描 `tauE` 或切换约束标度；监控 `tauE_used/tau_E`、`ignited`、`Pfus`、`Qfus`、`Ptrans`。
3. 扫描 r_s、l_s、Rw、f_shape；监控 beta、s_param、s_over_E、Pwall，并逐项复核默认 best 准则。
4. 扫描 `fHe`、`fimp`、`Zimp`；监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Qfus`。
