1. 核心结论
- 本次结果对应 `config=dipole`、`preset=Dipole-DD`。当前运行点 `valid=1.0`，说明该 0-D 计算点被程序判定为有效；但 `ignited=0.0`，表示该点未达到点火判据。
- 当前运行点的主要功率指标为：`Pfus=63.5308`、`Qfus=23.7736`、`Pheat=2.6723`、`Pwall=0.05268`。若按现有输出直接解读，这是一个“计算有效但未点火”的单点结果。
- 当前运行点与 POPCON 扫描需要分开看：运行点有效，不等于扫描已经找到工作窗。此次 `last_scan` 给出 `xkey=Ti0`、`ykey=n0`，`n_invalid=0`，但 `best` 矩阵全为 0，因此当前准则下未找到最佳区。
- 同时，扫描结果缺少 `nx`、`ny` 以及 `x/y` 轴数组；因此即使有 `best` 矩阵，也不能定位扫描坐标。扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 约束层面，`use_tauE=1.0` 且 `tau_E=5.0`，说明本次结果依赖固定约束时间假设；对约束时间取值较敏感的结论，不能脱离这一前提单独外推。

2. 关键指标解读
- 运行点状态：`valid=1.0` 说明该组输入在模型内可计算；`ignited=0.0` 说明虽然有聚变输出 `Pfus=63.5308`，但未满足点火判据。
- 功率收支：`Pfus=63.5308`、`Pn=21.3145`、`Pheat=2.6723`、`Ptrans=29.4749`。从数值关系看，系统存在非零聚变功率与较高的输运功率需求，但这里不应把它直接等同为“存在稳定运行窗口”。
- 墙负荷与体量：`Pwall=0.05268`，配合 `Sw=1256.6371`、`Vp=1890.3037`，可作为后续几何归一化和壁面相关校核的基础量；当前 JSON 未提供更细的壁面分布证据。
- 辐射与损失：`Pbrem=15.3633`、`Pcycl=0.05034`、`P_line=0.0`。在给定输入中，线辐射为 0，与成分设置 `fimp=0.0`、`fHe=0.0`、`Zeff=1.0` 一致；当前辐射损失主要体现在轫致辐射，回旋辐射较小。
- 约束与时间尺度：`tau_E=5.0`、`tauC_eff=487.8944`、`tau_eq_ie=0.05122`，且 `use_tauE=1.0`、`params.tauE=5.0`。这意味着能量约束不是由缩放律自洽求得，而是由固定输入控制，解读时应把它视为假设条件而不是独立预测结果。
- 成分与工况：`strcase="D-D"`，`Zimp=10` 但 `fimp=0.0`，`fHe=0.0`，`nbar=7.8823e19`，`ne0=2e21`，`ntau=1e22`。这些值说明本次是较“洁净”的 D-D 设定，未体现杂质扫描或氦灰积累影响。
- 几何与场相关量：`B_in=5.6975`、`B_out=0.01012`、`L_in=1.5`、`R_p=10.0`、`r_ring=1.0`、`ring_model=1.0`。当前输出包含偶极场构型相关几何量，但未提供 `geom_model` 字段，因此不能进一步判断是否使用了简化几何模型。
- POPCON 扫描本身：扫描维度定义为 `Ti0` 对 `n0`。`n_invalid=0` 说明扫描中没有被标成无效的点；但这不等于存在最佳区，因为 `best` 矩阵全为 0，当前准则下未找到最佳区。

3. 风险与不确定性
- 最大的不确定性来自约束时间处理：`use_tauE=1.0` 表明结果依赖固定约束时间假设；若 `tau_E` 改变，`Pfus`、`Qfus`、`Pheat`、`ignited` 等结论都可能变化。
- 扫描证据不完整：虽然给出了 `best` 矩阵，但未提供 `nx`、`ny` 和 `x/y` 数组。扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能说明最佳区位于高/低 `Ti0` 或高/低 `n0` 一侧。
- `best` 矩阵全 0 只说明“在当前准则下未找到最佳区”，不能反推“所有点都不可行”或“参数趋势单调”，因为缺少判据定义和网格坐标。
- 当前是单一成分设定：`fimp=0.0`、`fHe=0.0`、`Zeff=1.0`。这使得辐射和稀释影响的覆盖范围有限，尤其 `P_line=0.0` 仅对应本次输入，不代表更广泛成分条件。
- 边界类指标不完整：JSON 未提供 `betaN`、`betaT`、`betap`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 等更常用边界归一量，因此不能据此判断边界接近程度或主要受限方向。
- 结果是 0-D 初筛信息，且当前 `ignited=0.0`。这类结果适合比较输入假设下的点值与扫描标签，不足以单独支撑更强的工程结论。

4. 下一步建议
- 先补全一次可复核的 POPCON 导出：重新输出 `nx`、`ny`、`x`、`y` 数组以及 `best` 的判据定义；监控 `best`、`n_invalid`、`valid`、`ignited`，以确认“未找到最佳区”是判据导致还是网格覆盖不足。
- 围绕当前点 `Ti0=100.0`、`n0=2e21` 做二维加密扫描，仍以 `Ti0`-`n0` 为轴；监控 `Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`、`valid`、`ignited`，看当前点附近是否出现判据切换。
- 对固定约束时间做参数敏感性验证：扫描 `tauE`（因为 `use_tauE=1.0`）；监控 `Pfus`、`Qfus`、`ignited`、`Pheat`、`tau_E`，确认当前结论对约束时间假设的依赖程度。
- 做成分与辐射验证扫描：在保持其余参数不变时扫描 `fimp`、必要时联动 `Zimp` 或 `fHe`；监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`，检查当前“低辐射”结论是否只属于零杂质输入。
- 若后续工具支持，补充边界类输出并联扫：例如密度、压力或电流相关归一量；监控 `beta` 类、`q` 类、密度归一量以及 `valid/ignited`，用于把“单点有效”与“判据边界”区分开来。
