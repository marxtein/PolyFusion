1. 核心结论
- `config=dipole`（`config_label=偶极场 Dipole`），`preset=Dipole-DD`；当前运行点 `valid=1.0`、`ignited=0.0`、`Pfus=47.45684836719668`、`Qfus=8.68746029329331`、`Pheat=5.462683772360169`、`Pwall=0.04211202563060448`。
- 运行点层面：该 0-D 运行点给出了有效解（`valid=1.0`），但未点火（`ignited=0.0`）；这只说明当前输入下模型返回了结果，不等同于 POPCON 扫描已经找到满足准则的工作窗。
- POPCON 扫描层面：扫描键为 `xkey=Ti0`、`ykey=n0`；`best` 矩阵全为 0，因此“当前准则下未找到最佳区”。
- 扫描结果未提供 `nx`、`ny` 以及 `x/y` 坐标数组，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- `n_invalid=0` 仅表明扫描记录里没有显式标出的无效点，不能据此推出存在最佳区，也不能替代对工作窗范围的判断。

2. 关键指标解读
- 运行点功率：`Pfus=47.45684836719668`、`Qfus=8.68746029329331`、`Pheat=5.462683772360169`，同时 `Pn=15.921740166969887`；在当前点上，模型给出了聚变输出、加热功率和中子相关功率，但 `ignited=0.0` 表明该点不是点火态。
- 损失项：`Ptrans=25.47474387505478`、`Pbrem=11.476245058863512`、`Pcycl=0.04680303866866527`、`P_line=0.0`；当前解里显式给出的损失以输运和轫致辐射为主，线辐射在本次输入下为 0。
- 约束假设：`use_tauE=1.0`，参数侧 `tauE=5.0`，输出侧 `tau_E=5.0`；这意味着结果依赖固定约束时间假设。另有 `tauC_eff=453.5806612792675`，但 `use_tauC=0.0`，本次并非以固定 `tauC` 作为主约束。
- 成分与工况：反应情形为 `strcase=D-D`，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；在这组输入下没有引入 He 灰分和杂质分数，且对应 `P_line=0.0`。
- 几何与壁负荷：`Vp=1890.303723225459`、`Sw=1256.6370614359173`、`Pwall=0.04211202563060448`；当前结果给出了体积、润湿面积和壁负荷指标，可用于后续几何/负荷一致性复核。
- POPCON 记录：本次只明确了扫描维度名 `Ti0` 与 `n0`，并给出 `best` 与 `n_invalid`；由于没有 `x/y` 网格坐标和 `nx/ny`，当前只能确认“未标出最佳区”，不能定位其位置、边界或趋势。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：结果依赖固定约束时间假设，`Pfus`、`Qfus`、`ignited` 以及损失分配都可能对 `tauE=5.0` 的设定敏感。
- 扫描判据方面，`best` 全 0 已经表明“当前准则下未找到最佳区”；这与“当前运行点 valid=1.0”是两件不同的事，不能混为“运行点有效就代表扫描有工作窗”。
- 扫描证据不完整：缺少 `nx`、`ny` 和 `x/y` 数组，所以“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”；`n_invalid=0` 也无法弥补这一缺口。
- 边界化指标缺失：本 JSON 未提供 `betaN`、`betaT`、`betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能据此判断边界约束的接近程度或裕量。
- 辐射与成分覆盖有限：当前输入 `fHe=0.0`、`fimp=0.0`、`Zeff=1.0`，且输出 `P_line=0.0`；这意味着本次结果没有覆盖 He 灰分和杂质引起的辐射敏感性。
- 模型形式上还存在代理近似：`cyclotron_model=equatorial_shell_proxy` 表明回旋辐射采用代理模型，相关损失项存在模型形式不确定性。

4. 下一步建议
1. 重跑 `Ti0-n0` 的 POPCON 扫描，并强制导出 `x`、`y`、`nx`、`ny`、`best`、`n_invalid`；重点监控 `valid`、`ignited`、`best`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，先把扫描证据补完整。
2. 做固定约束时间敏感性扫描：保持 `use_tauE=1`，对 `tauE` 做多点扫描；重点监控 `tau_E`、`Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pbrem`、`Pcycl`、`Pwall`，并检查 `best` 是否仍全 0。
3. 以当前点 `Ti0=100.0`、`n0=1.7285714285714284e+21` 为中心做局部加密扫描；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`、`best`，用于确认当前点附近是否只是分辨率不足而未显现差异。
4. 做成分/辐射验证扫描：改变 `fimp`、`fHe`、`Zimp`，并记录 `Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`valid`、`ignited`；这样可以补足当前“无杂质、无线辐射”假设下的覆盖空白。
5. 如果工具支持，补充导出边界化指标并做一致性核对；优先监控 `betaN`、`betaT`、`betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，避免只凭当前功率结果解读边界约束。
