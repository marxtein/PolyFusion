1. 核心结论
- `config=dipole`（`config_label=偶极场 Dipole`），`preset=Dipole-DD`。
- 当前运行点输出为：`valid=1.0`，`ignited=0.0`，`Pfus=42.65036243655587`，`Qfus=4.3166472044014705`，`Pheat=9.8804373897101`，`Pwall=0.04180268228460552`。
- 运行点与 POPCON 扫描需要分开看：`valid=1.0` 只说明这个单点计算被模型接受；`ignited=0.0` 说明该单点未被标记为点火，这不等同于扫描已经找到可用工作窗。
- POPCON 扫描的判据轴为 `xkey=Ti0`、`ykey=n0`；`best` 矩阵全为 `0`，因此当前准则下未找到最佳区。
- 虽然返回了 `best` 矩阵且 `n_invalid=0`，但缺少显式 `x/y` 数组以及 `nx/ny` 字段，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- **运行点** `Pfus=42.65036243655587`、`Qfus=4.3166472044014705`、`Pheat=9.8804373897101`、`Pwall=0.04180268228460552` 构成当前单点的核心输出；其中 `valid=1.0` 与 `ignited=0.0` 说明“该点算得出来”与“该点被标记为点火”是两件事。
- **约束与约束时间** `use_tauE=1.0`，`tauE=5.0`，且输出 `tau_E=5.0`；结果依赖固定约束时间假设。JSON 未提供 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling`，因此不能用这些缩放关系交叉解释本次结果。
- **功率与辐射** 输出包含 `Pn=14.309167424849969`、`Ptrans=22.807966279319025`、`Pbrem=15.36332239285055`、`Pcycl=0.05034372924642186`、`P_line=0.0`、`P_ei=1267.2846101350826`；可见本次结果中线辐射项为 `0.0`，而轫致辐射与回旋辐射均被显式计算。
- **成分与等离子体状态** `Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`，`f_fast_ion=0.2526573296932049`，`nbar=7.882282451346217e+19`，`ne0=2e+21`；这些量描述了当前输入成分与粒子状态，但 JSON 未给出归一化密度裕量指标。
- **几何与场量** `Vp=1890.303723225459`、`Sw=1256.6370614359173`、`ring_model=1.0`、`L_in=1.5`、`B_in=5.697503605448386`、`B_out=0.010115158710444893`、`beta_in=2.3037358569368185`、`beta_out=0.5292174893406227`；这些是当前 0-D 结果可直接使用的几何和场量信息。

3. 风险与不确定性
- `use_tauE=1.0` 意味着结果对固定 `tauE=5.0` 的假设敏感；在没有 `tauE` 扫描或缩放对照时，不能判断结论对约束时间设定的稳定性。
- `best` 矩阵全为 `0`，因此当前准则下未找到最佳区；这与“当前运行点 `valid=1.0`”不是同一层面的结论，不能混用。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；原因是 JSON 未提供显式 `x/y` 数组，也未给出 `nx/ny`。
- `n_invalid=0` 只说明当前返回的扫描判定中没有被标记为无效的网格点，不代表存在“最佳区”。
- JSON 未提供 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`geom_model` 等字段，因此无法对归一化边界、磁安全因子边界或几何模型简化程度做进一步判断。

4. 下一步建议
- 对 `Ti0 × n0` 重新执行一次完整 POPCON 扫描，并输出显式 `x`、`y`、`nx`、`ny`；必要时在当前扫描轴上加密网格。需要监控的指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕固定约束时间做敏感性验证：扫描 `tauE`，或在工具支持时对比固定 `tauE` 与缩放模式。需要监控的指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`、`tau_E`。
- 做成分与辐射验证扫描，优先检查 `fimp`、`Zimp`、`fHe` 对结果的影响。需要监控的指标：`Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。
- 做几何/装置参数一致性扫描，例如 `B_ring`、`R_p`、`Rw`、`L_in_fac`、`ring_model`。需要监控的指标：`B_in`、`B_out`、`Vp`、`Sw`、`beta_in`、`beta_out`、`valid`、`Pwall`。
