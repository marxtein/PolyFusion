1. 核心结论
- 当前运行点来自 `config=dipole`、`preset=Dipole-DD`，单点结果为 `valid=1.0`、`ignited=0.0`、`Pfus=0.0804999665439032`、`Qfus=0.0829983892470554`、`Pheat=0.9698979374682163`、`Pwall=0.000835880093184475`；对应输入点为 `Ti0=59.285714285714285`、`n0=1e+20`，聚变工况标签为 `strcase=D-D`。
- 这表示当前单点求解是有效的，但不是点火态；同时 `Qfus` 明显低于 1，当前点的聚变输出相对外加加热仍较低。
- POPCON 扫描的坐标键是 `xkey=Ti0`、`ykey=n0`，`n_invalid=0`；但 `best` 矩阵全为 0，因此“当前准则下未找到最佳区”。
- 必须区分单点与扫描：`valid=1.0` 只说明当前运行点可解，不等于 POPCON 扫描已经找到了工作窗；结合 `best` 全零，当前数据不支持“扫描已找到可接受区域”的说法。

2. 关键指标解读
- 能量与输出方面，当前点 `Pheat=0.9698979374682163`，`Pfus=0.0804999665439032`，`Pn=0.027007683713942906`，`Pwall=0.000835880093184475`；从量级上看，当前点以外加加热为主，聚变功率与中子相关输出都较低。
- 损失项方面，`Ptrans=0.9737247142324659`，`Pbrem=0.03840830598212638`，`Pcycl=0.011257200083584353`，`P_line=0.0`；当前结果中线辐射为零，辐射损失主要体现为轫致和回旋两项。
- 约束假设方面，`use_tauE=1.0` 且输入 `tauE=5.0`，输出 `tau_E=5.0`；这说明结果依赖固定约束时间假设，而不是由某个约束时间标度在本次输出中直接给出。
- 成分方面，输入 `fHe=0.0`、`fimp=0.0`、`Zimp=10`，输出 `Zeff=1.0`；在这组输入下，结果对应的是无 He、无杂质分数的基线情形，且与 `P_line=0.0` 一致。
- 几何与场量方面，输出给出 `Vp=1890.303723225459`、`Sw=1256.6370614359173`、`B_in=5.697503605448386`、`B_out=0.010115158710444893`，并使用 `ring_model=1.0`；这些量可用于后续核对体积、受热面积和场分布假设，但本次数据未提供更完整的几何模型说明。
- 边界/状态量方面，已给出 `beta_in=0.09835180004614877`、`beta_out=0.022593515891080433`、`nbar=3.941141225673108e+18`；但未提供 `betaN`、`q95`、`nbar_o_nGw` 等更直接的工程边界比值，因此这里只能记录已有状态量，不能扩展到未给出的边界判据。

3. 风险与不确定性
- 结果对约束时间假设敏感：由于 `use_tauE=1.0`，本次判断建立在固定 `tau_E=5.0` 的前提上；如果 `tauE` 改变，`valid`、`ignited`、`Pfus`、`Qfus` 和损失平衡都可能随之变化。
- POPCON 证据不完整：虽然给出了 `best` 矩阵、`xkey`、`ykey` 和 `n_invalid=0`，但缺少 `x`/`y` 数组以及显式 `nx`/`ny` 字段，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 扫描结论偏保守：`best` 全零只能支持“当前准则下未找到最佳区”，不能反向推出“所有邻域都不可用”，因为还不知道扫描轴取值列表、步长和边界覆盖范围。
- 当前点并未点火：`ignited=0.0` 表明该点不是自持燃烧状态，因此对外加加热、损失项和固定 `tau_E` 的依赖不能忽略。
- 成分敏感性尚未展开：当前输入 `fHe=0.0`、`fimp=0.0`，导致 `P_line=0.0`；这意味着本次结果没有覆盖 He 积累或杂质引入后的辐射响应。

4. 下一步建议
1. 补齐并重跑 `Ti0-n0` 的 POPCON 导出，明确保存 `x`、`y`、`nx`、`ny` 和判优准则；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
2. 以当前点 `Ti0=59.285714285714285`、`n0=1e+20` 为中心做局部二维扫描，验证单点有效性在邻域内是否保持；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pcycl`、`beta_in`、`beta_out`。
3. 单独做 `tauE` 敏感性扫描，因为当前结果依赖固定约束时间假设；重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`。
4. 做成分/辐射验证任务，在当前基线附近扫描 `fHe` 与 `fimp`；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`valid`。
