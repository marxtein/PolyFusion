# 1. 核心结论

本次 0-D 结果对应 `config=dipole`，`preset=Dipole-DD`。当前运行点标记为 `valid=1.0`、`ignited=0.0`；对应 `Pfus=0.010978843217501777`、`Qfus=0.021273778193474025`、`Pheat=0.5160739722702224`、`Pwall=0.0004194153042768598`。

对“当前运行点”和“POPCON 扫描”需要分开看：
- 当前运行点：已有一个有效解（`valid=1.0`），但未点火（`ignited=0.0`）。
- POPCON 扫描：扫描轴记录为 `xkey=Ti0`、`ykey=n0`，`n_invalid=0`；但 `best` 矩阵全为 0，因此**当前准则下未找到最佳区**。
- 同时，扫描结果未提供 `x/y` 数组，且未给出 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

另外，`use_tauE=1.0` 且 `tauE=5.0`，说明本结果**依赖固定约束时间假设**。

# 2. 关键指标解读

当前运行点方面：
- 加热与聚变：`Pheat=0.5160739722702224`，`Pfus=0.010978843217501777`，`Qfus=0.021273778193474025`。从数值上看，聚变功率和增益都已给出，但点火标志仍为 `0.0`。
- 壁负荷与中子：`Pwall=0.0004194153042768598`，`Pn=0.0036833943900031617`。
- 损失项：`Ptrans=0.4737039150320104`，`Pbrem=0.03840830598212638`，`Pcycl=0.011257200083584353`，`P_line=0.0`。这里传输损失是已列出的主要功率项之一，线辐射在该次输入下为 0。
- 粒子与成分：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。输入里没有杂质分数，故线辐射也对应为 0。
- 约束与时间尺度：`tau_E=5.0`，`tauC_eff=109.0965117502878`，`tau_eq_ie=1.0244735938505818`。由于 `use_tauE=1.0`，这里的能量约束不是由缩放律自行求得，而是由固定值驱动。
- 几何量：`Vp=1890.303723225459`，`Sw=1256.6370614359173`。`geom_model` 未给出，不能进一步判断几何模型复杂度。
- 其他边界/状态量：`nbar=3.941141225673108e+18`，`ne0=1e+20`，`ntau=5e+20`，`beta_in=0.047846821644072376`，`beta_out=0.010991440163228321`。JSON 中未提供 `betaN/q95/nbar_o_nGw` 等字段，不能做对应判读。

POPCON 扫描方面：
- 已知扫描维度标签是 `Ti0` 与 `n0`。
- `n_invalid=0` 表示记录到的扫描点里没有被标成 invalid 的点。
- 但 `best` 矩阵全 0，表示在当前筛选准则下没有落入“最佳区”的网格点。

# 3. 风险与不确定性

最主要的不确定性有三类：
- 约束时间假设：`use_tauE=1.0`，结果直接依赖固定 `tauE=5.0`；因此对 `Pfus`、`Qfus`、`Pheat`、损失分配的判断，都受这个设定影响。
- 扫描证据不足：缺少 `x/y` 数组且未提供 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。只能确认“当前准则下未找到最佳区”，不能推断最佳区位置、边界或移动方向。
- 模型与字段覆盖有限：未提供 `geom_model`、`betaN`、`q95`、`LH_ratio`、约束缩放律名称等字段，因此不能对几何精细度、MHD/安全因子边界、L-H 阈值比较结果做进一步解读。

另外，`valid=1.0` 只说明当前运行点有有效解，不等于扫描中已经找到了可接受工作窗；这两者不能混同。

# 4. 下一步建议

建议直接做两类可执行任务，并统一监控以下指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`、`tau_E`、`n_invalid`、`best`。

1. 重新导出完整 POPCON 扫描数据  
需要补齐 `x`、`y`、`nx`、`ny`，并保留 `best` 矩阵。目标不是预设结论，而是先确认是否真的全域无最佳区，还是仅因当前输出不完整而无法判读。

2. 围绕 `Ti0` 与 `n0` 做复扫  
保持其余参数不变，继续以 `xkey=Ti0`、`ykey=n0` 扫描，重点检查 `best` 是否仍全 0，以及 `valid/ignited/Pfus/Qfus/Pwall` 如何随网格点变化。

3. 做 `tauE` 假设敏感性扫描  
既然 `use_tauE=1.0`，应对 `tauE` 做多点扫描，核对 `Pfus`、`Qfus`、`ignited`、`Ptrans`、辐射项是否明显变化，确认当前结论对固定约束时间设定的依赖程度。

4. 做成分与辐射一致性检查  
在 `fHe`、`fimp`、`Zimp` 上做受控扫描，监控 `P_line`、`Pbrem`、`Zeff`、`Pfus`、`Qfus`，验证当前 `P_line=0.0` 的结果是否仅由当前成分设定导致。
