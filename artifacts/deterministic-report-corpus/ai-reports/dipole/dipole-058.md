1. 核心结论
- 本次算例的 `config` 为 `dipole`，`preset` 为 `Dipole-DD`；当前运行点 `valid=1.0`，`ignited=0.0`，`Pfus=2.1911638205194297`，`Qfus=0.47952407145548653`，`Pheat=4.5694553223754735`，`Pwall=0.005379929774767082`。
- 就“当前运行点”而言，该点是一个数值上有效的解（`valid=1.0`），但未达到点火（`ignited=0.0`）；同时 `Qfus` 小于 1，表示聚变输出相对于加热功率的比值有限。
- 就“POPCON 扫描”而言，扫描轴标签给出为 `xkey=Ti0`、`ykey=n0`，且 `n_invalid=0`，说明已保存的扫描结果里没有被标记为无效的网格点；但这不等同于“找到了工作窗”。
- `best` 矩阵为 8x8 且全为 0，因此必须明确：当前准则下未找到最佳区。
- 由于输入中未提供 `x`/`y` 扫描数组，也未显式给出 `nx`/`ny`，必须明确：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0` 且 `tauE=5.0`，因此本次结果依赖固定约束时间假设。

2. 关键指标解读
- 运行点功率收支上，`Pfus=2.1911638205194297`，其中中子功率 `Pn=0.7351339630402055`；外加/总加热相关量中，`Pheat=4.5694553223754735`，而输运功率 `Ptrans=5.473911907036567`，显示该点的输运损失量级高于加热功率。
- 辐射与损失方面，`Pbrem=0.5298778539574985`，`Pcycl=0.021695418860632903`，`P_line=0.0`；在这组输入下线辐射为零，辐射项主要来自轫致辐射，其次是回旋辐射。
- 壁负荷相关量中，`Pwall=0.005379929774767082`，结合 `Sw=1256.6370614359173` 可知模型已给出面向壁的归一化结果；但这里只能报告当前数值，不能据此外推更广泛的壁载趋势。
- 约束时间方面，`tau_E=5.0` 为直接指定值，`tauC_eff=210.25605844102762`；这意味着当前输出不是由某个约束缩放自动闭合得到，而是建立在固定 `tauE` 假设上。
- 等离子体状态方面，`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`；在当前输入里杂质份额为零，因此 `Zimp` 更像预留参数而非当前起作用的成分源。
- 几何与体表量方面，`Vp=1890.303723225459`，`Sw=1256.6370614359173`，`ring_model=1`；这些量说明模型确实使用了给定几何/环参数，但输入中没有 `geom_model` 字段，因此不能进一步评价几何简化程度。
- 边界与剖面相关量中，`beta_in=0.5528966056648364`、`beta_out=0.1270121974417495`，`p_slope=-6.88107105240703`，`nbar=1.4638524552500115e+19`，`ne0=3.7142857142857145e+20`；这些值可作为后续扫描时的对照基线。
- POPCON 结果需要单独看：当前只知道扫描针对 `Ti0` 和 `n0`，且 `best` 全零、`n_invalid=0`。这表示“扫描点大体可计算”与“存在最佳区”是两件不同的事；本次证据只支持前者，不支持后者。

3. 风险与不确定性
- 最大的不确定性来自扫描证据不完整：虽然有 `best` 矩阵和轴标签，但缺少 `x`/`y` 数组以及显式 `nx`/`ny`，所以扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `best` 全零意味着当前准则下未找到最佳区；这可能来自筛选条件严格、扫描范围不合适，或当前点附近本就不满足“最佳”判据，但现有 JSON 不能区分是哪一种原因。
- 当前运行点 `valid=1.0` 只说明该单点求解有效，不代表附近参数空间一定存在连续可用区域；同样，`n_invalid=0` 也只说明扫描点未被标记为无效，不代表出现了满足目标的窗口。
- `use_tauE=1.0` 带来模型依赖性：若固定的 `tau_E=5.0` 与真实约束偏离，`Pfus`、`Qfus`、`ignited` 及损失分配都会随之变化。
- 成分设定较理想化：`fHe=0.0`、`fimp=0.0`、`Zeff=1.0` 使当前辐射与稀释结果偏向简化输入；尽管 `P_line=0.0` 与此一致，但这也意味着对杂质敏感性的证据目前不足。
- 本次是 `D-D` 设定（`strcase="D-D"`），因此这里的功率与增益结论仅对应这组燃料设定，不能外推到未提供的其他燃料场景。

4. 下一步建议
- 先补全一次 `Ti0`-`n0` 的 POPCON 导出，要求同时保存 `x`/`y` 数组与 `nx`/`ny`；监控指标至少包括 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，以便先确认“是否存在最佳区”，再讨论其位置。
- 以当前点 `Ti0=100.0`、`n0=3.7142857142857145e+20` 为中心做局部加密扫描；监控 `Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pcycl`、`Pwall` 与 `best`，重点验证当前点附近是“普遍无最佳区”还是“仅当前粗网格未采到”。
- 单独做 `tauE` 灵敏度扫描，因为 `use_tauE=1.0`；建议在固定其余输入下改变 `tauE`，监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`tauC_eff`，检验固定约束时间假设对结论的影响幅度。
- 做成分/辐射验证扫描，优先从 `fimp` 与 `Zeff` 相关设定入手；监控 `Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`，确认当前“零杂质、零线辐射”输入对结果是否过于敏感。
- 保持 `config=dipole` 与 `preset=Dipole-DD` 不变，复算并记录 `Vp`、`Sw`、`beta_in`、`beta_out`、`nbar`、`ne0` 这些基线量，避免后续扫描时把几何/基态变化与参数灵敏度混在一起。
