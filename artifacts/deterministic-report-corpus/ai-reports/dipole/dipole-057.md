## 1. 核心结论

- 本次报告对应 `config=dipole`、`preset=Dipole-DD`；当前运行点给出的状态为 `valid=1.0`、`ignited=0.0`，即 0-D 求解给出了一个有效运行点，但未达到点火。
- 当前运行点的关键功率量级为：`Pfus=0.1588269633216746`、`Qfus=0.1120179700651846`、`Pheat=1.417870393734606`、`Pwall=0.0012546958906771706`。
- 运行点与 POPCON 扫描需要分开看：`valid=1.0` 只说明这一个运行点在模型内可解；它不等同于“扫描中找到了工作窗”。
- POPCON 扫描部分仅给出 `xkey=Ti0`、`ykey=n0`，且 `best` 矩阵为 8x8 全 0；因此应明确写为：当前准则下未找到最佳区。
- 由于扫描结果中未提供 `nx`、`ny`，且缺少对应的 `x/y` 扫描数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0` 且 `tauE=5.0`，说明这组结果依赖固定约束时间假设；对运行点和扫描判读都应把这一前提单独记住。

## 2. 关键指标解读

- 当前运行点的能量增益偏低：`Qfus=0.1120179700651846`，同时 `Pfus=0.1588269633216746` 低于 `Pheat=1.417870393734606`，与 `ignited=0.0` 一致，表示该点仍依赖外加加热，未表现为自持燃烧。
- 壁负荷 `Pwall=0.0012546958906771706` 很小；就这份 0-D 输出而言，当前点的聚变功率没有对应出高壁面热负荷读数，但这不等于已经完成更完整的壁工况判定。
- 损失项里可直接看到 `Pbrem=0.03840830598212638`、`Pcycl=0.011257200083584353`、`P_line=0.0`；其中线辐射在当前输入下为 0，且成分参数为 `Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`，说明这次计算对应的是非常“干净”的成分设定，杂质辐射并未在结果中体现为显著通道。
- 传输/功率平衡相关量里，`Ptrans=1.4737455134329218` 与 `Pheat=1.417870393734606` 处于接近量级，`tau_E=5.0`、`tauC_eff=109.0965117502878`；由于 `use_tauE=1.0`，这里的平衡判断建立在固定能量约束时间上，而不是由缩放律自洽生成。
- 几何与体表参数为 `Vp=1890.303723225459`、`Sw=1256.6370614359173`，并采用 `ring_model=1.0`、`cyclotron_model=equatorial_shell_proxy`；这说明报告包含了特定偶极场环模型与回旋辐射代理模型的结果，但 JSON 中没有给出更完整的几何修正开关，不能外推到更细的三维几何效应。
- POPCON 扫描只表明扫描轴选的是 `Ti0` 与 `n0`，并且 `n_invalid=0`；这可以解读为扫描中没有出现被记录为无效的网格点，但不能据此推出存在最佳工作窗，因为 `best` 矩阵全 0，且没有 `x/y` 数组来支撑位置和边界判读。

## 3. 风险与不确定性

- 最重要的不确定性来自约束时间处理：`use_tauE=1.0` 表明结果依赖固定约束时间假设；因此 `Pfus`、`Qfus`、`ignited` 以及扫描优选判据都会对 `tauE=5.0` 的设定敏感。
- POPCON 证据不完整：虽然有 `best` 矩阵，但未给出 `nx`、`ny` 与 `x/y` 数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势；也不能描述最佳区位置、边界或随参数变化的移动方向。
- 当前准则下未找到最佳区：`best` 矩阵全 0 只支持这一结论，不支持“存在但很窄”“接近边界”之类更强说法。
- `valid=1.0` 不能代替工程判定；它只说明当前输入下 0-D 方程求解有效，不代表性能充足，也不代表扫描空间里存在可选运行窗。
- 成分设定较理想化：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`P_line=0.0` 会降低由杂质与灰分带来的不确定性暴露程度；因此这份结果对成分扰动的敏感性仍未验证。
- JSON 中没有提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`、`H98/H_ITPA20/H_ISS04` 等边界或缩放比较量，因此无法对稳定性边界、密度边界或经验约束一致性做进一步解读。

## 4. 下一步建议

- 开展 `Ti0-n0` 二维重扫，并在输出中显式保存 `x`、`y`、`nx`、`ny`、`best`；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`，先把“是否存在非零最佳区”确认清楚。
- 在保持其他参数不变的前提下，对 `tauE` 做参数扫描或切换到非固定 `tauE` 的计算设置；重点监控 `Pfus`、`Qfus`、`ignited`、`Ptrans`、`tau_E`，用来检验当前结论对固定约束时间假设的依赖程度。
- 对成分参数做验证性扫描，优先改变 `fimp`、`fHe` 与必要时的 `Zeff` 设定；重点监控 `Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`，确认“低线辐射、低杂质”假设是否主导了当前结果。
- 对几何相关输入做一致性检查或敏感性扫描，如 `L_in_fac`、`R_p`、`r_ring`、`Rw`；重点监控 `Vp`、`Sw`、`Pwall`、`B_in`、`B_out`、`Pfus`，判断结果是否对当前偶极场几何设定敏感。
- 若后续工具支持，补充输出边界类指标并与扫描同时记录；建议至少监控 `beta` 类、`q/q95` 类、密度归一化指标以及约束时间比较量，这样才能把“运行点有效”和“约束边界是否接近”分开验证。
