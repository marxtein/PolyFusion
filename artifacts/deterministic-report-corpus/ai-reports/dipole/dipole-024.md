1. 核心结论
- 本次报告的 `config=dipole`，`preset=Dipole-DD`；当前运行点 `valid=1.0`、`ignited=0.0`，报告值为 `Pfus=12.40949949526152`、`Qfus=0.6212485685528781`、`Pheat=19.97509551477586`、`Pwall=0.025770841879382885`。
- 运行点结论与扫描结论需要分开看：当前单点在模型中被判为有效 (`valid=1.0`)，但这不等于 POPCON 扫描已经找到工作窗。
- 本次 POPCON 扫描的 `xkey=Ti0`、`ykey=n0`，`n_invalid=0`；但 `best` 矩阵为 8x8 且全为 0，因此“当前准则下未找到最佳区”。
- 同时，扫描结果未提供 `x/y` 数组，也未给出 `nx/ny`；因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。

2. 关键指标解读
- **运行点状态**：`valid=1.0` 表示该组输入在当前 0-D 模型下得到有效解；`ignited=0.0` 表示当前报告并未标记为点火状态。
- **功率平衡**：报告给出 `Pfus=12.40949949526152`、`Pheat=19.97509551477586`、`Qfus=0.6212485685528781`、`Pn=4.163378592630489`、`Ptrans=12.807550295309913`；按报告值看，聚变输出小于总加热量，对应 `Qfus<1`。
- **辐射与损失**：已报告辐射项为 `Pbrem=15.36332239285055`、`Pcycl=0.05034372924642186`、`P_line=0.0`；在这些已给出的通道里，轫致辐射占比最高，线辐射在本次结果中为 0。
- **成分设定**：参数中 `fHe=0.0`、`fimp=0.0`、`Zimp=10`，输出 `Zeff=1.0`；这说明当前运行点对应的是较理想化的成分输入，辐射与功率结果需要结合这一设定理解。
- **约束时间假设**：`use_tauE=1.0` 且 `tauE=5.0`，因此结果依赖固定约束时间假设，不是由约束标度自行给出的闭合结果。
- **几何与壁负荷**：报告给出 `Vp=1890.303723225459`、`Sw=1256.6370614359173`，以及 `Pwall=0.025770841879382885`；说明壁负荷已被显式计算。未见 `geom_model` 字段，无法据此判断是否启用了更详细或更简化的几何开关。
- **边界类指标**：本次只给出 `beta_in=1.2936362888952901`、`beta_out=0.2971759747835805`；未提供 `betaN`、`betaT`、`betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能据这些常见边界量补充判断。
- **扫描信息**：POPCON 扫描轴是 `Ti0` 对 `n0`，且 `n_invalid=0` 说明未报告无效点；但 `best` 全零意味着在当前“最佳区”判据下没有任何网格点被选中。

3. 风险与不确定性
- `valid=1.0` 只说明当前单点可解，不能替代扫描工作窗结论；本报告不能把“运行点有效”和“扫描找到工作窗”视为同一件事。
- 因为 `use_tauE=1.0`，结果对固定 `tauE=5.0` 的假设敏感；如果约束时间假设变化，`Pfus`、`Qfus`、`Pheat` 和 `ignited` 状态都可能改变。
- 虽然提供了 `best` 矩阵，但没有 `x/y` 数组，也没有 `nx/ny`；因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- `best` 全为 0，说明“当前准则下未找到最佳区”；但由于缺少扫描坐标数组，不能进一步说明最佳区是否靠近边界、是否缩成孤岛、或随 `Ti0/n0` 如何变化。
- 成分输入较理想化：`fHe=0.0`、`fimp=0.0`、`Zeff=1.0`；因此当前辐射和功率平衡结论对实际成分偏离的敏感性尚未被本报告覆盖。
- 未提供 `H98/H_ITPA20/H_ISS04`、`tauE_scaling`、`geom_model` 等信息，约束标度来源与几何细化程度都无法从这份 JSON 单独核实。

4. 下一步建议
1. 重新执行 `Ti0-n0` 的 POPCON 扫描，并导出完整 `x/y/nx/ny` 与 `best` 数据；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，以验证是否仍然“当前准则下未找到最佳区”。
2. 围绕当前点做 `tauE` 灵敏度扫描（因为 `use_tauE=1.0`）；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pbrem`、`Pwall`，用来判断结果对固定约束时间假设的依赖程度。
3. 对成分与辐射相关输入做验证扫描，例如 `fimp`、`fHe`、`Zimp`；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`valid`，检查当前理想化成分设定对结论的影响。
4. 围绕当前 `n0=2e+21`、`Ti0=32.14285714285714` 做局部细化扫描；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`beta_in`、`beta_out`，确认当前有效点附近是否存在连续可解区域。
