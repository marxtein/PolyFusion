# 1. 核心结论

- `config=dipole`，`preset=Dipole-DD`。
- 当前运行点：`valid=1.0`，`ignited=0.0`，`Pfus=8.913055333680246`，`Qfus=1.1503051424734207`，`Pheat=7.748426921325524`，`Pwall=0.013258786300610335`。
- 这表示本次 **单一运行点** 被程序判为有效，但**未点火**；是否“有效”与 POPCON 扫描是否存在工作窗不是一回事，不能混同。
- POPCON 扫描方面：`xkey=Ti0`，`ykey=n0`，`n_invalid=0`；但给出的 `best` 矩阵为全 0，因此**当前准则下未找到最佳区**。
- 同时，本次扫描未提供 `nx/ny`，也未提供 `x/y` 扫描数组，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1` 且 `tauE=5.0`，因此结果**依赖固定约束时间假设**。

# 2. 关键指标解读

- 运行点功率相关：`Pfus=8.913`，`Pheat=7.748`，`Qfus=1.150`，`Pn=2.990`。从数值上看，聚变输出与加热功率接近同一量级，但 `ignited=0.0`，所以不能表述为点火态。
- 损失项：`Ptrans=10.426`，`Pbrem=3.211`，`Pcycl=0.034`，`P_line=0.0`。已给数据中线辐射为 0，辐射损失主要体现为轫致辐射，回旋损失较小。
- 壁负荷：`Pwall=0.01326`，同时 `Sw=1256.637`。本次报告可确认已计算壁面负荷，但仅凭这一次 0-D 输出不能外推更广泛的运行结论。
- 约束与时间尺度：`tau_E=5.0`，`tauC_eff=329.877`，`tau_eq_ie=0.112`，`use_tauE=1`。这说明能量约束没有由缩放律自行求出，而是直接使用固定值；因此对 `Pfus/Qfus/ignited` 的解读都依赖这个前提。
- 等离子体与成分：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。当前输入对应非常理想化的杂质设定；`P_line=0.0` 也应放在这个成分前提下理解。
- 几何量：`Vp=1890.304`，`Sw=1256.637`，`ring_model=1`。报告给出了体积和表面积，但未提供 `geom_model` 字段，因此不能追加“几何模型较简化”的判断。
- POPCON 扫描：当前只能确认扫描变量名是 `Ti0` 和 `n0`，且 `best` 全 0；这只支持“未找到最佳区”的结论，不支持描述最佳区位置、边界或趋势。

# 3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1`：结果依赖固定 `tauE=5.0` 假设，若约束时间改动，`valid/ignited/Pfus/Qfus` 都可能变化。
- 当前运行点 `valid=1.0` 只说明该点通过了程序内判据；这**不等于** POPCON 扫描已经证明周围存在可接受工作窗。
- `best` 全 0 说明按当前最佳区判据没有识别出最佳区；这**不等于** 参数空间一定完全不可用，也可能与判据设置或扫描证据不完整有关。
- 扫描缺少 `nx/ny` 以及 `x/y` 数组，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- 成分设置较理想化：`Zeff=1.0`、`fimp=0.0`、`P_line=0.0`。这会限制对辐射相关结论的外推。
- 未提供 H98、H_ITPA20、H_ISS04、`tauE_scaling`、`betaN/q95/nbar_o_nGw` 等信息，因此对约束、边界和运行裕量的判读是不完整的。

# 4. 下一步建议

- 执行一次**补全网格定义的 Ti0-n0 扫描**：输出 `nx`、`ny`、完整 `x/y` 数组以及 `best` 判据矩阵。  
  需监控：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

- 执行一次**固定约束时间敏感性扫描**：围绕当前设定改变 `tauE`，比较 `use_tauE=1` 条件下结果对约束时间的依赖。  
  需监控：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`。

- 执行一次**成分与辐射验证扫描**：对 `fimp`、`Zeff` 或相关杂质设定做参数扫描，检查 `P_line/Pbrem/Pcycl` 对运行点判定的影响。  
  需监控：`Zeff`、`fimp`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`valid`。

- 对当前单点做**复算核对**：保持 `Ti0`、`n0`、`tauE` 不变，确认 `valid=1.0` 与 `ignited=0.0` 是否稳定复现。  
  需监控：`valid`、`ignited`、`Pfus`、`Qfus_raw`、`Pheat`、`Ptrans`、`Pwall`。
