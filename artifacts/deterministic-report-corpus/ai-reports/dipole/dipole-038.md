# 1. 核心结论

- `config=dipole`，`preset=Dipole-DD`。
- 当前运行点结果：`valid=1`，`ignited=0`，`Pfus=17.09`，`Qfus=1.55`，`Pheat=11.03`，`Pwall=0.0224`。
- 这表示**当前单个运行点被模型判为有效**，但**未点火**；这只针对本次运行点本身，不能等同于扫描存在工作窗。
- POPCON 扫描方面：`xkey=Ti0`，`ykey=n0`，`n_invalid=0`，但 `best` 矩阵为全 0。**当前准则下未找到最佳区**。
- 同时，本次 JSON 未给出 `nx/ny`，也未给出 `x/y` 数组。**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1`，且 `tau_E=5.0`，因此结果**依赖固定约束时间假设**。

# 2. 关键指标解读

- 运行点功率平衡：
  - 聚变功率 `Pfus=17.09`。
  - 外加/总加热表征 `Pheat=11.03`。
  - 聚变增益 `Qfus=1.55`。
  - 中子功率 `Pn=5.73`。
  - 传输功率 `Ptrans=14.19`。
- 辐射与损失：
  - 韧致辐射 `Pbrem=8.16`。
  - 回旋辐射 `Pcycl=0.043`。
  - 线辐射 `P_line=0.0`。
  - 从已给数据看，辐射项里以 `Pbrem` 为主，`Pcycl` 很小，`P_line` 在本次输入中为 0。
- 壁负荷与几何：
  - `Pwall=0.0224`。
  - `Vp=1890.30`，`Sw=1256.64`。
  - 本次 JSON 未提供 `geom_model=0`，因此这里只能按给定 `Vp/Sw` 做记录，不额外推断几何简化程度。
- 约束与运行假设：
  - `use_tauE=1`，`tau_E=5.0`，说明本次结果建立在固定能量约束时间上。
  - `tauC_eff=416.45`，`use_tauC=0.0`，输入中未显示其被作为主控扫描约束使用。
- 等离子体与成分：
  - `Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。
  - 这表示本次计算输入里未引入 He 灰分和杂质分数，虽然设置了杂质电荷数 `Zimp`。
- 压力/边界相关已给量：
  - `beta_in=1.433`，`beta_out=0.329`。
  - `nbar=5.74e19`，`ne0=1.457e21`。
  - 但本次 JSON 没有给出 `betaN/betaT/betap/q/q95/nbar_o_nGw/nbar_o_Sudo`，因此不能按这些口径进一步判断边界接近程度。
- POPCON 扫描解读：
  - 扫描轴是 `Ti0` 与 `n0`。
  - `n_invalid=0` 仅说明扫描点没有被标记为无效；这**不等于**存在满足“最佳”判据的区域。
  - 由于 `best` 全 0，**当前准则下未找到最佳区**。

# 3. 风险与不确定性

- 最大的不确定性来自约束假设：`use_tauE=1` 意味着结果**依赖固定约束时间假设**，因此对 `Pfus/Qfus/Pheat/Ptrans` 的解读都要放在该前提下。
- 扫描结论有限：虽然给了 `best` 矩阵和扫描轴名，但未给 `nx/ny`，也未给 `x/y` 数组。**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- 当前运行点 `valid=1` 只说明这个点在模型内部通过了有效性检查；它**不能替代** POPCON 对参数空间的判断。
- `ignited=0` 表明该点未点火；不能把 `Qfus=1.55` 或当前功率平衡直接表述成更强的状态结论。
- 成分设定较理想化：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`，这会限制对辐射和成分敏感性的外推。
- 本次没有提供 L-H 阈值比较量、`H98/H_ITPA20/H_ISS04`、`tauE_scaling`、`q95` 等信息，因此不能对约束模式、经验标度一致性或边界余量做进一步判断。

# 4. 下一步建议

1. 做一组可执行的 `Ti0-n0` 二维复扫，补齐并输出 `nx/ny` 与 `x/y` 数组。  
   需要监控的指标：`valid`、`ignited`、`best`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`。

2. 在当前点附近做 `tauE` 敏感性扫描，因为 `use_tauE=1`。  
   需要监控的指标：`tau_E`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`ignited`、`valid`。

3. 做成分与辐射验证扫描，至少扫描 `fHe`、`fimp`，必要时配合 `Zimp`。  
   需要监控的指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`。

4. 做当前运行点的局部单参扰动扫描，优先围绕 `Ti0`、`n0`、`B_ring`。  
   需要监控的指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`beta_in`、`beta_out`、`n_invalid`。
