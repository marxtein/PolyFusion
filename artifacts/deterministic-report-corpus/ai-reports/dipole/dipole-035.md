1. 核心结论
- 本次结果对应 `config=dipole`（偶极场 Dipole），`preset=Dipole-DD`；当前运行点 `valid=1.0`、`ignited=0.0`、`Pfus=3.3267843316613055`、`Qfus=0.5872696569466805`、`Pheat=5.664832657893223`、`Pwall=0.007155301451383352`。
- `valid=1.0` 只说明这个单个运行点计算有效；`ignited=0.0` 表示该点未点火。这个“运行点有效”不等同于 POPCON 扫描已经找到工作窗。
- 当前点还给出 `Pn=1.1161338677700894`、`Ptrans=6.25965887720871`，以及辐射相关项 `Pbrem=1.5872820329348143`、`Pcycl=0.028542211640914587`、`P_line=0.0`。
- POPCON 扫描以 `Ti0` 为横轴、`n0` 为纵轴；已给出的 `best` 矩阵全为 0，因此当前准则下未找到最佳区。
- 尽管存在 `best` 矩阵，JSON 未提供 `x`/`y` 数组，且 `nx`/`ny` 未给出，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点层面：`Pfus=3.3268`、`Qfus=0.5873`、`Pheat=5.6648` 共同描述当前单点的聚变产出与加热水平；其中 `Qfus_raw` 与 `Qfus` 相同，说明此处未显示额外修正差异。
- 约束层面：`use_tauE=1.0` 且 `tau_E=5.0`，说明本结果依赖固定约束时间假设；同时给出了 `tauC_eff=276.61014324538604`，但 `use_tauC=0.0`，因此当前主导约束不是固定 `tauC`。
- 成分与辐射层面：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；在这组输入下，线辐射 `P_line=0.0`，主要辐射损失来自 `Pbrem`，另有较小的 `Pcycl`。
- 几何与体表面积层面：给出了 `Vp=1890.303723225459`、`Sw=1256.6370614359173`、`B_in=5.697503605448386`、`B_out=0.010115158710444893`，可用于关联壁负荷 `Pwall=0.007155301451383352` 与磁场/几何设定，但 JSON 未提供 `geom_model`，因此不能据此评价几何模型是否简化。
- POPCON 层面：扫描键为 `xkey=Ti0`、`ykey=n0`，`n_invalid=0` 说明扫描结果中未统计到无效点；但 `best` 全零只说明在当前筛选准则下没有入选点，不能据此推出最佳区的位置、边界或趋势。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：结果依赖固定 `tau_E=5.0` 的假设，若约束时间设定变化，`Pfus`、`Qfus`、`ignited` 和功率平衡相关结论都可能改变。
- 当前运行点有效，不代表扫描存在可用工作窗；相反，`best` 全零表明在当前准则下没有筛出最佳区，这与单点 `valid=1.0` 是两件不同的事。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；原因是缺少 `x`/`y` 数组且 `nx`/`ny` 未给出，所以无法确认采样范围、步长和边界位置。
- JSON 未提供 `betaN`/`betaT`/`betap`、`q`/`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo` 等边界指标，因此无法对归一化压力、磁安全因子或密度边界做进一步判读。
- 成分设定中 `fHe=0.0`、`fimp=0.0`、`Zeff=1.0`，使当前结果对氦灰与杂质影响的覆盖有限；因此辐射和功率平衡结论对成分扰动的敏感性仍待验证。

4. 下一步建议
- 重新执行 `Ti0`–`n0` 的 POPCON 扫描，并显式导出 `x`/`y` 数组与 `nx`/`ny`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，先补足网格证据。
- 在保持其他参数不变的前提下，对 `tauE` 做敏感性扫描，或比较 `use_tauE=1` 与非固定约束时间的设置；重点监控 `tau_E`、`Pfus`、`Qfus`、`Ptrans`、`ignited`、`valid`。
- 围绕当前点继续扫描 `Ti0` 与 `n0` 的局部邻域，但需要配套输出每个网格点的判据结果；重点监控 `Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`、`n_invalid` 与 `best`，确认“无最佳区”是准则过严还是参数位置偏离。
- 增补成分/辐射验证扫描，例如扫描 `fHe`、`fimp`、`Zeff` 或相关杂质设定；重点监控 `Pbrem`、`Pcycl`、`P_line`、`P_ei`、`Pfus`、`Pwall`、`valid`。
