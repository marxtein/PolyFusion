1. 核心结论
- 本次案例为 `config=dipole`（`config_label=偶极场 Dipole`），`preset=Dipole-DD`；当前运行点指标为 `valid=1.0`、`ignited=0.0`、`Pfus=0.1514632247965551`、`Qfus=0.06852306972142079`、`Pheat=2.2103975407454146`、`Pwall=0.0018795090786540628`。
- 运行点层面，`valid=1.0` 说明这个 0-D 计算点被模型接受；但 `ignited=0.0`，且 `Qfus=0.06852306972142079`，所以当前点不是点火状态。
- POPCON 扫描层面，扫描变量是 `xkey=Ti0`、`ykey=n0`，`n_invalid=0`；但 `best` 矩阵全为 0，因此当前准则下未找到最佳区。
- 必须区分“当前运行点有效”和“扫描找到工作窗”：本次数据只支持前者，不支持后者。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 结果依赖固定约束时间假设：`use_tauE=1.0`，且本次使用 `tauE=5.0`、`tau_E=5.0`。

2. 关键指标解读
- 运行点功率平衡上，`Pheat=2.2103975407454146`，`Pfus=0.1514632247965551`，`Qfus=0.06852306972142079`；从数值关系看，当前点主要仍由外加加热对应的功率项支撑，而不是由聚变功率主导。
- 损失项里，`Ptrans=1.7594716844046108`；辐射相关项为 `Pbrem=0.5298778539574985`、`Pcycl=0.021695418860632903`、`P_line=0.0`。在已给出的辐射项中，`Pbrem` 大于 `Pcycl` 和 `P_line`。
- 中子与壁负荷方面，`Pn=0.05081580831922729`，`Pwall=0.0018795090786540628`；这两项可作为后续扫描时需要持续跟踪的输出，但仅凭这一次结果不能外推更大参数范围的表现。
- 约束时间方面，当前输出给出 `tau_E=5.0`、`tauC_eff=210.25605844102762`；由于 `use_tauE=1.0`，本次结论依赖固定约束时间假设，而不是由约束时间标度自洽决定。
- 成分与辐射设定上，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；这说明本次运行对应的是较理想化、低杂质的输入设定，且 `P_line=0.0` 与这一设定一致。
- 几何与模型输出上，`Vp=1890.303723225459`、`Sw=1256.6370614359173`、`ring_model=1.0`，回旋辐射模型为 `cyclotron_model=equatorial_shell_proxy`；这些量说明本次功率结果与所选几何/模型近似绑定，不能脱离该设定单独解读。
- POPCON 扫描方面，已知 `best` 矩阵全 0、`n_invalid=0`，可解读为“扫描点并非因为大量无效而失败”，而是“在当前判据下没有点被标记为最佳”；但由于缺少 `x/y` 数组以及 `nx/ny` 信息，不能进一步判断最佳区边界、位置或趋势。

3. 风险与不确定性
- 最大不确定性来自固定约束时间假设：`use_tauE=1.0` 会直接影响 `Pfus`、`Qfus`、`Ptrans` 等结果，对 `tauE` 的敏感性在这份数据里没有展开。
- `valid=1.0` 只说明单个运行点可解，不等同于扫描存在可用工作窗；本次 `best` 全 0，二者不能混为一谈。
- 当前准则下未找到最佳区，但 JSON 没有给出“最佳”判据的详细定义，因此无法判断究竟是哪一条筛选条件在限制结果。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；虽然有 `best` 矩阵和 `xkey/ykey`，但缺少 `x/y` 扫描数组以及 `nx/ny`。
- 本次数据没有提供 `H98`、`H_ITPA20`、`H_ISS04`、`betaN`、`q95`、`nbar_o_nGw` 等常见约束/边界量，因此对约束裕度的判断不完整。
- `fHe=0.0`、`fimp=0.0`、`Zeff=1.0` 使本次结果对应较理想化成分设定；成分变化对 `P_line`、`Pbrem`、`Qfus` 的影响尚未被这份数据覆盖。
- `P_ei=-1.1813077722715346` 的符号已给出，但 JSON 未说明该项的符号约定；因此只能记录该数值，不能据此扩展解释能量交换方向的工程含义。

4. 下一步建议
- 任务 1：重新执行 `Ti0-n0` 二维扫描，并导出完整 `x`、`y`、`nx`、`ny`、`best` 数组。需要监控的指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 任务 2：围绕当前 `tauE=5.0` 做约束时间敏感性扫描，至少比较不同 `tauE` 取值，或在工具允许时对比固定 `tauE` 与标度法。需要监控的指标：`tau_E`、`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pcycl`、`valid`、`ignited`。
- 任务 3：在当前 `config=dipole`、`preset=Dipole-DD` 下，对当前运行点附近的 `Ti0` 与 `n0` 做局部加密扫描，核实“无最佳区”是否对网格分辨率敏感。需要监控的指标：`best`、`n_invalid`、`Pfus`、`Qfus`、`Pwall`、`Ptrans`。
- 任务 4：做成分参数验证扫描，改变 `fHe`、`fimp`、`Zeff` 或 `Zimp` 中的一个或多个输入，检查辐射与聚变输出的变化。需要监控的指标：`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`、`valid`。
- 任务 5：若工具支持，固定物理输入后对 `ring_model` 或回旋辐射模型做对比运行，确认结果对模型选项的依赖。需要监控的指标：`Pcycl`、`Pheat`、`Pfus`、`Qfus`、`Vp`、`Sw`、`Pwall`。
