## 1. 核心结论

- `config=dipole`，`preset=Dipole-DD`。
- 当前运行点：`valid=1.0`，`ignited=0.0`，`Pfus=0.028187418977376397`，`Qfus=0.0046721571098533735`，`Pheat=6.033063168601581`，`Pwall=0.0048233899616592305`。
- 这表示当前这一个 0-D 运行点在程序判定上是“有效”的，但没有点火；是否“有效”仅对应该单点解，不等同于 POPCON 扫描已经找到可工作的区域。
- POPCON 方面，`best` 矩阵为 8x8 全 0，需明确写为：**当前准则下未找到最佳区**。
- 同时，扫描结果里未提供 `x/y` 数组，也未提供 `nx/ny`，因此：**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1`，且 `tauE=5.0`，因此结果**依赖固定约束时间假设**。

## 2. 关键指标解读

- 运行点功率水平：
  - 聚变功率 `Pfus=0.02819`，外加热/总加热相关量 `Pheat=6.0331`，对应 `Qfus=0.00467`。
  - `ignited=0.0` 与很低的 `Qfus` 一致，说明该点并不是靠自身聚变功率闭合。
  - 壁负荷 `Pwall=0.004823`；中子功率 `Pn=0.00945686`。
- 损失与辐射：
  - `Pbrem=3.21062`，`Pcycl=0.03404`，`P_line=0.0`。
  - 这组结果里线辐射为 0，而轫致辐射占了已给出辐射项的主要部分。
  - `Ptrans=2.80713`，反映传输相关功率损失量级。
- 约束与模型：
  - `tau_E=5.0` 且 `use_tauE=1.0`，说明这次结果不是由约束时间标度自洽求出，而是建立在固定约束时间输入上。
  - JSON 未提供 `tauE_scaling`、`H98`、`H_ITPA20`、`H_ISS04`，因此不能进一步解读经验约束改善因子或标度一致性。
- 成分与等离子体参数：
  - `Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。
  - 在这组输入下，没有设置 He 灰分和杂质份额；因此当前辐射结果基本对应“低杂质/无杂质份额输入”的情形。
  - `nbar=3.6033291206154125e+19`，输入 `n0=9.142857142857142e+20`，`ne0=9.142857142857142e+20`。
- 几何相关：
  - `Vp=1890.303723225459`，`Sw=1256.6370614359173`。
  - JSON 未给出 `geom_model` 字段，因此不能据此判断几何模型是否简化。
- 边界与稳定性代理：
  - 已给出 `beta_in=0.28353672085376225`、`beta_out=0.06513446022653818`。
  - 但未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能对常见边界约束做完整判读。
- POPCON 扫描：
  - 扫描键为 `xkey=Ti0`、`ykey=n0`，`n_invalid=0`。
  - 这只能说明扫描中没有被标成 invalid 的点数记录为 0；**不能替代最佳区存在性的判断**。
  - 由于 `best` 全 0，当前准则下未找到最佳区。

## 3. 风险与不确定性

- 最大的不确定性来自约束时间处理：`use_tauE=1` 意味着结果依赖固定 `tauE=5.0` 的假设，不能把该单点结果直接外推成更普遍结论。
- POPCON 证据不完整：虽然有 `best` 矩阵和 `xkey/ykey`，但缺少 `x/y` 数组以及 `nx/ny`。因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `best` 全 0 只说明在“当前准则”下没有最佳区证据，不代表参数空间中一定不存在其他可接受区域；这需要补全扫描定义后再判断。
- 成分设置较理想化：`fHe=0.0`、`fimp=0.0`、`P_line=0.0`。这使得当前辐射与燃料成分相关结论对杂质、灰分的敏感性仍不清楚。
- 缺少常见边界与约束指标：未提供 `betaN`、`q95`、Greenwald/Sudo 比值、经验约束改善因子等，不能对边界接近程度做完整工程判断。
- `valid=1.0` 仅说明当前单点在程序内部判定有效，**不能等同于扫描已找到工作窗**。

## 4. 下一步建议

1. 先补全一次可复核的 POPCON 导出：围绕当前 `Ti0=5.0`、`n0=9.142857142857142e+20` 重新扫描，并输出完整 `x`、`y`、`best`、`nx`、`ny`。  
需监控指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 做约束时间敏感性扫描：在保持其余输入不变时，扫描 `tauE`，因为当前结果依赖固定约束时间假设。  
需监控指标：`Qfus`、`Pfus`、`ignited`、`Ptrans`、`Pbrem`、`Pwall`、`tau_E`。

3. 做成分/辐射验证扫描：对 `fHe`、`fimp`、`Zimp` 做参数扫描，检查当前 `P_line=0` 和 `Zeff=1.0` 设定对结果的影响。  
需监控指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`valid`。

4. 补充边界相关输出后再复核当前点：增加 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo` 等输出，再判断该单点约束接近程度。  
需监控指标：上述边界量连同 `valid`、`Pfus`、`Qfus`、`Pwall`。
