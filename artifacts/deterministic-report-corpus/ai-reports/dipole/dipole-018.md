1. 核心结论
- 本次案例为 `config=dipole`、`preset=Dipole-DD`；当前运行点 `valid=1.0`、`ignited=0.0`、`Pfus=0.4280011050406523`、`Qfus=0.16177165851078806`、`Pheat=2.645711300611474`、`Pwall=0.002445982615012084`。
- 就当前运行点而言，`valid=1.0` 表示该点在本次 0-D 计算下被接受；`ignited=0.0` 表示并未点火，不能把“运行点有效”理解成“已达到点火或高增益状态”。
- POPCON 扫描需要单独看待：本次扫描的 `xkey=Ti0`、`ykey=n0`，且 `best` 为 8x8 全 0 矩阵，因此“当前准则下未找到最佳区”。
- 同时，扫描结果里未给出 `x/y` 数组，也未给出 `nx/ny`；因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- `use_tauE=1.0` 且 `tauE=5.0`，说明本次结果依赖固定约束时间假设。

2. 关键指标解读
- 运行点输出显示：`Pfus=0.4280011050406523`、`Qfus=0.16177165851078806`、`Pheat=2.645711300611474`，且 `ignited=0.0`；按当前模型，这更接近“需要外加/既定加热支撑的非点火状态”，而不是自持点火状态。
- 损失项中，`Ptrans=2.37854505484327` 明显大于 `Pbrem=0.5298778539574985`、`Pcycl=0.021695418860632903`，而 `P_line=0.0`；在当前输入下，传输损失是主要功率项之一，线辐射没有贡献到输出结果。
- 中子相关输出为 `Pn=0.14359407799072502`；壁负荷输出为 `Pwall=0.002445982615012084`，结合 `Sw=1256.6370614359173`，当前报告给出了很低的面功率对应结果，但这里只能据实引用该 0-D 输出，不能外推更高负荷场景。
- 约束时间方面，`tau_E=5.0`、`use_tauE=1.0`，并给出 `tauC_eff=210.25605844102762`；这意味着结果并非来自约束时间标度自洽求解，而是直接受固定 `tauE` 设定影响。
- 成分与辐射设定较“理想化”：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；在这一组输入下，`P_line=0.0` 是和当前成分设定一致的输出，不代表其他成分条件下也会如此。
- 几何/场量方面，报告给出 `Vp=1890.303723225459`、`Sw=1256.6370614359173`、`B_in=5.697503605448386`、`B_out=0.010115158710444893`、`ring_model=1.0`；但未提供 `geom_model`，因此这里不能进一步判断是否采用了更简化或更完整的几何模型。
- POPCON 部分仅能确认：扫描轴是 `Ti0` 与 `n0`，`n_invalid=0`，且 `best` 全 0；这说明“没有无效点”不等于“找到了最佳区”，两者不能混为一谈。

3. 风险与不确定性
- 最大不确定性来自固定约束时间：`use_tauE=1.0` 使 `Pfus`、`Qfus`、`ignited`、`Ptrans` 等结果都依赖 `tauE=5.0` 这一假设，若该假设变化，结论可能随之变化。
- POPCON 证据不完整：虽然有 `best` 矩阵和 `n_invalid=0`，但缺少 `x/y` 数组以及 `nx/ny` 元数据；因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 当前准则下未找到最佳区，只能说明这次扫描输出的 `best` 没有命中满足准则的网格点；不能据此反推“运行点一定不好”，也不能据此反推“只差一点就会出现工作窗”。
- 报告未提供 `betaN`、`betaT`、`betap`、`q`/`q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`H98`、`H_ITPA20`、`H_ISS04` 等常见归一化边界或约束指标，因此无法从这份 JSON 直接判断这些边界余量。
- 成分设定为 `fHe=0.0`、`fimp=0.0`、`Zeff=1.0`，使当前结果没有覆盖非零氦灰或杂质情形；这意味着辐射和稀释相关结论的适用范围仅限于当前输入。
- `geom_model`、`best_fraction`、显式 `x/y` 网格坐标均未提供，因此不能描述最佳区位置、边界、倾斜方向或趋势。

4. 下一步建议
1. 重新输出一版完整的 `Ti0-n0` POPCON 扫描，确保同时保存 `x`、`y`、`nx`、`ny`、`best`、`n_invalid`，并继续记录 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`；目标是先补齐网格证据，再判断是否真的存在工作窗。
2. 以当前点 `Ti0=32.14285714285714`、`n0=3.7142857142857145e+20` 为中心做更细网格扫描，监控 `best`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pcycl`、`Pwall`；目标是确认“best 全 0”是准则本身导致，还是当前网格分辨率不足导致。
3. 针对 `use_tauE=1.0` 做约束时间敏感性验证：扫描不同 `tauE` 取值，或在工具支持时对比固定 `tauE` 与 `tauE_scaling` 模式；重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`。
4. 做成分/辐射假设验证，至少扫描 `fimp`、`fHe` 或等效 `Zeff` 设定；重点监控 `P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Pheat`、`valid`，以确认当前 `P_line=0.0` 是否只是零杂质输入下的特例。
5. 若后续要继续做边界判读，需在输出里补充或派生 `betaN`、`q95`、密度归一化指标以及约束提升因子；监控这些指标与 `valid`、`ignited`、`Pfus`、`Qfus` 的联动，避免只凭单点功率结果判断。
