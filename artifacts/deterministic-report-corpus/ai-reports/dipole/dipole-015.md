## 1. 核心结论

- `config=dipole`，`preset=Dipole-DD`。
- 当前运行点：`valid=1.0`，`ignited=0.0`，`Pfus=3.2804`，`Qfus=0.1871`，`Pheat=17.5315`，`Pwall=0.01656`。
- 这表示本次 **单点计算结果有效**，但 **未点火**；同时 `Qfus` 明显低于 1，对应当前加热下聚变输出有限。
- POPCON 扫描需要单独看：`xkey=Ti0`，`ykey=n0`，`n_invalid=0`，但 `best` 矩阵全为 0，因此**当前准则下未找到最佳区**。
- 另外，本次扫描结果未提供 `x/y` 数组，也未给出 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1.0` 且 `tauE=5.0`，说明结果依赖**固定约束时间假设**。

## 2. 关键指标解读

- **当前运行点**
  - `valid=1.0` 说明该组输入下模型完成了有效求解。
  - `ignited=0.0` 说明当前点不属于点火状态。
  - `Pfus=3.2804`、`Pheat=17.5315`、`Qfus=0.1871`：当前点有聚变功率输出，但相对于加热功率，增益偏低。
  - `Pwall=0.01656`：给出了壁负荷量级，但仅能作为该 0-D 点的结果读取，不能单独外推到更广工况。

- **功率收支与损失项**
  - `Pbrem=11.4762`，`Pcycl=0.0468`，`P_line=0.0`。
  - 从已给数据看，辐射损失里轫致辐射占主要部分，回旋辐射较小，线辐射在本次输入下为 0。
  - `Ptrans=8.1883` 也构成了重要功率流出项。
  - `Pn=1.1006` 给出中子功率分量；本次为 `strcase="D-D"`，应结合该反应情形理解其输出构成。

- **约束与模型设定**
  - `tau_E=5.0`，且 `use_tauE=1.0`：本次不是由约束时间标度自行给出，而是直接使用固定 `tauE`。
  - `tauC_eff=453.5807`、`tau_eq_ie=0.05927` 给出了粒子/能量相关时间尺度，但本报告不据此扩展到未提供的稳定性结论。

- **成分与辐射相关输入**
  - `Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。
  - 这说明本次输入中有效电荷数较低，He 与杂质份额设为 0；因此 `P_line=0.0` 与该输入设定一致。

- **几何与体表参数**
  - `Vp=1890.3037`，`Sw=1256.6371`。
  - `ring_model=1`，`cyclotron_model="equatorial_shell_proxy"`：表明回旋辐射和环结构采用了特定代理模型，解读时应按该模型边界理解。

- **POPCON 扫描**
  - 扫描轴定义为 `Ti0` 与 `n0`。
  - `n_invalid=0` 说明扫描中没有被标记为无效的点。
  - 但 `best` 矩阵全 0，仅能支持“当前准则下未找到最佳区”；这与“当前运行点 valid=1.0”不是一回事，不能混同。

## 3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1.0`：结果依赖**固定约束时间假设**，对 `Pfus`、`Qfus`、`Pheat` 的判读都受该假设直接影响。
- 当前点 `valid=1.0` 只说明该点可解，**不等于**扫描中存在满足最佳判据的区域。
- `best` 矩阵全 0，说明在本次准则下没有找到最佳区；但由于缺少 `x/y` 数组且未提供 `nx/ny`，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- 没有提供 `betaN`、`q95`、`nbar_o_nGw`、`LH_ratio` 等更多边界或阈值比较量，因此对运行边界的解释能力有限。
- `geom_model` 字段未提供，不能判断本次几何是否采用更简化或更完整的模型。
- 这是 0-D 初筛结果，只能用于当前输入与当前判据下的工程筛查，不能外推出更高维物理行为。

## 4. 下一步建议

1. 以 `Ti0` 和 `n0` 为主轴，重新执行显式网格扫描，并保存完整 `x`、`y`、`best`、`nx`、`ny`。
   重点监控：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best`。

2. 在固定其他输入时，对 `tauE` 做参数扫描，直接检验固定约束时间假设对结果的影响。
   重点监控：`tau_E`、`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`ignited`。

3. 对成分参数做独立扫描，至少覆盖 `fimp`、`fHe`、`Zeff` 相关变化，检查辐射项响应。
   重点监控：`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。

4. 对几何/装置参数做局部扰动扫描，例如 `B_ring`、`R_p`、`r_ring`、`L_in_fac`。
   重点监控：`Vp`、`Sw`、`Pwall`、`Pfus`、`Pheat`、`valid`。

5. 若后续需要判断扫描区分布，再补充输出完整 POPCON 坐标数组与判据定义；在现有证据下，不应进一步描述最佳区位置、边界或趋势。
