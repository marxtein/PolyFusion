1. 核心结论
- `config=dipole`，`preset=Dipole-DD`；当前运行点 `valid=1.0`、`ignited=0.0`，`Pfus=22.6395`、`Qfus=2.3169`、`Pheat=9.7713`、`Pwall=0.02579`。
- 这表示当前单个运行点在该 0-D 模型中被标记为有效，但未点火；这与 POPCON 扫描是否找到可接受区域不是同一件事，不能混同。
- `last_scan` 给出了 `xkey=Ti0`、`ykey=n0`，且 `n_invalid=0`，但 `best` 的 8x8 矩阵全为 0，因此“当前准则下未找到最佳区”。
- 同时，扫描结果缺少 `x/y` 轴数组，且未显式给出 `nx/ny`；因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。

2. 关键指标解读
- 运行点功率相关：除 `Pfus=22.6395`、`Qfus=2.3169`、`Pheat=9.7713` 外，还给出了 `Pn=7.5955`、`Ptrans=16.6172`、`P_ei=672.6954`；这些值说明当前点的功率项已被完整计算，但是否满足更高层级判据仍要看扫描准则。
- 辐射与损失：`Pbrem=8.1551`、`Pcycl=0.0430`、`P_line=0.0`；在给定成分下，线辐射项为 0，而韧致辐射与回旋辐射是已显式输出的损失通道。
- 约束时间与模型依赖：`tau_E=5.0` 且 `use_tauE=1.0`，因此结果依赖固定约束时间假设；`tauC_eff=416.4491` 已输出，但 `use_tauC=0.0`，说明当前并非用固定 `tauC` 直接约束。
- 等离子体与成分：`n0=1.4571e+21`、`Te0=20.0`、`Ti0=72.8571`、`nbar=5.7428e+19`、`ntau=7.2857e+21`；`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`，即杂质种类参数存在，但当前杂质分数为 0。
- 几何与场量：`Vp=1890.3037`、`Sw=1256.6371`、`ring_model=1`、`L_in=1.5`、`B_in=5.6975`、`B_out=0.0101`；这些值可用于理解体积、受壁面积与壁负荷输出 `Pwall` 的对应关系。
- 可用边界量有限：当前只看到 `beta_in=1.6784`、`beta_out=0.3856`，未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此边界约束只能做局部解读，不能扩展到更完整的运行边界判断。

3. 风险与不确定性
- 最主要的不确定性来自 `use_tauE=1.0`：结果依赖固定约束时间假设，`Pfus`、`Qfus`、`ignited` 与扫描判据都可能对 `tau_E` 取值敏感。
- 虽然当前运行点 `valid=1.0`，但扫描 `best` 全 0；这只说明“单点有效”和“扫描准则下存在最佳区”是两件不同的事，不能把前者当成后者的证据。
- `best` 矩阵虽已提供，但缺少 `x/y` 数组且未给出 `nx/ny`，所以“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”；也不能描述最佳区位置、边界或朝哪个方向改善。
- 当前成分设定下 `fHe=0.0`、`fimp=0.0` 且 `P_line=0.0`，这使线辐射结论高度依赖当前输入；若后续成分设定变化，损失结构可能改变。
- 未提供 H 因子、经验约束标度名、`geom_model`、L-H 比值或阈值比较结果，因此不能对约束标度来源、几何简化程度或 L-H 阈值关系作进一步判断。

4. 下一步建议
- 先补做一次完整的 `Ti0-n0` POPCON 导出，确保同时输出 `x`、`y`、`nx`、`ny` 与 `best`；重点监控 `n_invalid`、`best` 中是否出现非零点、以及各网格点的 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕当前点 `Ti0=72.8571`、`n0=1.4571e+21` 做局部加密扫描；重点监控 `Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pn`、`Pbrem`、`Pcycl`、`P_line`，确认“单点有效”附近是否仍保持相同判定。
- 针对固定约束时间假设做 `tauE` 敏感性复算；可在 `use_tauE=1` 下改变 `tauE`，并比较 `valid`、`ignited`、`Pfus`、`Qfus`、`Pwall` 以及 `best` 矩阵是否发生变化。
- 若要评估成分对损失的影响，可增加 `fHe`、`fimp` 的参数扫描；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Qfus` 与 `best` 判据响应。
