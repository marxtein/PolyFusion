1. 核心结论
- 本次结果对应 `config=dipole`，`preset=Dipole-DD`。当前运行点 `valid=1.0`，说明该 0-D 求解点在模型内部是可计算/可接受的；但 `ignited=0.0`，表示并未达到点火状态。
- 当前运行点的主要输出为：`Pfus=0.04740799056033839`、`Qfus=0.005239808427330723`、`Pheat=9.047657222172356`、`Pwall=0.007237622931747744`。从这些值看，聚变输出相对外加/总加热较低，壁负荷数值也已给出。
- 运行点约束上，`use_tauE=1.0` 且 `tauE=5.0`，因此结果依赖固定约束时间假设；这意味着这里的功率平衡、增益和点火结论都建立在给定 `tau_E` 前提下。
- POPCON 扫描需要与“当前运行点有效”分开看：本次 `last_scan` 给出了 `xkey=Ti0`、`ykey=n0` 和 `best` 矩阵，但 `best` 矩阵全为 0，因此当前准则下未找到最佳区。
- 同时，扫描结果缺少 `nx`/`ny` 以及 `x`/`y` 扫描数组；按现有证据，只能确认有一组 `best` 判据输出，不能据此判断工作窗形状、边界或敏感性趋势。应明确写为：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点状态：`valid=1.0` 说明该点在模型中成立，`ignited=0.0` 说明该点不是点火态；这两个结论不能等同于“存在扫描工作窗”。
- 功率相关：`Pfus=0.04740799056033839`、`Qfus=0.005239808427330723`、`Pheat=9.047657222172356`。其中 `Qfus` 很低，表明该点的聚变输出相对加热输入占比较小。中子功率 `Pn=0.015905348406187403` 也已给出，可作为 D-D 反应输出的一部分参考。
- 损失通道：辐射/相关损失中，`Pbrem=5.3998942838952795`，`Pcycl=0.038763270462999745`，`P_line=0.0`；另有 `Ptrans=3.640502309968229`。就已给数值看，轫致辐射和输运功率是当前功率平衡中的主要项，线辐射在该输入下为 0。
- 壁面与几何：`Pwall=0.007237622931747744`，几何量给出 `Vp=1890.303723225459`、`Sw=1256.6370614359173`。这些量可用于后续把体积、表面积与功率负荷联合检查，但本次不能据此外推更强结论。
- 成分与等离子体参数：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。当前输入中没有额外杂质分数，且 `P_line=0.0` 与此一致。密度方面给出 `nbar=4.673067453298114e+19`、`n0=1.1857142857142858e+21`、`ne0=1.1857142857142858e+21`，温度扫描键为 `Ti0`，当前参数中 `Ti0=5.0`、`Te0=20.0`。
- 约束时间与交换：`tau_E=5.0`、`tauC_eff=375.66513521537416`、`tau_eq_ie=0.0864013874331816`。由于 `use_tauE=1.0`，本次不是由某个约束时间标度自洽生成 `tau_E`，而是直接采用固定值；因此结果对 `tau_E` 设定敏感，需单独验证。

3. 风险与不确定性
- 最大不确定性来自约束时间假设：`use_tauE=1.0` 意味着 `tau_E` 被固定为 5.0，若这一设定变化，`Pfus`、`Qfus`、`ignited` 和损失分配都可能明显改变。
- 当前运行点“有效”不代表扫描存在可用工作窗。这里 `valid=1.0` 仅说明单点可计算，而 `best` 矩阵全 0 表明按当前最佳判据没有识别出最佳区；两者不能混用。
- POPCON 证据不足：虽然有 `best` 矩阵和扫描轴名称 `Ti0/n0`，但缺少 `x`/`y` 数组以及 `nx`/`ny` 信息，因此不能判断最佳区位置、边界、面积变化，也不能描述任何趋势或敏感性。
- 功率平衡中，`Pbrem` 与 `Ptrans` 占比较重，而 `Pfus` 与 `Qfus` 较低；这提示当前点更像是“加热维持下的低聚变输出状态”。但由于不能引入外部阈值，这里只能说输出结构如此，不能进一步下结论。
- 本次数据未提供 `geom_model`，因此不能判断是否采用简化几何模型；未提供 L-H 比值或阈值比较结果，因此不能对 H 模进入或维持做任何表述。

4. 下一步建议
- 执行一组显式 POPCON 复扫：围绕 `xkey=Ti0`、`ykey=n0` 重新输出完整 `x`/`y` 数组与 `nx`/`ny`，并保留 `best`、`n_invalid`。需要监控的指标：`best` 是否仍全 0、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`ignited`。
- 执行 `tauE` 敏感性扫描：在保持其余参数不变时扫描 `tauE`，因为 `use_tauE=1.0` 下结果依赖固定约束时间假设。需要监控：`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`ignited`、`valid`。
- 执行功率平衡核对任务：对当前点及邻近点逐项比较 `Pbrem`、`Pcycl`、`P_line`、`Ptrans` 与 `Pheat`、`Pfus` 的变化，确认是哪些损失项主导了低 `Qfus`。需要监控：`Pbrem`、`Pcycl`、`P_line`、`Ptrans`、`P_ei`、`Pfus`、`Qfus`。
- 执行成分参数验证：在 `fimp`、`Zimp`、`fHe` 上做受控扫描，检查辐射与聚变输出的变化，但不要把结果解释成外推结论。需要监控：`Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`。
- 执行单点复算一致性检查：固定当前 `preset` 和参数，重复计算并记录 `valid`、`ignited`、`tau_E`、`tauC_eff`、`Pfus`、`Qfus`，确认本次报告值是否可重复，避免将一次性数值波动误判为扫描特征。
