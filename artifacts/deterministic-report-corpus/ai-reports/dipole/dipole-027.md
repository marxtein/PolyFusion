1. 核心结论
- 本次报告对应 `config=dipole`，`preset=Dipole-DD`。
- 当前运行点结果为：`valid=1.0`，`ignited=0.0`，`Pfus=2.2711935681003954`，`Qfus=0.4289479185089234`，`Pheat=5.294800301153922`，`Pwall=0.006020826618473999`。
- 这表示当前 0-D 求解给出了一个有效运行点，但该运行点未点火；同时 `Qfus` 小于 1，报告中的聚变输出低于外加加热功率。
- POPCON 扫描与单点运行需要分开看：本次扫描的坐标是 `xkey=Ti0`、`ykey=n0`，且 `n_invalid=0`，说明已记录的扫描点没有被标记为无效；但 `best` 矩阵全为 0，因此“当前准则下未找到最佳区”。
- 由于 JSON 中未提供 `nx`、`ny` 以及 x/y 扫描数组，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。

2. 关键指标解读
- 当前运行点：`valid=1.0` 仅说明该组输入得到了有效求解结果，不等于 POPCON 扫描已经找到了可接受区域；`ignited=0.0` 则说明该点不是自持点火态。
- 功率相关：报告给出 `Pfus=2.2711935681003954`、`Pheat=5.294800301153922`、`Qfus=0.4289479185089234`、`Ptrans=5.188185736064878`、`Pn=0.76198388861371`、`Pwall=0.006020826618473999`。就当前单点而言，可以直接读出聚变功率、加热功率、输运功率和壁负荷，但不能据此外推更大参数空间的趋势。
- 辐射与损失：`Pbrem=1.5872820329348143`，`Pcycl=0.028542211640914587`，`P_line=0.0`。结合输入参数 `fimp=0.0`、`fHe=0.0`、`Zeff=1.0`，当前算例中的线辐射为零，辐射图景主要由轫致辐射和回旋辐射构成。
- 约束假设：`use_tauE=1.0`，`tauE=5.0`，输出中 `tau_E=5.0`。这意味着本次结果依赖固定约束时间假设，而不是从约束时间标度自行推导；因此对 `Pfus`、`Qfus`、`ignited` 等结果的解读需要把这一假设单独记账。
- 成分与几何：输入给出 `Zimp=10`，但 `fimp=0.0`，所以该杂质电荷数并未在当前工况中形成非零杂质占比效应。几何相关输出包括 `Vp=1890.303723225459`、`Sw=1256.6370614359173`，并使用了 `ring_model=1` 和 `cyclotron_model=equatorial_shell_proxy`；这些量可用于后续几何敏感性复核，但当前 JSON 不足以支持更深入的几何趋势判断。
- POPCON 扫描：当前只知道扫描轴是 `Ti0` 与 `n0`，且 `best` 为 8x8 的全零矩阵、`n_invalid=0`。这说明“扫描点并非因为无效而失败”，而是“在当前准则下没有任何点被标记为最佳”；但由于缺少 x/y 数组与 `nx`/`ny`，不能判断最佳区位置、边界、收缩方向或对 `Ti0`、`n0` 的敏感性。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：结果依赖固定 `tauE=5.0` 假设，若约束时间改动，`Pfus`、`Qfus`、`ignited` 和功率平衡都可能随之变化。
- POPCON 证据不完整：虽然有 `best` 矩阵和 `n_invalid=0`，但缺少 `nx`、`ny` 与 x/y 数组，因此不能判断工作窗形状、边界位置、单调性或敏感性趋势。
- “运行点有效”与“扫描找到工作窗”不能混为一谈：这里前者成立，后者不成立；当前只能说单点求解有效，不能说扫描已支持某个参数窗口。
- 成分模型较理想化：`fimp=0.0`、`fHe=0.0`、`P_line=0.0` 使当前辐射与稀释影响偏简化，对含杂质或有氦灰累积的情形缺少直接证据。
- 边界信息不足：JSON 中没有 `betaN`、`q95`、`nbar_o_nGw`、`LH_ratio` 等字段，因此无法对常见运行边界或阈值比较给出更多判断。

4. 下一步建议
- 先补全一次可复核的 `Ti0`-`n0` POPCON 输出，要求显式保存 x/y 数组以及 `nx`、`ny`；监控指标建议至少包括 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，用于区分“数值可解”与“满足当前最佳准则”。
- 做 `tauE` 敏感性扫描，建议以当前点为中心对 `tauE` 进行一维扫描，或与 `Ti0`/`n0` 做二维联扫；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`、`Pbrem`、`Pcycl`，用来评估固定约束时间假设对结论的影响。
- 做成分扫描，至少扫描 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`，以验证当前“零杂质、零氦灰”设定是否显著影响功率平衡。
- 做几何/场参数复核扫描，优先考虑 `B_ring`、`R_p`、`r_ring`、`Rw`、`L_in_fac`；监控 `B_in`、`B_out`、`Vp`、`Sw`、`Pfus`、`Qfus`、`Pwall`，用于检查当前单点结果对几何和磁场设定的依赖。
- 若后续仍使用“最佳区”判据，建议同时输出该判据对应的布尔条件或评分字段；监控 `best` 与各基础物理量的对应关系，避免出现“扫描点均有效但 best 全零”却无法追溯原因的情况。
