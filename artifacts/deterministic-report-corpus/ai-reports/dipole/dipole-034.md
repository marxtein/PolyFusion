# 1. 核心结论

本次 0-D 初筛对应 `config=dipole`，`preset=Dipole-DD`。当前运行点结果为：`valid=1.0`、`ignited=0.0`、`Pfus=1.1105709670138484`、`Qfus=0.32375425954001874`、`Pheat=3.430289901333553`、`Pwall=0.0036135022654502254`。

对“当前运行点”和“POPCON 扫描”需要分开看：
- 当前运行点：给出的单点解是有效的（`valid=1.0`），但未点火（`ignited=0.0`）。
- POPCON 扫描：扫描轴为 `xkey=Ti0`、`ykey=n0`；`n_invalid=0`，但 `best` 矩阵全为 0，因此**当前准则下未找到最佳区**。

另外，`use_tauE=1.0` 且 `tauE=5.0`，说明这组结果**依赖固定约束时间假设**。

# 2. 关键指标解读

当前运行点方面：
- 聚变与增益：`Pfus=1.1105709670138484`，`Qfus=0.32375425954001874`，对应的是一个有效但未点火的单点结果。
- 加热与功率平衡：`Pheat=3.430289901333553`，`Ptrans=3.6166917957205884`。从数值上看，传输功率高于外加热功率。
- 辐射与损失：`Pbrem=0.5298778539574985`、`Pcycl=0.021695418860632903`、`P_line=0.0`。在已给出的辐射项里，轫致辐射高于回旋辐射，线辐射被设为 0。
- 壁负荷：`Pwall=0.0036135022654502254`，中子功率为 `Pn=0.37259579980868174`。
- 约束设定：`tau_E=5.0`，且 `use_tauE=1.0`，因此这里不是由经验缩放自洽求出的约束时间，而是固定假设输入。
- 成分：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。这表示当前算例里没有给出 He 灰分或杂质分数的非零输入。
- 几何：`Vp=1890.303723225459`、`Sw=1256.6370614359173`，并使用 `ring_model=1`。输入里未给出 `geom_model` 字段，因此不能进一步判断几何模型分级。

POPCON 扫描方面：
- 已知扫描对象是 `Ti0` 与 `n0`。
- `best` 矩阵已提供，且所有元素均为 0，所以只能得出“当前准则下未找到最佳区”。
- 虽然 `best` 矩阵存在，但未提供 `x/y` 数组，也未给出 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

# 3. 风险与不确定性

- 最直接的不确定性来自约束时间：`use_tauE=1.0`、`tauE=5.0`，结果对固定约束时间假设敏感。
- 当前运行点“有效”不等于扫描中“存在工作窗”。这里单点 `valid=1.0`，但扫描 `best` 全零，这两件事不能混同。
- POPCON 证据有限：缺少 `x/y` 数组以及 `nx/ny`，所以不能判断最佳区位置、边界、连通性或参数敏感方向。
- `ignited=0.0`，说明该单点结果不是点火态；不能把当前结果外推成更强结论。
- 辐射项里 `P_line=0.0`，且 `fimp=0.0`、`fHe=0.0`，这使得成分与辐射相关结论更依赖当前输入设定，外推空间有限。
- 输入中未提供 `geom_model`、`H98/H_ITPA20/H_ISS04`、`betaN/q95/nbar_o_nGw` 等字段，相关边界与经验约束无法展开判断。

# 4. 下一步建议

1. 重新执行 `Ti0-n0` 二维扫描，并显式输出 `x`、`y`、`nx`、`ny` 和 `best_fraction`。  
需监控指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`、`n_invalid`、`best`。

2. 在当前点附近做 `tauE` 敏感性扫描，而不是只固定 `tauE=5.0`。  
需监控指标：`Pfus`、`Qfus`、`ignited`、`Pheat`、`Ptrans`、`Pbrem`、`Pcycl`。

3. 对成分与辐射设定做参数扫描，至少覆盖 `fimp`、`fHe`、`Zeff` 或等效输入变化。  
需监控指标：`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`、`valid`。

4. 补充几何与模型元数据输出，特别是 `geom_model` 及相关几何修正量。  
需监控指标：`Vp`、`Sw`、`Pwall`，以及不同几何设定下的 `Pfus`、`Qfus`、`valid`。
