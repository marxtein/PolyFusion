## 1. 核心结论

本次 0-D 初筛对应 `config=dipole`，`preset=Dipole-DD`。当前运行点给出的主要状态是：`valid=1.0`、`ignited=0.0`、`Pfus=4.3915`、`Qfus=0.1999`、`Pheat=21.9696`、`Pwall=0.0210`。

需要把“当前运行点”和“POPCON 扫描”分开看：
- 当前运行点：模型判定为 `valid=1.0`，说明这个单点计算有有效输出；但 `ignited=0.0`，表示该点未达到点火。
- POPCON 扫描：`best` 矩阵为 8x8 全 0，按要求应明确写为：**当前准则下未找到最佳区**。这不等同于“当前运行点无效”，只是说扫描准则下没有标出最佳单元。

另外，`use_tauE=1.0` 且 `tauE=5.0`，因此结果**依赖固定约束时间假设**。

## 2. 关键指标解读

当前运行点方面：
- 功率面：`Pfus=4.3915`，`Pheat=21.9696`，`Qfus=0.1999`。从数值关系看，当前融合输出相对外加热功率不高。
- 中子功率：`Pn=1.4734`。
- 损失项：`Pbrem=15.3633`，`Pcycl=0.0503`，`P_line=0.0`，`Ptrans=9.4741`。其中当前给出的辐射损失里，轫致辐射数值最大，线辐射为 0。
- 壁负荷：`Pwall=0.0210`。
- 约束时间：输出 `tau_E=5.0`，参数中 `use_tauE=1.0`、`tauE=5.0`，应理解为这次结果是基于固定能量约束时间输入，而不是由另一套约束时间标度自行求得。
- 成分：`Zeff=1.0`，参数中 `fHe=0.0`、`fimp=0.0`、`Zimp=10`，本次输入没有引入非零杂质份额。
- 几何量：`Vp=1890.3037`，`Sw=1256.6371`。同时参数与输出里都给出 `ring_model=1`。未看到 `geom_model` 字段，因此这里不额外推断几何模型复杂度。
- 其他状态量：`nbar=7.8823e19`，输入 `n0=2e21`，以及 `beta_in=0.9569`、`beta_out=0.2198`。这些值可作为后续扫描的监控量，但仅凭当前 JSON 不能外推边界裕度。

POPCON 扫描方面：
- 扫描坐标键为 `xkey=Ti0`、`ykey=n0`，说明本次扫的是中心离子温度和密度输入。
- `n_invalid=0`，表示扫描中没有被标成无效的网格点。
- 但 `best` 矩阵全为 0，所以只能说明在这套“best”判据下没有被选中的最佳单元；**不能据此把当前运行点 `valid=1.0` 解释成已经找到工作窗**。

## 3. 风险与不确定性

- **当前准则下未找到最佳区**。这意味着扫描结果没有给出被“best”判据选中的区域。
- **扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。原因是 JSON 里没有给出 `nx`、`ny`，也没有提供对应的 `x/y` 数组坐标；虽然有 `best` 矩阵，但缺少足够网格定义，不能描述最佳区位置、边界或随参数变化的趋势。
- 结果**依赖固定约束时间假设**：`use_tauE=1.0`、`tauE=5.0`。因此对 `Pfus`、`Qfus`、功率平衡和扫描判据的解读，都受这个固定 `tauE` 设定影响。
- `ignited=0.0`，所以当前点不能按点火状态解读。
- `P_line=0.0` 且 `fimp=0.0`，说明当前成分设定较理想化；若后续加入杂质或氦灰，辐射分配和功率平衡可能变化。
- 未提供 `H98`、`H_ITPA20`、`H_ISS04`、`LH_ratio`、`geom_model` 等字段，本次不能对约束标度比较、L-H 阈值比较或几何简化程度做进一步判断。

## 4. 下一步建议

1. 重新执行 `Ti0-n0` 二维扫描，并输出完整 `x`、`y` 数组以及 `nx`、`ny`。  
需要监控的指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Pbrem`、`Ptrans`。

2. 在固定其他参数的前提下，对 `tauE` 做参数扫描，而不是只取 `tauE=5.0` 单值。  
需要监控的指标：`Pfus`、`Qfus`、`ignited`、`Pheat`、`Pbrem`、`Pwall`、`tau_E`。

3. 对成分假设做验证扫描，至少改变 `fHe` 和 `fimp`，检查零杂质输入是否主导了当前结果。  
需要监控的指标：`Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`valid`。

4. 保留 `xkey=Ti0`、`ykey=n0` 的同时，补做单参数扫描，分别围绕当前 `Ti0=18.5714` 和 `n0=2e21` 展开。  
需要监控的指标：`Pfus`、`Qfus`、`Ptrans`、`Pwall`、`beta_in`、`beta_out`、`nbar`。
