## 1. 核心结论

- `config`: `dipole`（`config_label`: 偶极场 Dipole）
- `preset`: `Dipole-DD`
- 当前运行点 `valid = 1.0`
- 当前运行点 `ignited = 0.0`
- `Pfus = 39.6876`
- `Qfus = 5.1248`
- `Pheat = 7.7443`
- `Pwall = 0.03775`

对当前运行点的直接解读是：这一次 0-D 计算给出了一个“有效”的单点解，但该点**未点火**（`ignited=0.0`）。同时，POPCON 扫描部分给出的 `best` 矩阵全为 0，因此**当前准则下未找到最佳区**。

需要把两件事分开看：
- `valid=1.0` 只说明这个**当前运行点**通过了程序内部有效性检查。
- `best` 全 0 则说明这次 **POPCON 扫描** 在现有判据下没有标出最佳区；这**不能**等同于“当前运行点无效”，也**不能**等同于“扫描已经证明存在工作窗”。

此外，`use_tauE=1.0`，且 `tauE=5.0`、输出中 `tau_E=5.0`，说明这组结果**依赖固定约束时间假设**。

## 2. 关键指标解读

当前运行点：
- 聚变与加热：`Pfus=39.6876`，`Qfus=5.1248`，`Pheat=7.7443`。就这组输出而言，程序给出了正的聚变功率和大于 1 的 `Qfus`，但 `ignited=0.0`，因此不能把该点表述为点火状态。
- 损失与功率分项：`Ptrans=22.5937`，`Pbrem=11.4762`，`Pcycl=0.0468`，`P_line=0.0`，`Pn=13.3152`。在给出的辐射项里，轫致辐射高于回旋辐射，线辐射在该次计算中为 0。
- 壁负荷：`Pwall=0.03775`，对应壁面积 `Sw=1256.6371`。
- 约束相关：输出 `tau_E=5.0`，输入 `use_tauE=1.0`、`tauE=5.0`，说明本次不是从约束标度自洽求得，而是使用固定值。
- 密度与成分：`nbar=6.8125e19`，`ne0=1.7286e21`；`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。就输入而言，本次杂质和氦灰分数均取 0。
- 几何与模型：`Vp=1890.3037`，`Sw=1256.6371`；`ring_model=1`，`cyclotron_model="equatorial_shell_proxy"`。

POPCON 扫描：
- 扫描轴是 `xkey="Ti0"`、`ykey="n0"`。
- `n_invalid=0`，表示扫描中没有被计为无效的网格点。
- 但 `best` 矩阵全为 0，因此**当前准则下未找到最佳区**。
- 同时，输入里没有提供 `x`/`y` 数组，`nx`/`ny` 也未给出，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

## 3. 风险与不确定性

- 最主要的不确定性来自 `use_tauE=1.0`：结果**依赖固定约束时间假设**，因此对 `Pfus`、`Qfus`、`ignited` 的判读都受 `tauE=5.0` 的设定影响。
- 当前运行点 `valid=1.0`，但 `ignited=0.0`；这说明“单点可算”与“达到点火”不是一回事。
- POPCON 部分虽然有 `best` 矩阵，但矩阵全 0，且缺少 `x`/`y` 数组及 `nx`/`ny`，所以不能据此描述最佳区的位置、边界、范围，亦不能描述随 `Ti0` 或 `n0` 变化的趋势。
- `n_invalid=0` 只说明没有无效点记录，不代表扫描找到了合格区。
- 本次是 `D-D` 工况（`strcase="D-D"`），结论仅对应这次输入，不应外推到其他燃料、其他约束假设或其他几何设定。

## 4. 下一步建议

1. 重新做 `Ti0`-`n0` 二维扫描，并在输出中显式保存 `x`、`y`、`nx`、`ny` 与 `best_fraction`。  
需要监控的指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best`。

2. 围绕当前点 `Ti0=86.4286`、`n0=1.7286e21` 做局部加密扫描。  
需要监控的指标：`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pcycl`、`Pwall`，以及 `best` 是否仍全 0。

3. 做 `tauE` 敏感性扫描，因为本次 `use_tauE=1.0`。  
建议改变固定 `tauE` 后重复计算当前点和 POPCON。需要监控的指标：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`。

4. 做成分与辐射校验扫描，重点检查 `fHe`、`fimp`、`Zeff` 变化对辐射项的影响。  
需要监控的指标：`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。
