1. 核心结论
- `config=dipole`，`preset=Dipole-DD`；当前运行点 `valid=1.0`、`ignited=0.0`、`Pfus=4.361685817492684`、`Qfus=0.430428302940033`、`Pheat=10.133362020341751`、`Pwall=0.0115347925687242`。
- 就“当前运行点”而言，这是一组已通过模型内部有效性判定的结果（`valid=1.0`），但未达到点火（`ignited=0.0`）；同时 `Qfus<1`，说明聚变输出低于外加加热量级。
- 就“POPCON 扫描”而言，不能把运行点有效直接等同于扫描中找到了可用工作窗。此次扫描仅给出 `xkey=Ti0`、`ykey=n0`、`n_invalid=0`，且 `best` 矩阵全为 0，因此当前准则下未找到最佳区。
- 由于扫描缺少 `x/y` 网格数组，且未显式给出 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0` 且 `tauE=5.0`，说明本次结果依赖固定约束时间假设；这会直接影响对 `Qfus`、`Pfus`、功率平衡和扫描判定的解读。

2. 关键指标解读
- 运行点功率平衡：`Pfus=4.3617`，其中中子功率 `Pn=1.4633`；总加热功率 `Pheat=10.1334`，传输损失 `Ptrans=7.5930`。在这个 0-D 点上，系统需要外部加热支撑，且未表现为点火态。
- 辐射与损失：`Pbrem=5.3999` 明显高于 `Pcycl=0.03876`，`P_line=0.0`。按给定输入，线辐射未贡献损失，当前辐射损失主要来自韧致辐射，回旋辐射较小。
- 壁负荷与几何相关量：`Pwall=0.01153`，配合 `Sw=1256.6371`、`Vp=1890.3037`，说明本次结果中单位壁面承载并不高；但这仍只是当前运行点的 0-D 输出，不能外推为空间分布或局部热点结论。
- 约束与时间尺度：`tau_E=5.0`、`tauC_eff=375.6651`、`tau_eq_ie=0.08640`。其中 `use_tauE=1.0` 表明能量约束时间不是由缩放律在线求得，而是固定输入值；因此当前输出对该假设敏感。
- 成分设定：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。这意味着本次计算采用了非常“干净”的等离子体成分设定，杂质和氦灰未实际占比进入主成分；对应的辐射和稀释效应解读应限定在该设定内。
- POPCON 信息：扫描维度标签为 `Ti0` 与 `n0`，`n_invalid=0` 说明网格点没有被标记为无效；但 `best` 矩阵全零，只能说明在当前判据下没有点被标记为“最佳”，不能反推存在隐藏工作窗。

3. 风险与不确定性
- 最大的不确定性来自固定约束时间假设：`use_tauE=1.0`、`tauE=5.0`。若 `tau_E` 取值变化，`Pfus`、`Qfus`、`Ptrans` 及是否接近点火的判断都可能明显变化。
- 扫描结论有限：虽然给出了 `best` 矩阵，但缺少 `x/y` 网格数组且未明确 `nx/ny`，因此不能判断最佳区位置、边界、连通性，也不能判断参数敏感性是单调还是非单调。
- 当前准则下未找到最佳区，但这不等于“绝对不存在更优点”；它只说明在本次扫描对象、扫描范围和判据输出中，没有被 `best` 标记出来的区域。
- 运行点有效不等于扫描找到工作窗：`valid=1.0` 只说明当前单点计算成立；它不能替代 POPCON 对参数空间的覆盖结论。
- 成分与辐射假设偏理想化：`Zeff=1.0`、`fimp=0.0`、`fHe=0.0`、`P_line=0.0` 会压缩杂质辐射和燃料稀释相关不确定性；若后续引入非零杂质或氦灰，结果可能发生变化。
- 几何模型字段 `geom_model` 未提供，因此无法判断几何建模是简化还是更完整；与几何细节相关的工程外推应谨慎。

4. 下一步建议
- 开展显式 POPCON 复扫：围绕 `xkey=Ti0`、`ykey=n0` 重新输出完整 `x/y` 网格数组，并记录 `nx/ny`、`best`、`n_invalid`。重点监控 `best` 是否仍全零、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`。
- 做约束时间敏感性扫描：在保持其余输入不变时，对 `tauE` 做多点扫描，验证固定 `tau_E` 假设对结果的影响。重点监控 `Qfus`、`Pfus`、`ignited`、`Ptrans`、`Pbrem`。
- 做成分/辐射敏感性扫描：逐步改变 `fimp`、`fHe`，必要时联动 `Zeff`，检查理想化成分设定是否主导了当前结论。重点监控 `Pbrem`、`P_line`、`Pfus`、`Qfus`、`Pheat`。
- 做运行点邻域验证：以当前 `Ti0=32.14285714285714`、`n0=1.1857142857142858e+21` 为中心做小范围局部扫描，确认单点 `valid=1.0` 是否在邻域内连续保持。重点监控 `valid`、`n_invalid`、`Pfus`、`Qfus`、`Pwall` 的变化。
- 补充模型元数据导出：下次计算建议同时导出 `tauE_scaling`、几何模型标识、以及扫描判优准则说明。重点是让“单点有效性”和“扫描判优失败”的原因可以被拆分验证。
