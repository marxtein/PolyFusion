# 1. 核心结论

- `config=dipole`，`preset=Dipole-DD`
- 当前运行点标记：`valid=1.0`，`ignited=0.0`
- 关键输出：`Pfus=0.0003372030102274033`，`Qfus=0.0009459459563897893`，`Pheat=0.35647174973329493`，`Pwall=0.00028393954284287035`

就当前单个运行点看，模型将该点标记为 `valid=1.0`，但 `ignited=0.0`，且 `Pfus`、`Qfus` 相对 `Pheat` 很低，说明该点主要仍由外加加热支撑。

POPCON 扫描需要单独看，不能和单点 `valid` 混为一谈。本次扫描给出 `xkey=Ti0`、`ykey=n0`，`best` 为 `8x8` 全零，`n_invalid=0`，因此**当前准则下未找到最佳区**。同时，结果里没有 `nx/ny`，也没有 `x/y` 数组，所以**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

# 2. 关键指标解读

- 约束与约定：`use_tauE=1.0`，`tauE=5.0`，输出 `tau_E=5.0`。这意味着结果**依赖固定约束时间假设**，不是基于此处可见的约束时间标度自洽求得。`H98/H_ITPA20/H_ISS04` 或 `tauE_scaling` 未提供，无法做标度对照。
- 运行点能量分配：`Ptrans=0.3070303152985253`，`Pbrem=0.03840830598212638`，`Pcycl=0.011257200083584353`，`P_line=0.0`，`Pn=0.00011313137928627998`。已给出的辐射项中，线辐射为零，主要可见损失项是输运、轫致辐射和回旋辐射。
- 成分与辐射：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。在当前输入下没有杂质份额，且输出 `P_line=0.0`，因此这次结果对杂质线辐射的覆盖很有限。
- 密度与温度：参数中 `Ti0=5.0`，`Te0=20.0`，`n0=1e+20`；输出中 `nbar=3.941141225673108e+18`，`ne0=1e+20`。这些量说明当前扫描轴与单点参数是围绕温度、密度在看，但扫描坐标值本身未导出。
- 几何与磁场：`Vp=1890.303723225459`，`Sw=1256.6370614359173`，`B_in=5.697503605448386`，`B_out=0.010115158710444893`，`L_in=1.5`，`ring_model=1.0`，`cyclotron_model=equatorial_shell_proxy`。这些是本次 0-D 结果成立所依赖的几何/磁场背景。
- 边界类指标：仅见 `beta_in=0.031011828843380246`、`beta_out=0.0071240815872776145`。`betaN/betaT/betap`、`q/q95`、`nbar_o_nGw`、`nbar_o_Sudo` 未提供，不能据此判断这些边界的接近程度。

# 3. 风险与不确定性

- `valid=1.0` 只对应当前单点，不等于扫描已经找到可用工作窗；本次 `best` 全零，二者必须分开解读。
- `use_tauE=1.0` 表明结果依赖固定 `tauE=5.0` 假设，对约束时间假设敏感；这里没有标度律对照信息。
- 虽然 `best` 矩阵存在，但 `nx/ny`、`x/y` 数组缺失，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `best` 判据本身未随结果一起说明，只能确认“未被标为最佳”，不能进一步解释是被哪一条准则卡住。
- `geom_model` 字段未提供，因此无法判断是否使用了更简化或更完整的几何模型。
- 若只看当前输入，`fimp=0.0`、`P_line=0.0` 会弱化对杂质辐射影响的检验；这属于当前案例覆盖范围有限，不应外推。

# 4. 下一步建议

- 重新执行 `Ti0-n0` 二维 POPCON 扫描，并显式导出 `nx`、`ny`、`x`、`y`、`best` 判据定义。重点监控：`best` 矩阵、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕当前 `tauE=5.0` 做约束时间敏感性扫描；由于 `use_tauE=1.0`，这是优先验证项。重点监控：`tau_E`、`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pcycl`、`Pwall`、`valid`、`ignited`。
- 做成分/辐射验证扫描，至少改变 `fimp`、`Zimp`，并可联动 `fHe`。重点监控：`Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`。
- 做几何/磁场参数敏感性扫描，优先检查 `B_ring`、`R_p`、`Rw`、`r_ring`、`L_in_fac`。重点监控：`B_in`、`B_out`、`Vp`、`Sw`、`Pwall`、`U_ratio`、`valid`。
- 在单点复算中保留当前点 `Ti0=5.0`、`n0=1e+20` 作为基准，并逐次只改一个参数，建立“哪一项改变会让 `best` 从全零转为非零”的证据链。重点监控：`best` 标记是否出现、`n_invalid` 是否上升，以及 `Pfus/Qfus/Pheat/Pwall` 的同步变化。
