1. 核心结论
- 当前运行点：`config=dipole`（偶极场 Dipole），`preset=Dipole-DD`，工况为 `D-D`；`valid=1.0`，`ignited=0.0`，`Pfus=0.1348812040909613`，`Qfus=0.0062838780601481925`，`Pheat=21.464643775691027`，`Pwall=0.017188355844845873`。
- 这表示当前单个运行点在模型中有效，但未点火；同时 `Pfus` 远小于 `Pheat`，对应的 `Qfus` 也很低。
- POPCON 扫描需要单独看：扫描轴为 `xkey=Ti0`、`ykey=n0`，`best` 矩阵为 8x8 且全为 0，`n_invalid=0`。当前准则下未找到最佳区。
- 当前运行点 `valid=1.0` 不等于扫描中已经找到可选工作窗；这里“单点有效”和“扫描出现最佳区”是两个不同判断。
- 同时，`x/y` 网格数组以及 `nx/ny` 未提供，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 功率收支：当前点 `Pheat=21.4646`，`Pfus=0.1349`，`Qfus=0.00628`；已给出的损失/去向包括 `Pbrem=15.3633`、`Pcycl=0.05034`、`P_line=0.0`、`Ptrans=6.1406`、`Pn=0.04525`。在已提供的辐射项中，`Pbrem` 最大。
- 约束与输运：`use_tauE=1.0`、`tau_E=5.0`，说明结果依赖固定约束时间假设；`tauC_eff=487.8944` 已给出，但 `tauE_scaling`、`H98`、`H_ITPA20`、`H_ISS04` 未提供，不能判断与经验约束标度的一致性。
- 成分与辐射：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`，且 `P_line=0.0`；按当前输入，杂质和氦灰贡献被设得很低，当前已显示的辐射主要来自 `Pbrem`，不是线辐射。
- 边界相关：已给出 `beta_in=0.6202`、`beta_out=0.1425`、`nbar=7.882e19`，但 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw`、`nbar_o_Sudo` 未提供，不能按常见边界指标判断离限制有多近。
- 几何与模型：`Vp=1890.3037`、`Sw=1256.6371`、`ring_model=1.0`，回旋辐射模型为 `equatorial_shell_proxy`；`geom_model` 未给出，因此这里只能按当前几何输出解读，不能进一步判断几何简化程度。
- 扫描信息：`n_invalid=0` 只说明扫描结果里没有被标成无效的格点；它不表示已经出现“最佳区”，因为 `best` 矩阵仍然全为 0。

3. 风险与不确定性
- `valid=1.0` 只说明当前运行点被模型接受；它不等于“扫描找到工作窗”。本次结果里单点有效与 `best` 全 0 同时出现，说明两者判据并不相同。
- 由于 `use_tauE=1.0`，结果对固定 `tau_E=5.0` 的假设敏感；如果约束时间改动，`Pfus`、`Qfus`、`Ptrans` 以及是否出现 `best` 区都可能变化。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。虽然 `best` 矩阵已提供，但 `x/y` 网格数组和 `nx/ny` 缺失，无法定位网格范围、步长和边界。
- 边界与稳定性证据不完整：缺少 `q/q95`、标准 beta 指标、密度极限比值及 H 因子，不能从这份 0-D 数据继续判断更细的运行边界。
- `P_ei=-359.6348`、`U_ratio=4832.0719`、`Ecrit`/`Eth` 等量虽然已输出，但没有附带判据或定义说明；只能记录其数值，不能单独据此下额外结论。

4. 下一步建议
- 重新导出一次 `Ti0-n0` POPCON 扫描，补齐 `x`、`y`、`nx`、`ny` 与当前 `best` 判据；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，先把“单点有效”和“扫描无最佳区”分开核实。
- 围绕当前运行点 `Ti0=5.0`、`n0=2e21` 做局部加密双参数扫描；重点监控 `Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pwall`、`valid`、`ignited`，检查是否只是粗网格下没有触发 `best` 判据。
- 对 `tauE` 做敏感性扫描，或在可用时对比固定 `tauE` 与约束时间标度模式；重点监控 `tau_E`、`tauC_eff`、`Pfus`、`Qfus`、`Ptrans`、`best`，因为当前结果依赖固定约束时间假设。
- 做成分/辐射验证扫描，例如变化 `fimp`、`fHe`、`Zimp`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pheat`、`Pwall`、`valid`，确认当前“`P_line=0` 且 `Zeff=1.0`”设定对结果的影响。
