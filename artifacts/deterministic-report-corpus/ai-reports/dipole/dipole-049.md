1. 核心结论
- 本次工况 `config=dipole`，`preset=Dipole-DD`；当前运行点 `valid=1`、`ignited=0`，输出为 `Pfus=0.13282508869769483`、`Qfus=0.10471241662584158`、`Pheat=1.268475057473895`、`Pwall=0.00111511922509302`。
- 这表示当前 0-D 运行点在模型内被判为有效，但未达到点火；同时 `Qfus<1`，聚变功率相对外加加热功率仍较低。
- 当前结果依赖固定约束时间假设：`use_tauE=1`，且本次采用 `tau_E=5.0`；因此对能量约束的判读不能脱离该假设单独外推。
- POPCON 扫描与当前运行点需要分开看：当前运行点“有效”不等于扫描中“找到了工作窗”。
- 本次扫描中 `best` 矩阵全为 0，因此当前准则下未找到最佳区。
- 扫描数据缺少 `nx`/`ny` 以及 `x`/`y` 数组坐标证据，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点功率收支：`Pheat=1.268475057473895`，`Pfus=0.13282508869769483`，`Qfus=0.10471241662584158`；按这组结果看，聚变输出存在但对总加热的放大有限。
- 点火状态：`ignited=0`，说明在该组 0-D 假设下，当前点并未表现为自持点火工况。
- 壁负荷与中子功率：`Pwall=0.00111511922509302`、`Pn=0.044562726406442765`；可作为后续比较不同扫描点时的壁面与中子相关负担指标。
- 辐射与损失项：`Pbrem=0.03840830598212638`、`Pcycl=0.011257200083584353`、`P_line=0.0`，且 `Ptrans=1.3070719136994362`；当前给出的线辐射为 0，但输运功率项和其他辐射项仍构成总损失的重要部分。
- 约束时间相关：`tau_E=5.0`、`tauC_eff=109.0965117502878`、`ntau=5e+20`；其中最需要强调的是本次结果直接依赖固定 `tau_E` 输入，而不是由某个约束标度自洽求得。
- 成分与杂质：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；本次输入对应无 He 灰分、无杂质份额的理想化成分设定，因此辐射和约束判读对成分扰动可能较敏感。
- 几何量：`Vp=1890.303723225459`、`Sw=1256.6370614359173`；这些量会影响体积功率密度和壁负荷换算，但本次未提供更完整的几何模型标签用于细分解释。
- POPCON 扫描信息：扫描轴为 `xkey=Ti0`、`ykey=n0`，`n_invalid=0`；这只能说明已评估点中未记录无效点，不代表存在满足“最佳”判据的区域。由于 `best` 全零，当前准则下未找到最佳区。

3. 风险与不确定性
- 最大的不确定性来自固定约束时间假设：`use_tauE=1` 且 `tau_E=5.0`，结果对该单一设定敏感，不能据此判断在其他约束水平下是否仍保持同样结论。
- 扫描结论有限：虽然有 `best` 矩阵和 `xkey/ykey`，但缺少 `nx`/`ny` 及 `x`/`y` 数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- “运行点有效”和“扫描找到最佳区”不是同一件事：本次 `valid=1` 只说明当前点在模型内可计算且满足其有效性条件，不代表在扫描平面上存在可接受的最佳区域。
- 成分设定较理想化：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0` 会弱化杂质和灰分带来的辐射/稀释影响；若后续引入非零杂质或 He 灰分，`Pfus`、`Qfus`、`P_line`、`Pheat` 可能变化。
- 本次未提供 L-H 阈值比较字段，如 `LH_ratio`；因此不能对 L-H 阈值模型比较做任何判读，更不能据此表述进入或维持某种约束模态。
- 未见更完整的边界类指标，如 `betaN`、`betaT`、`betap`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo`；因此对 MHD、密度边界或安全因子相关限制无法做进一步工程解读。

4. 下一步建议
- 执行 `Ti0-n0` 二维重扫，并补充保存完整扫描网格坐标与尺寸信息：至少输出 `x`、`y`、`nx`、`ny`、`best`。重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`。
- 在固定其他参数的前提下，对 `tauE` 做参数扫描，直接检验固定约束时间假设的敏感性。重点监控 `Qfus`、`Pfus`、`ignited`、`Ptrans`、`Pbrem`、`Pcycl`、`Pwall`。
- 对成分参数做独立扫描：至少扫描 `fHe`、`fimp`，必要时联动 `Zimp`。重点监控 `Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pheat`。
- 保持当前 `preset` 与 `config` 不变，围绕当前运行点做局部加密扫描，而不是只看单点输出。建议优先在当前 `Ti0=86.42857142857143`、`n0=1e+20` 附近加密。重点监控 `best` 是否仍全零，以及 `valid` 与 `Qfus` 的局部变化。
- 若后续工具支持，请补充边界类与阈值比较类输出字段，再做二次判读。建议新增监控 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`、以及 `LH_ratio` 这类“比较指标”，但对 `LH_ratio` 仅做阈值模型比较，不把它解释为保证进入或维持 H 模。
