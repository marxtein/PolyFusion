1. 核心结论
- 本次结果对应 `config=dipole`、`preset=Dipole-DD`。
- 当前运行点为单点结果：`valid=1.0`、`ignited=0.0`、`Pfus=31.85938553232726`、`Qfus=3.1653435471696043`、`Pheat=10.065064046781075`、`Pwall=0.03336241693461011`。
- `valid=1.0` 说明当前运行点被标记为有效，`ignited=0.0` 说明当前运行点未被标记为点火；这只反映该单点状态，不等同于 POPCON 扫描已经找到可用工作窗。
- POPCON 扫描信息显示扫描轴为 `xkey=Ti0`、`ykey=n0`，且 `n_invalid=0`；但 `best` 矩阵全为 0，因此“当前准则下未找到最佳区”。
- 本次扫描未提供 `nx/ny`，也未提供 `x/y` 扫描数组，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。

2. 关键指标解读
- 功率相关量上，已报告 `Pfus=31.85938553232726`、`Qfus=3.1653435471696043`、`Qfus_raw=3.1653435471696043`、`Pheat=10.065064046781075`、`Pn=10.688802054450425`、`Ptrans=19.712599427125728`、`P_ei=946.6486722952928`；这些是当前单点的输出值，不代表扫描区域内普遍表现。
- 辐射与成分方面，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；已报告辐射项为 `Pbrem=11.476245058863512`、`Pcycl=0.04680303866866527`、`P_line=0.0`，其中在已给出的辐射项里 `Pbrem` 明显高于另外两项。
- 约束与时间尺度方面，`tau_E=5.0`，且 `use_tauE=1.0`，因此结果依赖固定约束时间假设；同时给出了 `tauC_eff=453.5806612792675`、`tau_eq_ie=0.059267067412843585`，但 JSON 中未提供 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling` 字段，无法进一步比较不同约束标度。
- 几何与磁场方面，已报告 `Vp=1890.303723225459`、`Sw=1256.6370614359173`、`B_in=5.697503605448386`、`B_out=0.010115158710444893`、`L_in=1.5`，并使用 `ring_model=1.0`、`cyclotron_model=equatorial_shell_proxy`；这些说明当前结果受所选几何/回旋辐射模型设定影响。
- 状态与边界相关已给出 `nbar=6.812544118663514e+19`、`ne0=1.7285714285714284e+21`、`ntau=8.642857142857142e+21`、`beta_in=1.9910859906382499`、`beta_out=0.4573951157872525`；但未提供 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此边界接近程度无法按这些常用指标展开。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：当前结果建立在固定 `tau_E=5.0` 的前提上，因此 `Pfus`、`Qfus`、`Pheat` 等输出对约束时间假设的敏感性尚未被这份 JSON 单独证明。
- POPCON 方面，虽然给出了 `best` 矩阵和 `n_invalid=0`，但由于 `best` 全 0，必须据此写明“当前准则下未找到最佳区”；同时缺少 `nx/ny` 与 `x/y` 数组，故“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 成分设定较理想化：`fHe=0.0`、`fimp=0.0`、`Zeff=1.0`、`P_line=0.0`，意味着这份结果没有展示非零氦灰或杂质分数对线辐射、总辐射和输出功率的影响范围。
- 几何简化程度无法按 `geom_model` 字段判断，因为该字段未出现在 JSON 中；同时 `cyclotron_model=equatorial_shell_proxy` 表明至少回旋辐射部分依赖代理模型设定，模型选择本身会带来解释边界。
- 若按更完整工程判据评估，当前 JSON 还缺少若干常见约束字段，如 `betaN`、`q95`、`LH_ratio`、`nbar_o_nGw`；因此不能把这份 0-D 结果外推为更广义的运行窗口结论。

4. 下一步建议
- 重新执行 `Ti0`-`n0` 的 POPCON 扫描，并显式导出 `x`、`y`、`nx`、`ny`、`best_fraction`、`best`、`n_invalid`；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，先补足工作窗证据链。
- 围绕固定 `tau_E=5.0` 开展 `tauE` 敏感性扫描，或补充可报告的 `tauE` 标度对比；重点监控 `tau_E`、`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`valid`，用来判断当前单点对固定约束时间假设的依赖程度。
- 对成分参数做验证扫描，至少覆盖 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`，检验当前零杂质、零氦灰设定是否显著改变结果。
- 对几何/装置输入做一致性扫描或核对，例如 `B_ring`、`L_in_fac`、`R_p`、`r_ring`、`Rw`；重点监控 `Vp`、`Sw`、`B_in`、`B_out`、`Pcycl`、`Pwall`，确认当前结论是否对几何输入敏感。
