1. 核心结论
- `config=dipole`，`preset=Dipole-DD`，对应标签为“偶极场 Dipole”。
- 当前运行点：`valid=1.0`，`ignited=0.0`，`Pfus=0.0046520252023209115`，`Qfus=0.0027545025937665817`，`Pheat=1.6888803128551808`，`Pwall=0.0013476702144391308`。就这一个运行点而言，它被判为有效，但未点燃；同时数值上 `Pheat` 明显高于 `Pfus`。
- 该结果依赖固定约束时间假设：`use_tauE=1.0`，输入 `tauE=5.0`，输出 `tau_E=5.0`。
- POPCON 扫描需要与当前运行点分开看：本次扫描轴是 `xkey=Ti0`、`ykey=n0`，并给出 `n_invalid=0`；但 `best` 矩阵为 8x8 全 0，因此“当前准则下未找到最佳区”。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是本 JSON 未提供 `x/y` 数组，也未显式提供 `nx/ny`，因此不能把“当前运行点有效”直接等同为“扫描已找到工作窗”。

2. 关键指标解读
- 功率分解上，当前可见项包括 `Ptrans=1.1403983139659513`、`Pbrem=0.5298778539574985`、`Pcycl=0.021695418860632903`、`P_line=0.0`、`Pn=0.0015607512734188832`；从数值对比看，本次输出里外加/总加热侧的量级明显大于聚变功率 `Pfus`。
- 约束与时间尺度方面，已知 `tau_E=5.0`、`tauC_eff=210.25605844102762`、`tau_eq_ie=0.27581981372900277`、`ntau=1.8571428571428574e+21`；但 JSON 未提供 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling`，因此无法比较不同经验约束标度的差异。
- 成分与辐射方面，`Zeff=1.0`，输入 `fHe=0.0`、`fimp=0.0`、`Zimp=10`；对应本次结果中 `P_line=0.0`，而轫致辐射 `Pbrem` 与回旋辐射 `Pcycl` 为非零。
- 几何/体量相关输出包括 `Vp=1890.303723225459`、`Sw=1256.6370614359173`、`L_in=1.5`，磁场给出 `B_in=5.697503605448386`、`B_out=0.010115158710444893`；模型标签还包含 `ring_model=1.0` 与 `cyclotron_model=equatorial_shell_proxy`。
- 边界类可见量中，本次仅直接给出 `beta_in=0.11518679284684093`、`beta_out=0.02646087446703115`，以及密度相关 `nbar=1.4638524552500115e+19`、`ne0=3.7142857142857145e+20`；未见 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`。

3. 风险与不确定性
- 最主要的不确定性来自固定约束时间：`use_tauE=1` 使本次结果直接依赖 `tauE=5.0` 的设定；在缺少 `tauE_scaling` 或 H 因子信息时，不能判断改用其他约束时间模型后 `Pfus`、`Qfus`、`Pwall` 会如何变化。
- `valid=1.0` 只说明这个单点通过了当前有效性判定，不代表在 `Ti0-n0` 扫描平面上已经存在可选工作窗，更不代表存在最佳区；这两者必须分开解读。
- 虽然扫描结果给出了 `best` 矩阵和 `n_invalid=0`，但由于缺少 `x/y` 数组以及 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势；也不能可靠描述最佳区位置、边界或朝哪个方向扩展。
- 当前输入 `fimp=0.0` 且结果 `P_line=0.0`，说明这组输入没有覆盖非零杂质分数下的线辐射影响；因此对辐射敏感性的认识仍不完整。
- 本 JSON 未提供 `LH_ratio`，也未给出可直接用于模式比较的完整阈值判据输出，因此不能把任何单个阈值相关量表述成进入或维持某种约束模式的保证。

4. 下一步建议
- 先补全一次 `Ti0-n0` 的 POPCON 扫描输出，显式保存 `x`、`y`、`nx`、`ny`、`best` 和若有的 `best_fraction`；重点监控 `valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，用来区分“单点有效”与“扫描面上是否出现候选区域”。
- 单独做 `tauE` 敏感性扫描，或将 `tauE` 与 `Ti0/n0` 组成二维扫描，直接检验固定约束时间假设对结果的影响；重点监控 `tau_E`、`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pwall`、`valid`。
- 做成分/辐射参数扫描，优先扫 `fimp`、`Zimp`、`Zeff`，必要时补扫 `fHe`；重点监控 `P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`、`valid`。
- 做几何与场参数的验证扫描，例如 `B_ring`、`L_in_fac`、`R_p`、`r_ring`；重点监控 `B_in`、`B_out`、`Vp`、`Sw`、`beta_in`、`beta_out`、`Pfus`、`Pwall`、`valid`。
