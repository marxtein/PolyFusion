1. 核心结论
- 本次结果对应 `config=dipole`（`config_label=偶极场 Dipole`），`preset=Dipole-DD`；当前运行点 `valid=1.0`、`ignited=0.0`，`Pfus=9.26976949541448`，`Qfus=0.5641063578354646`，`Pheat=16.432662682589786`，`Pwall=0.02045334565306784`。
- 就当前运行点看，模型给出了有效解（`valid=1`），但未点火（`ignited=0`）；同时 `Qfus` 数值低于 1，且 `Pheat` 大于 `Pfus`，说明该点的聚变输出未超过加热输入。
- POPCON 扫描需要与单点运行分开理解：当前运行点“有效”不等于扫描中“找到了最佳区”。本次 `last_scan` 的扫描轴为 `xkey=Ti0`、`ykey=n0`，但 `best` 为 `8x8` 全 0 矩阵，因此当前准则下未找到最佳区。
- `n_invalid=0` 只能说明扫描结果中记录的无效点计数为 0，不能据此推断存在最佳运行区域，也不能替代对 `best` 判据结果的判断。
- 虽然给出了 `best` 矩阵，但缺少 `x/y` 数组以及 `nx/ny` 元数据；扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 功率平衡方面：`Pfus=9.26976949541448`，其中 `Pn=3.110001325239947`；总加热功率 `Pheat=16.432662682589786`，输运功率 `Ptrans=11.069382755232137`。辐射/损失项中，`Pbrem=11.476245058863512` 明显存在，`Pcycl=0.04680303866866527` 较小，`P_line=0.0`。
- 约束假设方面：`use_tauE=1.0` 且 `tauE=5.0`，输出中 `tau_E=5.0`，说明结果依赖固定约束时间假设，而不是由约束时间标度自行给出；`use_tauC=0.0`，但仍给出了 `tauC_eff=453.5806612792675` 作为输出量。
- 等离子体与成分方面：工况为 `strcase=D-D`；`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，输入杂质电荷数 `Zimp=10`。在这组输入下，线辐射 `P_line=0.0`，更接近“未显式加入杂质/氦灰影响”的理想化成分设置。
- 密度相关量方面：输入中心密度 `n0=1.7285714285714284e+21`，输出给出 `ne0=1.7285714285714284e+21`、`nbar=6.812544118663514e+19`、`ntau=8.642857142857142e+21`。这些量可作为后续重新扫描 `Ti0-n0` 或做 `tauE` 敏感性分析时的基线。
- 几何与磁场方面：`Vp=1890.303723225459`，`Sw=1256.6370614359173`，`L_in=1.5`，`B_in=5.697503605448386`，`B_out=0.010115158710444893`；并使用了 `ring_model=1` 与 `cyclotron_model=equatorial_shell_proxy`。JSON 未提供 `geom_model`，因此不能据该字段判断几何模型是否为简化版。
- 稳定性/边界类字段方面：本 JSON 未提供 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`H98`、`H_ITPA20`、`H_ISS04` 等指标，因此无法直接解读这些边界或标度裕量。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1`：当前结果直接依赖固定 `tauE=5.0` 的假设，若约束时间设定变化，`Pfus`、`Qfus`、`ignited` 以及扫描判据结果都可能改变。
- `best` 矩阵全为 0，意味着在当前判据下没有任何扫描点被标记为“最佳”；但由于未提供“best”的具体判定条件，不能进一步判断是判据过严、扫描范围不合适，还是物理量组合本身不满足要求。
- 尽管 `n_invalid=0`，仍不能把它解释成“扫描找到了可用区域”；它只说明未记录到无效点，并不等同于出现最佳区，也不等同于性能对参数变化不敏感。
- 扫描元数据不完整：缺少 `x/y` 数组和 `nx/ny`，因此无法定位当前运行点在扫描平面中的相对位置，也无法判断 `Ti0`、`n0` 变化对结果的局部梯度、边界形状或趋势方向。
- 成分设置较理想化：`fHe=0`、`fimp=0`、`Zeff=1`、`P_line=0` 使当前结果缺少氦灰与杂质辐射扰动的检验，后续若引入非零杂质或氦灰，功率平衡和最佳区判据都可能变化。
- JSON 未提供 L-H 阈值比较量、`LH_ratio` 或相关模式边界信息，因此不能对 L-H 阈值比较结果做任何判断，更不能将其表述为进入或维持某种约束模式的保证。

4. 下一步建议
1. 重新执行 `Ti0-n0` 二维扫描，并显式导出 `x`、`y` 数组及 `nx/ny`；可先以当前点 `Ti0=32.14285714285714`、`n0=1.7285714285714284e+21` 为中心加密网格。需要监控：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Pbrem`、`Pcycl`、`Ptrans`。
2. 对 `tauE` 做敏感性扫描，因为当前 `use_tauE=1`；建议保持其余参数不变，单独改变 `tauE`，检查结果对固定约束时间假设的依赖程度。需要监控：`tau_E`、`Pfus`、`Qfus`、`ignited`、`Pheat`、`Ptrans`、`ntau`、`best`。
3. 做成分与辐射验证扫描，至少测试非零 `fHe` 或 `fimp` 的情况，并结合 `Zimp=10` 观察辐射项变化。需要监控：`Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`、`valid`。
4. 做几何/磁场输入的交叉验证扫描，例如围绕 `B_ring`、`L_in_fac`、`R_p`、`Rw`、`r_ring` 做有限扰动，确认 `Pwall`、`Pcycl` 与功率平衡对几何设定的敏感性。需要监控：`B_in`、`B_out`、`Vp`、`Sw`、`Pwall`、`Pcycl`、`Pfus`、`Qfus`。
5. 在后续每轮扫描中，把“单点是否有效（`valid`/`ignited`）”与“扫描是否出现 `best` 区”分别记录和汇报，避免将单点可解误读成扫描已找到最佳运行区域。需要监控：`valid`、`ignited`、`best`、`n_invalid` 以及对应扫描轴定义。
