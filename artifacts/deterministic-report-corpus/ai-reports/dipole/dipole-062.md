1. 核心结论
- `config=dipole`，`preset=Dipole-DD`。
- 当前运行点输出为：`valid=1.0`，`ignited=0.0`，`Pfus=33.723`，`Qfus=4.643`，`Pheat=7.264`，`Pwall=0.0326`。
- 就单点结果看，本次计算给出了一个 `valid=1.0` 的运行点，但 `ignited=0.0`，因此“当前运行点有效”不等于“已点火”，也不等于“扫描已找到工作窗”。
- POPCON 扫描信息显示：`xkey=Ti0`、`ykey=n0`、`n_invalid=0`；但 `best` 矩阵全为 0，当前准则下未找到最佳区。
- 本次扫描未提供 `x`/`y` 数组，且 `nx`/`ny` 未显式给出，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1` 且输出 `tau_E=5.0`，因此本次结果依赖固定约束时间假设。

2. 关键指标解读
- 运行点功率项中，`Pfus=33.723`，`Qfus=4.643`，`Pheat=7.264`；同时给出了 `Pn=11.314`、`Ptrans=21.475`、`Pbrem=8.155`、`Pcycl=0.043`、`P_line=0.0`，其中已列出的损失项里 `Ptrans` 数值高于 `Pbrem`、`Pcycl` 和 `P_line`。
- 壁面与几何相关输出为 `Pwall=0.0326`、`Sw=1256.637`、`Vp=1890.304`；这些是当前 0-D 结果中的壁负荷与体积/表面积量化信息。
- 约束与能量时间方面，参数中 `use_tauE=1`、`tauE=5.0`，输出中 `tau_E=5.0`、`tauC_eff=416.449`；本次并未提供 `H98`、`H_ITPA20`、`H_ISS04` 或其他缩放对比，因此不能据此比较不同约束标度。
- 边界/等离子体状态方面，给出了 `nbar=5.743e19`、`ne0=n0=1.457e21`、`ntau=7.286e21`、`beta_in=2.169`、`beta_out=0.498`；但 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 未提供，边界判读信息不完整。
- 成分与辐射方面，输入为 `fHe=0.0`、`fimp=0.0`、`Zimp=10`，输出为 `Zeff=1.0`；在这组输入下，结果给出 `P_line=0.0`，同时 `Pbrem=8.155`、`Pcycl=0.043`。
- POPCON 方面只能确认扫描维度键为 `Ti0` 与 `n0`，且 `n_invalid=0`；但由于没有提供 `x`/`y` 网格值，且 `best` 矩阵全 0，当前只能判断“未出现被标记为最佳的格点”，不能描述最佳区位置、边界或趋势。

3. 风险与不确定性
- 最重要的不确定性是：单点 `valid=1.0` 仅说明当前运行点在该模型判据下有效，不能替代 POPCON 工作窗证据；而本次 `best` 矩阵全 0，当前准则下未找到最佳区。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；原因是结果未提供 `x`/`y` 数组，且 `nx`/`ny` 未显式给出。
- 由于 `use_tauE=1`，本次结果依赖固定约束时间假设；因此 `Pfus`、`Qfus`、`Pheat`、`ignited` 等结论都对所设定的 `tau_E=5.0` 敏感。
- `ignited=0.0` 说明当前输出不是点火态；因此不能把本次结果解读为自持燃烧或更强结论。
- 若干常见边界与约束指标未提供，如 `q95`、`betaN`、`LH_ratio`、`nbar_o_nGw`、`nbar_o_Sudo`、`geom_model`；这限制了对边界裕量、L-H 阈值模型比较以及几何简化程度的判断。
- 当前成分输入为 `fimp=0.0`、`fHe=0.0`、`Zeff=1.0`，使辐射结果主要体现这组理想化成分设定；对杂质或氦灰敏感性的外推证据不足。

4. 下一步建议
- 执行一次完整的 `Ti0-n0` 二维 POPCON 重扫，并明确导出 `x`、`y`、`nx`、`ny`、`best`；重点监控 `best`、`valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，先补足“是否存在被判为最佳的网格点”这一证据。
- 在保持其他输入不变的前提下，对 `tauE` 做参数敏感性扫描，或补做“固定 `tauE`”与“若可用则按标度计算 `tauE`”的对照；重点监控 `tau_E`、`tauC_eff`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`valid`、`ignited`，以验证固定约束时间假设的影响。
- 对成分项做独立扫描，例如 `fimp`、`fHe` 或 `Zeff` 的扰动扫描；重点监控 `P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`、`valid`，确认当前“`P_line=0.0`”是否只对应于这组输入设定。
- 补充或复核几何/磁场相关输出的一致性检查，至少记录 `Vp`、`Sw`、`B_in`、`B_out`、`ring_model` 与对应扫描结果；若后处理支持，也建议补充 `geom_model` 字段，便于判断几何近似对结果解释的限制。
