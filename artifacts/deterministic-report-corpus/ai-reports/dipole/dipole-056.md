1. 核心结论
- 本次结果对应 `config=dipole`、`preset=Dipole-DD`；当前运行点 `valid=1.0`、`ignited=0.0`，报告值为 `Pfus=53.1300`、`Qfus=8.5006`、`Pheat=6.2502`、`Pwall=0.04725`。
- 对当前运行点的直接含义是：这个 0-D 单点计算给出了有效结果，但并未达到点火；“运行点有效”不等于“扫描已经找到满足准则的工作窗”。
- POPCON 扫描与运行点需分开看：扫描轴名给的是 `xkey=Ti0`、`ykey=n0`，`n_invalid=0`，但 `best` 矩阵为 8x8 且全为 0，因此“当前准则下未找到最佳区”。
- 由于扫描结果里未提供 `x/y` 数组，也未提供 `nx/ny`，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 该结果采用 `use_tauE=1`、`tauE=5.0`，因此结论依赖固定约束时间假设；这会直接影响对 `Pfus`、`Qfus` 和功率平衡的解读。

2. 关键指标解读
- 运行点功率侧：报告 `Pfus=53.1300`、`Qfus=8.5006`、`Pheat=6.2502`、`Pn=17.8251`、`Ptrans=26.1414`，可用于描述当前单点的聚变输出、加热输入和输运功率规模，但不能据此替代二维扫描结论。
- 辐射与损失：`Pbrem=15.3633`、`Pcycl=0.05034`、`P_line=0.0`；在给定输入下，线辐射被报告为 0，主要已报出的辐射项来自轫致辐射，回旋辐射较小。
- 运行状态：`ignited=0.0` 表明该点不是点火态；`valid=1.0` 仅说明模型在该参数点返回了有效解。
- 约束时间与约束假设：输出 `tau_E=5.0`，且输入 `use_tauE=1.0`，说明这里不是由约束时间标度自洽求得，而是采用固定约束时间假设；JSON 中未提供 `H98`、`H_ITPA20`、`H_ISS04` 等对比指标。
- 成分与辐射相关输入：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；这些值说明当前点是在较“干净”的成分设定下计算，相关辐射结果应结合这一输入前提理解。
- 几何与壁负荷：`Vp=1890.3037`、`Sw=1256.6371`、`Pwall=0.04725`；这些是当前 0-D 模型给出的体积、受热面积和壁负荷结果。JSON 中未给出 `geom_model`，因此这里只能按已报输出读取，不能补充几何模型复杂度判断。
- POPCON 扫描信息：扫描只明确了轴名 `Ti0` 和 `n0`，并给出 `best` 掩码与 `n_invalid=0`；这说明扫描中没有记录为无效的点，但也没有任何点被标记为最佳区，且缺少坐标数组，无法判断最佳区位置、边界或趋势。

3. 风险与不确定性
- 最大的不确定性来自约束时间处理：`use_tauE=1` 说明结果依赖固定约束时间假设；如果 `tauE` 改变，`Pfus`、`Qfus`、`Ptrans` 和是否接近点火都可能随之变化。
- 扫描证据不足：虽然有 `best` 掩码且全 0，但缺少 `x/y` 数组和 `nx/ny` 字段，因此不能判断是否存在窄小可行带、边界敏感区或多峰结构；当前只能确认“当前准则下未找到最佳区”。
- 边界类指标不全：JSON 未提供 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 等常见边界判据，因此无法基于本数据判断相关运行边界接近程度。
- 成分设定较理想化：`fHe=0`、`fimp=0`、`Zeff=1.0` 会影响辐射与功率平衡的呈现；若后续引入 He 灰分或杂质，`P_line`、`Pbrem`、`Pfus`、`Qfus` 可能变化。
- 当前点非点火：`ignited=0.0` 明确表明该点不是自持燃烧状态，因此不能把当前单点结果外推为更高性能区已经存在。
- 几何模型信息缺失：本 JSON 没有 `geom_model` 字段，因而不能判断是否使用了更简化或更完整的几何处理，只能按现有 `Vp`、`Sw`、`Pwall` 读取结果。

4. 下一步建议
- 建议先补做一次完整的 `Ti0-n0` 二维 POPCON 导出，必须保存 `x`、`y` 数组和 `nx/ny`；重点监控 `best`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`，用于确认是否真的不存在最佳区，还是仅当前导出信息不足。
- 建议做 `tauE` 敏感性扫描，至少围绕当前 `tauE=5.0` 改变取值，并保留 `use_tauE=1` 与其他条件不变；重点监控 `Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pcycl`、`valid`、`ignited`，检验结果对固定约束时间假设的依赖程度。
- 建议做成分与辐射扫描，如对 `fHe`、`fimp`、`Zimp` 分别扫描；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`，检查当前“零线辐射、低杂质”设定对结论的影响。
- 建议做几何/装置参数扫描，如 `R_p`、`Rw`、`L_in_fac`、`B_ring`；重点监控 `Vp`、`Sw`、`Pwall`、`B_in`、`B_out`、`Pfus`、`Qfus`，确认当前结果对几何和场强输入的敏感性。
- 若工具支持，建议补充输出边界类与约束类诊断量，再重复单点和二维扫描；优先监控 `beta` 类、`q/q95`、密度归一化量、以及约束时间相关标度对比指标，以便把“单点有效”与“扫描是否存在可接受区域”明确区分开。
