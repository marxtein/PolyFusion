1. 核心结论
- 本次结果对应 `config=dipole`（偶极场 Dipole），`preset=Dipole-DD`。
- 当前运行点 `valid=1.0`、`ignited=0.0`；这表示该单点求解有效，但不表示扫描中已经找到满足准则的工作窗。
- 该运行点的关键输出为 `Pfus=0.031023748738153785`、`Qfus=0.04634368711861937`、`Pheat=0.6694277185746289`、`Pwall=0.0005574015670940026`。
- 从 `Pfus`、`Qfus` 与 `Pheat` 的组合看，本次点主要依赖外加加热，且结果未显示点火状态。
- POPCON 扫描记录的轴为 `xkey=Ti0`、`ykey=n0`；`best` 矩阵为全 0，因此“当前准则下未找到最佳区”。

2. 关键指标解读
- 运行点能量相关输出包括 `Ptrans=0.6403775147654958`、`P_ei=0.7278323774424456`、`Pn=0.01040844648157622`，辐射/辐射样损失项包括 `Pbrem=0.03840830598212638`、`Pcycl=0.011257200083584353`、`P_line=0.0`。
- 约束时间方面，`use_tauE=1.0` 且 `tau_E=5.0`，必须明确说明：结果依赖固定约束时间假设；同时给出了 `tauC_eff=109.0965117502878`，但 `use_tauC=0.0`，说明当前并未把 `tauC` 作为固定输入约束。
- 等离子体与边界相关量中，已给出 `nbar=3.941141225673108e+18`、`ne0=1e+20`、`ntau=5e+20`、`beta_in=0.06468181444476451`、`beta_out=0.014858798739179026`；JSON 中未提供 `q`、`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此这几类边界不能展开判断。
- 成分与辐射设置为 `strcase=D-D`、`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；在该输入下，输出的 `P_line=0.0`，可见当前结果没有显示线辐射功率贡献。
- 几何与场相关输出包括 `Vp=1890.303723225459`、`Sw=1256.6370614359173`、`L_in=1.5`、`B_in=5.697503605448386`、`B_out=0.010115158710444893`，并使用了 `ring_model=1.0` 与 `cyclotron_model=equatorial_shell_proxy`；JSON 中未给出 `geom_model`，因此不对几何简化程度额外下结论。
- POPCON 侧只明确给出扫描变量名 `Ti0` 与 `n0`，以及 `n_invalid=0`；这说明记录中未标出无效点，但这与“找到最佳区”是两回事，不能混同。

3. 风险与不确定性
- `use_tauE=1` 是本报告最直接的不确定性来源之一；如果实际约束时间偏离 `tau_E=5.0`，则 `Pfus`、`Qfus`、`Pheat` 乃至 `ignited` 判定都可能变化。
- `best` 矩阵全为 0，必须单独说明：“当前准则下未找到最佳区”；这与当前运行点 `valid=1.0` 不矛盾，因为前者说的是扫描准则，后者说的是单点求解有效性。
- JSON 中没有提供 `x`/`y` 数组，也没有 `nx`/`ny` 字段，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 由于缺少 `H98`、`H_ITPA20`、`H_ISS04`、`LH_ratio` 等字段，不能对约束品质或 L-H 阈值模型比较做进一步解读；尤其不能把这份 0-D 结果表述为保证进入或维持某种运行模态。
- 当前成分输入为 `fHe=0.0`、`fimp=0.0`，且 `P_line=0.0`；这意味着本次结果对杂质与氦灰效应的覆盖很有限，辐射结论只适用于这组输入。

4. 下一步建议
1. 重新执行 `Ti0-n0` 的 POPCON 扫描，并在输出中补齐 `x`/`y` 数组与 `nx`/`ny`；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best`，先把“是否存在最佳区”和“最佳区是否只因网格证据缺失而不可见”分开验证。
2. 围绕当前 `tau_E=5.0` 做约束时间敏感性扫描，或在模型支持时对比固定 `use_tauE=1` 与非固定约束的结果；重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pbrem`、`Pcycl`，验证结论对固定约束时间假设的依赖程度。
3. 对成分参数做专项扫描，优先检查 `fimp`、`fHe`，必要时连同 `Zimp` 一起变化；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`，确认当前“低线辐射/零线辐射”是否只是这组成分输入的结果。
4. 若目标是理解壁负荷和几何量的耦合，可对 `B_ring`、`R_p`、`r_ring`、`L_in_fac` 做参数扫描；重点监控 `B_in`、`B_out`、`Vp`、`Sw`、`Pwall`、`Pn`、`Ptrans`，把场型与几何变化对输出的影响单独拆开。
