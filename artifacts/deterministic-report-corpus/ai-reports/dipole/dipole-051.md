## 1. 核心结论

- 本次结果的 `config=dipole`，`preset=Dipole-DD`。当前运行点给出的关键状态为：`valid=1.0`，`ignited=0.0`，`Pfus=5.48920009413943`，`Qfus=0.8616115918365784`，`Pheat=6.370852186933628`，`Pwall=0.00943792972930543`。
- 这表示当前输入下 0-D 求解器给出了一个`有效运行点`，但`未点火`；其中 `Qfus<1`，且 `ignited=0.0`。
- 必须与 POPCON 扫描分开看：运行点 `valid=1.0` 只说明这个单点结果有效，**不等同于**扫描中已经找到了工作窗。
- POPCON 扫描方面，`xkey=Ti0`，`ykey=n0`，`n_invalid=0`，但 `best` 矩阵全为 0，因此**当前准则下未找到最佳区**。
- 同时，扫描结果未提供 `nx`、`ny` 和 `x/y` 数组，因此必须写明：**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

## 2. 关键指标解读

- 运行点功率收支里，已给出 `Pfus=5.4892`、`Pheat=6.3709`、`Ptrans=8.4026`。从这些数值看，当前点不是 `ignited=1` 的状态，且传输功率项高于加热功率项。
- 辐射与相关损失中，`Pbrem=1.5873`，`Pcycl=0.02854`，`P_line=0.0`。在当前输入下，列出的辐射项里以轫致辐射为主；线辐射为 0。
- 壁负荷结果为 `Pwall=0.0094379`。该字段已给出，但仅凭这一次 0-D 输出不能外推更广泛结论。
- 约束方面，`use_tauE=1.0`，`tauE=5.0`，且运行结果里 `tau_E=5.0`，因此**结果依赖固定约束时间假设**。当前 JSON 未提供 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling` 对比，不能从约束时间标度一致性继续判断。
- 成分方面，`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，输入里 `Zimp=10`，但当前杂质份额为 0；这与 `P_line=0.0` 一致地表明本次结果没有体现杂质线辐射负担。
- 几何相关字段给出了 `Vp=1890.3037`、`Sw=1256.6371`。但 `geom_model` 字段未提供，因此这里不能评价几何模型复杂度或其修正方式。

## 3. 风险与不确定性

- 最大的不确定性来自扫描证据不完整。虽然有 `best` 矩阵，但缺少 `nx`、`ny`、`x/y` 数组；因此只能确认“当前准则下未找到最佳区”，**不能**判断最佳区位置、边界、连通性或对 `Ti0/n0` 的敏感性趋势。
- `valid=1.0` 只是单个运行点的有效性标记，不代表在邻近参数空间仍然有效，也不代表存在扫描意义上的可用区域。
- `use_tauE=1` 使结果直接受固定 `tauE=5.0` 假设约束；如果 `tauE` 改变，`Pfus`、`Qfus`、`ignited` 和功率平衡都可能变化，但当前 JSON 没有给出这种敏感性。
- 当前缺少若干常见边界/约束指标，例如 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`、`LH_ratio` 等，因此不能从这份数据判断边界接近程度，也不能做 L-H 阈值模型比较。
- `fimp=0.0`、`P_line=0.0` 说明本次结果对杂质辐射并未形成压力测试；若后续引入非零杂质，结果可能显著变化。

## 4. 下一步建议

1. 重新执行同一组 `Ti0-n0` 的 POPCON 扫描，并强制导出完整网格信息：`x`、`y`、`nx`、`ny`、`best_fraction`。  
   需要监控的指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`。

2. 在保持其他输入不变的前提下，对固定约束时间做敏感性扫描，例如围绕当前 `tauE=5.0` 取多个点重新运行。  
   需要监控的指标：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`。

3. 做一次成分/辐射验证扫描，从当前 `fimp=0.0` 基线出发，改变 `fimp`，必要时联动 `Zimp` 或 `Zeff`。  
   需要监控的指标：`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`、`valid`。

4. 补充导出边界与约束相关字段后，复跑当前点和扫描。  
   优先补充的指标：`betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`、`LH_ratio`、`geom_model`、以及任何 `tauE_scaling/H` 因子输出。这样才能把单点结果、固定约束假设和扫描结果分开核对。
