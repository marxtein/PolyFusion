1. 核心结论

本次 0-D 结果对应 `config=mirror`、`preset=BEAM`。当前运行点给出的状态是 `valid=1.0`、`ignited=1.0`，并报告 `Pfus=294.8596991676962`、`Qfus=1000.0`、`Pheat=-32.54308008911991`、`Pwall=11.524910029402125`。

需要把“当前运行点”和“POPCON 扫描”分开看：当前运行点只是说明这一个参数点被模型判为有效且点火标志为 1；而扫描部分仅说明在 `xkey=Ti0`、`ykey=ni0` 的扫描判据下，`best` 矩阵不是全 0，且 `n_invalid=0`。这不等于已经证明当前运行点落在某个工作窗内。

扫描还缺少 `x/y` 数组以及 `nx/ny` 明确信息，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。在现有证据下，只能确认 `best` 矩阵中存在被标记为 1 的网格单元，不能描述其具体位置、边界或延展方向。

2. 关键指标解读

功率收支方面，`Pfus=294.8596991676962`，`Pn=235.88775933415695`，`Pwall=11.524910029402125`。辐射相关项里，`Pbrem=0.7625488061766331`、`Pcycl=0.0015337913133576452`、`P_line=0.0`，当前数据下线辐射没有被计入。传输项给出 `Ptrans=25.664777146929335`。

需要特别注意增益与净加热口径：一方面给出了 `Qfus=1000.0`，另一方面同时给出 `Qfus_raw=-9.060595934994987`，且 `Pheat=-32.54308008911991`。这说明这里至少存在口径差异或数值截断/封顶处理；仅看 `Qfus=1000.0` 不足以独立解释当前点的净功率状态。

约束方面，`use_tauE=1.0`，参数中 `tauE=1.0`，输出里 `tau_E=1.0`，因此**结果依赖固定约束时间假设**。当前 JSON 没有给出 `tauE_scaling`、`H98`、`H_ITPA20`、`H_ISS04` 一类标度对照，所以不能从这份数据里判断经验约束的一致性。

成分与辐射方面，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。这表示当前算例里没有引入非零杂质份额，且辐射结果主要体现为轫致辐射与很小的回旋辐射。

几何量方面，输出给出 `Vp=2.88398205599543`、`Sw=22.76083877525805`、`Sp=19.736508888662737`，对应磁镜配置。`geom_model` 字段未提供，因此这里不能补充几何模型复杂度判断。

边界类信息只部分给出，如 `beta=0.99`、`beta_avg=0.396`；但 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw`、`nbar_o_Sudo` 都未提供，所以不能把当前点放到更完整的边界判据里解读。

3. 风险与不确定性

最大的解释风险在于：`valid=1.0`、`ignited=1.0` 与 `Pheat<0`、`Qfus_raw<0` 同时出现。它们并不自动互相否定，但说明不同输出字段的定义口径需要复核，尤其是 `Qfus` 与 `Qfus_raw` 的关系。

由于 `use_tauE=1`，本结果对固定 `tauE` 假设敏感；如果 `tauE` 改变，`valid`、`ignited`、`Pfus`、`Pheat` 可能一起变化。当前数据又没有提供经验约束标度的交叉验证，因此这一点不能忽略。

POPCON 方面虽然 `best` 矩阵非全 0，但缺少 `x/y` 数组和 `nx/ny`，所以无法判断最佳区是否靠近当前点，也无法判断边界陡峭程度、单调性或多峰特征。现阶段不能把“扫描里出现 best 单元”表述成稳定的工作窗结论。

4. 下一步建议

建议先做一次补全证据的 `Ti0-ni0` 重新扫描：导出完整 `x`、`y`、`nx`、`ny`、`best_fraction`，并保留 `best` 矩阵。需要监控的指标是 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`Qfus_raw`。这样才能判断扫描区域的覆盖度，以及当前点与被标记单元的相对关系。

建议围绕当前运行点 `Ti0=144.28571428571428`、`ni0=6e+20` 做局部加密扫描。重点监控 `Pheat` 的正负变化、`Qfus_raw`、`Pfus`、`Pn`、`Pbrem`、`Pcycl`、`Ptrans`、`Pwall`，用来确认当前点附近的功率收支是否连续，还是只是单点口径异常。

建议单独做 `tauE` 敏感性验证，因为 `use_tauE=1`。可在固定其余参数下扫描 `tauE`，或切换到可用的约束时间标度再对比。需要监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Pheat`、`Qfus_raw`、`tau_Past`，以判断当前结论对固定约束时间假设的依赖程度。

建议补做成分扫描，例如小范围扫描 `fimp` 或 `fHe`。需要监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Pheat`、`Pwall`，用来确认当前“低辐射、零线辐射”的结果是否仅来自于当前成分设定。
