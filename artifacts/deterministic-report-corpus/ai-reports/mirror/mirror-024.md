## 1. 核心结论

- 本次数据的 `config=mirror`（磁镜），`preset=BEAM`。
- 当前运行点结果：`valid=1.0`，`ignited=1.0`，`Pfus=268.35815523302404`，`Qfus=1000.0`，`Pheat=-41.14452559010554`，`Pwall=9.982656258253051`。
- 当前点还有较高聚变中子功率 `Pn=214.68652418641923`；辐射相关损失中，`Pbrem=0.7625488061766331`、`Pcycl=0.0015337913133576452`、`P_line=0.0`。
- 约束侧，`use_tauE=1.0`，且输出里 `tau_E=1.0`，因此本次结果依赖固定约束时间假设。
- POPCON 扫描需要单独看：本次扫描的 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`；`best` 矩阵不是全 0，在给出的 8x8 矩阵里有 10 个单元为 1。  
  但这不等同于“当前运行点 valid=1/ignited=1”。前者是单一点结果，后者是对一组扫描网格点的判据标记。
- 同时，由于 JSON 未提供 `x/y` 扫描数组，也未提供 `nx/ny` 字段，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

## 2. 关键指标解读

- 运行点状态：`valid=1.0` 表示该参数点给出了有效计算结果；`ignited=1.0` 表示在该模型记号下被标记为点火状态。
- 功率项：
  - `Pfus=268.36`，对应较高的聚变功率输出。
  - `Pwall=9.98`，给出了壁负荷相关量级。
  - `Pheat=-41.14` 为负，说明这里的加热项定义/符号需要结合模型口径理解，不能直接当作常规外加热功率解读。
  - `Qfus=1000.0`，但同时存在 `Qfus_raw=-6.522329553790237`，两者需要结合后处理或截断规则一起看。
- 约束与输运：
  - `use_tauE=1.0`、`tauE=1.0`、输出 `tau_E=1.0`，说明能量约束时间是固定输入，而不是由标度律在本次结果中自由生成。
  - 还给出了 `tauC_eff=1084.5398350837027`、`tau_m=0.11124960130835765`、`tau_gd=0.0024013643469364392` 等时间尺度，可作为后续一致性核查对象。
- 成分与辐射：
  - `Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。
  - 辐射项里 `Pbrem` 非零、`Pcycl` 很小、`P_line=0.0`，说明当前输入下线辐射没有被体现出来。
- 等离子体与几何：
  - `beta=0.99`，`beta_avg=0.396`；JSON 未提供 `betaN/betaT/betap`，因此不能按这些归一化边界进一步判读。
  - `Vp=2.88398205599543`，`Sw=22.76083877525805`。
  - 未提供 `geom_model` 字段，因此这里不能确认几何是否采用简化模型，只能按现有 `Vp/Sw` 和镜机参数解读。

## 3. 风险与不确定性

- `Qfus=1000.0` 与 `Qfus_raw=-6.5223` 同时出现，且 `Pheat` 为负，这说明增益与加热项的口径存在明显需要复核之处；在未核对定义前，不宜单独用 `Qfus` 做强结论。
- 本次结果显式依赖 `use_tauE=1.0` 的固定约束时间假设；如果 `tauE` 改动，`valid/ignited/Pfus/Qfus/Pwall` 可能一起变化。
- POPCON 扫描虽有 `best` 矩阵且 `n_invalid=0`，但缺少 `x/y` 数组与 `nx/ny`，所以只能确认“有些扫描格点被标记为 best”，不能判断这些格点对应的参数位置、边界连通性或趋势敏感性。
- 未提供 `q/q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`H98/H_ITPA20/H_ISS04` 等字段，边界与约束判读不完整。
- 未提供 `geom_model`，几何建模复杂度本身也是不确定项。

## 4. 下一步建议

- 补全并重跑 `Ti0-ni0` POPCON 扫描导出：至少同时保存 `x`、`y`、`nx`、`ny`、`best`。  
  需要监控的指标：`n_invalid`、`best` 占比、`valid`、`ignited`、`Pfus`、`Qfus_raw`、`Qfus`、`Pheat`、`Pwall`。
- 围绕当前运行点 `Ti0=60.714285714285715`、`ni0=6e20` 做更细网格扫描。  
  需要监控的指标：`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`Pbrem`、`Pcycl`、`P_line`、`beta`、`beta_avg`。
- 单独做约束时间敏感性验证：改变 `tauE`，或在模型允许时对比 `use_tauE=1` 与非固定 `tauE` 的结果。  
  需要监控的指标：`tau_E`、`Pfus`、`Qfus_raw`、`Qfus`、`Pheat`、`ignited`、`Pwall`。
- 做功率口径核查任务，重点对齐 `Qfus`、`Qfus_raw`、`Pheat` 的定义与后处理规则。  
  需要监控的指标：`Pfus`、`Pheat`、`P_alpha_loss`、`P_end_flux`、`P_coll_flux`、`Pn`、`Qfus_raw`、`Qfus`。
