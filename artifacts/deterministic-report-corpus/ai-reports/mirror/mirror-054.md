# 1. 核心结论

本次 0-D 结果对应 `config=mirror`、`preset=BEAM`。当前运行点给出 `valid=1`、`ignited=1`、`Pfus=155.09`、`Qfus=1000.0`、`Pheat=-8.24`、`Pwall=6.45`。

对“当前运行点”和“POPCON 扫描”需要分开看：

- 当前运行点：从 `last_run.outputs` 看，这个单点解被标记为有效且点火。
- POPCON 扫描：`last_scan` 提供了 `best` 矩阵，且矩阵中存在非零元素，说明在当前判据下并非“最佳区全无”。
- 但由于扫描结果里未提供 `nx/ny` 与 `x/y` 数组坐标，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

另外，`use_tauE=1`，说明本次结果**依赖固定约束时间假设**；这会直接影响对功率平衡和点火状态的解读。

# 2. 关键指标解读

当前运行点方面：

- 聚变与供热：`Pfus=155.09`，同时 `Pheat=-8.24`，说明按该输出定义，净加热项为负，需结合模型定义谨慎理解。
- 墙负荷：`Pwall=6.45`。
- 增益：结果给出 `Qfus=1000.0`，但同时存在 `Qfus_raw=-18.83`，这说明 `Qfus` 的可解释性需要结合程序内部取值规则核对，不能只看表面数值。
- 辐射与损失：`Pbrem=0.415`、`Pcycl=0.00132`、`P_line=0.0`；在已给出的辐射项里，轫致辐射大于回旋辐射，线辐射为零。
- 中子功率：`Pn=124.07`。
- 其他功率相关项中，`P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0`、`Past_domain=0.0`。

约束与物理状态方面：

- 约束时间：`tau_E=1.0`，且 `use_tauE=1`，结果依赖固定约束时间输入，不是由约束时间标度自洽求得。
- 等离子体参数：`beta=0.99`、`beta_avg=0.396`。
- 密度相关：`nbar=3.48e20`，`ne0=4.43e20`，`ni0=4.43e20`。
- 成分：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。按输入看，杂质份额与氦灰份额都设为零。
- 几何：`Vp=2.884`、`Sw=22.761`，配置标签为磁镜；本次 JSON 未给出 `geom_model` 字段，因此不能补充几何模型分级判断。

POPCON 扫描方面：

- 扫描轴为 `xkey=Ti0`、`ykey=ni0`。
- `n_invalid=0`，表示扫描结果中未报告无效点数量。
- `best` 矩阵中有非零元素，说明在离散扫描点上存在满足当前“best”判据的点。
- 但由于缺少 `x/y` 数组以及 `nx/ny`，不能据此描述最佳区的具体位置、边界范围或随参数变化的趋势。

# 3. 风险与不确定性

最主要的不确定性有三点：

- **固定约束时间假设**：`use_tauE=1` 会把结果强烈绑定到 `tauE=1.0` 这个输入上，点火、功率平衡和增益判断都依赖该假设。
- **Q 值解释存在歧义**：同时出现 `Qfus=1000.0` 与 `Qfus_raw=-18.83`，提示这里可能有截断、封顶或特殊记账规则；在未查看程序定义前，不能把 `Qfus=1000.0` 直接当作常规物理增益结论。
- **扫描证据不完整**：虽然 `best` 矩阵非零，但缺少 `x/y` 坐标与 `nx/ny`，所以**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。这也意味着“运行点有效”不等于“扫描已清楚显示可接受区域的范围和稳定性”。

此外，当前 JSON 中未提供 `betaN/q95/nbar_o_nGw/L-H 阈值比较/H98` 等字段，因此不能对这些边界或模式判据作进一步解读。

# 4. 下一步建议

建议直接做可执行的扫描和核对任务，并统一监控同一组指标：

1. 对 `Ti0` 与 `ni0` 重新做完整 POPCON 导出，补齐 `x/y` 数组以及 `nx/ny`。  
需监控：`best`、`n_invalid`、`Pfus`、`Pheat`、`Pwall`、`ignited`、`valid`。

2. 在当前点附近做 `tauE` 参数扫描，因为 `use_tauE=1`。  
建议至少围绕 `tauE=1.0` 做上下扰动，检查 `ignited`、`Pheat`、`Qfus_raw`、`Pfus` 是否对固定约束时间高度敏感。

3. 单独核对 `Qfus` 的程序定义和输出规则。  
重点监控：`Qfus`、`Qfus_raw`、`Pheat`，确认是否存在封顶、裁剪或分母接近零时的特殊处理。

4. 做成分与辐射敏感性扫描，优先扫 `fimp`、`Zimp`、`fHe`。  
需监控：`Pbrem`、`Pline`、`Pcycl`、`Pfus`、`Pwall`、`ignited`。

5. 对磁镜相关几何和端损参数做局部扫描，例如 `R_mirror`、`B_expand`、`f_throat`、`L_c`。  
需监控：`P_end_flux`、`Pwall`、`Pfus`、`tau_m`、`valid`、`ignited`。
