1. 核心结论
- 本次记录的工况为 `config=mirror`（`config_label=磁镜 Magnetic Mirror`），`preset=BEAM`。
- 当前运行点输出为 `valid=1.0`、`ignited=0.0`、`Pfus=64.55337149953377`、`Qfus=38.16907950194148`、`Pheat=1.6912477938130586`、`Pwall=2.9104647657079052`。
- 就当前运行点而言，`valid=1.0` 说明该 0-D 点得到了有效解；`ignited=0.0` 说明它不是点火解。这是“单个运行点”的结论，不能直接替代扫描结论。
- 就 POPCON 扫描而言，`xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，且给出的 `best` 矩阵包含非零项，说明扫描中存在满足当前 `best` 判据的离散网格点；这不等同于“当前运行点有效”或“当前运行点一定落在最佳区内”。
- `use_tauE=1.0` 且 `tauE=1.0`，因此本次结果依赖固定约束时间假设。

2. 关键指标解读
- **功率收支** `Pfus=64.55337149953377`，其中 `Pn=51.642697199627015`；外加/维持加热为 `Pheat=1.6912477938130586`，壁负荷为 `Pwall=2.9104647657079052`，传输项为 `Ptrans=14.427950115667945`。
- **辐射与损失分解** `Pbrem=0.1729135614913`、`Pcycl=0.0010584165605665881`、`P_line=0.0`；同时 `P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0`、`Past_domain=0.0`。这些字段表明本次输出里已显式列出若干损失通道。
- **约束时间与模型约束** `tau_E=1.0`、`tauC_eff=748.4035879260994`、`tau_Past=1.1073953160601921`、`tau_m=0.678125250050219`、`tau_gd=0.0011156922180795864`、`tau_eq_ie=0.15846517429712803`；由于 `use_tauE=1.0`，这里更应把结果理解为“在固定 `tauE` 条件下的输出”，而不是独立约束标度自行给出的结果。`H98`、`H_ITPA20`、`H_ISS04`、`tauE_scaling` 未提供，不能做标度对比。
- **等离子体状态与边界字段** `Te0_used=10.0`、`Ti0=172.14285714285714`、`nbar=2.243994752564138e+20`、`ne0=2.8571428571428572e+20`、`ntau=2.8571428571428572e+20`、`beta=0.99`、`beta_avg=0.396`；但 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 未给出，因此边界判读不完整。
- **成分与辐射相关参数** `Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`、`f_fast_ion=0.1698293068137596`、`fTavg=0.4950000000000001`；这些字段可用于后续做杂质、氦灰和快离子敏感性检查。
- **几何与磁镜相关量** `B0=0.30000000000000016`、`B_vac=3.0`、`B_expand=100.0`、`R_mirror=10.0`、`M=2.5`、`A_throat=0.0028274333882308154`、`Vp=2.88398205599543`、`Sw=22.76083877525805`、`Sp=19.736508888662737`、`R_mc=99.99999999999996`；`geom_model` 未提供，因此不能判断几何模型细化程度。

3. 风险与不确定性
- 结果依赖固定约束时间假设：`use_tauE=1.0` 使 `valid`、`ignited`、`Pfus`、`Qfus` 以及扫描中的 `best` 判据结果都对 `tauE=1.0` 这一设定敏感。
- `best` 矩阵虽包含非零项，但 `x/y` 数组以及显式 `nx/ny` 未提供，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 当前 JSON 只给出了 `best` 的布尔矩阵，没有给出 `best` 的具体判据定义，因此只能确认“某些网格点满足该判据”，不能进一步解释其物理含义。
- 运行点与扫描必须分开：`valid=1.0` 是单点求解结果，`best` 非零是扫描离散网格结果，两者不能互相替代。
- 若要做边界或经验约束解读，当前缺少 `H98/H_ITPA20/H_ISS04`、`q/q95`、`betaN/betap`、密度归一化等字段；因此这部分判断空间有限。
- `geom_model` 未提供，所以无法判断本次几何描述是简化还是细化模型，只能依据现有 `Vp`、`Sw`、`A_throat` 等输出做有限解读。

4. 下一步建议
1. 重新执行 `Ti0`-`ni0` 的 POPCON 扫描，并显式导出 `x`、`y`、`nx`、`ny` 与 `best` 判据定义；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
2. 围绕当前点做 `tauE` 固定值敏感性扫描，直接检验固定约束时间假设对结果的影响；重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`tauC_eff`、`Pwall`。
3. 对磁镜相关参数做单参或双参扫描，如 `R_mirror`、`B_expand`、`L_c`、`Rw`；重点监控 `A_throat`、`Vp`、`Sw`、`Pwall`、`Pfus`、`Qfus`、`n_invalid`。
4. 做成分/辐射验证扫描，如 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pcycl`、`valid`、`Qfus`、`Pwall`。
