1. 核心结论
- 本次结果对应 `config=mirror`（磁镜 Magnetic Mirror）与 `preset=BEAM`。
- 当前运行点的单点评估结果为：`valid=1.0`、`ignited=0.0`、`Pfus=14.093191815001678`、`Qfus=16.685106963040827`、`Pheat=0.8446569654134972`、`Pwall=0.656296058678348`；就 0-D 单点结果看，该点被模型接受，但不是点火态。
- 当前运行点的功率分解里，已给出 `Pn=11.274553452001342`、`Ptrans=3.6214609712147405`、`Pbrem=0.03608958107922352`、`Pcycl=0.005744776119868737`、`P_line=0.0`；在已提供数据中，线辐射为零，轫致与回旋辐射存在但数值较小。
- POPCON 扫描需要与“运行点有效”分开解读：本次扫描的轴是 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵非全零，说明在当前判据下，扫描的离散格点中存在被标记为 `best` 的点；这不等同于“当前运行点 valid=1.0”。
- `n_invalid=0` 说明扫描未报告无效点；但未提供 `nx`、`ny`，且缺少 `x/y` 数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 约束与约束时间：`use_tauE=1.0`、`tauE=1.0`，输出里 `tau_E=1.0`，因此该结果依赖固定约束时间假设；同时给出了 `tauC_eff=63.95281271484073`、`tauC=1.0`，但 `use_tauC=0.0`。
- 运行点状态：`Qfus=16.685106963040827` 对应当前输入下较高的聚变增益比，`Pheat=0.8446569654134972` 说明该点仍有外加热输入；`ignited=0.0` 表明不能把该点表述为点火运行。
- 压力与等离子体量：已提供 `beta=0.5670346493406173`、`beta_avg=0.22681385973624693`、`nbar=1.009797638653862e+20`、`ne0=1.2857142857142857e+20`、`ni0=1.2857142857142857e+20`；但未提供 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此边界判读不完整。
- 成分与辐射：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；辐射项里 `Pbrem=0.03608958107922352`、`Pcycl=0.005744776119868737`、`P_line=0.0`。这说明当前点的输入成分设定下，杂质辐射并未在结果中体现为显著项。
- 几何与体表量：已提供 `Vp=2.9724893768505614`、`Sw=22.76083877525805`、`Sp=20.558935114235403`、`A_throat=0.018604540713528787`、`R_mc=15.19754468421126`；`geom_model` 字段未提供，因此无法直接判断几何模型复杂度。
- POPCON 证据：从给出的 `best` 数组本身看，矩阵并非全零，说明扫描中至少存在一部分满足当前准则的离散格点；但由于没有 `x/y` 数值轴，不能把这些 `1` 可靠地转写成具体的 `Ti0` 或 `ni0` 区间。

3. 风险与不确定性
- “运行点有效”与“扫描存在 `best` 格点”不是同一件事：`valid=1.0` 只对应当前单个运行点；扫描里的 `best` 反映的是另一组离散网格判据结果，不能直接互相替代。
- 结果对约束时间假设敏感：由于 `use_tauE=1.0`，当前 `Pfus`、`Qfus`、`Pwall` 等结论都依赖固定 `tauE=1.0` 的设定，而不是由额外约束时间标度自行决定。
- 扫描网格信息不全：缺少 `nx`、`ny` 和 `x/y` 数组，虽然有 `best` 矩阵，但仍不足以判断工作窗形状、边界连续性、对 `Ti0`/`ni0` 的敏感性，也无法确认当前运行点是否正好落在某个 `best` 格点上。
- 边界类指标缺失：未提供 `betaN`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`H98/H_ITPA20/H_ISS04` 等信息，因此对稳定性、密度边界和经验约束的解读只能停留在局部。
- 成分与辐射覆盖有限：当前输入是 `fimp=0.0`、`fHe=0.0`、`Zeff=1.0`，且 `P_line=0.0`，所以这份结果没有覆盖杂质与氦灰变化对辐射和功率平衡的影响。
- 几何判据不完整：虽然给出了 `Vp`、`Sw` 等几何量，但 `geom_model` 未提供，不能判断结果是否来自简化几何还是更完整几何描述。

4. 下一步建议
- 补全 POPCON 导出内容后重扫一次 `Ti0-ni0`：至少输出 `x`、`y`、`nx`、`ny`、`best`、`n_invalid`；重点监控 `best`、`n_invalid`、`valid`、`Pfus`、`Qfus`、`Pwall`，用来把当前离散矩阵证据转成可解释的网格信息。
- 围绕当前点 `Ti0=88.57142857142857`、`ni0=1.2857142857142857e+20` 做局部加密二维扫描；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`、`Pbrem`、`Pcycl`、`beta`、`beta_avg`。
- 做约束时间敏感性验证：在其余参数不变时扫描 `tauE`，或在工具支持时比较 `use_tauE=1` 与非固定约束时间设置；重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做成分/辐射补充扫描：从当前 `fimp=0.0`、`fHe=0.0` 出发，扫描 `fimp`、`fHe`、`Zimp`；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`valid`、`Pwall`。
- 如需继续做镜机参数验证，可对 `B_vac`、`R_mirror`、`B_expand`、`Rw` 做参数扫描；重点监控 `B0`、`A_throat`、`Vp`、`Sw`、`Pfus`、`Qfus`、`Pwall`、`valid`。
