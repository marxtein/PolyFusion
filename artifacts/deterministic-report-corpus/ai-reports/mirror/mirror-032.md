1. 核心结论
- 本次案例为 `config=mirror`（`config_label=磁镜 Magnetic Mirror`），`preset=BEAM`；当前运行点输出为 `valid=1.0`、`ignited=1.0`、`Pfus=297.7797544991354`、`Qfus=1000.0`、`Pheat=-42.394927347354454`、`Pwall=11.220360974982809`。
- 以上结论只对应当前单个运行点：它在这次 0-D 求解中被判为有效且点火；这不等同于 POPCON 扫描已经充分证明存在明确工作窗。
- POPCON 扫描信息显示扫描轴为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`；同时 `best` 矩阵不是全 0，说明在当前判据下，扫描网格中至少有部分单元被标记为 `best`。
- 但 JSON 未提供 `nx`、`ny` 以及 `x`/`y` 数组，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能把 `best` 单元准确映射到具体参数坐标。
- `use_tauE=1.0` 且输出中 `tau_E=1.0`，因此本次结果依赖固定约束时间假设。

2. 关键指标解读
- 运行点解读：当前点 `valid=1.0`、`ignited=1.0`，对应 `Pfus=297.7797544991354`；中子功率为 `Pn=238.22380359930833`，壁负荷为 `Pwall=11.220360974982809`。`Pheat` 为负值，说明这个量的符号和定义需要结合程序口径理解，不能只凭数值正负做额外推断。
- 功率与损失：已给出的辐射/损失项包括 `Pbrem=0.7625488061766331`、`Pcycl=0.0015337913133576452`、`P_line=0.0`、`Ptrans=16.396940954982632`；同时 `P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0`。在已输出的辐射项里，轫致辐射高于回旋辐射，线辐射为 0。
- 约束与假设：本次使用固定约束时间，参数中 `use_tauE=1.0`、`tauE=1.0`，输出中 `tau_E=1.0`；同时给出了 `tauC_eff=1084.5398350837027`。JSON 未提供 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling` 结果，因此这里不能用缩放律角度补充判断。
- 边界与等离子体状态：输出含 `beta=0.99`、`beta_avg=0.396`、`nbar=4.71238898038469e+20`、`ne0=6e+20`，参数含 `ni0=6e+20`。JSON 未提供 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，所以边界约束信息是不完整的。
- 成分与几何：参数/输出显示 `Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`，几何相关量有 `Vp=2.88398205599543`、`Sw=22.76083877525805`、`Sp=19.736508888662737`。JSON 未给出 `geom_model`，因此无法从本次数据判断几何模型是否采用了更简化或更详细的处理。
- POPCON 扫描解读：`best` 矩阵存在 `1`，说明扫描判据并非在所有网格点都失败；但由于缺少 `nx`、`ny` 和 `x`/`y` 数组，当前只能确认“有被标记的 best 单元”，不能据此描述最佳区位置、边界、连通性或参数敏感性。

3. 风险与不确定性
- `Qfus=1000.0` 与 `Qfus_raw=-7.023947748732669` 同时出现，且数值符号与量级不一致；在未确认程序对 `Qfus` 的裁剪、封顶或回退逻辑前，不宜直接把 `Qfus=1000.0` 当作唯一增益结论。
- `Pheat=-42.394927347354454` 为负值，但 JSON 未给出该量的精确定义与符号约定；这会影响对功率平衡口径的理解。
- 本次结果依赖固定约束时间假设（`use_tauE=1.0`），因此对 `tau_E` 设定较敏感；如果改用其他约束时间模型，当前运行点和扫描标记结果都可能变化。
- 扫描侧虽然 `n_invalid=0` 且 `best` 非全 0，但缺少 `nx`、`ny`、`x`、`y` 数组；因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 边界与约束指标不全：未见 `betaN`、`q95`、密度分数等常用约束量，也未见 `geom_model`；这限制了对约束完备性的核对。
- 成分设定中 `fHe=0.0`、`fimp=0.0`，当前辐射结果是在这一特定设定下得到的；若成分改变，`Pbrem`、`P_line`、`Zeff` 及相关功率项可能随之变化。

4. 下一步建议
- 补全 POPCON 导出：重新输出 `Ti0`–`ni0` 扫描的 `x`/`y` 数组、`nx`、`ny`、`best_fraction`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pwall`，先把“有无 best 单元”和“best 单元具体落点”区分清楚。
- 在当前点附近做局部加密扫描：以 `Ti0=88.57142857142857`、`ni0=6e+20` 为中心细化网格；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pwall`、`beta`、`beta_avg`，核对当前点是否位于 `best` 标记区域内。
- 做约束时间敏感性验证：围绕 `tauE=1.0` 进行参数扫描，或在工具支持时对比固定 `tauE` 与其他 `tauE` 处理方式；重点监控 `tau_E`、`tauC_eff`、`valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`。
- 做功率口径一致性复核：针对 `Qfus` 与 `Qfus_raw`、以及 `Pheat` 的定义重新导出或核对计算链；重点监控 `Qfus`、`Qfus_raw`、`Pheat`、`P_ei`、`Pbrem`、`Pcycl`、`P_line`、`Ptrans`、`P_alpha_loss`、`P_end_flux`、`P_coll_flux`。
- 做成分/辐射验证扫描：在保持其余条件不变时扫描 `fHe`、`fimp`、`Zimp`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Pwall`，确认当前“低杂质、零氦灰”设定对结果的影响范围。
