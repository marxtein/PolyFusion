1. 核心结论
- 本次报告配置为 `config=mirror`（`config_label=磁镜 Magnetic Mirror`），预设为 `preset=BEAM`。
- 当前运行点结果：`valid=1.0`、`ignited=0.0`、`Pfus=2.170690346032705`、`Qfus=1.5565232693817042`、`Pheat=1.3945762255741707`、`Pwall=0.15664038600732344`。可直接解读为：该单点求解有效，但未点火。
- 需要单独区分两件事：`valid=1.0` 只说明“当前运行点”求解成立；`last_scan.best` 是否非零说明的是“扫描判据下是否有格点被标记为最佳”，两者不能混为一谈。
- 当前 POPCON 扫描给出 `xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，且 `best` 矩阵非全 0，说明在当前判据下确实存在被标记为最佳的扫描格点。
- 但本数据未提供 `x`/`y` 数组，也未提供 `nx`/`ny`；因此必须明确：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0`，且本次输出 `tau_E=1.0`，说明结果依赖固定约束时间假设，而不是由这里可见的数据直接给出约束时间标度扫描结果。

2. 关键指标解读
- 运行点功率水平上，`Pfus=2.170690346032705`，`Pheat=1.3945762255741707`，`Qfus=1.5565232693817042`；同时 `ignited=0.0`，因此该点表现为“有聚变输出、但未达到点火”的状态。
- 相关功率分项中，`Pn=1.7365522768261643`、`Ptrans=1.8182099119610853`、`P_ei=1.4085750549494496`；这些值说明模型中已显式计算了中子功率、输运功率和电子-离子能量交换项，但仅凭这份 JSON 不能进一步推断更高层级的运行结论。
- 辐射与损失项方面，`Pbrem=0.005493807117694442`、`Pcycl=0.005010575701931979`、`P_line=0.0`，并且 `P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0`。就当前输出而言，可见的辐射项存在，但数值上明显小于 `Pfus` 和 `Pheat`。
- 约束时间与相关时间尺度方面，`tau_E=1.0`、`tauC_eff=28.701934055202788`、`tau_m=2.0050051597436886`、`tau_Past=2.0116869364836503`、`tau_eq_ie=0.9055152816978742`。其中最关键的是：由于 `use_tauE=1.0`，当前结果对固定 `tauE` 假设敏感。
- 等离子体状态与边界相关输出中，`beta=0.2828325002145833`、`beta_avg=0.11313300008583331`、`nbar=3.926990816987241e+19`、`ne0=5e+19`、`ntau=5e+19`。这些是当前 0-D 点的状态量，但本 JSON 未提供 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能补充这些边界判据的判断。
- 成分与几何方面，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`，说明模型中定义了杂质原子序数，但当前运行点没有给出非零杂质分数；几何量给出 `Vp=2.9919946193869484`、`Sw=22.76083877525805`、`Sp=20.70495298178924`、`A_throat=0.023944329610332223`，可作为后续壁负荷和体积效应复核的输入背景。

3. 风险与不确定性
- 最大的不确定性来自扫描证据不完整：虽然 `best` 矩阵非全 0，且 `n_invalid=0`，但缺少 `x`/`y` 数组与 `nx`/`ny`，所以扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 也因此，不能仅凭这份数据判断当前参数点 `Ti0=116.42857142857143`、`ni0=5e+19` 在扫描图中的精确位置，更不能判断它与“最佳区”边界的距离。
- `use_tauE=1.0` 意味着结果依赖固定约束时间假设；如果 `tauE` 取值变化，`Pfus`、`Qfus`、`ignited`、`Pwall` 等关键结论都可能随之改变，而这份 JSON 没有给出这种敏感性。
- `best` 只有 0/1 标记，没有附带“最佳”的判定公式、裕度或连续评分；因此只能确认“有格点被标记”，不能确认这些格点之间优劣差距有多大。
- 若从约束判据完整性看，当前 JSON 缺少 `H98`、`H_ITPA20`、`H_ISS04`、`tauE_scaling`、`geom_model`、`q/q95`、Greenwald/Sudo 归一化密度等字段，所以这些方向上的风险不能在本报告中被消除或替代判断。

4. 下一步建议
- 1）补做带显式坐标的 `Ti0`-`ni0` 扫描：输出完整 `x`、`y`、`nx`、`ny`、`best`，并保留 `n_invalid`；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，用来确认当前点是否落在被标记格点内，以及扫描覆盖是否足够。
- 2）做 `tauE` 敏感性扫描：在 `use_tauE=1` 前提下，对 `tauE` 做多点扰动或分段扫描；重点监控 `Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pwall`、`tau_E`，验证当前结论对固定约束时间假设的依赖程度。
- 3）做成分/辐射验证扫描：围绕 `fimp`、`Zimp`、`fHe`、`Te0` 进行组合扫描；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pwall`，确认当前“低辐射输出”是否会随成分设定变化。
- 4）做运行点复核扫描：以当前 `Ti0=116.42857142857143`、`ni0=5e+19` 为中心做局部加密扫描；重点监控 `beta`、`beta_avg`、`nbar`、`ne0`、`ntau`、`valid`、`Pfus`、`Qfus`，用于判断当前点附近结果是否连续，避免只依赖单个 0-D 点。
