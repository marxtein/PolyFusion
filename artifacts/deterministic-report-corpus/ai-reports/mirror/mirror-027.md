1. 核心结论
- 本次结果对应 `config=mirror`（`config_label=磁镜 Magnetic Mirror`），`preset=BEAM`。
- 当前运行点返回 `valid=1.0`、`ignited=1.0`，并给出 `Pfus=35.98953911495234`、`Qfus=1000.0`、`Pheat=-1.3627560226579591`、`Pwall=1.521331592135132`。
- 这表示模型把“当前单个运行点”判为有效且已点火；但这不等同于 POPCON 扫描已经充分证明存在可接受的工作窗。
- POPCON 扫描给出 `xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，且 `best` 矩阵非全零；按已提供的 8x8 `best` 矩阵直接计数，被标记为 `best` 的点为 10/64，约 15.6%。
- 由于缺少 `x`/`y` 网格数组，且未提供 `nx`/`ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0`，因此本次结果依赖固定约束时间假设；对 `tau_E=1.0` 的依赖需要单独验证。

2. 关键指标解读
- 当前运行点方面：`Pfus=35.98953911495234`，`ignited=1.0`，同时 `Pheat=-1.3627560226579591`，说明报告输出里存在一个“已点火且净加热项为负”的组合，需结合模型定义解读，不能只看单一指标。
- 增益相关方面：报告给出 `Qfus=1000.0`，但同时存在 `Qfus_raw=-26.40937814001166`；这两个值并存，说明增益指标的工程解读应以输出定义核对为前提。
- 损失与辐射方面：`Pbrem=0.09216133626382525`、`Pcycl=0.0028140681663239558`、`P_line=0.0`、`Ptrans=5.740176395902361`、`P_ei=17.444630104634236`；在已给数据里，线辐射为 0，韧致辐射和回旋辐射为非零但较小量级。
- 成分方面：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；当前结果是在零氦灰、零杂质分数的输入条件下给出的，成分扰动尚未在这份数据里体现。
- 几何与体量方面：输出包含 `Vp=2.924396448212207`、`Sw=22.76083877525805`、`A_throat=0.008313047302980152`、`R_mc=34.0119968668674`；这些几何相关量已给出，但 `geom_model` 字段未提供，无法据此判断所用几何模型层级。
- 约束与边界方面：可见 `tau_E=1.0`、`tauC_eff=206.93767933023938`、`beta=0.9135558239376612`、`beta_avg=0.3654223295750645`、`nbar=1.626896195609e+20`；但 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`H98`、`H_ITPA20`、`H_ISS04` 未提供，所以边界判据解读仍不完整。
- POPCON 扫描方面：当前只能确认扫描变量名是 `Ti0` 与 `ni0`，`n_invalid=0`，且存在非零 `best` 点；这说明“扫描里有点被当前准则标成 best”，不等于“当前运行点必然处在该区域内”，也不等于“已经看清区域边界或趋势”。

3. 风险与不确定性
- 最大的不确定性来自功率与增益定义：`ignited=1.0`、`Qfus=1000.0`、`Qfus_raw=-26.40937814001166`、`Pheat=-1.3627560226579591` 同时出现，提示至少需要先核对模型对这些字段的截断、上限或特殊值处理逻辑。
- 结果依赖固定约束时间假设：`use_tauE=1.0` 且 `tauE=1.0`，因此只要约束时间设定变化，`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat` 都可能随之变化。
- 扫描证据不完整：虽然 `best` 矩阵非全零、`n_invalid=0`，但缺少 `x`、`y`、`nx`、`ny`，所以不能判断最佳区位置、边界、连通性或对 `Ti0`/`ni0` 的敏感性趋势。
- 边界判据缺失：这份数据未给出 `betaN`、`q95`、密度极限归一化量及 H 因子，因而无法对运行点与扫描结果做更完整的边界交叉校验。
- 成分假设偏理想化：当前输入是 `fHe=0.0`、`fimp=0.0`，对应输出里 `P_line=0.0`；因此对氦灰积累、杂质引入和线辐射抬升的敏感性，这份 0-D 结果还没有覆盖。
- 几何模型信息不足：虽然有 `Vp`、`Sw` 等几何结果，但未提供 `geom_model`，因此无法判断是否存在额外几何简化对结果的影响。

4. 下一步建议
1. 重新导出或补跑 `Ti0-ni0` 的 POPCON 扫描，确保同时保存 `x`、`y`、`nx`、`ny` 和 `best`；重点监控 `best` 占比、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`。
2. 围绕当前点对 `tauE` 做敏感性扫描，因为 `use_tauE=1.0`；重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`。
3. 对当前点做单点复算或输出核对，专门检查 `Qfus`、`Qfus_raw`、`Pheat` 的定义与触发逻辑；重点监控 `ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat` 是否一致变化。
4. 补做成分相关扫描，如 `fHe`、`fimp`，必要时连同 `Zimp` 一起扫描；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Pheat`、`Pwall`。
5. 若后处理支持，补充导出边界与几何模型字段，再做同一组点的复核；重点监控 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`、`geom_model`，并与 `valid`、`ignited` 联动检查。
