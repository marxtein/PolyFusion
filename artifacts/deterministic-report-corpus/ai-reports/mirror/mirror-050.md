1. 核心结论
- 本次结果对应 `config=mirror`、`preset=BEAM`；当前运行点 `valid=1`、`ignited=0`，表示该 0-D 运行点被模型接受，但未达到点火。
- 当前运行点给出的主要功率量级为：`Pfus=13.0721`、`Qfus=3.3399`、`Pheat=3.9139`、`Pwall=0.7463`。
- 当前运行点的聚变输出高于外加加热，且 `Qfus>1`；但 `ignited=0` 说明仍不能把它解读为自持点火状态。
- 本次结果依赖固定约束时间假设：`use_tauE=1`，且 `tau_E=1.0` 被直接使用，因此结论对约束时间设定敏感。
- POPCON 扫描与当前运行点需要分开看：当前运行点 `valid=1` 只说明这个单点可计算且满足模型内部判据；扫描结果中的 `best` 矩阵存在非零元素，说明在该扫描判据下出现了候选单元，但这不等同于“当前运行点就是扫描最佳区”。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是已给出 `xkey=Ti0`、`ykey=ni0` 和 `best` 矩阵，但缺少 `x/y` 数组以及 `nx/ny` 信息，无法把矩阵单元可靠映射到物理坐标与边界。

2. 关键指标解读
- 运行点功率平衡：`Pfus=13.0721`，其中中子功率 `Pn=10.4576`；外加加热 `Pheat=3.9139`，得到 `Qfus=3.3399`。这说明该单点下聚变功率相对外加热已有放大，但仍未点火。
- 损失与传输：`Ptrans=6.4926`，高于辐射损失中的 `Pbrem=0.0350`、`Pcycl=0.00071`，且 `P_line=0`。从这个 0-D 点看，传输损失是显著项，线辐射在输入设定下未体现。
- 壁负荷与几何：`Pwall=0.7463`，几何量给出 `Vp=2.8840`、`Sw=22.7608`、`Sp=19.7365`。这些量可用于后续把功率结果与体积/受热面积一起检查，但当前数据不足以进一步判断壁面裕量。
- 约束与时间尺度：`tau_E=1.0`、`tauC_eff=502.0444`、`tau_m=1.5064`、`tau_gd=0.00112`、`tau_eq_ie=0.3521`。其中最重要的是 `use_tauE=1`，所以本次输出不是由约束标度自洽求得，而是受固定约束时间假设控制。
- 等离子体状态：`nbar=1.0098e20`、`ne0=1.2857e20`、`ntau=1.2857e20`、`Te0_used=10.0`；`beta=0.99`、`beta_avg=0.396`。这些参数说明该点是高压强占比的设定点，但仅凭这份 JSON 不能据此给出超出模型内部定义的稳定性结论。
- 成分与辐射设定：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。在本次输入里有效杂质分数为零，因此 `P_line=0` 与低辐射结果是和输入设定一致的，不应外推到含杂质或含氦灰情形。
- POPCON 扫描信息：扫描轴是 `Ti0` 对 `ni0`，`n_invalid=0`，说明扫描单元未报告无效点；同时 `best` 矩阵含非零元素，表示当前判据下存在被标记为“best”的网格单元。不过由于缺少 `x/y` 数组及 `nx/ny`，只能确认“有候选单元”，不能严谨描述其物理位置、边界或趋势。

3. 风险与不确定性
- 最大不确定性来自固定约束时间假设：`use_tauE=1`。只要 `tau_E` 取值变化，`Pfus`、`Qfus`、`ignited` 以及损失分配都可能明显改变。
- 当前运行点有效，不代表扫描工作窗已经被充分刻画。`valid=1` 仅是单点结论；扫描虽然有 `best` 非零单元，但缺少 `x/y` 与 `nx/ny`，因此不能判断工作窗形状、宽窄、边界陡峭程度或对 `Ti0/ni0` 的敏感性。
- `ignited=0` 是一个明确约束：即使 `Qfus=3.3399`，也不能把当前点解释为点火态或自持燃烧态。
- 辐射结论对成分设定敏感。当前 `fimp=0.0`、`fHe=0.0`、`P_line=0`，意味着杂质线辐射和氦灰效应在这次结果里基本未被展开；若后续改变成分，功率平衡可能变化。
- 几何与端损相关量已给出，如 `A_throat`、`R_mc`、`M`、`P_end_flux=0`，但仅凭这份 JSON 不能判断这些结果对几何模型细节的鲁棒性；尤其磁镜装置对几何与损失通道常较敏感，仍需参数扫描验证。
- `n_invalid=0` 说明扫描点都可计算，但这不等同于扫描判据充分；“都能算”与“存在清晰可用的候选窗”是两回事。

4. 下一步建议
- 对固定约束时间做显式敏感性扫描：围绕 `tauE=1.0` 扩展多个取值重新跑点和扫图，重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`、`tau_E`。
- 补全 POPCON 网格证据：在 `Ti0-ni0` 扫描中输出完整 `x`、`y` 数组以及 `nx/ny`，并保留 `best`、`n_invalid`。监控指标应包括 `best` 占比、`n_invalid`、各网格点的 `valid`、`Pfus`、`Qfus`、`Pwall`，这样才能判断工作窗位置与敏感性。
- 做成分/辐射验证扫描：改变 `fimp`、`fHe`，必要时联动 `Zimp`，检查 `Pbrem`、`Pcycl`、`P_line`、`Ptrans`、`Pfus`、`Qfus` 的变化，确认当前“低辐射”是否只是零杂质输入的结果。
- 做几何与磁镜参数扫描：围绕 `B_expand`、`R_mirror`、`L_c`、`Rw`、`a_c`、`f_throat` 扫描，监控 `Pwall`、`P_end_flux`、`tauC_eff`、`tau_m`、`Pfus`、`Qfus`，验证结果是否对几何/端部设定敏感。
- 对高压强相关量做约束复核：在参数扰动下持续记录 `beta`、`beta_avg`、`nbar`、`ne0`、`Te0_used`、`Ti0`，并与功率输出联看，避免只盯 `Qfus` 而忽略运行点状态变化。
