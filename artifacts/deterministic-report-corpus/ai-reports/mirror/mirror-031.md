1. 核心结论
- 当前运行点：`config=mirror`（`config_label=磁镜 Magnetic Mirror`），`preset=BEAM`，`valid=1.0`，`ignited=1.0`，`Pfus=224.89630268224104`，`Qfus=1000.0`，`Pheat=-30.152197967077953`，`Pwall=8.556104045113564`。
- 这组 `valid=1.0` 与 `ignited=1.0` 只说明“当前单一运行点”在本次 0-D 求解中被标记为有效且点火，不能等同于 POPCON 扫描已经确认出连续工作窗。
- POPCON 扫描与运行点需要分开看：本次扫描轴为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且提供的 `best` 矩阵不全为 0，说明按当前 `best` 判据至少有部分扫描点被选中。
- 但本次未提供 `x/y` 坐标数组，也未给出 `nx/ny`；扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 功率与损失：输出给出 `Pfus=224.8963`，其中 `Pn=179.9170`；辐射相关项中 `Pbrem=0.5759`、`Pcycl=0.0014298`、`P_line=0.0`，当前列出的辐射项以轫致辐射为主，线辐射为 0。
- 热功率口径：`Pheat=-30.1522` 为负值，说明按当前模型记账口径，净加热项显示为负；`Ptrans=14.2497`，`P_ei=109.0104`，需要结合模型定义理解各功率项符号。
- 约束条件：`use_tauE=1.0`、`tauE=1.0`、输出 `tau_E=1.0`，结果依赖固定约束时间假设；同时 `use_tauC=0.0`，但给出了 `tauC_eff=1011.0376`，说明粒子约束并非直接由固定 `tauC` 输入锁定。
- 组分与辐射设定：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`；这表示杂质种类参数已设，但当前杂质份额为 0，且当前输出没有线辐射功率。
- 状态与几何量：`beta=0.99`、`beta_avg=0.396`；几何相关输出包括 `Vp=2.88398205599543`、`Sw=22.76083877525805`、`Sp=19.736508888662737`、`A_throat=0.0028274333882308154`。当前 JSON 未提供 `q/q95`、`betaN`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此这些边界口径无法据此解读。

3. 风险与不确定性
- `Qfus=1000.0` 与 `Qfus_raw=-7.458703439390947` 符号和量级明显不一致；仅凭当前 JSON 无法判断 `Qfus` 是否经过截断、上限封顶或其他后处理，建议优先核对定义。
- `Pheat` 为负值会直接影响对能量平衡的理解；如果不先确认符号约定和记账边界，后续对“需要外加热还是净出热”的解释容易偏差。
- POPCON 方面虽然 `best` 矩阵中存在被选中的点，但缺少 `x/y` 数组与 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势；因此不能从这份数据推出最佳区边界、面积演化或参数单调性。
- `n_invalid=0` 只表明扫描中未记录无效点，不等于“所有点都表现良好”，也不等于“当前运行点周围连续可用”；这与单点 `valid=1.0`、`ignited=1.0` 是两类证据。
- 当前 JSON 未给出 `H98`、`H_ITPA20`、`H_ISS04`、`geom_model` 等字段，因此无法判断约束时间与几何模型的经验标度来源或几何简化程度。

4. 下一步建议
1. 重新导出一版完整 `Ti0-ni0` 扫描结果，至少包含 `x`、`y`、`nx`、`ny`、`best`、`valid`、`ignited`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，先把“单点状态”和“扫描入选区域”分开证实。
2. 围绕当前点 `Ti0=88.57142857142857`、`ni0=5.2142857142857145e+20` 做局部加密扫描，并单独扫描 `tauE`；因为 `use_tauE=1`，需要直接检验结果对固定约束时间假设的依赖。建议监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`beta`、`beta_avg`、`tau_E`。
3. 做一次功率记账核对任务，逐项导出并复核 `Qfus`、`Qfus_raw`、`Pn`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`、`P_alpha_loss`、`P_end_flux`、`P_coll_flux` 的定义和符号，确认 `Pheat<0` 与 `Qfus` 异常值是否来自后处理规则。
4. 在当前组分基线附近补做成分/辐射敏感性扫描，例如改变 `fHe`、`fimp`，必要时连同 `Zimp` 一起扫；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Pwall`、`valid`，以确认当前“零杂质、零线辐射”设定对结果的影响范围。
