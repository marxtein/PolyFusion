# 1. 核心结论

- `config=mirror`（磁镜），`preset=BEAM`。
- 当前运行点结果：`valid=1.0`，`ignited=0.0`，`Pfus=14.183627138225372`，`Qfus=7.777575131535761`，`Pheat=1.8236567179807714`，`Pwall=0.7032818084721335`。按这次 0-D 输出，当前点可计算，但未点火。
- 本次结果依赖固定约束时间假设：`use_tauE=1.0`，`tauE=1.0`。
- 必须把运行点和 POPCON 扫描分开看：运行点是否有效以 `valid/ignited` 为准；POPCON 只说明扫描离散点里是否出现满足 `best` 判据的点。当前扫描的 `xkey=Ti0`、`ykey=ni0`，且 `best` 矩阵不是全 0，说明记录的离散扫描点中存在满足当前 `best` 判据的点，但这不等同于“当前运行点就在最佳区”。

# 2. 关键指标解读

- 功率与损失：当前输出给出 `Pfus=14.1836`、`Pheat=1.8237`、`Qfus=7.7776`。损失项中，`Pbrem=0.03589738714650024`，`Pcycl=0.004303787483325922`，`P_line=0.0`；同时 `P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0`。
- 传输与约束：`tau_E=1.0`，`tauC_eff=84.91080108380105`，`tau_Past=0.9355160067443348`，`tau_m=0.9258402837888694`。由于 `use_tauE=1.0`，这里更像是在固定约束时间条件下查看功率平衡结果，而不是比较不同约束标度。
- 等离子体状态：`beta=0.7272835719803571`，`beta_avg=0.2909134287921428`，`nbar=1.009797638653862e+20`，`ne0=1.2857142857142857e+20`，参数输入 `ni0=1.2857142857142857e+20`。
- 成分与辐射相关输入：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。就这组输入看，杂质分数被设为 0，因此当前线辐射项也输出为 `P_line=0.0`。
- 几何相关量：`Vp=2.9566594778539232`，`Sw=22.76083877525805`，`Sp=20.433161361563368`，`A_throat=0.014765495722013366`，`M=2.5`。
- POPCON 记录：`n_invalid=0`，说明已记录的扫描结果里没有被标成无效的点；`best` 矩阵存在非零单元，说明在这批离散扫描点中，至少有一部分点满足当前 `best` 判据。

# 3. 风险与不确定性

- `ignited=0.0` 明确表示当前运行点不是点火态；不能把 `Qfus` 为正或扫描中存在 `best=1` 的点，解释成当前点已经点火。
- 结果依赖固定约束时间假设：`use_tauE=1.0`。如果 `tauE` 设定变化，`Pfus/Qfus/Pheat/ignited` 都可能跟着变化。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是虽然给出了 `best` 矩阵和 `xkey/ykey`，但没有提供 `x`/`y` 数组，也没有 `nx/ny`。
- 只能确认“存在满足当前 `best` 判据的离散点”，不能据此描述最佳区的具体位置、边界或趋势。
- 本次 JSON 没有给出 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`、`LH_ratio` 等字段，因此不能对这些边界或阈值比较作进一步判断。

# 4. 下一步建议

1. 复跑一次完整的 `Ti0-ni0` POPCON 扫描，并导出 `x`、`y`、`nx`、`ny`。重点监控：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
2. 在当前基准点周围做 `tauE` 敏感性扫描，直接检验固定约束时间假设对结果的影响。重点监控：`Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pbrem`、`Pcycl`、`Pwall`。
3. 对成分相关输入做单参数扫描或一致性核对，优先检查 `fHe`、`fimp`、`Zimp`。重点监控：`Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`。
4. 对几何相关输入做单参数扫描或交叉验证，优先检查 `Rw`、`a_c`、`R_mirror`、`B_expand`。重点监控：`Vp`、`Sw`、`A_throat`、`Pwall`、`Pfus`、`valid`。
