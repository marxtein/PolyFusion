1. 核心结论
- `config=mirror`（`config_label=磁镜 Magnetic Mirror`），`preset=BEAM`。
- 当前运行点输出为：`valid=1.0`，`ignited=1.0`，`Pfus=284.68036831294387`，`Qfus=1000.0`，`Pheat=-25.87329582219609`，`Pwall=11.370717707120772`。
- 这只说明“当前单个运行点”在本次求解中被标记为有效且点火标记为 1；这不等同于 POPCON 扫描已经证明存在完整、连续的工作窗。
- POPCON 扫描单独看：`xkey=Ti0`，`ykey=ni0`，`best` 矩阵并非全 0，且 `n_invalid=0`，说明在当前 `Ti0`-`ni0` 扫描中至少有部分网格点被标记为满足当前 `best` 准则。
- 但本 JSON 未提供 `x/y` 数组，且未给出 `nx/ny`；因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- `use_tauE=1.0`，并且 `params.tauE=1.0`、`outputs.tau_E=1.0`，所以本结果依赖固定约束时间假设。

2. 关键指标解读
- 功率相关：`Pfus=284.68`，其中 `Pn=227.7442946503551`；列出的辐射/损失项里，`Pbrem=0.7625488061766331`，`Pcycl=0.0015337913133576452`，`P_line=0.0`，`Pwall=11.370717707120772`，`Ptrans=30.298695242902692`。
- 当前运行点的加热项为 `Pheat=-25.87329582219609`，且 `ignited=1.0`；从 JSON 只能确认符号和数值，不能仅凭此断言其具体物理含义，需要结合代码定义理解该负号。
- 约束时间与约束：`tau_E=1.0`，`tauC_eff=1084.5398350837027`，`tau_Past=0.5273311028858056`，`tau_m=0.3231353962114971`，`tau_gd=0.0011156922180795864`，`tau_rho=0.8317115631854997`；由于 `use_tauE=1`，这里不是由外部约束时间标度自行决定的结果。
- 等离子体状态：`beta=0.99`，`beta_avg=0.396`，`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`，`f_fast_ion=0.1698293068137596`，`nbar=4.71238898038469e+20`，`ne0=6e+20`，`ni0=6e+20`。
- 几何与体积/表面积：`Vp=2.88398205599543`，`Sw=22.76083877525805`，`Sp=19.736508888662737`，`A_throat=0.0028274333882308154`；磁场/镜比相关量包括 `B0=0.30000000000000016`，`R_mc=99.99999999999996`，`M=2.5`。
- POPCON 结果只能谨慎解读为：在 `Ti0` 与 `ni0` 这两个扫描轴下，`best` 矩阵存在通过点；但由于缺少显式网格坐标与尺寸信息，不能据此描述最佳区的位置、边界或趋势。

3. 风险与不确定性
- `Qfus=1000.0`，但同时给出 `Qfus_raw=-11.002864508228724`；两者符号和量级明显不一致，提示这里可能存在限幅、占位值或后处理规则，`Qfus` 不能脱离 `Qfus_raw` 单独解读。
- `Pheat` 为负值，单看 JSON 无法区分这是“无需外加加热”的结果、记号约定，还是某种边界处理；这一点直接影响对运行点能量平衡的判断。
- 结果依赖固定 `tauE=1.0` 假设；如果约束时间不是固定值，`valid`、`ignited`、`Pfus`、`Pwall` 和 `Qfus_raw` 都可能改变。
- 虽然 `best` 矩阵非全零且 `n_invalid=0`，但由于缺少 `x/y` 数组以及 `nx/ny`，仍然是“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 约束边界信息不完整：JSON 未提供 `q/q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`H98/H_ITPA20/H_ISS04`、`geom_model` 等字段，因此无法判断这些边界是否参与限制，也无法判断几何模型是否为简化模型。
- 若干损失通道当前为 0，例如 `P_line=0.0`、`P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0`；这可能与当前模型设定、成分设定或通道关闭有关，不能直接推广为普遍结论。

4. 下一步建议
1. 先做当前点复算与定义核查：固定 `Ti0=172.14285714285714`、`ni0=6e+20`、`tauE=1.0` 重新运行，并同时导出 `Qfus`、`Qfus_raw`、`Pheat`、`Pfus`、`valid`、`ignited`、`tau_E`，确认 `Qfus` 的限幅/记号规则和 `Pheat` 的符号定义。
2. 重新做 `Ti0`-`ni0` 二维扫描，但这次必须保存 `x`、`y`、`nx`、`ny`、`best`、`n_invalid`；重点监控 `best`、`valid`、`ignited`、`Pfus`、`Qfus_raw`、`Pheat`、`Pwall`、`beta`，以便判断哪些网格点真正通过当前准则。
3. 做 `tauE` 敏感性扫描：在当前点附近改变 `tauE`，并在模型支持时增加一组非固定约束时间对照；重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus_raw`、`Pheat`、`Pwall`、`beta`，检验当前结论对固定约束时间假设的依赖程度。
4. 做成分与辐射验证扫描：围绕当前 `fimp=0.0`、`fHe=0.0` 的设定，扫描 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Pwall`、`valid`、`ignited`，确认当前低辐射结果是否主要由成分设定驱动。
