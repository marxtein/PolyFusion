1. 核心结论

- `config=mirror`，`preset=BEAM`，当前运行点 `valid=1.0`、`ignited=0.0`、`Pfus=8.1363`、`Qfus=1000.0`、`Pheat=0.00337`、`Pwall=0.3576`。
- 就“当前运行点”而言，这是一组被模型判为有效的 0-D 解，但并未点火（`ignited=0.0`）；因此“运行点有效”不等同于“已达到点火”。
- 就“POPCON 扫描”而言，`last_scan.best` 矩阵中存在非零单元，说明在当前 best 准则下，扫描结果里出现过被标记为最佳的格点；这与单个运行点 `valid=1.0` 是两件不同的事，不能混为一谈。
- 但本次扫描未提供 `x/y` 数组，且未给出 `nx/ny`，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0`，说明本次结果依赖固定约束时间假设；对运行点和扫描结论都应把这一前提单独记住。
- 报告中给出 `Qfus_raw=2411.1316` 而展示值 `Qfus=1000.0`，可理解为当前输出中的 `Qfus` 可能经过封顶或截断；解读时应优先把它当作“报告值”而非无限精度物理量。

2. 关键指标解读

- 运行点功率面：`Pfus=8.1363`，中子功率 `Pn=6.5090`，外加/维持加热 `Pheat=0.00337`；在该解里聚变功率明显高于外加加热，但由于 `ignited=0.0`，不能把这直接表述为点火状态。
- 损失项：`Ptrans=1.5860`、`Pbrem=0.03635`、`Pcycl=0.00831`、`P_line=0.0`；已给出的辐射项里轫致和回旋辐射均非零，线辐射为零。
- 壁负荷与几何量：`Pwall=0.3576`，几何相关量包括 `Vp=2.9940`、`Sw=22.7608`、`Sp=20.7197`、`A_throat=0.02454`；这些量说明模型已把体积、表面积和喉部面积纳入当前 0-D 估算。
- 约束时间假设：`tau_E=1.0` 且 `use_tauE=1.0`，表示能量约束时间不是由其他缩放动态求得，而是按固定值使用；因此 `Pfus`、`Qfus`、`beta` 等结果都依赖这一设定。
- 等离子体状态：`Te0_used=10.0`，输入点 `Ti0=32.8571`、`ni0=1.2857e+20`；输出 `nbar=1.0098e+20`、`ne0=1.2857e+20`、`ntau=1.2857e+20`，可作为当前运行点的温度/密度背景。
- 压力与组成：`beta=0.2465`、`beta_avg=0.09861`，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；其中当前解未体现 He 灰分和杂质分数贡献，但保留了杂质电荷数参数。
- 镜机相关参数：`R_mirror=10.0`、`B_vac=3.0`、`B0=2.6041`、`B_expand=100.0`、`M=2.5`、`R_mc=11.5204`；这些是本次 `mirror` 构型结果的关键背景。
- POPCON 元信息：扫描键为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`；这表示扫描中未记录无效点数量，但因为缺少 `x/y` 网格数组与 `nx/ny`，仍不能据此判断最佳区边界、延展方向或趋势。

3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1.0`：结果依赖固定约束时间假设，若 `tauE` 取值改变，`Pfus`、`Qfus`、`Pwall` 以及是否仍为 `valid=1.0` 都可能变化。
- 当前运行点 `valid=1.0` 仅表示该组输入在模型内得到有效解，不代表扫描范围内存在宽裕工作窗；单点有效与扫描找到可用区域必须分开看。
- `ignited=0.0` 明确表示未点火，因此不能把高 `Qfus` 报告值直接解读为点火或自持燃烧结论。
- `Qfus=1000.0` 与 `Qfus_raw=2411.1316` 并存，说明输出展示值可能有上限处理；若后续用 `Qfus` 做排序或比较，需要先确认采用报告值还是原始值。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；虽然 `best` 矩阵里有非零值，但缺少 `x/y` 数组和 `nx/ny`，因此不能可靠描述最佳区位置、边界或随参数变化的细节。
- 本次 0-D 数据未提供更完整的边界指标，如 `q/q95`、`betaN/betaT/betap`、`nbar_o_nGw` 或 `nbar_o_Sudo`；因此对运行边界的判断信息不完整。
- `geom_model` 字段未提供，无法判断本次几何处理是简化模型还是更高阶模型；这也是解读几何相关结果时的一个信息缺口。

4. 下一步建议

- 执行 `tauE` 敏感性扫描：在当前 `config=mirror`、`preset=BEAM` 下，对 `tauE` 做多点扫描，并持续监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`、`Pbrem`、`Pcycl`。
- 补全 POPCON 网格证据：重新导出包含 `x`、`y` 数组以及 `nx/ny` 的扫描结果，沿 `Ti0`–`ni0` 网格复跑；重点监控 `best`、`n_invalid`、`valid` 占比，以及 `Pfus`、`Qfus`、`Pwall` 在网格上的变化。
- 做当前运行点邻域扫描：围绕 `Ti0=32.8571` 和 `ni0=1.2857e+20` 做局部细网格扫描，检查单点 `valid=1.0` 是否只是在孤立格点出现；需监控 `valid`、`ignited`、`Pfus`、`Qfus_raw`、`Pwall`、`beta`、`nbar`。
- 核对功率平衡输出：针对 `Pheat` 很小而 `Pfus` 较高、且 `Qfus`/`Qfus_raw` 不一致的情况，建议单独导出功率平衡明细；重点监控 `Pheat`、`Pfus`、`Pn`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`、`P_alpha_loss`。
- 做组成参数验证：对 `fHe`、`fimp`、`Zimp` 做小范围扰动扫描，确认当前“零 He、零杂质分数”设定对 `Pfus`、`Qfus`、`Zeff`、`Pbrem`、`P_line`、`Pwall` 的影响大小。
- 做磁场与镜比参数扫描：围绕 `B_vac`、`R_mirror`、`B_expand`、`f_throat` 进行分组扫描，监控 `Pfus`、`Pwall`、`beta`、`tauC_eff`、`tau_m`、`P_end_flux`、`P_coll_flux`，以验证当前点对镜机参数的依赖。
