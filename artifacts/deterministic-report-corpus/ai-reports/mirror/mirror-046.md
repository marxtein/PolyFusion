## 1. 核心结论

- `config=mirror`（磁镜 Magnetic Mirror），`preset=BEAM`。
- 当前运行点输出为：`valid=1.0`，`ignited=1.0`，`Pfus=160.63501751709526`，`Qfus=1000.0`，`Pheat=-12.767211155020984`，`Pwall=6.4965886284829075`。
- 仅就当前运行点看，模型把该点标记为“有效”且“点火”；这是单点结果，不等同于 POPCON 扫描已经证明存在可接受工作窗。
- POPCON 扫描信息显示：`xkey=Ti0`，`ykey=ni0`，`best` 矩阵中存在非零项，`n_invalid=0`。这只能说明在扫描判据下有部分网格点被标记为 `best`，不能直接外推到当前运行点本身。
- `use_tauE=1`，因此本次结果依赖固定约束时间假设。

## 2. 关键指标解读

- 运行点功率相关量：
  - 聚变功率 `Pfus=160.63501751709526`，中子功率 `Pn=128.50801401367622`，壁负荷相关输出 `Pwall=6.4965886284829075`。
  - 辐射/损失项中，`Pbrem=0.41542483148284826`，`Pcycl=0.0013177179911902583`，`P_line=0.0`，`Ptrans=18.943049798924033`，`P_alpha_loss=0.0`，`P_end_flux=0.0`。
  - `Pheat` 为负值，同时还给出了 `Qfus_raw=-12.581840745535256` 与 `Qfus=1000.0`；这些字段组合说明 Q 值和加热量的定义/截断方式需要单独核验，不能只看 `Qfus=1000.0` 做判断。
- 约束与约束时间：
  - `tau_E=1.0`，参数侧 `tauE=1.0`，且 `use_tauE=1.0`，说明这次计算把能量约束时间作为固定输入。
  - `tauC_eff=931.7549528454466`，但 `use_tauC=0.0`，因此粒子约束并非以固定 `tauC` 直接锁定。
- 等离子体状态与边界相关量：
  - `beta=0.99`，`beta_avg=0.396`。
  - `nbar=3.4781918664744134e+20`，`ne0=4.428571428571428e+20`，`ni0=4.428571428571428e+20`。
  - JSON 中未提供 `betaN`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此这里不能对归一化 beta、磁安全因子或密度极限接近程度做判断。
- 成分与辐射：
  - `Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。
  - 在线辐射项 `P_line=0.0`，当前辐射主要来自 `Pbrem`，`Pcycl` 很小。
- 几何相关：
  - `Vp=2.88398205599543`，`Sw=22.76083877525805`，`Sp=19.736508888662737`，`A_throat=0.0028274333882308154`。
  - 本次 JSON 未提供 `geom_model` 字段，因此不能判断是否采用简化几何模型。
- POPCON 扫描：
  - 扫描轴是 `Ti0` 与 `ni0`，`best` 矩阵存在非零元素，且 `n_invalid=0`。
  - 但缺少 `x/y` 数组，也未给出 `nx/ny`。因此只能确认“有 best 标记点存在”，不能报告最佳区的实际坐标范围、边界或趋势。

## 3. 风险与不确定性

- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 当前运行点的 `valid=1.0`、`ignited=1.0` 是单点判定；即使扫描里存在 `best` 标记点，也不能把这两件事混为一谈。
- `use_tauE=1` 表明结果依赖固定约束时间假设；若 `tau_E` 假设改变，`valid`、`ignited`、`Pfus`、`Pheat`、`Qfus` 都可能随之变化。
- `Qfus=1000.0`、`Qfus_raw=-12.581840745535256` 与 `Pheat<0` 同时出现，说明至少有一个指标带有截断、符号约定或后处理规则；在未核实定义前，不宜把 Q 值当作独立结论使用。
- 由于缺少 `betaN`、`q95`、密度极限比值、H 因子等信息，当前无法判断该点对常见边界约束的接近程度。
- 本次 JSON 未提供 `geom_model`，因此几何建模层面的不确定性无法直接量化。

## 4. 下一步建议

1. 重新导出一次 `Ti0-ni0` POPCON 扫描，补齐 `x`、`y`、`nx`、`ny`。
   - 需要监控的指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`。

2. 在当前点附近做约束时间敏感性扫描，重点比较固定 `tau_E=1.0` 假设下结果是否稳定；若工具支持，再对比不同 `tauE` 或 `tauE_scaling`。
   - 需要监控的指标：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Ptrans`。

3. 对当前单点做功率平衡核验，重点检查 `Qfus`、`Qfus_raw`、`Pheat` 的定义和符号约定。
   - 需要监控的指标：`Pheat`、`Qfus`、`Qfus_raw`、`Pfus`、`P_alpha_loss`、`P_ei`、`Ptrans`、`Pbrem`、`Pcycl`、`Pwall`。

4. 补做成分与密度相关扫描，例如围绕 `fHe`、`fimp`、`Zeff`、`ni0` 的局部扰动，确认辐射和功率平衡对杂质/稀释是否敏感。
   - 需要监控的指标：`Zeff`、`Pfus`、`Pbrem`、`P_line`、`Pheat`、`beta`、`nbar`。
