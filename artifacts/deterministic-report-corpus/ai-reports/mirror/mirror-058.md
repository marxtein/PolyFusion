1. 核心结论
- 当前运行点：`config=mirror`（`config_label=磁镜 Magnetic Mirror`），`preset=BEAM`，`valid=1.0`，`ignited=0.0`，`Pfus=12.5517`，`Qfus=2.5049`，`Pheat=5.0109`，`Pwall=0.7716`。就这一个 0-D 运行点而言，模型给出了有效解，且聚变功率高于外加加热功率，但未被标记为点火态。
- 当前运行点的功率相关输出还包括：`Pn=10.0414`，`Ptrans=7.4856`，`Pbrem=0.0350`，`Pcycl=0.00071`，`P_line=0.0`。从已给数据看，线辐射未出现，辐射项以轫致辐射为主且数值较小。
- POPCON 扫描需要单独看：`last_scan` 的扫描轴是 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵并非全 0，说明扫描中至少存在满足当前 `best` 判据的单元；这不等同于“当前运行点有效”，两者不是同一层面的证据。
- 但本次扫描缺少 `x`/`y` 数组且未给出 `nx`/`ny`，因此必须写明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 约束前提上，`use_tauE=1.0`、`tauE=1.0`、输出 `tau_E=1.0`，因此本次结果依赖固定约束时间假设。

2. 关键指标解读
- 运行点状态：`valid=1.0` 说明这组输入在模型内可求得自洽解；`ignited=0.0` 说明该点未被模型标记为点火，不能把 `Qfus=2.5049` 直接当作点火证据。
- 功率收支：`Pfus=12.5517` 对应 `Qfus=2.5049`，而 `Pheat=5.0109`；同时 `Pwall=0.7716` 给出了壁面负荷量级，适合后续与参数扫描联动观察。
- 传输与约束：`Ptrans=7.4856`，`tau_E=1.0`；由于 `use_tauE=1.0`，这里的能量约束时间不是由缩放律自由预测，而是由固定输入主导。`use_tauC=0.0` 下，粒子相关时间由模型给出，输出为 `tauC_eff=502.0444`。
- 等离子体与边界类量：输出含 `beta=0.99`、`beta_avg=0.396`、`nbar=1.0098e20`、`ne0=1.2857e20`、`ntau=1.2857e20`。但 `q`/`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 未提供，因此无法做这些常见边界比值的交叉判断。
- 成分与辐射：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；辐射项为 `Pbrem=0.0350`、`Pcycl=0.00071`、`P_line=0.0`。在这组输入下，没有看到由杂质或线辐射主导的证据。
- 几何与磁镜相关量：输出含 `Vp=2.8840`、`Sw=22.7608`、`Sp=19.7365`、`A_throat=0.002827`、`M=2.5`、`B0=0.3000`，参数中还有 `B_vac=3.0`、`R_mirror=10.0`、`B_expand=100.0`、`L_c=10.0`、`Rw=0.8`。`geom_model` 字段未提供，因此不能判断是否使用了更简化或更完整的几何模型。

3. 风险与不确定性
- 单点与扫描不能混用：`valid=1.0` 只说明当前这一个运行点在模型内有效；`best` 矩阵非全 0 只说明扫描中存在满足当前判据的单元，不能把它表述成“当前点一定处在最佳区”。
- 扫描信息不完整：虽然给出了 `best` 矩阵和 `xkey/ykey`，但缺少 `x`、`y`、`nx`、`ny`。因此必须明确：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 约束假设敏感：`use_tauE=1.0` 使结果直接依赖固定 `tauE=1.0` 假设；如果这个假设变化，`Pfus`、`Qfus`、`ignited`、`Pwall` 可能同步变化。
- 约束覆盖不全：建议判读框架中常见的 `H98`、`H_ITPA20`、`H_ISS04`、`q/q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`LH_ratio` 等字段本次未提供，因此无法完成更完整的约束一致性检查。
- 几何与端损相关不确定性：`P_end_flux=0.0`、`P_coll_flux=0.0`、`Past_domain=0.0` 是当前输出结果，但缺少更多几何模型说明与扫描坐标，暂时不能判断这些量对参数变化是否敏感。

4. 下一步建议
- 补全一次可复核的 POPCON 导出：继续做 `Ti0`–`ni0` 扫描，但必须同时导出 `x`、`y`、`nx`、`ny`、`best`、`n_invalid`。需要监控的指标：`valid`、`best`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`。
- 做 `tauE` 敏感性扫描：在 `use_tauE=1` 条件下对 `tauE` 做多点扫描，专门检验固定约束时间假设对结论的影响。需要监控的指标：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`、`Ptrans`。
- 做成分与辐射扫描：围绕 `fHe`、`fimp`、`Zimp` 进行参数扫描，验证当前“低辐射、低杂质”结论是否稳定。需要监控的指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`。
- 做磁镜与几何参数扫描：对 `B_vac`、`R_mirror`、`B_expand`、`f_throat`、`L_c`、`Rw` 做分组扫描，观察磁镜几何改变后功率与负荷如何响应。需要监控的指标：`A_throat`、`M`、`Vp`、`Sw`、`P_end_flux`、`Pfus`、`Qfus`、`Pwall`。
- 做单点复算一致性检查：保持当前 `preset=BEAM`，仅重复导出关键输出，确认 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Pbrem`、`Pcycl` 是否稳定复现，避免把一次性结果直接当作扫描规律。
