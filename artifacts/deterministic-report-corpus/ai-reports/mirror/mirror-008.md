1. 核心结论
- 本次结果来自 `config=mirror`（磁镜），`preset=BEAM`。
- 当前运行点单点结果为：`valid=1.0`、`ignited=0.0`、`Pfus=1.791428695326771`、`Qfus=0.5915267671600877`、`Pheat=3.0284828933902634`、`Pwall=0.2117633553099314`。
- 就单点而言，`valid=1.0` 表示该运行点在此 0-D 模型下被判为有效；`ignited=0.0` 表示该点未达到点火状态。这是“当前运行点”的结论。
- POPCON 扫描是另一层证据：`last_scan.best` 中存在 `1`，说明在扫描准则下至少有部分网格单元被标记为最佳区证据；这不等同于“当前运行点有效”，两者不能混为一谈。
- 但 `last_scan` 缺少 `x/y` 数组，且未给出 `nx/ny`；扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0`，且 `tauE=1.0`、`tau_E=1.0`，因此本次结果依赖固定约束时间假设。

2. 关键指标解读
- 功率相关：当前点 `Pfus=1.791428695326771`，中子功率 `Pn=1.4331429562614169`，总加热功率 `Pheat=3.0284828933902634`，壁负荷 `Pwall=0.2117633553099314`，聚变增益 `Qfus=0.5915267671600877`。
- 损失与辐射：报告了 `Pbrem=0.789163977657373`、`Pcycl=0.015328714914825235`、`P_line=0.0`；在已给出的辐射项里，`Pbrem` 高于 `Pcycl`，而当前输入下线辐射项为 0。
- 约束时间与输运：输出含 `tau_E=1.0`、`tauC_eff=112.30669386320439`、`tau_m=1.5054429337178783`、`tau_Past=1.7569043809177283`；其中能量约束采用固定值输入，而不是由缩放律自行给出。
- 边界与等离子体状态：已给出 `beta=0.4026767799665254`、`beta_avg=0.16107071198661016`、`nbar=4.71238898038469e+20`、`ne0=6e+20`、`ntau=6e+20`；但 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo` 未提供，因此边界裕度无法进一步展开。
- 成分设定：输入为 `Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`，对应输出中 `P_line=0.0`；这说明当前报告采用了较“干净”的成分设定，但这里只能按给定输入解读。
- 几何量：已给出 `Vp=2.984641418840089`、`Sw=22.76083877525805`、`Sp=20.650985438092807`、`A_throat=0.021852296318385745`、`R_mc=12.938838770239045`、`M=2.5`；可用于后续把功率、壁负荷与体积/表面积做一致性复核。

3. 风险与不确定性
- 最大的不确定性来自约束时间处理：`use_tauE=1.0` 说明结果依赖固定约束时间假设，因此 `Pfus`、`Qfus`、`ignited` 对 `tauE` 的敏感性尚未被当前结果覆盖。
- 当前运行点与扫描结论必须分开看：单点 `valid=1.0` 只说明该点有效，不自动代表扫描存在宽裕工作窗；反过来，扫描中有 `best=1` 的单元，也不代表当前点本身就是最佳点。
- 虽然 `best` 矩阵中有非零单元，但 `x/y` 坐标数组缺失，且 `nx/ny` 未给出；扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 当前点 `ignited=0.0`，说明该点并未显示点火；因此后续若关注自持能力，需要继续看 `ignited`、`Pfus`、`Qfus` 与损失项的联动，而不能只看 `valid`。
- 边界类关键信息不完整：未提供 `betaN/betaT/betap`、`q/q95`、Greenwald/Sudo 归一化密度等字段，因此对稳定性边界或密度边界只能保持未知。
- 辐射与杂质结论也受输入设定限制：当前 `fimp=0.0`、`fHe=0.0`，所以 `P_line=0.0` 不能外推到含杂质或含灰分更高的情形。

4. 下一步建议
- 执行一次补全版 `Ti0-ni0` POPCON 扫描：保留当前 `xkey=Ti0`、`ykey=ni0`，但必须输出明确的 `x`、`y` 数组以及 `nx/ny`。重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕当前点 `Ti0=5.0`、`ni0=6e20` 做局部加密扫描，验证 `best` 非零单元是否在当前点附近连续出现。重点监控 `best` 矩阵是否连通，以及 `Pbrem`、`Pcycl`、`beta`、`nbar` 是否同步恶化。
- 做 `tauE` 敏感性扫描，因为 `use_tauE=1.0`。可在固定其他参数下改变 `tauE` 输入，检查 `Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pwall` 对固定约束时间假设的依赖程度。
- 做成分/辐射验证扫描：在流程允许时改变 `fimp`、`fHe` 或等效 `Zeff` 设定，复核 `P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus` 的变化，避免只依赖当前 `fimp=0.0` 的结果判断。
- 若后续要做壁面与几何一致性检查，可把 `Pwall` 与 `Sw`、`Vp`、`A_throat` 联合核对；重点监控 `Pwall`、`Sw`、`Vp`、`A_throat` 以及 `Pn`。
