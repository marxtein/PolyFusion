## 1. 核心结论

- `config=mirror`，`preset=BEAM`。
- 当前运行点输出为：`valid=1.0`，`ignited=1.0`，`Pfus=215.00307295063465`，`Qfus=1000.0`，`Pheat=-16.092265556254713`，`Pwall=8.739168593848307`。
- 这表示当前单点在本次 0-D 计算中被判为有效且点火；但这只是“当前运行点”的结论，不能直接替代参数扫描对工作窗的判断。
- `last_scan` 显示进行了以 `Ti0` 为横轴、`ni0` 为纵轴的 POPCON 扫描，并给出了 `best` 矩阵，`n_invalid=0`。按 `best` 矩阵证据，扫描中存在被标记为最佳的离散网格点。
- 但本次 JSON 未提供 `x/y` 数值数组，也未提供 `nx/ny` 元数据，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1.0`，因此结果依赖固定约束时间假设。

## 2. 关键指标解读

- 运行点方面：`valid=1.0` 与 `ignited=1.0` 说明该组输入在当前模型下被接受，并被标记为点火状态。功率相关输出中，`Pfus=215.00307295063465`，`Pn=172.00245836050772`，`Pwall=8.739168593848307`。
- 功率收支方面：显式给出的损失/通道包括 `Ptrans=26.331008961094007`、`Pbrem=0.5759102307419611`、`Pcycl=0.0014298420362469874`、`P_line=0.0`，以及 `P_ei=224.95775420714398`。`P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0` 也都为零。
- 约束时间方面：输出中 `tau_E=1.0`，参数中 `tauE=1.0` 且 `use_tauE=1.0`，说明这次结果直接依赖固定的能量约束时间输入，不是由缩放律自行求出的结果。
- 边界/状态量方面：给出了 `beta=0.99`、`beta_avg=0.396`、`nbar=4.095290423429552e+20`、`ne0=5.2142857142857145e+20`、`ntau=5.2142857142857145e+20`。本次数据未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能按这些口径继续展开。
- 成分与辐射方面：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`，同时 `P_line=0.0`，当前结果里线辐射没有贡献，轫致辐射仅表现为 `Pbrem=0.5759102307419611`。
- 几何方面：给出了 `Vp=2.88398205599543`、`Sw=22.76083877525805`、`Sp=19.736508888662737`、`A_throat=0.0028274333882308154`、`R_mc=99.99999999999996`。但未提供 `geom_model` 字段，无法判断几何模型口径是否为简化模型。
- POPCON 扫描方面：`best` 矩阵不是全零，因此当前判据下并非“完全没有最佳点”；但这与“当前运行点 valid=1”是两件事。前者描述的是扫描网格内哪些离散点被标记为最佳，后者描述的是当前单点是否有效。

## 3. 风险与不确定性

- `Qfus=1000.0`，但同时给出了 `Qfus_raw=-13.360646591310298` 和 `Pheat=-16.092265556254713`。这说明当前增益相关口径至少存在“报告值”和“原始值”并存的情况，解释时必须谨慎，不能只看 `Qfus=1000.0` 单独下结论。
- 由于 `use_tauE=1.0`，结果对固定 `tauE` 假设敏感；这里不能把单次输出直接推广为对约束时间变化不敏感的结论。
- 虽然有 `best` 矩阵和 `n_invalid=0`，但缺少 `x/y` 数值数组与 `nx/ny` 元数据，所以**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `fimp=0.0`、`P_line=0.0` 表明当前样例没有给出杂质线辐射负担的证据；对杂质与辐射敏感性的判断仍是不确定的。
- 未提供 `geom_model`、`LH_ratio` 或其他 L-H 阈值字段，因此不能对几何模型细节或 L-H 阈值比较作进一步判断。

## 4. 下一步建议

- 做一次当前运行点的功率收支复核，重点核对 `Qfus`、`Qfus_raw` 与 `Pheat` 的计算口径是否一致。需要监控：`valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pwall`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`、`Pn`。
- 重新输出一版完整 POPCON 扫描，保留 `xkey=Ti0`、`ykey=ni0`，同时补齐 `x/y` 数值数组和 `nx/ny`；并在当前 `Ti0=172.14285714285714`、`ni0=5.2142857142857145e+20` 附近加密网格。需要监控：`best`、`n_invalid`、各网格点的 `valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`beta`、`beta_avg`。
- 针对固定约束时间假设做敏感性扫描，直接扫描 `tauE`，或在工具支持时切换到可用的 `tauE_scaling` 口径做对照。需要监控：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pwall`。
- 做成分/辐射验证扫描，至少扫描 `fimp`、`fHe`、`Zeff`，检查 `P_line=0.0` 是否只是在当前输入下成立。需要监控：`P_line`、`Pbrem`、`Pfus`、`Pwall`、`valid`、`ignited`。
