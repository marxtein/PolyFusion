1. 核心结论
- `config=mirror`（`config_label=磁镜 Magnetic Mirror`），`preset=BEAM`。
- 当前运行点与 POPCON 扫描需要分开看：当前运行点报告 `valid=1.0`、`ignited=1.0`，并给出 `Pfus=98.92284038564279`、`Qfus=1000.0`、`Pheat=-12.360445040910676`、`Pwall=3.8031285313979257`。
- 就当前运行点本身看，这是一组“已计算出有效且点火标志为 1”的结果；但 `Pheat` 为负，且同时存在 `Qfus_raw=-8.003177883824359`，说明功率平衡/增益定义还需要单独核对，不能仅凭 `valid` 和 `ignited` 两个标志替代一致性检查。
- POPCON 扫描单独看：`xkey=Ti0`、`ykey=ni0`，`best` 矩阵并非全 0，按矩阵可数出 64 个网格单元中有 10 个为 1，`n_invalid=0`。这说明扫描中存在满足当前“best”判据的网格点，但这不等同于“当前运行点有效”。
- 由于缺少 `x/y` 数组，且未提供 `nx/ny`，必须明确：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0`，且输出 `tau_E=1.0`，因此结果依赖固定约束时间假设。

2. 关键指标解读
- 功率相关：`Pfus=98.92284038564279`，中子功率 `Pn=79.13827230851423`，壁负荷 `Pwall=3.8031285313979257`；损失项中 `Pbrem=0.28109260839929445`、`Pcycl=0.001195120562953398`、`P_line=0.0`，输运项 `Ptrans=7.141835307255634`，电子离子交换 `P_ei=34.34219826780031`。
- 点火/增益相关：输出给出 `ignited=1.0` 与 `Qfus=1000.0`，但同时有 `Qfus_raw=-8.003177883824359` 和负的 `Pheat`；因此这里更适合表述为“模型输出包含点火与高增益标志”，而不是直接把它当成功率平衡已自洽的结论。
- 约束与假设：`use_tauE=1.0`、`tauE=1.0`、输出 `tau_E=1.0`，表示本次不是由约束时间标度自洽求得，而是使用固定约束时间输入；`use_tauC=0.0`，但输出仍给出 `tauC_eff=845.066631270186` 供参考。
- 等离子体状态：`nbar=2.861093309519276e+20`，`ne0=3.642857142857143e+20`，`ntau=3.642857142857143e+20`；`beta=0.99`、`beta_avg=0.396` 反映该解对应较高的 beta 水平，但 JSON 中未提供 `betaN/betaT/betap`，因此不能进一步做归一化 beta 判读。
- 成分与辐射：`Zeff=1.0`，参数中 `fHe=0.0`、`fimp=0.0`、`Zimp=10`；在这组输入下，线辐射 `P_line=0.0`，轫致辐射和回旋辐射都较小于 `Pfus`。
- 几何与装置量：`Vp=2.88398205599543`、`Sw=22.76083877525805`、`Sp=19.736508888662737`、`A_throat=0.0028274333882308154`、`B0=0.30000000000000016`、`M=2.5`、`R_mc=99.99999999999996`，这些量说明本次解是在磁镜构型参数下得到的；但未提供 `geom_model`，因此不能额外评价几何模型简化程度。

3. 风险与不确定性
- 最大的不确定性来自约束时间处理：`use_tauE=1.0` 意味着结果依赖固定约束时间假设，`valid=1.0` 和 `ignited=1.0` 只能说明在该假设下模型求得了一个解。
- `Qfus=1000.0` 与 `Qfus_raw=-8.003177883824359` 同时出现，且 `Pheat=-12.360445040910676`，这三者之间存在明显需要复核的一致性问题；在未澄清定义、截断规则或符号约定前，不宜把增益结果直接用于下游判断。
- POPCON 部分虽然有 `best` 矩阵且 `n_invalid=0`，但由于缺少 `x/y` 数组且未提供 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- JSON 未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo` 一类边界比值，因此无法基于这份 0-D 数据判断接近哪些运行边界。
- 若干通道为 0（如 `P_line=0.0`、`P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0`）是当前模型输出事实，但仅凭这份 JSON 不能区分是物理上很小、参数设置关闭，还是模型路径未激活。

4. 下一步建议
1. 重新执行 `Ti0-ni0` 的 POPCON 扫描，并显式导出 `x`、`y`、`nx`、`ny`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pwall`。
2. 围绕固定约束时间做敏感性扫描，例如对 `tauE` 在当前值 `1.0` 附近做单参数或二维扫描，并与当前固定 `tauE` 结果对照；重点监控 `tau_E`、`Pfus`、`Pheat`、`Qfus`、`Qfus_raw`、`ignited`、`beta`、`beta_avg`、`nbar`。
3. 对当前运行点做一次功率平衡复算或逐项审计，核对 `Pheat`、`Ptrans`、`P_ei`、`Pbrem`、`Pcycl`、`P_line`、`P_alpha_loss`、`P_end_flux`、`P_coll_flux`、`Pn`、`Pwall` 的定义和符号，特别确认为什么会出现 `ignited=1.0` 但 `Pheat<0`、`Qfus_raw<0`。
4. 在当前基线附近增加成分扫描，如 `fHe`、`fimp` 的小范围扫描；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Pwall`、`valid`、`ignited`，以验证当前“低辐射、低杂质”解对成分扰动是否敏感。
