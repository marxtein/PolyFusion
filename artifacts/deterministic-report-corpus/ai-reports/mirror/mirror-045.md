1. 核心结论
- `config=mirror`（`config_label=磁镜 Magnetic Mirror`），`preset=BEAM`。
- 当前运行点结果：`valid=1.0`，`ignited=1.0`，`Pfus=108.69190441258188`，`Qfus=1000.0`，`Pheat=-5.873907028632749`，`Pwall=4.517320227043498`。
- 这表示本次 0-D 求解中，当前单个运行点被判为“有效”且“点火”；但这只对应本次输入点（如 `Ti0=144.28571428571428`、`ni0=3.642857142857143e+20`、`Te0=10.0`），不能直接等同于“扫描已明确找到工作窗”。
- POPCON 扫描与运行点需要分开看：`last_scan` 给出 `xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，且 `best` 矩阵存在非零项，说明扫描中至少有部分格点满足当前 `best` 判据；但由于缺少 `x/y` 数组且未显式提供 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0` 且 `tauE=1.0`，因此这些结果依赖固定约束时间假设。

2. 关键指标解读
- 运行点功率相关字段显示：`Pfus=108.6919`，`Pn=86.9535`，`Ptrans=15.5822`，`Pbrem=0.2811`，`Pcycl=0.001195`，`P_line=0.0`，`Pwall=4.5173`；其中 `Pheat=-5.8739` 为负，说明按当前字段定义，外加/净加热项需要结合功率平衡口径一起理解。
- 约束时间与约束假设：输出中 `tau_E=1.0`，参数中 `use_tauE=1.0`、`tauE=1.0`；这意味着结果不是从额外约束标度自由求出的扫描结论，而是建立在固定约束时间输入之上。`H98`、`H_ITPA20`、`H_ISS04` 等字段未提供，不能据此做交叉判读。
- 等离子体状态与成分：`beta=0.99`，`beta_avg=0.396`，`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`，`f_fast_ion=0.1698293068137596`；`nbar=2.861093309519276e+20`，`ne0=3.642857142857143e+20`，`ntau=3.642857142857143e+20`。这些值说明本次输出已给出密度、有效电荷数和快离子份额，但没有给出 Greenwald/Sudo 比值类边界指标。
- 辐射与损失项方面：`Pbrem` 与 `Pcycl` 非零，但 `P_line=0.0`，同时 `P_alpha_loss=0.0`、`P_coll_flux=0.0`、`P_end_flux=0.0`、`Past_domain=0.0`。从字段上看，当前运行点的损失记账较依赖模型定义与开关状态。
- 几何相关输出包含 `Vp=2.88398205599543`、`Sw=22.76083877525805`、`Sp=19.736508888662737`、`A_throat=0.0028274333882308154`；输入还给出 `B_vac=3.0`、`B_expand=100.0`、`R_mirror=10.0`、`Rw=0.8`、`L_c=10.0`。`geom_model` 字段未提供，因此这里只能按现有几何量解读，不能判断是否采用了更简化或更完整的几何模型。
- POPCON 扫描解读应独立于单点：`best` 矩阵不是全零，说明扫描中确有格点被标记为 `best`；但由于没有 `x/y` 数组与显式 `nx/ny`，不能描述最佳区位于哪里、边界如何，或 `Ti0`/`ni0` 改变时是否呈现单调趋势。

3. 风险与不确定性
- `Qfus=1000.0` 与 `Qfus_raw=-18.504192164219827` 同时出现，且 `Pheat=-5.873907028632749` 为负；这提示 `Qfus` 可能经过截断、封顶或特殊后处理，直接把 `Qfus` 当作未处理增益指标存在口径风险。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是 `last_scan` 未提供 `x/y` 数组，且 `nx/ny` 未显式给出；因此即使 `best` 矩阵有非零项，也不能据此判断最佳区位置、边界或随参数变化的趋势。
- “当前运行点有效/点火”和“扫描中有 `best` 格点”不是同一件事：前者是单点求解结果，后者是扫描判据结果；不能用 `valid=1.0`、`ignited=1.0` 去替代对扫描工作窗的判断。
- `use_tauE=1.0` 使结果显著依赖固定约束时间假设；在没有额外 `tauE` 敏感性扫描前，`Pfus`、`Qfus`、`ignited` 对约束时间变化的敏感程度仍不清楚。
- 多个损失项在本次输出中为 `0.0`，包括 `P_alpha_loss`、`P_coll_flux`、`P_end_flux`、`Past_domain`、`P_line`；这可能来自模型设定、开关状态或特定记账方式，需要先确认字段定义，再做工程解释。
- `geom_model`、`H98/H_ITPA20/H_ISS04`、`nx/ny` 等关键信息缺失，限制了对几何近似程度、约束标度一致性和扫描分辨率的判断。

4. 下一步建议
1. 重新导出完整 POPCON 扫描数据，至少补齐 `x`、`y`、`nx`、`ny` 和 `best_fraction`（若程序支持）；重点监控 `n_invalid`、`best` 非零格点数、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
2. 围绕当前运行点对 `Ti0` 与 `ni0` 做更细网格扫描；重点监控 `valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`beta`、`nbar`，并记录哪些格点进入 `best`。
3. 做 `tauE` 敏感性验证：在保持其余参数不变时扫描 `tauE`，或在模型支持时比较 `use_tauE=1` 与非固定 `tauE` 的结果；重点监控 `tau_E`、`Pfus`、`Qfus`、`Qfus_raw`、`ignited`、`Ptrans`、`Pbrem`、`Pwall`。
4. 做功率平衡字段一致性核对，重点检查 `Qfus` 与 `Qfus_raw` 的关系，以及 `Pheat` 为负时各损失/输运项的记账定义；重点监控 `P_alpha_loss`、`P_coll_flux`、`P_end_flux`、`Past_domain`、`P_line`、`Pbrem`、`Pcycl`。
5. 若后续版本可输出更多几何或约束标度字段，补充导出 `geom_model` 及相关修正量、约束标度指标；重点监控 `Vp`、`Sw`、`Sp`、`Pwall`、`tau_E` 与新增几何/标度字段之间的一致性。
