1. 核心结论

- `config=mirror`，`preset=BEAM`。当前运行点 `valid=1.0`、`ignited=1.0`，报告值为 `Pfus=273.35`、`Qfus=1000.0`、`Pheat=-18.97`、`Pwall=11.18`。
- 当前运行点与 POPCON 扫描需要分开看：就单点结果而言，这个运行点被模型判为“有效”且“点火”；但这不等同于扫描已经证明存在宽广或稳定的工作窗。
- POPCON 扫描给出了 `xkey=Ti0`、`ykey=ni0` 和 `best` 矩阵，且 `n_invalid=0`，说明扫描中的网格点评价未出现无效点；按 `best` 矩阵计数，64 个格点中有 11 个为 1，最佳区占比约 `17.2%`。
- `best` 矩阵并非全 0，因此不属于“当前准则下未找到最佳区”的情形；但由于未提供 `x/y` 取值数组，不能把这些格点精确映射到具体的 `Ti0`、`ni0` 数值边界。
- `use_tauE=1.0` 且 `tauE=1.0`，因此本次结果依赖固定约束时间假设；这会直接影响对 `Pfus`、`Qfus`、点火状态和扫描最佳区的解读。

2. 关键指标解读

- 运行点功率面：`Pfus=273.35`，中子功率 `Pn=218.68`，壁负荷 `Pwall=11.18`；同时 `Ptrans=34.93`，`Pheat=-18.97`，说明按该 0-D 收支，净加热项呈负值，这一点需要与“`ignited=1.0`”一起谨慎解读。
- 增益指标：`Qfus=1000.0`，但同时存在 `Qfus_raw=-14.41`。这两个量明显不一致，至少说明当前输出中有截断、封顶、保护值或定义差异的可能；仅凭这份 JSON，不能把 `Qfus=1000` 直接当作无歧义的物理结论。
- 约束与时间尺度：`tau_E=1.0`、`tauC_eff=1084.54`、`tau_Past=0.648`、`tau_m=0.377`、`tau_rho=0.896`、`tau_gd=0.00102`、`tau_eq_ie=0.0755`。其中本次最关键的是 `tau_E` 来自固定输入而非自洽标度，因此结果对该假设敏感。
- 辐射与损失：`Pbrem=0.763`、`Pcycl=0.00153`、`P_line=0.0`、`P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0`。就已给数值看，线辐射为 0，回旋辐射很小，轫致辐射存在但相对聚变功率较小；不过端损失和碰撞通量损失均为 0，也值得在后续核实其模型定义。
- 等离子体状态：`beta=0.99`、`beta_avg=0.396`、`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`、`f_fast_ion=0.1698`。这表示本次计算采用了非常“干净”的成分设定，没有 He 灰分和杂质分数输入，对辐射和功率平衡会有直接影响。
- 几何与装置量：`Vp=2.884`、`Sw=22.761`、`Sp=19.737`、`A_throat=0.00283`、`B0=0.3`、`B_vac=3.0`、`R_mirror=10.0`、`B_expand=100.0`。JSON 中未给出 `geom_model` 字段，因此这里不能进一步判断几何模型是否为简化版本。
- POPCON 扫描本身：扫描维度键为 `Ti0` 和 `ni0`，`best` 矩阵显示最佳格点主要集中在矩阵左上区域，后半区域大多为 0；这只能说明在已扫描离散网格上，“满足当前 best 准则”的点分布不均匀，不能据此外推出连续边界或单调趋势。

3. 风险与不确定性

- 最大不确定性来自固定约束时间假设：`use_tauE=1.0` 使结果强依赖输入的 `tauE=1.0`，因此单点的 `valid/ignited` 判定和扫描中的最佳区分布，都不是独立于约束时间模型的结论。
- `Qfus=1000.0` 与 `Qfus_raw=-14.41` 同时出现，且 `Pheat` 为负、`ignited=1.0` 为正，说明关键指标之间存在明显张力；仅凭当前 JSON，无法判断是定义差异、数值截断还是报告口径不同。
- 扫描结果不能替代运行点结论：`n_invalid=0` 只说明扫描点评价成功，不代表工作窗一定宽，也不代表当前运行点附近一定有连续的可接受区域。
- 尽管给出了 `best` 矩阵，但未提供 `x/y` 数组、`nx/ny` 显式字段和每个网格对应的数值坐标，因此不能判断最佳区的精确数值边界、面积对步长的敏感性，或其对 `Ti0`、`ni0` 的局部斜率。
- 成分设定较理想化：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0` 会压低辐射和稀释效应；因此当前功率平衡和最佳区分布对杂质、灰分的鲁棒性尚无证据。
- 若干损失通道为 0：`P_line=0`、`P_end_flux=0`、`P_coll_flux=0`、`P_alpha_loss=0`。这些“零值”可能来自模型假设、开关状态或当前参数组合，而不是普遍结论，需要单独确认。

4. 下一步建议

- 开展 `tauE` 敏感性扫描：围绕当前 `tauE=1.0` 做多点扫描，并持续监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pwall`、`tau_E`，优先确认固定约束时间假设对结论的影响幅度。
- 对 `Ti0`-`ni0` 进行加密网格扫描：保留 `xkey=Ti0`、`ykey=ni0`，输出完整 `x/y` 数组和显式 `nx/ny`，同时监控 `best`、`n_invalid`、最佳区占比、`Pfus`、`Pheat`、`Pwall`、`beta`，用于判断最佳区是否受网格分辨率影响。
- 做成分与辐射验证扫描：分别改变 `fHe`、`fimp`、`Zimp`、`Zeff` 相关输入，监控 `Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`Pwall`、`ignited`，检查当前“低辐射、零杂质”设定对结果的依赖程度。
- 核对功率收支口径：重点复查 `Qfus` 与 `Qfus_raw` 的定义和计算路径，并同步跟踪 `Pn`、`Ptrans`、`P_ei`、`P_alpha_loss`、`P_end_flux`、`P_coll_flux`、`Pheat`，确认是否存在封顶值、符号约定或统计口径差异。
- 扩展镜机相关参数扫描：对 `R_mirror`、`B_expand`、`B_vac/B0`、`f_throat`、`L_c` 做参数扫描，监控 `Pwall`、`Pfus`、`tauC_eff`、`tau_m`、`tau_rho`、`beta`、`valid`，用于辨别当前结论对几何与约束参数的敏感性。
