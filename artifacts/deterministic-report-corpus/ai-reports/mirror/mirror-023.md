1. 核心结论
- 本次 0-D 结果对应 `config=mirror`（磁镜）、`preset=BEAM`。
- 当前运行点判定为 `valid=1.0`、`ignited=1.0`；功率相关字段给出 `Pfus=202.6758233045331`、`Qfus=1000.0`、`Pheat=-29.735197579703687`、`Pwall=7.598165754454659`。
- 运行点结果显示聚变功率较高，但同时存在 `Qfus_raw=-6.8160240994286605`，与 `Qfus=1000.0` 明显不一致；这一点需要优先核对定义、截断规则或后处理逻辑。
- POPCON/扫描与运行点必须分开看：当前运行点的 `valid=1.0`、`ignited=1.0` 只说明这个单点在当前模型下被判为有效且点火；扫描层面则是 `xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，且 `best` 矩阵存在若干 `1`，说明按当前离散判据有部分扫描点被标记为“最佳”，但这不等同于当前运行点一定落在最佳区。
- 由于 `use_tauE=1.0`，本次结果依赖固定约束时间假设，具体为 `tauE=1.0`、输出中 `tau_E=1.0`。
- 扫描缺少 `x/y` 数组，也未显式提供 `nx/ny`；因此即使有 `best` 矩阵，仍应写明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点功率平衡上，输出给出 `Pfus=202.6758233045331`、中子功率 `Pn=162.1406586436265`、输运功率 `Ptrans=10.22262700842473`、韧致辐射 `Pbrem=0.5759102307419611`、回旋辐射 `Pcycl=0.0014298420362469874`、线辐射 `P_line=0.0`；其中 `Pheat=-29.735197579703687` 需要结合程序符号定义理解，不能直接把负值解释成单一物理结论。
- 约束与时间尺度方面，结果直接依赖固定 `tauE`：`use_tauE=1.0`、`tauE=1.0`、`tau_E=1.0`；同时给出 `tauC_eff=1011.0375725054447`、`tau_Past=0.1624640345979931`、`tau_m=0.12779592541838158`、`tau_gd=0.0024013643469364392`、`tau_rho=0.5683686392942408`。输入中未提供 `H98/H_ITPA20/H_ISS04`，因此不能做对应约束标尺比较。
- 边界与等离子体状态方面，给出 `beta=0.99`、`beta_avg=0.396`、`nbar=4.095290423429552e+20`、`ne0=5.2142857142857145e+20`、`ntau=5.2142857142857145e+20`；但未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，所以不能对归一化边界余量做进一步判断。
- 成分与辐射方面，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；在当前输入下，杂质分数为零、线辐射也为零，辐射损失主要体现为 `Pbrem` 和很小的 `Pcycl`。这说明当前结果对“成分设定”的依赖值得后续单独扫描。
- 几何与磁镜相关量方面，输出给出 `Vp=2.88398205599543`、`Sw=22.76083877525805`、`A_throat=0.0028274333882308154`、`B0=0.30000000000000016`、`R_mc=99.99999999999996`；输入参数还包括 `R_mirror=10.0`、`B_expand=100.0`、`L_c=10.0`、`Rw=0.8`。输入中未提供 `geom_model`，因此不能对几何模型是否简化作额外判定。
- POPCON 方面，只能谨慎解读：`best` 矩阵中确有 `1`，按矩阵索引看，这些 `1` 主要出现在前几行、前几列的部分单元；但因为缺少 `x/y` 实际取值数组，不能把这些单元转写成 `Ti0/ni0` 的具体数值区间，也不能判断最佳区边界或敏感性趋势。

3. 风险与不确定性
- 最大的不确定性来自 `Qfus=1000.0` 与 `Qfus_raw=-6.8160240994286605` 的冲突；在未澄清这两个量的关系前，不宜把 `Qfus` 直接当作稳定结论使用。
- `Pheat=-29.735197579703687` 说明功率平衡量的符号和定义需要核对；如果定义未澄清，围绕“自持程度”或“外加加热需求”的解读会有歧义。
- 结果依赖固定约束时间假设，因为 `use_tauE=1.0`；这意味着当前结论对 `tauE=1.0` 的设定敏感，不能把该单次结果外推为一般趋势。
- 扫描层面虽然 `n_invalid=0` 且 `best` 存在 `1`，但缺少 `x/y` 数组以及显式 `nx/ny`，所以扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 若干损失或通道在当前输出中为零，例如 `P_line=0.0`、`P_alpha_loss=0.0`、`P_end_flux=0.0`、`P_coll_flux=0.0`；这些零值需要确认是模型结果、参数选择还是通道未启用，不能直接视为“可忽略”。
- 当前输入未提供若干常见边界比较量，如 `q95`、`betaN`、`nbar_o_nGw`、`nbar_o_Sudo`，因此对边界接近程度的判断证据不足。

4. 下一步建议
- 先做一次完整的 `Ti0-ni0` 扫描重导出，要求同时保存 `x`、`y`、`best`、`nx`、`ny`；重点监控 `best`、`valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pwall`，用来补足工作窗位置与形状证据。
- 对固定约束时间做敏感性验证：围绕当前 `tauE=1.0` 做多点扫描，检查 `use_tauE=1` 假设下结果是否显著漂移；重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus_raw`、`Qfus`、`Pheat`、`ntau`、`beta`、`beta_avg`。
- 单独做成分/辐射扫描，至少扰动 `fHe`、`fimp`，必要时联动 `Zimp`；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`Pwall`，判断当前“低辐射”结果对成分设定的依赖。
- 针对磁镜参数做几何/损失敏感性扫描，例如 `R_mirror`、`B_expand`、`Rw`、`L_c`、`f_throat`；重点监控 `A_throat`、`Sw`、`Vp`、`tau_gd`、`tau_m`、`Pwall`、`valid`、`ignited`，验证壁负荷和损失时间尺度对几何设定的响应。
- 单独增加一次结果一致性核对任务，明确 `Qfus` 与 `Qfus_raw` 的计算关系，并复核 `Pheat` 的符号定义；这一步应至少记录 `Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Ptrans`、`Pbrem`、`Pcycl`、`Pn`。
