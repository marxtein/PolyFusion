1. 核心结论
- 本次结果对应 `config=mirror`（磁镜 Magnetic Mirror）、`preset=BEAM`；当前运行点给出 `valid=1.0`、`ignited=1.0`、`Pfus=222.69094910213337`、`Qfus=1000.0`、`Pheat=-21.656936274721787`、`Pwall=8.832451862272476`。
- 这说明在当前单点参数下，0-D 模型把该运行点判为“有效”且“点火标志为 1”；但这只针对当前运行点，不等同于 POPCON 扫描已经确认了工作窗。
- 当前点还报告 `Pn=178.15275928170672`、`Pbrem=0.5759102307419611`、`Pcycl=0.0014298420362469874`、`P_line=0.0`；从已给数字看，聚变功率显著高于已列出的辐射项，但 `Pheat` 为负，需结合功率平衡定义单独核对。
- POPCON 侧，`last_scan` 的扫描轴是 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵并非全 0，说明在已记录的离散采样点中存在满足当前最佳判据的点；但“运行点有效”和“扫描找到工作窗”不是一回事。
- 由于缺少 `x/y` 数组且未给出 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- **运行点**：当前输入点为 `Ti0=144.28571428571428`、`Te0=10.0`、`ni0=5.2142857142857145e+20`；对应输出 `Pfus=222.69094910213337`、`Pn=178.15275928170672`、`Pwall=8.832451862272476`、`Ptrans=22.303913472926684`、`P_ei=186.30862462970515`。
- **约束时间**：`use_tauE=1.0`，且输入 `tauE=1.0`、输出 `tau_E=1.0`，所以结果依赖固定约束时间假设；JSON 中未提供 `tauE_scaling`、`H98`、`H_ITPA20`、`H_ISS04`，不能据此比较不同约束标度。
- **边界/状态**：已给出 `beta=0.99`、`beta_avg=0.396`、`M=2.5`、`nbar=4.095290423429552e+20`、`ne0=5.2142857142857145e+20`、`ntau=5.2142857142857145e+20`；但未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能判断归一化边界裕量。
- **成分与辐射**：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；对应辐射项 `Pbrem=0.5759102307419611`、`Pcycl=0.0014298420362469874`、`P_line=0.0`。在当前输入里杂质与氦灰分数都设为 0，因此线辐射为 0 需要结合该输入前提理解。
- **几何**：已给出 `Vp=2.88398205599543`、`Sw=22.76083877525805`、`Sp=19.736508888662737`、`A_throat=0.0028274333882308154`，以及 `B0=0.30000000000000016`、`B_vac=3.0`、`B_expand=100.0`、`R_mirror=10.0`；JSON 中没有 `geom_model` 字段，因此这里不对几何模型复杂度做额外判断。
- **POPCON 扫描**：`n_invalid=0`，说明已记录扫描结果里没有被计为无效点的单元；`best` 矩阵存在若干 `1`，说明“有命中点”，但由于缺少 `x/y` 数组和 `nx/ny`，不能据此判断最佳区位置边界，也不能判断随 `Ti0`、`ni0` 变化的趋势强弱。

3. 风险与不确定性
- `Qfus=1000.0` 与 `Qfus_raw=-10.28266169680061` 同时出现，数值含义不一致，需先确认 `Qfus` 是否经过截断、封顶或特殊编码，否则对增益结论的解读会有偏差。
- `Pheat=-21.656936274721787` 为负，说明当前输出中的净加热项定义需要核对；在未澄清符号约定前，不宜把它直接当作常规外加热需求使用。
- `use_tauE=1.0` 使结果明显依赖固定约束时间假设；如果 `tauE` 轻微变化就会改变 `valid/ignited/Pfus`，则当前点结论对假设较敏感。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；虽然 `best` 矩阵不是全 0，但缺少 `x/y` 数组与 `nx/ny`，无法把离散命中点可靠映射到参数空间边界。
- 缺少 `betaN/q95/nbar_o_nGw/nbar_o_Sudo` 等归一化判据，也缺少 H 因子/约束标度信息；因此当前报告更像“点态 0-D 结果”，不能据此扩展为更强的边界或运行区判断。

4. 下一步建议
1. 补做一次 `Ti0`–`ni0` POPCON 扫描，并强制输出 `x`、`y`、`nx`、`ny` 与 `best_fraction`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`Qfus_raw/Qfus`。
2. 做 `tauE` 敏感性扫描，因为当前 `use_tauE=1`；可围绕 `tauE=1.0` 做多点扰动，检查 `tau_E`、`valid`、`ignited`、`Pfus`、`Pheat`、`Ptrans`、`tauC_eff` 对固定约束时间假设的响应。
3. 做成分/辐射验证扫描，优先扫 `fimp`、`fHe`，必要时联动 `Zeff` 一起核对；监控 `Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pwall`、`valid`，确认当前“零杂质、零氦灰”前提对结果有多大影响。
4. 对当前单点做一次功率平衡复核，逐项导出并检查 `P_ei`、`Ptrans`、`Pbrem`、`Pcycl`、`P_alpha_loss`、`P_end_flux`、`P_coll_flux`、`Past_domain`、`Pheat`、`Qfus_raw`；先澄清符号和记账方式，再决定是否扩展参数扫描。
