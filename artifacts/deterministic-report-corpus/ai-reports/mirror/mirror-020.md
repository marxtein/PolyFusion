# 1. 核心结论

本次 0-D 报告对应 `config=mirror`（磁镜）、`preset=BEAM`。当前运行点给出的状态为：`valid=1.0`、`ignited=1.0`、`Pfus=61.75944644003595`、`Qfus=1000.0`、`Pheat=-6.4879419905275455`、`Pwall=2.4283597364430505`。

运行点与 POPCON 扫描需要分开看：
- 对单一运行点，当前解被标记为有效且点火。
- 对 POPCON，扫描轴为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵并非全 0，说明扫描中存在满足当前 `best` 判据的网格点。
- 但本次数据未提供 `x/y` 扫描数组，也未给出 `nx/ny`。**扫描网格证据不足，不能判断工作窗形状或敏感性趋势。**

另外，`use_tauE=1.0`，且运行结果中 `tau_E=1.0`，这意味着本次结果**依赖固定约束时间假设**。

# 2. 关键指标解读

当前运行点的功率项显示：
- 聚变功率 `Pfus=61.75944644003595`。
- 中子功率 `Pn=49.407557152028765`。
- 壁负荷相关输出 `Pwall=2.4283597364430505`。
- 热输运/平衡项里，`Ptrans=5.684952399402446`，`Pbrem=0.17549156317640263`，`Pcycl=0.003503334900797072`，`P_line=0.0`。
- `Pheat=-6.4879419905275455` 为负值，说明当前输出里的净加热相关量采用了某种特定记号或平衡定义，不能直接按“外加热功率大小”去理解。
- `Qfus=1000.0`，但同时有 `Qfus_raw=-9.519111997333717`，这两个量需要结合程序定义一起核对，不能只取其一做扩展结论。

约束与等离子体状态方面：
- 约束时间使用值为 `tau_E=1.0`，参数侧 `tauE=1.0` 且 `use_tauE=1.0`，属于固定约束时间输入。
- 等离子体参数中，`beta=0.903968281557506`，`beta_avg=0.36158731262300237`。
- 密度相关量给出 `nbar=2.243994752564138e+20`、`ne0=2.8571428571428572e+20`、`ni0=2.8571428571428572e+20`。
- 成分方面，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；当前这组输入下线辐射 `P_line=0.0`。
- 几何相关输出包括 `Vp=2.9269799014856237`、`Sw=22.76083877525805`，还有 `A_throat=0.008761929054136496`、`Sp=20.176202046771536`、`R_mc=32.26953072504035`。

对 POPCON 本身，只能确认两点：
- 扫描维度名是 `Ti0- ni0`。
- `best` 矩阵存在非零元素，表示扫描内至少有部分网格点满足当前判据。

# 3. 风险与不确定性

首先，**“当前运行点有效/点火”不等于“扫描已经充分证明存在清晰工作窗”**。本次只知道 `best` 矩阵里有非零项，但缺少 `x/y` 数组与 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**，也不能描述最佳区的位置、边界或扩展方向。

其次，结果对约束假设敏感。`use_tauE=1.0` 表明本次结论建立在**固定约束时间假设**上，而不是由其他约束时间模型自动给出；因此 `Pfus`、`ignited`、`Pheat`、`Qfus` 对 `tau_E` 的依赖需要单独验证。

再次，功率与增益指标存在需要复核的地方：`Qfus=1000.0`、`Qfus_raw=-9.519111997333717`、`Pheat<0` 同时出现，提示应先核对程序中的 `Qfus` 定义、裁剪/封顶逻辑，以及功率平衡的符号约定，再做进一步工程解读。

最后，当前成分输入较理想化：`fHe=0.0`、`fimp=0.0`、`P_line=0.0`。这使得辐射与杂质影响在本次点上表现较弱，但不能据此外推到更一般情形。

# 4. 下一步建议

1. 补做一轮完整的 `Ti0- ni0` 扫描导出，明确保存 `x/y` 数组和 `nx/ny`。  
需要监控的指标：`best`、`valid`、`ignited`、`n_invalid`、`Pfus`、`Pheat`、`Pwall`、`beta`、`nbar`。

2. 做 `tauE` 敏感性扫描，或在可用时对比固定 `tauE` 与其他约束时间设定。  
需要监控的指标：`tau_E`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`ignited`、`Ptrans`、`Pbrem`、`Pcycl`。

3. 对当前功率平衡做一次结果一致性校核，重点检查 `Qfus`、`Qfus_raw` 与 `Pheat` 的定义和符号。  
需要监控的指标：`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pn`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`、`P_alpha_loss`。

4. 在当前基线附近增加成分/辐射扫描，例如 `fimp`、`Zimp`、`fHe`。  
需要监控的指标：`Zeff`、`Pbrem`、`P_line`、`Pfus`、`Pwall`、`valid`、`ignited`。
