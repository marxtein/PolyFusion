1. 核心结论
- 本次结果的 `config` 为 `mirror`，`preset` 为 `BEAM`；当前运行点 `valid=1.0`，说明该单点计算给出了有效结果；`ignited=0.0`，说明该运行点并未达到点火状态。
- 当前运行点的核心输出为：`Pfus=0.21459562648378244`、`Qfus=0.2237245504291335`、`Pheat=0.9591956987829875`、`Pwall=0.05157065329871448`。从这些数值看，聚变输出已被算出，但仍依赖外加加热，且壁负荷结果也已给出。
- 约束模型方面，`use_tauE=1.0` 且 `tau_E=1.0`，因此本次结果依赖固定约束时间假设；这意味着单点结果和扫描判读都要放在该假设前提下理解。
- POPCON 扫描与当前运行点需要分开看：当前运行点 `valid=1.0` 只代表这个单点可计算且有效，不等同于“扫描已经找到可接受工作窗”。扫描侧仅能确认存在 `best` 矩阵且其中有非零单元，表示在已扫描网格里有部分点满足当前 `best` 判据。
- 但本次扫描缺少 `nx`、`ny`，也未提供 `x`/`y` 数组；因此必须保守表述为：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点功率平衡：`Pheat=0.9591956987829875` 明显高于 `Pfus=0.21459562648378244`，对应 `Qfus=0.2237245504291335`；结合 `ignited=0.0`，可直接解读为该点不是靠自身聚变功率维持的点火态。
- 聚变与中子相关输出：`Pn=0.17167650118702596` 已给出，说明该 D-T 工况下中子功率分量被显式计算；`P_alpha_loss=0.0` 与 `f_alpha_used=1.0` 同时出现，表明模型里 alpha 相关处理是开启的，但该输出项本身为零。
- 辐射与能量损失：`Pbrem=0.09453412163464683`、`Pcycl=0.011586340041657223`、`P_line=0.0`。这表明当前辐射损失里轫致和回旋项存在，线辐射项在该组输入下为零。另有 `Ptrans=0.89599436240344`，是当前结果中不可忽视的能量输出项。
- 约束与经验量：`tau_E=1.0`、`tauC_eff=51.554638720079296`、`tau_m=4.350740674133738`、`tau_gd=0.06171858022605471`、`tau_rho=35.34987725169047`。其中最关键的是 `tau_E` 被固定使用，而不是由某个约束标度自洽求出。
- 等离子体状态：`nbar=1.626896195609e+20`，`ne0=2.0714285714285714e+20`，`ntau=2.0714285714285714e+20`；`beta=0.1390193645122528`，`beta_avg=0.05560774580490112`；`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。这说明当前输入假设中有效电荷较低，且 He 与杂质分数输入为零。
- 几何量：`Vp=2.9996879467093285`、`Sw=22.76083877525805`、`Sp=20.760108668653658`、`A_throat=0.02623548360160851`、`R_mc=10.777134628680763`。这些量说明模型已显式计算体积和受载面积，因此 `Pwall` 是基于该几何集合得到的结果。
- POPCON 扫描对象：`xkey="Ti0"`、`ykey="ni0"`，说明扫描是围绕离子温度和离子密度展开；`n_invalid=0` 说明已扫描网格中没有被标记为无效的点。与此同时，虽然 `best` 矩阵存在且不全为零，但由于未给出 `x`/`y` 轴数组和 `nx`/`ny`，不能严谨说明最佳区的具体位置、边界或趋势。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：结果依赖固定约束时间假设。如果 `tau_E` 假设变化，`Pfus`、`Qfus`、`ignited`、`Pwall` 等结论都可能明显变化。
- 当前运行点有效，不代表扫描层面已经建立了清晰工作窗。这里必须区分：`valid=1.0` 仅说明该单点成立；扫描是否存在连续、宽裕或敏感的区域，现有证据不足。
- 扫描网格信息不完整：缺少 `nx`、`ny`，也没有 `x`/`y` 数组；因此不能判断最佳区占比、连通性、边界斜率，或 `Ti0`、`ni0` 的敏感性趋势。按当前证据，只能说 `best` 矩阵中存在非零单元。
- 成分假设较理想化：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`P_line=0.0`。这会降低对杂质辐射和灰化效应的覆盖范围，因此对辐射相关结论应保守理解。
- 镜机端损相关项在结果中为零，如 `P_end_flux=0.0`、`P_coll_flux=0.0`；这并不自动表示实际端损可忽略，只能说明在当前 0-D 模型和这组输入下它们没有体现为非零输出。
- 未提供 `geom_model` 字段，因此不能判断几何模型是否采用简化版本，也不能补充相应几何不确定性说明。

4. 下一步建议
- 建议做 `tauE` 参数扫描：在保持 `config=mirror` 和 `preset=BEAM` 不变的前提下，围绕当前 `tauE=1.0` 做多点扫描，监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`、`Pbrem`、`Pcycl`。
- 建议补全 POPCON 网格证据：输出并保存 `x`、`y`、`nx`、`ny` 以及 `best_fraction`，继续以 `xkey=Ti0`、`ykey=ni0` 扫描，监控 `n_invalid`、`best` 矩阵、`valid` 分布，以及 `Pfus`、`Qfus`、`Pwall` 在网格上的变化。
- 建议做成分敏感性扫描：对 `fHe`、`fimp`、`Zimp` 分别扫描，重点监控 `Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`，验证当前“低杂质、无线辐射”的结果是否稳定。
- 建议做温度与密度的局部单参扫描：分别在当前 `Ti0=5.0`、`ni0=2.0714285714285714e+20` 附近做单变量扰动，监控 `valid`、`ignited`、`Pfus`、`Qfus`、`nbar`、`beta`、`Pwall`，用于判断当前运行点附近的响应方向。
- 建议做损失通道复核：针对 `P_end_flux`、`P_coll_flux`、`P_alpha_loss` 当前为零的情况，检查模型开关和输入覆盖范围，并在复核时同步监控 `tauC_eff`、`tau_m`、`tau_gd`、`tau_rho`、`Ptrans`。
