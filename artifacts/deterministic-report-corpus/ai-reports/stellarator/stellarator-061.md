1. 核心结论
- 本次案例 `config=stellarator`，`preset=HELIAS`；当前运行点 `valid=1`、`ignited=0`，对应 `Pfus=7662.22`、`Qfus=15.19`、`Pheat=504.37`、`Pwall=5.12`。
- 当前运行点可计算且被标记为有效，但未点火；这说明 0-D 求解在该参数点给出了自洽结果，不等同于已证明存在扫描工作窗。
- 约束时间处理上，`use_tauE=1` 且 `tauE_used=1.0`，因此本次结果依赖固定约束时间假设；同时给出了 `tau_ISS04=0.5145` 与 `H_ISS04=1.9435`，表示若对照 ISS04 标度，当前点相当于使用了高于标度值的约束水平。
- POPCON 扫描与当前运行点需要分开看：扫描给出了 `xkey=Ti0`、`ykey=ni0` 的 `best` 矩阵，矩阵中存在非零单元，因此在当前准则下并非“未找到最佳区”；但由于未提供 `nx`、`ny` 以及 `x/y` 数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点功率面：`Pfus=7662.22`，`Qfus=15.19`，外加加热量级 `Pheat=504.37`，表明该点的聚变功率相对外加加热较高；但 `ignited=0` 说明仍不能把它解读为点火态。
- 损失与壁负荷：辐射/损失项中 `Pbrem=166.35`、`Pcycl=55.42`、`P_line=0.0`，同时 `Ptrans=1815.06`；`Pwall=5.12` 给出壁面功率负荷量级，需与后续工程约束联动核查。
- 约束与经验标度：`use_tauE=1`、`tauE_used=1.0` 是本次结果的重要前提；对照经验量，`tau_ISS04=0.5145`、`H_ISS04=1.9435`、`PL_ISS04=2036.82`，说明当前解并不是直接由 ISS04 自洽给出的，而是依赖固定约束时间输入。
- 密度边界：`nbar=1.7116e20`，`n_Sudo=3.3067e20`，`nbar_o_Sudo=0.5176`，表示线平均密度约为 Sudo 限的 51.8%。
- 压强与几何相关量：`betaT=0.1059`，而 `beta_soft_limit=0.05`、`beta_o_limit=2.1173`；按给定数据，当前总 beta 已高于软限对应值，需把 beta 相关约束作为重点复核对象。几何方面给出 `Vp=1149.07`、`Sw=1593.49`，且 `geom_is_measured=0`，说明这里使用的不是测量几何输入，几何表征仍偏模型化。
- 成分与辐射背景：`Zeff=1.8673`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`；当前 `P_line=0.0`，因此本次结果中的总辐射构成主要体现在轫致和回旋项，而不是线辐射项。
- POPCON 证据：已提供 `best` 矩阵且 `n_invalid=0`，说明扫描点没有被标成无效；按矩阵可数得 64 个网格中有 14 个 `best=1` 单元，但因缺少 `x/y` 数组与 `nx/ny`，只能确认“存在满足当前准则的离散网格点”，不能可靠描述其物理位置、边界或趋势。

3. 风险与不确定性
- 最大不确定性来自约束时间假设：`use_tauE=1` 使结果直接依赖固定 `tauE` 输入，若改用经验标度或改变 `tauE`，`Pfus`、`Qfus`、`Pheat` 及是否仍为 `valid=1` 都可能明显变化。
- 当前运行点有效，不代表扫描意义上的工作窗已经被充分证明；运行点判断是单点求解结果，POPCON 扫描则需要完整网格与坐标信息来判断区域范围、连续性和敏感性，这两者不能混为一谈。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；原因是 JSON 未提供 `nx`、`ny` 以及 `x/y` 数组，因此无法把 `best` 单元映射到具体 `Ti0`、`ni0` 数值位置。
- `ignited=0` 提醒当前点并非点火态；即使 `Qfus` 较高，也不能据此外推为更强自持状态。
- `betaT=0.1059` 相对 `beta_soft_limit=0.05` 偏高，提示 beta 约束可能是主要风险源之一；但仅凭这份 0-D 数据，不能进一步下结论其后果程度。
- 几何输入存在模型化成分：`geom_is_measured=0`，且 `Vp_geom=Vp`、`Sw_geom=Sw`、覆盖比均为 1.0，这意味着几何不确定性尚未通过测量几何或独立修正得到压缩。
- 本次数据中未提供 `LH_ratio` 或 L-H 阈值比较结果，因此不能对 L-H 阈值满足情况作任何判读，更不能写成进入或维持某种约束模态的保证。

4. 下一步建议
- 开展 `Ti0`-`ni0` 二维重扫，并显式输出 `x`、`y`、`nx`、`ny` 与 `best_fraction`；监控指标至少包括 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid` 与 `best` 矩阵。
- 进行约束时间敏感性扫描：在保持其余参数不变时扫描 `tauE` 或切换到经验 `tauE_scaling` 方案；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`Ptrans` 以及 `valid/ignited` 是否跳变。
- 围绕 beta 约束做专门验证：扫描 `B0`、密度或温度输入，检查 `betaT`、`beta_o_limit`、`beta_soft_limit`、`Pwall` 与 `Pfus` 的联动变化，确认当前高 beta 解对输入扰动是否敏感。
- 做成分与辐射扫描：改变 `fHe`、`fimp`、`Zimp`，观察 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pheat`、`Pfus` 的变化，以确认当前结果对杂质和灰分设定的依赖程度。
- 补做几何一致性验证：若后续有更详细几何来源，替换或交叉检查 `Vp`、`Sw`、`iota_geom` 等输入；重点监控 `Pwall`、`betaT`、`nbar_o_Sudo`、`Pfus`、`Qfus` 对几何参数的响应。
