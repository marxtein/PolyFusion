1. 核心结论
- `config=stellarator`（仿星器），`preset=HELIAS`；当前运行点对应参数里给出的 `Ti0=10.0`、`ni0=2.6428571428571428e+20`。
- 当前运行点单点结果为：`valid=1.0`、`ignited=0.0`、`Pfus=1205.1322796734125`、`Qfus=2.2492844308506363`、`Pheat=535.7847425359427`、`Pwall=1.0925161541815192`。
- 这表示：该单点求解是有效的，但不是点火解；有聚变输出和有限增益，但“运行点有效”不等于 POPCON 扫描已经充分证明存在或刻画清楚某个工作窗。
- POPCON 扫描单独看：`xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，且 `best` 矩阵并非全 0，按矩阵计数有 `14/64` 个网格点为 1，说明在当前 `best` 判据下，扫描中至少有部分网格点被标记为可取。
- 但本次扫描缺少 `x`/`y` 数组，也未提供 `nx`/`ny`；因此必须强调：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 由于 `use_tauE=1.0`，本次结果依赖固定约束时间假设；这会直接影响对 `Pfus`、`Qfus`、`ignited` 和扫描判据的解读。

2. 关键指标解读
- 约束与输运：`tauE_used=1.0`，同时给出 `tau_ISS04=1.0981723740823808`、`H_ISS04=0.9106038574641687`；可理解为当前结果是在固定 `tauE=1.0` 的前提下，与 ISS04 标度做对照，而不是完全由约束时间标度自洽决定。
- 功率平衡：`Pfus=1205.13`、`Pheat=535.78`、`Qfus=2.249`；`ignited=0.0` 说明该点不能直接按“自持燃烧”理解。损失相关量里，`Pbrem=153.136` 明显高于 `Pcycl=1.851`，`P_line=0.0`，就已给出的辐射项看，轫致辐射是主要已显式列出的辐射损失项。
- 壁负荷与几何：`Pwall=1.0925`，几何量给出 `Vp=1149.0701`、`Sw=1593.4932`，且 `Vp_geom=Vp`、`Sw_geom=Sw`，`Sw_override=0.0`、`Vp_override=0.0`；这说明当前壁负荷直接建立在这组几何输入之上。
- 密度相关：`nbar=2.345535515117665e+20`，`n_Sudo=2.042087565626156e+20`，`nbar_o_Sudo=1.1485969331576944`；按这个比值字段本身看，当前点的平均密度高于对应的 Sudo 参考值。
- 压强与边界：`betaT=0.03626852231904212`，输出中同时有 `beta_soft_limit=0.05`；就这两个字段直接比较，当前 `betaT` 没有超过该软限值字段。另有 `beta_o_limit=0.7253704463808422`，但其具体定义仍需结合代码判据。
- 成分与杂质：`Zeff=1.8672566371681416`，输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；这些设定与 `Pbrem`、`Pfus`、`Qfus` 的结果耦合，应作为后续敏感性扫描的重要轴。
- POPCON 结果本身：`best` 矩阵非全零，只能支持“存在被当前判据选中的网格点”这一点；由于缺少坐标数组，不能据此描述最佳区位于高/低 `Ti0`、高/低 `ni0`，也不能描述边界走向。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：当前结果依赖固定约束时间假设，若改用标度或改变 `tauE`，`Pfus`、`Qfus`、`ignited` 和 `best` 判定都可能变化。
- POPCON 证据不完整：虽然 `best` 有非零项且 `n_invalid=0`，但缺少 `x`/`y`/`nx`/`ny`，所以不能判断工作窗形状、连通性、边界陡峭程度或对 `Ti0`、`ni0` 的敏感方向。
- 当前点 `valid=1.0` 只说明单点方程求解成立，不等于该点周围参数区域同样成立；单点有效性与扫描工作窗是两件不同的事。
- `ignited=0.0` 表明该解不是点火状态，因此不能把当前 `Pfus`、`Qfus` 直接外推为自持运行能力。
- 几何侧信息有限：虽然给出了 `Vp`、`Sw` 等量，但 `geom_is_measured=0.0`，且没有提供 `geom_model` 字段；这意味着几何相关结论应限于当前输入，不宜外推到更细致的三维几何修正。
- `P_line=0.0` 的含义仅能按输出字面理解为当前结果中的线辐射项为零；这是模型设定、数值关闭还是该点自然结果，仅凭这份 JSON 不能进一步判断。
- 密度比值 `nbar_o_Sudo=1.1486` 偏高是一个需要继续核查的信号，但仅凭这份 0-D 数据，不能直接推出更强的工程结论。

4. 下一步建议
1. 补做完整 `Ti0` × `ni0` 扫描导出，明确输出 `x`、`y`、`nx`、`ny` 和 `best_fraction`；重点监控 `best`、`valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`nbar_o_Sudo`、`betaT`。
2. 做约束时间敏感性验证：在保持其余参数不变时扫描 `tauE`，并与当前 `use_tauE=1.0` 结果对照；若工具支持，再做固定 `tauE` 与标度模式的对比。重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`ignited`、`Pwall`。
3. 做成分与辐射扫描：分别扫描 `fHe`、`fimp`、`Zimp`，检查杂质和灰分对输出的影响。重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pheat`。
4. 做几何输入一致性检查或几何敏感性扫描：围绕 `R0`、`a`、`Rw` 或 `Sw_override`、`Vp_override` 做小范围扰动，确认 `Pwall` 和体表比的响应。重点监控 `Vp`、`Sw`、`Pwall`、`Pfus`、`Qfus`。
5. 对当前单点做边界复核：围绕现点附近小步长扫描 `ni0`，重点检查 `nbar_o_Sudo` 与 `betaT` 是否快速逼近或越过对应参考字段；同时继续记录 `valid`、`ignited`、`Pfus`、`Qfus`，避免把单点有效误判为区域性结论。
