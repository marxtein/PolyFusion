1. 核心结论
- 本次结果对应 `config=stellarator`、`preset=HELIAS`。当前运行点 `valid=1`，说明该 0-D 求解点给出了有效结果；`ignited=0`，说明该点并未显示点火。
- 当前运行点的关键功率量为：`Pfus=10736.964379120527`、`Qfus=685.4051524099995`、`Pheat=15.665135199768429`、`Pwall=6.7478353617425935`。
- 运行点与 POPCON 扫描需要分开看：当前运行点“有效”只代表这一个参数点求解成立，不等同于“扫描中已经找到完整工作窗”。
- POPCON 数据中存在 `best` 矩阵，且并非全 0，因此在当前准则下并非“未找到最佳区”；但由于缺少 `x/y` 扫描数组且 `nx/ny` 未给出，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1`，说明本次结果依赖固定约束时间假设；因此对功率平衡、增益和扫描结论的解读，都应放在这一假设前提下进行。

2. 关键指标解读
- 运行点功率平衡：聚变功率 `Pfus=10736.964379120527`，外加/净加热量 `Pheat=15.665135199768429`，对应 `Qfus=685.4051524099995`；同时壁负荷 `Pwall=6.7478353617425935` 是直接需要关注的面功率指标。
- 点火状态：`ignited=0`，因此该点不能按“自持燃烧”理解；虽然 `Qfus` 数值较高，但从给定字段看，系统仍未被标记为点火。
- 辐射与损失：`Pbrem=266.9002085072224`、`Pcycl=30.684478858677497`、`P_line=0.0`。在已给数据里，轫致辐射和回旋辐射是显式给出的辐射通道，线辐射在该结果中为 0。
- 约束与经验标度：`tauE_used=1.0`，`tau_ISS04=0.5879919412277763`，`H_ISS04=1.700703580923093`。但由于 `use_tauE=1`，这里应理解为结果采用了固定约束时间输入，相关输出对该假设敏感。
- 边界相关量：`betaT=0.1088055669571264`，`beta_soft_limit=0.05`，`beta_o_limit=2.176111339142528`；密度侧给出 `nbar_o_Sudo=0.6883214565848882`。这些字段可用于比较当前点与模型边界的相对位置，但不能单凭 0-D 输出推导更强工程结论。
- 成分与杂质：`Zeff=1.8672566371681416`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`。这些参数会影响辐射、稀释和功率平衡，是后续扫描的重要敏感量。
- 几何量：`Vp=1149.0700858727764`、`Sw=1593.4931630494737`。本次输出未提供 `geom_model` 字段，因此不对“几何模型是否简化”做额外判断。
- POPCON 扫描：扫描轴标签为 `xkey=Ti0`、`ykey=ni0`，并给出 `best` 矩阵，`n_invalid=0` 说明扫描中没有被标记为无效的点；但这不等于最佳区范围已被充分解析，因为 `x/y` 数组和 `nx/ny` 未提供。

3. 风险与不确定性
- 固定约束时间假设风险：`use_tauE=1` 使结果依赖预设 `tauE=1.0`，因此 `Pfus`、`Qfus`、`Pheat` 以及扫描中的“best”判定，都可能随约束时间设定而变化。
- 运行点与扫描不可混同：当前点 `valid=1` 只说明该单点可解；扫描里有 `best` 标记也只说明在离散网格上出现满足当前准则的点，不能据此替代对单点物理状态的判断。
- 扫描证据不完整：缺少 `x/y` 数组，且 `nx/ny` 未给出，因此必须保留“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”的不确定性。
- 最佳区定位能力有限：虽然 `best` 矩阵不全为 0，说明存在满足当前判据的离散网格点，但没有对应坐标数组时，不能把这些 1 的分布可靠地解释成最佳区边界、宽度或单调趋势。
- 0-D 模型边界含义有限：如 `betaT`、`nbar_o_Sudo`、`H_ISS04` 等量都来自当前模型框架，适合做相对比较，不宜外推出更强的装置级运行结论。
- 辐射项信息并不完整到可做详细机理判断：当前只有 `Pbrem`、`Pcycl`、`P_line` 显式给出，且 `P_line=0.0`，但这不足以单独说明所有杂质辐射行为。

4. 下一步建议
- 执行一组约束时间敏感性扫描：围绕 `tauE` 或改用非固定 `tauE_scaling` 方案重复计算，并持续监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`tauE_used`、`H_ISS04`。
- 补全 POPCON 扫描元数据并重跑：输出 `x/y` 数组以及 `nx/ny`，继续以 `Ti0`–`ni0` 为轴扫描，并监控 `best`、`n_invalid`、`Pfus`、`Qfus`、`Pwall`、`betaT`、`nbar_o_Sudo`，以便判断离散最优点是否稳定存在。
- 做成分与辐射参数扫描：针对 `fHe`、`fimp`、`Zimp` 开展参数扫描，并监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`Qfus`，检查结果对杂质和灰分设定的敏感性。
- 做边界相关扫描：围绕密度和温度输入扫描 `ni0`、`Ti0`，并监控 `betaT`、`beta_soft_limit`、`nbar_o_Sudo`、`valid`、`ignited`、`Pwall`，用于识别当前运行点附近哪些约束最先变得敏感。
- 做几何覆盖值验证：若后续可以提供几何替代参数或实测几何数据，建议对 `Vp`、`Sw` 相关输入做一致性复核，并监控 `Pwall`、`Pfus`、`tauE_used`、`betaT`，确认几何取值对结果的影响幅度。
