1. 核心结论
- 当前案例是 `config=stellarator`、`preset=HELIAS`。本次单点结果给出：`valid=1.0`、`ignited=1.0`、`Pfus=13834.91976973602`、`Qfus=1000.0`、`Pheat=-272.818162551755`、`Pwall=8.510925507349164`。
- 这说明当前运行点在该 0-D 模型内被标记为“有效”且“点火”；但这是单个运行点的判定，不能直接等同于 POPCON 扫描已经证明存在完整工作窗。
- POPCON 扫描是另一层证据：本次扫描的轴键为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵不是全零，说明当前准则下扫描中存在被标记为 `best` 的网格点。
- 同时，因 JSON 未提供扫描 `x`/`y` 数组，也未给出 `nx`/`ny`，必须明确：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点功率侧：`Pfus=13834.91976973602`，其中 `Pn=11067.935815788816`；损失项里 `Ptrans=2117.564313341484` 较大，辐射项以 `Pbrem=343.90939942055536` 为主，`Pcycl=32.69207863340958`，`P_line=0.0`。
- 约束与约化：`use_tauE=1.0`，`tauE_used=1.0`，所以结果依赖固定约束时间假设；相对 ISS04 标度，`tau_ISS04=0.5772503398691734`、`H_ISS04=1.7323506474273147`，可理解为当前点相对该标度所需的约束提升水平。
- 边界与态参：`betaT=0.12350902195133265`，并同时给出 `beta_soft_limit=0.05`、`beta_o_limit=2.470180439026653`；密度侧 `nbar=2.6624997739173498e+20`，`n_Sudo=3.6591421498681006e+20`，对应 `nbar_o_Sudo=0.7276295002677946`；旋转变换为 `iota=0.8220570026198248`。
- 成分与辐射相关：`Zeff=1.8672566371681416`，输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；在该结果里线辐射 `P_line=0.0`，而轫致辐射 `Pbrem` 非零。
- 几何量方面，输出使用了 `Vp=1149.0700858727764`、`Sw=1593.4931630494737`，并且 `Vp_geom`/`Sw_geom` 与其相同；输入里 `Vp_override=0.0`、`Sw_override=0.0`，说明本次并未使用额外覆盖值。
- POPCON 侧只能做有限解读：`best` 矩阵中有非零元素，按矩阵本身可数得 64 个网格里有 14 个点为 `1`；但因为缺少实际 `x`/`y` 取值数组，不能把这些点可靠映射到具体 `Ti0`/`ni0` 坐标。

3. 风险与不确定性
- `Qfus=1000.0`、`Qfus_raw=-50.711139025106014` 与 `Pheat=-272.818162551755` 同时出现，说明 `Qfus` 的读法需要非常谨慎；仅凭这份 JSON，不能把 `Qfus=1000` 直接当作稳定、可比的性能结论。
- 由于 `use_tauE=1.0`，本结果明确依赖固定 `tauE=1.0` 的假设；这会把结论对约束时间设定的敏感性保留下来，而不是完全由经验标度自洽决定。
- 扫描证据不完整：虽然有 `best` 矩阵且 `n_invalid=0`，但缺少 `x`/`y` 数组和 `nx`/`ny`，因此不能判断最佳区的实际位置、边界、连通性，也不能判断对 `Ti0` 或 `ni0` 的敏感性趋势。
- `valid=1.0` 和 `ignited=1.0` 只说明当前单点通过了模型内部判据；它们不自动推出相邻点也通过，更不等同于扫描层面的工作窗已经被充分刻画。
- 若关心辐射或能量通道完整性，`P_line=0.0`、`P_ei=0.0` 这类零值本身也值得复核，确认它们是场景设定结果还是模型简化结果。
- 几何不确定性仍存在：JSON 未提供 `geom_model` 字段，且 `geom_is_measured=0.0`、`iota_geom_authoritative=0.0`，说明这里看不到更强的几何权威性证据。

4. 下一步建议
1. 重新导出一版完整 POPCON 扫描，补齐 `x`/`y` 数组和 `nx`/`ny`，仍以 `Ti0`-`ni0` 为轴；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`Qfus_raw`。
2. 做一次约束时间敏感性扫描：在保留当前点附近参数的同时，改变 `tauE` 或对比固定 `use_tauE=1` 与按 `tau_ISS04` 标度的结果；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`。
3. 单独核对功率收支与增益定义，尤其是 `Qfus`、`Qfus_raw` 与负 `Pheat` 的对应关系；重点监控 `Qfus`、`Qfus_raw`、`Pn`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`。
4. 围绕当前输入点 `Ti0=30.0`、`ni0=3e+20` 做局部细化扫描，并补做成分敏感性检查（如 `fHe`、`fimp`、`Zimp`）；重点监控 `betaT`、`beta_soft_limit`、`nbar_o_Sudo`、`Zeff`、`Pbrem`、`Pwall`、`valid`、`ignited`。
