# 1. 核心结论
- 本次报告对象为 `config=stellarator`（仿星器），`preset=HELIAS`。
- 当前运行点的 0-D 输出为：`valid=1.0`，`ignited=0.0`，`Pfus=302.31882132116425`，`Qfus=1.2061180889440608`，`Pheat=250.6544127746563`，`Pwall=0.3470195209608516`。按当前判据，这个运行点被标记为有效，但未点火。
- 需要和 POPCON 扫描分开看：`last_scan.best` 矩阵存在非零单元，且 `n_invalid=0`，说明在 `xkey=Ti0`、`ykey=ni0` 的扫描里，有部分网格点满足当前最佳判据。
- 但本 JSON 未提供扫描 `x/y` 数组，且未给出 `nx/ny`，因此必须写明：**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。这也意味着“当前运行点有效”不等于“已充分证明扫描工作窗”。

# 2. 关键指标解读
- 约束时间与约束因子：`use_tauE=1.0`，`tauE=1.0`，`tauE_used=1.0`，`tau_ISS04=0.7809126916358639`，`H_ISS04=1.2805528847343866`。这表示本次结果依赖**固定约束时间假设**，并可与 ISS04 标度下的约束时间作对照。
- 功率与增益：`Pfus=302.3188`，`Pheat=250.6544`，`Qfus=1.2061`，`Pn=241.8551`。这些量描述了当前运行点的聚变功率、加热功率和功率增益。
- 辐射与损失：`Pbrem=8.6777`，`Pcycl=8.3343`，`P_line=0.0`。当前输出中的辐射项主要体现为轫致辐射和回旋辐射，线辐射项为 0。
- 阈值模型比较：`Ptrans=294.1061546307617`，`PL_ISS04=311.11817703888914`。这只能表述为**与给定阈值模型的比较结果**，不能写成保证进入或维持 H 模。
- 密度与边界相关量：`nbar=4.4374996231955825e+19`，`n_Sudo=1.2923475229887744e+20`，`nbar_o_Sudo=0.3433673639837299`，`betaT=0.01715403082657398`，`iota=0.8220570026198248`。
- 成分与杂质：`Zeff=1.8672566371681416`，参数中有 `fHe=0.04`，`fimp=0.01`，`Zimp=10`。这些量会影响功率平衡和辐射项。
- 几何相关量：`Vp=1149.0700858727764`，`Sw=1593.4931630494737`，`geom_is_measured=0.0`。JSON 未提供 `geom_model` 字段，因此不能据此直接判断几何模型简化程度。

# 3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：当前结论依赖固定 `tauE=1.0` 的假设，而不是完全由标度关系自洽决定。
- 当前运行点 `ignited=0.0`，因此不能把 `valid=1.0` 解释成点火成立。
- POPCON 方面，虽然 `best` 矩阵有非零单元，但由于缺少扫描 `x/y` 数组且未给出 `nx/ny`，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**，也不能描述最佳区的位置、边界或变化趋势。
- `Ptrans` 与 `PL_ISS04` 只能用于阈值模型比较，不能外推成模式进入或维持的保证性结论。
- `geom_is_measured=0.0` 且未提供 `geom_model`，说明几何相关输出还需要结合几何来源进一步核验，尤其是 `Vp`、`Sw` 对 `Pwall` 的影响。
- `P_line=0.0` 会直接影响对辐射分量的理解，后续应核对这是否由当前模型设置或当前工况本身导致。

# 4. 下一步建议
1. 重新导出同一组 `Ti0-ni0` POPCON 扫描的完整网格数据，至少补齐 `x`、`y` 数组和 `nx/ny`。监控指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`、`betaT`、`nbar_o_Sudo`、`Ptrans`、`PL_ISS04`。
2. 针对约束时间假设做敏感性扫描：在同一配置下扫描 `tauE`，并与可用的标度结果对照。监控指标：`tauE_used`、`tau_ISS04`、`H_ISS04`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`。
3. 针对成分与辐射做输入验证或扫描，优先检查 `fHe`、`fimp`、`Zimp` 对输出的影响。监控指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pwall`、`valid`。
4. 做几何量交叉核验，重点检查 `Vp`、`Sw` 及其覆盖设置对壁负荷结果的影响。监控指标：`Vp`、`Sw`、`Pwall`、`nbar_geom`、`nbar_o_Sudo`。
