1. 核心结论
- 本次结果对应 `config=stellarator`（`config_label=仿星器 Stellarator`），`preset=HELIAS`。当前运行点为 `valid=1.0`、`ignited=0.0`，并给出 `Pfus=7617.786990581088`、`Qfus=42.7543890111685`、`Pheat=178.1755549960294`、`Pwall=4.892372760895914`。
- 这表示 0-D 模型在这组单点参数下给出了有效解，但该点未被标记为点火态；“当前运行点有效”只说明这一组参数可计算，不能等同于“扫描已经证明存在明确工作窗”。
- 就单点输出来看，模型给出较高的聚变功率与增益，同时壁相关输出 `Pwall` 为 4.892372760895914；但这些都仍是当前假设下的单点结果。
- POPCON 扫描给出 `xkey=Ti0`、`ykey=ni0`、`best` 矩阵且 `n_invalid=0`，说明扫描中存在被当前准则标记为 `best` 的网格点。
- 但由于缺少 `x/y` 数组，且未提供 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 功率与状态：`Qfus=42.7543890111685`，且 `Qfus_raw` 与 `Qfus` 一致；同时 `ignited=0.0`，说明当前单点虽然给出较高聚变增益，但并未被模型判为点火。
- 约束时间：`tauE_used=1.0`、`tau_ISS04=0.7288612845892107`、`H_ISS04=1.372003179677192`。由于 `use_tauE=1`，结果依赖固定约束时间假设。
- 密度边界：`nbar=2.6624997739173498e+20`、`n_Sudo=3.022472279542492e+20`、`nbar_o_Sudo=0.8809013045176279`；当前点给出了相对 Sudo 参考密度的比值，但现有数据不足以扩展成更完整的边界趋势判断。
- beta 与几何：`betaT=0.0823393479675551`，同时给出 `beta_soft_limit=0.05` 与 `beta_o_limit=1.646786959351102`，说明 beta 相关约束被显式跟踪，但仅凭这些字段不能还原内部组合判据。几何上 `Vp=1149.0700858727764`、`Sw=1593.4931630494737`、`aspect_geom=10.0`，且 `geom_is_measured=0.0`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，当前更像模型几何值而非测量覆盖值。
- 成分与辐射：`Zeff=1.8672566371681416`，输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`。损失项中 `Pbrem=278.5147188844249`、`Pcycl=11.508692000165738`、`P_line=0.0`，本次输出里轫致辐射高于回旋辐射，线辐射为零。
- 阈值模型比较：输出含 `PL_ISS04=1701.732953112247`。若将其用于阈值模型比较，它只能作为模型量对比，不能表述为保证进入或维持 H 模。

3. 风险与不确定性
- 最大不确定性来自约束时间假设：`use_tauE=1.0` 且输入 `tauE=1.0` 被固定，`Pfus`、`Qfus`、`ignited`、`Pwall` 等结果都可能对该假设敏感。
- POPCON 证据不完整：虽然 `best` 矩阵不全为 0 且 `n_invalid=0`，但缺少 `x/y` 数组和 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 单点有效不代表扫描充分：`valid=1.0` 仅说明当前运行点求解通过，不能替代对 `Ti0`-`ni0` 平面内边界位置、失效区或敏感方向的判断。
- beta 相关判据的内部逻辑未完全暴露：`betaT`、`beta_soft_limit`、`beta_o_limit` 同时存在，但未给出这些量如何共同决定可接受性，因此不能仅凭单个数值下更强结论。
- 几何与辐射建模仍需核对：`geom_is_measured=0.0` 提示几何不是测量覆盖值，`P_line=0.0` 也意味着当前结果里没有体现线辐射贡献；这两点都可能影响对功率平衡和壁相关输出的解读。

4. 下一步建议
- 补做一次完整 POPCON 导出：明确输出 `x`、`y`、`nx`、`ny`，并尽量同时给出 `best_fraction`；需要重点监控 `xkey`、`ykey`、`best`、`n_invalid`、`valid`，把“单点有效”与“扫描中是否存在被当前准则选中的区域”分开验证。
- 围绕约束时间做敏感性扫描：在相同 `config/preset` 下扫描 `tauE` 或 `H_fac`，并尽可能对比不同约束时间处理方式；需要监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`ignited`、`Pwall`。
- 沿当前扫描轴重跑参数网格：既然 `xkey=Ti0`、`ykey=ni0`，建议显式给出这两个轴数组后重新扫描；需要监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`nbar_o_Sudo`、`betaT`。
- 做成分与辐射验证任务：对 `fHe`、`fimp`、`Zimp` 做小范围扰动扫描；需要监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`。
- 做几何输入一致性核对：核查 `Vp`、`Sw`、`aspect_geom`、`geom_is_measured`、`Vp_override`、`Sw_override` 的取值来源与覆盖关系；需要监控 `Vp`、`Sw`、`Pwall`、`betaT`、`valid`。
