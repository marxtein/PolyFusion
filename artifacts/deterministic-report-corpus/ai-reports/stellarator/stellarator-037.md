1. 核心结论
- `config=stellarator`（`config_label=仿星器 Stellarator`），`preset=HELIAS`。
- 当前运行点结果为：`valid=1`、`ignited=0`、`Pfus=4497.763688635281`、`Qfus=11.826009839022205`、`Pheat=380.328086130458`、`Pwall=3.0612567960006287`。
- 这表示当前单一运行点计算是有效的，但未达到点火；“运行点有效”不等同于“POPCON 扫描已经证明存在清晰工作窗”。
- POPCON 扫描给出了 `best` 矩阵，且矩阵中存在 `1`，说明在当前 `best` 判据下至少有部分扫描网格点被标记为 best。
- 但本次扫描缺少 `x/y` 数组，且未给出 `nx/ny`；因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1` 且 `tauE_used=1.0`，本次结果依赖固定约束时间假设。

2. 关键指标解读
- 运行点：`valid=1` 说明该组输入在模型内得到有效解；`ignited=0` 说明该解不是点火状态。就输出量看，`Pfus` 明显高于 `Pheat`，对应 `Qfus=11.826009839022205`，同时还有 `Pn=3598.210950908225` 与 `Pwall=3.0612567960006287` 需要联动看。
- 约束时间与约束指标：输出给出 `tauE_used=1.0`、`tau_ISS04=0.6831200663068253`、`H_ISS04=1.463871505643705`；但由于 `use_tauE=1`，这里的功率与增益结果依赖固定 `tauE=1.0` 的设定，不是完全由 `ISS04` 缩放自洽生成。
- 边界相关量：`betaT=0.06616554747392821`，同时输出含 `beta_soft_limit=0.05`；`nbar=1.711606997518296e+20`，`n_Sudo=2.6212074167450786e+20`，对应 `nbar_o_Sudo=0.6529841883492411`。这些字段可作为当前点约束接近程度的直接证据，但不能单凭某一个字段替代整体判定。
- 成分与辐射：`Zeff=1.8672566371681418`，参数侧有 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；辐射/损失项中 `Pbrem=129.10311057440953`、`Pcycl=16.36825970730976`、`P_line=0.0`。当前显式辐射项里轫致辐射高于回旋辐射，而线辐射在该次输出中为 0。
- 几何与扫描：几何量给出 `Vp=1149.0700858727764`、`Sw=1593.4931630494737`，且 `geom_is_measured=0`；POPCON 轴键为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，说明扫描结果里没有被标记为 invalid 的网格点，但由于缺少 `x/y` 数组和 `nx/ny`，不能据此说明 best 区的具体位置、边界或趋势。

3. 风险与不确定性
- 最大不确定性来自 `use_tauE=1`：`Pfus`、`Qfus`、`Pheat`、`ignited` 等结果都依赖固定约束时间假设，对 `tauE` 取值敏感。
- POPCON 证据不完整：虽然 `best` 矩阵中有 `1`，但没有 `x/y` 数组与 `nx/ny`，所以只能确认“存在被标记的 best 网格点”，不能判断工作窗形状、宽窄、连续性或对 `Ti0/ni0` 的敏感性趋势。
- 几何输入存在模型化不确定性：`geom_is_measured=0`，且 `Vp_override=0`、`Sw_override=0`，说明当前几何量不是测量覆盖值；`Vp`、`Sw` 及其引申结果值得单独核对。
- 约束字段需要复核而非单点下结论：例如输出同时给出 `betaT` 与 `beta_soft_limit`，这提示应检查约束判据如何参与 `best/valid`，但不能把单一字段直接写成总体结论。
- 若后续使用 `PL_ISS04=1279.8808238575143` 或 `Ptrans=1134.409453575795` 做 L-H 相关讨论，也只能表述为阈值模型比较，不能写成保证进入或维持 H 模。
- `P_line=0.0` 需要谨慎看待：它可能是该次模型设定或该点结果，而不是对所有成分/参数条件下线辐射都可忽略的证明。

4. 下一步建议
- 先补全一次 POPCON 导出：围绕当前 `Ti0=25.0`、`ni0=1.9285714285714286e+20` 重新扫描，并明确输出 `x`、`y`、`nx`、`ny` 与 `best_fraction`；重点监控 `best`、`n_invalid`、`valid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaT`、`nbar_o_Sudo`。
- 做约束时间敏感性扫描：保持其余参数不变，对 `tauE` 做多点扫描，或补做 `use_tauE=0`/相应 `tauE_scaling` 的对照；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`ignited`、`valid`。
- 做成分与辐射敏感性扫描：对 `fimp`、`Zimp`、`fHe` 做分组扰动；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pheat`、`Pfus`、`Pwall`。
- 做几何一致性验证：核对 `Vp`、`Sw`、`R0`、`a`、`Rw`、`N_fp` 与目标几何定义是否一致，必要时用 `Vp_override`、`Sw_override` 做对照运行；重点监控 `Vp`、`Sw`、`Pwall`、`Pfus`、`Qfus`、`betaT`。
- 若需要评估阈值模型比较，可补做加热相关扫描，并同步记录 `PL_ISS04`、`Ptrans`、`Pheat`；解读时仅作为阈值模型比较，不把它写成 H 模保证结论。
