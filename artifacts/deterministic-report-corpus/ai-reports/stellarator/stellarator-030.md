1. 核心结论
- 本次 0-D 结果对应 `config=stellarator`、`preset=HELIAS`；当前运行点 `valid=1.0`，说明该单点解在模型内被判为有效，但 `ignited=0.0`，不属于点火状态。
- 当前运行点给出的主要功率量级为：`Pfus=4422.1167110856195`、`Qfus=12.185900580435382`、`Pheat=362.88796891921015`、`Pwall=3.002839793079343`。
- 约束时间处理上，`use_tauE=1.0` 且 `tauE_used=1.0`，因此本次结果依赖固定约束时间假设；它不是由扫描中自洽改变约束时间后再得到的结论。
- POPCON 扫描需要单独看待：`last_scan.best` 矩阵中存在若干 `1`，说明在当前“best”判据下，扫描中至少有部分网格点被标记为满足该判据；这不能等同于“当前运行点有效”，两者含义不同。
- 但本次扫描未提供 `x`/`y` 数组，也未给出 `nx`/`ny`，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势；只能确认存在被标记的网格点，不能可靠描述最佳区位置、边界或延展方向。

2. 关键指标解读
- 运行点状态：`valid=1.0` 表示该 0-D 解被模型接受；`ignited=0.0` 表示等离子体没有达到点火，仍需外部加热支撑，这与 `Pheat=362.88796891921015` 相一致。
- 能量与功率平衡：`Pfus=4422.1167110856195`、`Pn=3537.693368868496`、`Qfus=12.185900580435382` 反映该点具有较高聚变输出/加热比；同时 `Ptrans=1075.5882226496426`、`Eth=1075.5882226496426` 给出输运相关功率尺度，当前结果还显示 `P_ei=0.0`。
- 辐射与损失：本点的辐射/辐射相关功率中，`Pbrem=161.67747853608338`、`Pcycl=10.045609950608041`、`P_line=0.0`；这表示在线辐射项未贡献，但轫致辐射和回旋辐射已被计入当前功率平衡。
- 约束与经验标度：结果给出 `H_ISS04=1.3147090487349777`、`tau_ISS04=0.7606245662964037`，而实际采用的是 `tauE_used=1.0`；因此应区分“经验标度对应的约束时间”与“本次求解实际使用的固定约束时间”。
- 密度与边界：`nbar=2.0285712563179805e+20`、`n_Sudo=2.587641170852827e+20`、`nbar_o_Sudo=0.7839461201838159`，说明当前线平均密度已被拿来与 Sudo 型密度界做比较，但这里只能做比值解读，不能外推成更强的运行结论。
- 压强与组成：`betaT=0.06273474130861342`，同时给出 `beta_soft_limit=0.05` 与 `beta_o_limit=1.2546948261722684`；成分方面 `Zeff=1.8672566371681418`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`，这些会直接影响辐射、稀释和功率平衡。
- 几何量：`Vp=1149.0700858727764`、`Sw=1593.4931630494737`、`Sp=1521.056092587038`；`geom_is_measured=0.0` 表明当前几何并非“实测 authoritative”输入，且 `Vp_geom`、`Sw_geom`、`Sp_geom` 与对应值一致，说明本次使用的是当前模型几何值而非额外覆盖值。
- POPCON 信息：扫描轴定义为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0` 说明扫描中没有被记录为无效的网格点；但由于缺失 `x`/`y` 数组以及 `nx`/`ny`，不能把 `best` 矩阵进一步解释成具体参数区间、边界位置或趋势斜率。

3. 风险与不确定性
- 最大的不确定性来自约束时间处理：`use_tauE=1.0` 使结果依赖固定约束时间假设，若 `tauE` 设定变化，`Pfus`、`Qfus`、`Pheat` 及是否保持 `valid` 都可能改变。
- 当前运行点有效，不等于扫描已充分证明存在明确工作窗；本次只知道 `best` 矩阵里有被标记点，但缺少 `x`/`y` 数组和 `nx`/`ny`，因此不能判断工作窗形状或敏感性趋势。
- `ignited=0.0` 表明该点并非点火态，因此不能把当前高 `Qfus` 直接解读为无需外部加热的运行状态。
- 压强相关指标需要谨慎看：结果同时给出 `betaT=0.06273474130861342` 和 `beta_soft_limit=0.05`，但这里不能仅凭 0-D 输出替代更高保真稳定性分析。
- 成分与辐射结论受输入假设影响明显，例如 `fHe=0.04`、`fimp=0.01`、`Zimp=10`、`P_line=0.0`；如果杂质种类、浓度或线辐射模型变化，功率平衡可能明显移动。
- 几何信息虽完整给出了 `Vp`、`Sw`、`Sp`，但 `geom_is_measured=0.0` 提示其并非基于已声明的实测几何，几何假设变化会传导到壁负荷、体积功率密度和输运相关量。

4. 下一步建议
- 进行 `Ti0`–`ni0` 二维重扫，并显式输出 `x`/`y` 数组与 `nx`/`ny`；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best` 矩阵占比，先补足工作窗判读所需证据。
- 在保持其他参数不变的前提下，对固定约束时间 `tauE` 做参数扫描，或切换 `use_tauE`/约束时间处理方式对比；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`valid`，验证结果对固定约束时间假设的敏感性。
- 对成分参数做扰动扫描，包括 `fHe`、`fimp`、`Zimp`；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`Pwall`，检查辐射与稀释假设对当前结论的影响。
- 对边界相关量做邻域验证，围绕当前点检查 `betaT`、`beta_soft_limit`、`nbar_o_Sudo` 的变化；重点监控 `betaT`、`nbar`、`n_Sudo`、`nbar_o_Sudo`、`valid`、`Pfus`，确认当前点是否靠近模型边界。
- 如果后续允许修改几何输入，可对 `R0`、`a`、`Rw`、`Sw_override`、`Vp_override` 做受控扫描；重点监控 `Vp`、`Sw`、`Pwall`、`tauE_used`、`Pfus`、`Qfus`，评估几何假设对壁负荷和功率平衡的影响。
