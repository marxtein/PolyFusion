1. 核心结论

- 本次对象为 `config=stellarator`，`preset=HELIAS`。
- 当前运行点结果：`valid=1.0`，`ignited=0.0`，`Pfus=426.07`，`Qfus=1.253`，`Pheat=340.09`，`Pwall=0.4808`。
- 这表示当前单一运行点在模型内被判为有效，但未达到点火；“运行点有效”仅说明该点可计算且满足模型内部有效性条件，不等同于 POPCON 扫描已证明存在可接受工作窗。
- POPCON 扫描与当前运行点需分开看：扫描给出了 `xkey=Ti0`、`ykey=ni0`、`best` 矩阵和 `n_invalid=0`，说明扫描过程中未出现无效点；同时 `best` 矩阵并非全 0，因此不能写“当前准则下未找到最佳区”。
- 依据 `best` 矩阵可直接统计，最佳区单元约为 14/64，约占 21.9%；但由于未提供 `nx`、`ny` 以及 `x/y` 扫描数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0`，因此本次结果依赖固定约束时间假设；`tauE_used=1.0` 为当前实际采用值，`tau_ISS04=1.198` 与 `H_ISS04=0.835` 更适合作为对照参考，而不是本次解的主约束来源。

2. 关键指标解读

- 聚变与增益：`Pfus=426.07`、`Qfus=1.253`、`Pn=340.85`，说明该点已有明显聚变输出，但 `ignited=0.0` 表明并未进入点火状态，仍需外部/总加热支撑。
- 加热与阈值模型比较：`Pheat=340.09`，`Ptrans=369.73`，`PL_ISS04=425.30`。按当前阈值模型比较，`Pheat` 低于这两个参考功率，因此这里最多只能说“与阈值模型相比偏低”，不能据此写成保证进入或维持 H 模。
- 辐射与功率损失：`Pbrem=54.14`、`Pcycl=1.427`、`P_line=0.0`。当前辐射损失以轫致辐射为主，回旋损失较小；线辐射在本次输入/模型结果中为 0。
- 壁负荷：`Pwall=0.4808`。数值已给出，可作为后续壁面热负荷与工程裕量核查的直接监控量，但仅凭 0-D 结果不能外推出更细的局部分布。
- 约束与边界：`betaT=0.02157`，同时结果中给出 `beta_soft_limit=0.05`；密度侧 `nbar=1.395e20`，`n_Sudo=1.511e20`，`nbar_o_Sudo=0.923`，说明平均密度接近但未超过 Sudo 参考值。
- 成分与几何：`Zeff=1.867`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`；`Vp=1149.07`，`Sw=1593.49`。这些参数共同影响辐射、功率平衡和壁负荷换算。

3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1.0`：本次解依赖固定 `tauE=1.0` s 假设，而不是由约束时间标度自洽求得；因此对 `Pfus`、`Qfus`、`Pheat`、`Pwall` 的判断都带有该假设前提。
- 当前运行点有效，不代表扫描已充分证明存在清晰工作窗。虽然 `best` 矩阵存在非零单元且 `n_invalid=0`，但缺少 `nx`、`ny` 和 `x/y` 数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 由于没有完整扫描坐标，不能可靠说明最佳区位于高/低 `Ti0`、高/低 `ni0`，也不能描述边界斜率、连通性或窗口宽窄。
- `ignited=0.0` 说明该点对外部条件仍有依赖，若固定约束时间、杂质、密度或温度设定变化，聚变增益可能明显变化。
- 阈值相关结论只能停留在模型比较层面：`Pheat<Ptrans` 且 `Pheat<PL_ISS04` 只是当前模型下的相对关系，不能替代真实运行模态判定。
- 几何方面仅看到 `geom_is_measured=0.0`，意味着结果未显示为“基于实测几何”的输入；但 JSON 未提供 `geom_model` 字段，因此这里不能进一步判定几何模型是否为简化模型。

4. 下一步建议

- 开展显式 POPCON 重扫：围绕 `Ti0` 与 `ni0` 重新输出完整 `x/y` 数组及 `nx/ny`，并保留 `best`、`valid/invalid` 掩码；重点监控 `best_fraction`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做约束时间敏感性扫描：在保持其余参数不变时扫描 `tauE` 或切换到标度约束对照，比较 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`ignited` 的变化，验证固定约束时间假设对结论的影响。
- 做功率平衡与阈值比较扫描：扫描 `Ti0`、`ni0` 或辅助加热分配，持续监控 `Pheat`、`Ptrans`、`PL_ISS04`、`Pbrem`、`Pcycl`、`P_line`，确认当前点与阈值模型比较结果是否稳定。
- 做边界接近度核查：在扫描中同步记录 `betaT`、`beta_soft_limit`、`nbar`、`n_Sudo`、`nbar_o_Sudo`、`Pwall`，查看输出改善是否伴随更接近密度或压力边界。
- 做成分参数验证：对 `fHe`、`fimp`、`Zimp` 做小范围扰动，监控 `Zeff`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`，判断当前结果对灰分与杂质设定的敏感性。
