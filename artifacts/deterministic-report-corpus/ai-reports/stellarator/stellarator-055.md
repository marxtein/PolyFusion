1. 核心结论
- `config=stellarator`（`config_label=仿星器 Stellarator`），`preset=HELIAS`。
- 当前运行点结果为：`valid=1.0`，`ignited=1.0`，`Pfus=12717.4194254542`，`Qfus=1000.0`，`Pheat=-31.222698556748128`，`Pwall=7.961249549775181`。
- 对“当前运行点”的解读应独立于 POPCON：本次 0-D 单点求解给出了一个被标记为有效且点火的结果，但同时存在 `Pheat<0`，且 JSON 还给出 `Qfus_raw=-407.31326929797353`，因此不能只凭 `valid/ignited/Qfus` 单独下结论，需连同功率平衡一起复核。
- 对“POPCON 扫描”的解读应与运行点分开：扫描键为 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵存在非零单元，说明当前 best 准则下至少有部分离散网格点被标记为 best；这不等同于“当前运行点有效”或“当前运行点点火”。
- 由于未提供 `x`/`y` 扫描数组，且 `nx`/`ny` 缺失，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 功率与损失：`Pfus=12717.4194254542`，`Pn=10173.93554036336`，`Ptrans=2176.385544267636`，`Pbrem=290.089639835005`，`Pcycl=45.78600243145088`，`P_line=0.0`；当前净加热项 `Pheat=-31.222698556748128`，说明这一运行点的功率平衡需要重点核验。
- 约束与约束时间：`use_tauE=1.0`，`tauE_used=1.0`，`tau_ISS04=0.5366899151013595`，`H_ISS04=1.8632733201464005`；因此该结果依赖固定约束时间假设，不能把它当作完全独立于该假设的结果。
- 边界相关：`betaT=0.12693982811664745`，`beta_soft_limit=0.05`，`beta_o_limit=2.5387965623329487`；密度侧给出 `nbar=2.345535515117665e+20`、`n_Sudo=3.672391862568371e+20`、`nbar_o_Sudo=0.6386942360440973`。当前 JSON 未提供 `q`/`q95`、`betaN`、`betap`，因此边界判断信息不完整。
- 成分与辐射：`Zeff=1.8672566371681416`，参数中 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；辐射项里 `Pbrem` 明显非零、`Pcycl` 非零、`P_line=0.0`，说明当前结果中的辐射解释主要落在韧致辐射和回旋辐射上，线辐射在此数据中未体现。
- 几何与壁负荷：`Vp=1149.0700858727764`，`Sw=1593.4931630494737`，`Pwall=7.961249549775181`；`geom_is_measured=0.0`，且 `Vp_geom/Vp`、`Sw_geom/Sw` 对应值为 1，表示本次输出未标记为实测几何主导。`geom_model` 字段未提供，因此不对几何模型简化程度额外下结论。
- POPCON 扫描信息：`n_invalid=0`，说明扫描中未报告无效点；但缺少 `x`/`y` 数组和 `nx`/`ny`，所以即使 `best` 矩阵有非零单元，也只能确认“存在被标记的离散网格点”，不能描述其具体位置、边界或趋势。

3. 风险与不确定性
- 最需要优先核验的是一致性：`valid=1.0`、`ignited=1.0`、`Qfus=1000.0` 与 `Pheat=-31.222698556748128`、`Qfus_raw=-407.31326929797353` 同时出现，显示当前输出中至少存在需要解释的数据并置，不能直接把 `Qfus` 当成单一可信结论。
- 结果对约束时间假设敏感：`use_tauE=1.0` 表明本次结果依赖固定 `tauE` 输入；而 `tauE_used=1.0` 与 `tau_ISS04=0.5366899151013595` 并不相同，因此对约束标度或固定值的选择应视为关键不确定性来源。
- POPCON 证据不完整：虽然 `best` 矩阵非零且 `n_invalid=0`，但由于缺少 `x`/`y` 数组以及 `nx`/`ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能据此比较不同区域的边界特征。
- 边界判据解释仍需小心：`betaT`、`beta_soft_limit`、`beta_o_limit` 已给出，但 JSON 未提供更多边界量和判据说明；因此只能把这些字段当作需要继续核对的约束信号，不能扩展成更强的运行结论。
- 几何与辐射信息也有限：`geom_is_measured=0.0` 表示当前几何未标记为实测输入；同时 `P_line=0.0` 使杂质线辐射对结果的影响在这份数据里缺少展开证据。

4. 下一步建议
- 补全并重跑 `Ti0`-`ni0` 的 POPCON 扫描输出，明确导出 `x`、`y`、`nx`、`ny`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaT`、`nbar_o_Sudo`。
- 围绕固定约束时间假设做敏感性扫描，至少对 `tauE` 和 `H_fac` 做参数扫描；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`。
- 针对当前运行点做功率平衡核验任务，逐项检查 `Qfus`、`Qfus_raw`、`Pheat`、`Pn`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line` 的计算链和符号约定；必要时在当前点附近做小步长 `Ti0`/`ni0` 扰动复算，观察这些量是否连续变化。
- 做成分与辐射相关扫描，可选 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Pwall`、`nbar_o_Sudo`。
- 若后续能提供更完整几何输入，可做 `Sw_override`、`Vp_override` 或等价几何校核任务；重点监控 `Sw`、`Vp`、`Pwall`、`iota` 以及对 `Pfus`、`Pheat` 的联动影响。
