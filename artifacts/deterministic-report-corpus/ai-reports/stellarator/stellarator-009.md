1. 核心结论
- 本次对象为 `config=stellarator`，`preset=HELIAS`；当前运行点 `valid=1`、`ignited=0`。
- 当前运行点的主要输出为：`Pfus=43.13`、`Qfus=0.374`、`Pheat=115.30`、`Pwall=0.0994`。
- 这表示：当前 0-D 运行点在该模型下被判定为“有效”，但未达到点火；同时 `Qfus` 小于 1，聚变输出相对于加热输入仍偏低。
- 当前运行点与 POPCON 扫描需要分开看：运行点 `valid=1` 仅说明这个单点可解且满足当前判据；它不等同于“扫描已经证明存在明确工作窗”。
- POPCON 方面，`best` 矩阵不是全 0，说明扫描网格中存在被当前 `best` 准则选中的点；因此不能写“当前准则下未找到最佳区”。
- 但本次扫描只给出 `best` 矩阵、`xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，未提供 `x/y` 扫描数组，也未显式给出 `nx/ny`；因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点功率平衡：`Pfus=43.13`，中子功率 `Pn=34.51`，总加热 `Pheat=115.30`，`Qfus=0.374`；就该单点而言，聚变增益有限，且仍依赖外加加热。
- 点火状态：`ignited=0`，说明在当前设定下没有达到点火判定；这与 `valid=1` 不冲突，前者是点火状态，后者是单点求解/约束判定结果。
- 约束时间假设：`use_tauE=1` 且 `tauE=1.0`、`tauE_used=1.0`，说明结果依赖固定约束时间假设；同时给出了 `H_ISS04=0.730` 与 `tau_ISS04=1.369`，可作为与经验缩放的对照，但当前求解并不是直接由经验缩放自洽决定。
- 辐射与损失：`Pbrem=5.48`、`Pcycl=0.805`、`P_line=0.0`；在给定模型输出里，线辐射为 0，主要已列出的辐射损失来自轫致和回旋部分。
- 密度边界相关：`nbar=4.44e19`，`n_Sudo=8.16e19`，`nbar_o_Sudo=0.544`；这表明当前线平均密度约为所给 Sudo 参考值的 54.4%。
- 压力与几何相关：`betaT=0.00686`，同时给出 `beta_soft_limit=0.05` 与 `beta_o_limit=0.137`；壁面相关量 `Pwall=0.0994`，几何量包括 `Vp=1149.07`、`Sw=1593.49`。`geom_is_measured=0` 说明这里不是“实测几何”输入，几何结果应按模型化几何理解。

3. 风险与不确定性
- 最大不确定性来自约束时间处理：`use_tauE=1` 表明结果依赖固定约束时间假设，因此 `Pfus`、`Qfus`、`Pheat` 以及是否接近点火，对 `tauE` 设定敏感。
- 几何表征存在模型化简化因素：虽然没有直接给出 `geom_model` 字段，但 `geom_is_measured=0` 表明几何并非来自实测几何；据此应将 `Vp`、`Sw`、`Pwall` 等结果视为模型几何下的估计。
- POPCON 扫描证据不完整：虽然 `best` 矩阵中有非零项、且 `n_invalid=0`，但缺少 `x/y` 扫描数组，也未显式给出 `nx/ny`，因此不能判断最佳区在 `Ti0`–`ni0` 平面中的实际坐标范围、边界形状或趋势敏感性。
- 不能把“运行点有效”外推成“扫描窗口明确”：`valid=1` 只对应当前单点；扫描中虽有 `best` 命中点，但现有证据不足以支持对工作窗宽窄、连通性或稳健性的判断。
- 成分与辐射结论受输入设定影响：`fHe=0.04`、`fimp=0.01`、`Zimp=10`、`Zeff=1.867` 均会影响损失与增益；当前 `P_line=0.0` 也应理解为本次模型/参数下的输出结果，而不是普遍结论。

4. 下一步建议
- 开展 `tauE`/约束时间敏感性扫描：在保持当前 `config=stellarator` 与 `preset=HELIAS` 不变的前提下，系统扫描 `tauE` 或切换到经验缩放驱动的求解方式；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`H_ISS04`、`tau_ISS04`。
- 补全 POPCON 扫描输出：重新导出包含 `x`、`y` 数组以及明确 `nx/ny` 的 `Ti0`–`ni0` 扫描结果；重点监控 `best` 分布、`n_invalid`、以及各网格点的 `valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`，以验证是否存在连续命中区域。
- 做密度与温度的二维复扫：围绕当前 `Ti0=10.0`、`ni0=5e19` 做局部加密扫描；重点监控 `nbar_o_Sudo`、`betaT`、`Pfus`、`Qfus`、`Ptrans`、`Pheat`，区分是温度还是密度对结果更敏感。
- 做杂质/有效电荷敏感性检查：扫描 `fimp`、`Zimp`、必要时联动 `fHe`；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`，确认当前低线辐射结果对成分设定是否敏感。
- 做几何假设核验：若后续有更权威几何输入，替换或对照当前模型化几何；重点监控 `Vp`、`Sw`、`Pwall`、`iota_geom`、`aspect_vol`，验证几何假设变化对功率与壁负荷的影响。
