1. 核心结论
- 本次报告配置为 `config=stellarator`，预设为 `preset=HELIAS`。当前运行点给出 `valid=1`、`ignited=0`、`Pfus=9512.52`、`Qfus=39.74`、`Pheat=239.34`、`Pwall=6.12`。
- 运行点层面，`valid=1` 只表示该 0-D 计算点被判为有效；`ignited=0` 则表示该点未达到点火。两者不能等同于“扫描已找到可接受工作窗”。
- 约束层面，`use_tauE=1`，且 `tauE_used=1.0`，因此本次结果依赖固定约束时间假设；同时输出给出 `tau_ISS04=0.547` 与 `H_ISS04=1.83`，可作为与 ISS04 标度的对照。
- POPCON 扫描层面，`best` 矩阵不是全 0，说明在当前判据下扫描中存在被标记为 `best` 的网格点；但 `nx`、`ny` 未提供，且缺少显式 `x/y` 扫描数组，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 本次扫描轴标签为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0` 表示扫描中没有被记为无效的网格点；这仍不等同于“最佳区范围已经明确”。

2. 关键指标解读
- 功率与增益：当前点 `Pfus=9512.52`、`Qfus=39.74`、`Pheat=239.34`，同时有 `Pn=7610.01`。从字段含义看，聚变功率、增益和加热功率均已给出，但这里只能据实报告数值，不能据此外推更高层级工程结论。
- 辐射与损失：`Pbrem=216.98`、`Pcycl=42.58`、`P_line=0.0`，`Ptrans=1882.28`。当前结果中线辐射项为 0，而轫致和回旋辐射均有贡献，能量外输项 `Ptrans` 也被显式给出。
- 壁负荷：`Pwall=6.12`，是当前运行点直接输出的壁面负荷指标，可与 `Sw=1593.49`、`Sp=1521.06` 一并看作几何尺度下的结果量。
- 约束与标度：由于 `use_tauE=1`，本次不是单纯由约束时间标度自洽决定，而是采用固定 `tauE=1.0` 的输入假设；在此假设下，模型反推出 `H_ISS04=1.83`，对应的标度值为 `tau_ISS04=0.547`。
- 边界与运行状态：输出给出 `betaT=0.1098`、`beta_soft_limit=0.05`、`beta_o_limit=2.196`，以及 `nbar_o_Sudo=0.598`。这些字段可用于边界对照，但本报告不把它们直接解释为是否“满足工程运行窗口”。
- 成分与几何：等离子体成分包括 `Zeff=1.867`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`；几何量包括 `Vp=1149.07`、`Sw=1593.49`。另有 `geom_is_measured=0`，表示这里的几何并未标记为实测几何输入。

3. 风险与不确定性
- 最大不确定性来自约束时间处理：`use_tauE=1` 说明结果依赖固定约束时间假设，因此 `Pfus`、`Qfus`、`Pheat` 等输出对该假设敏感，不能直接当作仅由标度自然推出的结果。
- 运行点与扫描不能混读：当前点 `valid=1`、`ignited=0` 只描述单一运行点；扫描中 `best` 矩阵存在非零元素，只能说明“某些网格点满足当前最佳判据”，不能把这件事表述成当前运行点本身已经处于扫描最佳区。
- 扫描证据不足：虽然有 `best`、`xkey`、`ykey` 和 `n_invalid`，但缺少 `nx`、`ny` 及显式 `x/y` 数组，因此不能判断工作窗形状、边界连续性，也不能判断对 `Ti0` 或 `ni0` 的敏感性趋势。
- 模型表征有限：`P_line=0.0`、`P_ei=0.0` 等字段意味着部分损失或通道在当前结果中未体现为非零贡献；这会影响对功率平衡结构的细分理解，但不能据此补充未提供的物理原因。
- 几何输入存在来源不确定性：`geom_is_measured=0` 表明几何并非标记为实测值，因此 `Vp`、`Sw`、`Pwall` 等与几何直接相关的量，应结合该输入来源属性审慎使用。

4. 下一步建议
- 执行一次保留完整网格信息的 `Ti0-ni0` 二维扫描，明确输出 `nx`、`ny`、显式 `x/y` 数组和 `best_fraction`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`。
- 在相同几何与成分参数下，做 `use_tauE=1` 与 `use_tauE=0` 的对照扫描或复算；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`Pheat`，用来识别固定约束时间假设对结果的影响。
- 围绕当前点做局部敏感性扫描，优先扫描 `Ti0`、`ni0`、`H_fac`；重点监控 `valid`、`ignited`、`betaT`、`nbar_o_Sudo`、`Ptrans`、`Pwall`、`Pbrem`、`Pcycl`。
- 做成分参数验证任务，扫描 `fHe`、`fimp`、`Zimp`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Qfus`，以检查辐射与聚变输出对杂质/氦灰设定的响应。
- 做几何一致性复算，至少检查 `Sw_override`、`Vp_override` 或几何来源设置对结果的影响；重点监控 `Vp`、`Sw`、`Pwall`、`Pfus`、`Qfus`，确认壁负荷与体积/表面积输入的耦合程度。
