1. 核心结论
- 本次算例 `config=stellarator`，`preset=HELIAS`；当前运行点 `valid=1.0`、`ignited=0.0`。
- 当前运行点的主要输出为：`Pfus=2986.1695820294594`、`Qfus=6.983731339044409`、`Pheat=427.5894127448007`、`Pwall=2.142311667181136`。
- 对当前运行点的解读应与 POPCON 扫描分开：`valid=1.0` 只说明这一个输入点在模型内得到可接受解，`ignited=0.0` 则表明该点未达到点火标记。
- POPCON 方面，`last_scan` 给出了 `xkey=Ti0`、`ykey=ni0`、`best` 矩阵和 `n_invalid=0`；`best` 矩阵并非全零，说明离散扫描格点中存在满足当前 `best` 准则的点。
- 但本次 JSON 未提供扫描 `x/y` 数组，且未显式给出 `nx/ny`，因此不能把“当前运行点有效”直接等同于“已明确找到工作窗”。

2. 关键指标解读
- 约束时间与约束模型：`use_tauE=1.0`、`tauE=1.0`、`tauE_used=1.0`，说明结果依赖固定约束时间假设；同时给出 `tau_ISS04=0.7004003918877517` 和 `H_ISS04=1.4277547693894823`，可理解为该固定约束时间相对于 ISS04 标度对应的增强因子水平。
- 功率与损失：除 `Pfus`、`Pheat` 外，还给出 `Pn=2388.9356656235677`、`Pbrem=85.71454803568486`、`Pcycl=14.775152275471095`、`P_line=0.0`；在已给出的辐射/辐射相关通道中，`Pbrem` 大于 `Pcycl`，而本次 `P_line=0.0`。
- 成分与辐射背景：`Zeff=1.8672566371681414`，输入参数里 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；这些量会直接影响辐射损失和功率平衡解读。
- 密度与边界量：`nbar=1.3946427387186119e+20`、`n_Sudo=2.3455307104491946e+20`、`nbar_o_Sudo=0.5945958125832949`；同时给出 `betaT=0.05391266831208965`、`beta_soft_limit=0.05`、`beta_o_limit=1.078253366241793`，说明当前点至少需要结合代码内部准则继续核对 beta 相关限制的实际判定方式。
- 几何与构型：本例是仿星器，`N_fp=5`，`iota=0.8220570026198248`；几何量给出 `Vp=1149.0700858727764`、`Sw=1593.4931630494737`、`aspect_vol=10.009223084112334`、`kappa_eff=2.5664096887270573`。同时 `geom_is_measured=0.0` 且 `Vp_override=0.0`、`Sw_override=0.0`，表示这里使用的是模型内几何值而非显式覆盖值。
- 阈值模型比较：`Ptrans=924.3336288395368`，`PL_ISS04=1024.8233291506926`；这只能作为阈值模型比较，不能表述为保证进入或维持 H 模。

3. 风险与不确定性
- 结果依赖固定约束时间假设：由于 `use_tauE=1.0`，当前 `Pfus`、`Qfus`、`valid` 等输出对 `tauE=1.0` 的设定敏感，不能直接当作纯 ISS04 自洽标度结果。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势：虽然 `best` 矩阵非零，但缺少扫描 `x/y` 数组且未显式给出 `nx/ny`，因此不能可靠说明最佳区位置、边界或趋势。
- 当前运行点未点火：`ignited=0.0`，所以即使 `valid=1.0` 且 `Qfus` 为正，也不能把该点解读成点火运行状态。
- beta 与阈值比较仍需谨慎：JSON 给出了 `betaT`、`beta_soft_limit`、`beta_o_limit`，但未附带完整判据定义；同理，`Ptrans` 与 `PL_ISS04` 仅是模型量比较，不能替代运行状态结论。
- 几何来源存在模型化成分：`geom_is_measured=0.0` 且没有几何覆盖输入，意味着壁面积、体积、进而 `Pwall` 等结果对所用几何参数化有依赖。

4. 下一步建议
- 补齐并重跑 `Ti0`-`ni0` POPCON 扫描，显式导出 `x`、`y`、`nx`、`ny` 和 `best_fraction`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做约束时间敏感性验证：在保持其他输入不变时扫描 `tauE`，或对比 `use_tauE=1` 与 `use_tauE=0` 的结果；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`valid`、`best`。
- 围绕当前运行点做局部 `Ti0`、`ni0` 细网格扫描，检查功率与边界量如何联动；重点监控 `betaT`、`beta_soft_limit`、`beta_o_limit`、`nbar_o_Sudo`、`Ptrans`、`PL_ISS04`、`Pwall`。
- 做成分/辐射敏感性扫描，例如扫描 `fimp`、`Zimp` 或对应的 `Zeff` 约束；重点监控 `Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做几何输入核对或覆盖值验证：若有更权威的体积/壁面积数据，可用 `Vp_override`、`Sw_override` 进行对比计算；重点监控 `Vp`、`Sw`、`aspect_vol`、`kappa_eff`、`Pwall`、`Pfus`、`Qfus`。
