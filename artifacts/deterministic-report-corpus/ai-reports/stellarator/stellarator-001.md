1. 核心结论
- `config=stellarator`（仿星器 Stellarator），`preset=HELIAS`。
- 当前运行点结果为：`valid=1`，`ignited=0`，`Pfus=4.32253583244003`，`Qfus=0.0697096382052742`，`Pheat=62.0077215106388`，`Pwall=0.04162569308809733`。
- 这表示本次 0-D 单点计算在模型内被判为有效，但未点火；同时 `Qfus` 远小于 1，当前结果下外加加热占主导。
- 必须把单点和扫描分开看：`valid=1` 只说明当前运行点可解且满足该点判据，不等于 POPCON 一定存在可用工作窗。
- POPCON 扫描轴为 `Ti0` 和 `ni0`，`n_invalid=0`，且 `best` 矩阵非全 0，说明在当前判据下至少有部分扫描格点被标记为 `best`。
- 但由于缺少 `x/y` 数组且未显式给出 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点功率侧：`Pfus=4.3225`，`Pheat=62.0077`，`Qfus=0.0697`，`ignited=0`；按这组输出，聚变功率贡献明显小于总加热需求。
- 辐射与损失：`Pbrem=3.91096`，`Pcycl=0.14003`，`P_line=0.0`；在已给输出里，轫致辐射高于回旋辐射，线辐射本次为 0。
- 约束时间与约束模型：`tauE_used=1.0`，`tau_ISS04=2.0712239749289743`，`H_ISS04=0.48280630781820283`；由于 `use_tauE=1`，结果依赖固定约束时间假设。
- 阈值模型比较：`Ptrans=58.821230926152325`，`Eth=58.821230926152325`；这只能表述为当前输出与阈值模型的比较结果，不能据此表述为保证进入或维持 H 模。
- 边界/密度相关：`betaT=0.003430806165314797`，`beta_soft_limit=0.05`，`beta_o_limit=0.06861612330629593`，`nbar_o_Sudo=0.7638222048442335`；可直接看出本次输出同时给出了 beta 与 Sudo 密度比值的参照量。
- 成分与几何：`Zeff=1.8672566371681416`，参数中 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；几何输出为 `Vp=1149.0700858727764`、`Sw=1593.4931630494737`，且 `geom_is_measured=0`。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1`：结果依赖固定约束时间假设，若实际约束时间偏离 `tauE_used=1.0`，则 `Pfus`、`Qfus`、`ignited` 等结论都可能变化。
- POPCON 方面虽然 `best` 矩阵有非零项，但缺少 `x/y` 数组与明确 `nx/ny`；因此只能确认“有 best 点”，不能确认最佳区位置、边界、连通性或对 `Ti0`/`ni0` 的敏感性趋势。
- `n_invalid=0` 只说明这次扫描结果里没有被标成无效的格点，不等于约束余量充分，也不等于单点结果对参数扰动不敏感。
- `geom_is_measured=0` 表明这里不是测量几何输入；在未提供 `geom_model` 的前提下，无法进一步判断几何模型复杂度，但几何来源本身仍是一个不确定性来源。
- `P_line=0.0` 仅代表这次输入与模型输出下的结果，不能外推为其他杂质、成分或参数条件下线辐射仍可忽略。

4. 下一步建议
- 重新执行一次 `Ti0-ni0` POPCON 扫描，并显式导出 `x`、`y`、`nx`、`ny`、`best_fraction`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaT`、`nbar_o_Sudo`。
- 在当前 `use_tauE=1` 设置下，对 `tauE` 做灵敏度扫描；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`ignited`、`Ptrans`、`Eth`、`Pbrem`、`Pcycl`。
- 以当前点 `Ti0=5.0`、`ni0=5e19` 为中心做更细局部扫描，检查单点 `valid=1` 是否稳定延续到邻域；重点监控 `valid`、`best`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaT`、`nbar_o_Sudo`。
- 对成分参数做单独扫描，如 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。
- 若要压缩几何不确定性，可做 `Vp_override`、`Sw_override` 的几何敏感性复算，或换用更明确的几何输入再跑同一组参数；重点监控 `Vp`、`Sw`、`A_flux`、`Pwall`、`Pfus`、`Qfus`。
