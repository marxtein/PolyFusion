# 核心结论
`config=stellarator（仿星器 Stellarator）`，`preset=HELIAS`，`valid=1.0`，`ignited=0.0`，`Pfus=901.4283815818659`，`Qfus=1.9026549277012594`，`Pheat=473.77397154772007`，`Pwall=0.863011141194893`。

就当前运行点看，这是一组`有效`的 0-D 求解结果，但`未点火`。同时，`use_tauE=1.0`且`tauE_used=1.0`，因此本次结果依赖固定约束时间假设。

POPCON 扫描需要单独看：本次扫描的`xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且`best`矩阵含非零元素，说明按当前判据确实有部分网格点被标记为最佳候选；但这不等于“当前运行点有效”就代表扫描已经确认了工作窗。由于未提供`nx`、`ny`以及对应`x/y`扫描数组，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

# 关键指标解读
约束与约束时间方面：`tauE_used=1.0`，`tau_ISS04=1.1276866151579796`，`H_ISS04=0.8867711885184593`。这表示当前结果不是直接由自洽约束时间扫描给出，而是建立在固定`tauE`输入上，再与 ISS04 标度做对照。

功率与损失方面：`Pfus=901.428...`，`Qfus=1.90265...`，`Pheat=473.773...`。辐射/功率损失项里，`Pbrem=114.544...`，`Pcycl=1.721...`，`P_line=0.0`；当前输出下线辐射未体现，损失主要落在轫致与回旋辐射字段上。`Pwall=0.863...`给出了当前壁负荷水平。

边界与成分方面：`betaT=0.031367...`，并给出了`beta_soft_limit=0.05`与`beta_o_limit=0.627347...`这两个对照字段；密度上，`nbar=2.0285712563179805e+20`，`n_Sudo=1.8738089119075136e+20`，`nbar_o_Sudo=1.0825923835813764`。成分上，`Zeff=1.8672566371681418`，参数里`fHe=0.04`、`fimp=0.01`、`Zimp=10`。

几何方面：`Vp=1149.0700858727764`，`Sw=1593.4931630494737`，且`geom_is_measured=0.0`，`Vp_geom=Vp`、`Sw_geom=Sw`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明这次结果里的几何量没有体现额外的实测修正或覆盖修正。

# 风险与不确定性
首要不确定性来自`use_tauE=1.0`。当前结论对固定`tauE=1.0`的假设敏感，若改用标度约束或改变`tauE`输入，`Pfus`、`Qfus`、`ignited`等结果都可能变化。

扫描证据也不完整。虽然`best`矩阵存在非零元素、`n_invalid=0`，但缺少`nx/ny`和显式`x/y`数组，所以现在只能确认“有最佳候选被打到”，不能判断最佳区的实际范围、边界连续性、形状，或对`Ti0`/`ni0`的敏感性趋势。

几何来源需要复核。`geom_is_measured=0.0`意味着本次几何不是以实测几何为标记来源；这会影响对`Vp`、`Sw`以及由此关联的`Pwall`等量的解释置信度。

此外，`nbar_o_Sudo=1.082592...`值得单独做敏感性核查；而`P_line=0.0`也意味着当前结果没有展示线辐射贡献，对辐射相关判断要保守。

# 下一步建议
1. 重新做`Ti0-ni0`的 POPCON 扫描，并显式输出`nx`、`ny`、`x`数组、`y`数组和`best_fraction`。重点监控：`best`矩阵、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 做约束时间敏感性验证：在当前点附近扫描`tauE`或切换到标度约束模式做对照。重点监控：`tauE_used`、`tau_ISS04`、`H_ISS04`、`valid`、`ignited`、`Pfus`、`Qfus`。

3. 做密度与杂质扫描，至少覆盖`ni0`、`fHe`、`fimp`、`Zimp`。重点监控：`nbar_o_Sudo`、`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pwall`。

4. 做几何输入校核或替代几何重跑，必要时引入`Vp/Sw`覆盖值对照。重点监控：`Vp`、`Sw`、`Pwall`、`A_flux`、`aspect_vol`，并复核`geom_is_measured`对应的数据来源。
