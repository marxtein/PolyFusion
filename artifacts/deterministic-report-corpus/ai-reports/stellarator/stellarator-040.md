我先按报告约束核对字段，重点区分“单一运行点结果”和“POPCON 扫描证据”。目前扫描里有 `best` 矩阵，但缺少 `x/y` 数组，所以位置和趋势表述要严格收敛。
我已经把强制项筛完了：这份 JSON 能支持单点功率平衡、约束时间假设、成分与几何的解读；但扫描侧只能确认 `best` 非全零、`n_invalid=0`，不能把它扩展成参数空间里的窗口形状或边界。
# 1. 核心结论

当前运行点：`config=stellarator`，`preset=HELIAS`，`valid=1.0`，`ignited=1.0`，`Pfus=10883.48`，`Qfus=1000.0`，`Pheat=-79.25`，`Pwall=6.78`。按这次 0-D 单点输出，模型把该点判为有效且点火；这只是当前输入点的判定，不等同于扫描已经证明存在参数工作窗。

POPCON 扫描：`xkey=Ti0`，`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵非全 0，说明当前准则下至少有部分栅格被标记为最佳。  
但本次 JSON 未提供 `x/y` 扫描数组，也未给出 `nx/ny`。**扫描网格证据不足，不能判断工作窗形状或敏感性趋势。**

另外，本次计算使用 `use_tauE=1.0`、`tauE_used=1.0`，结果依赖固定约束时间假设。

# 2. 关键指标解读

功率与点火相关字段显示：`ignited=1.0`，`Pfus=10883.48`，`Pn=8706.78`，`Pwall=6.78`。同时 `Pheat=-79.25`，表明在这套模型记账下，当前解并不是依赖正的外加加热功率来闭合。需要注意的是，`Qfus=1000.0` 的同时还有 `Qfus_raw=-137.34`，这两个字段不能简单按同一物理含义直接并读。

约束时间侧，输出给出 `tauE_used=1.0`、`tau_ISS04=0.6416`、`H_ISS04=1.5586`。由于 `use_tauE=1.0`，这里更应把 `H_ISS04` 理解为固定约束时间相对 ISS04 标度的对比，而不是独立预测出的约束结果。

边界与密度侧，已给出 `betaT=0.1029`、`beta_soft_limit=0.05`、`beta_o_limit=2.0585`、`nbar_o_Sudo=0.7935`。这些字段说明模型显式跟踪了 beta 与密度参照比值，但 JSON 没有提供更完整的判据定义，不能仅凭这些数字外推约束裕度。

成分与辐射侧，`Zeff=1.8673`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`；损失项中 `Pbrem=312.40`、`Pcycl=20.41`、`P_line=0.0`，在已列出的辐射/电磁损失里，韧致辐射项大于回旋项，线辐射在本次输出中为 0。

几何侧，`Vp=1149.07`，`Sw=1593.49`，`aspect_vol=10.009`，`N_fp=5`。同时 `geom_is_measured=0.0`、`iota_geom_authoritative=0.0`，说明这里的几何/旋转变换信息未被标记为测量权威值。

关于阈值模型，输出含 `Ptrans=1764.64` 与 `PL_ISS04=2097.45`。这些量只能作为阈值模型比较项解读，不能写成保证进入或维持 H 模。

# 3. 风险与不确定性

第一，单点 `valid=1.0`、`ignited=1.0` 只说明当前 `Ti0=25.0`、`ni0=3e20` 这一运行点在模型中成立；这不等于 POPCON 扫描已经证明存在连续、宽裕或低敏感的参数区。

第二，扫描侧虽然有非零 `best` 矩阵且 `n_invalid=0`，但缺少 `x/y` 数组与 `nx/ny`，因此不能判断最佳区对应的实际参数位置、边界、连通性，也不能判断对 `Ti0` 或 `ni0` 的敏感性趋势。扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

第三，`use_tauE=1.0` 使结果直接依赖固定约束时间假设。若改用约束标度或改变 `tauE` 取值，`valid`、`ignited`、`Pfus`、`Pheat`、`Qfus` 的判定都可能变化。

第四，`Qfus=1000.0` 与 `Qfus_raw=-137.34` 同时出现，且 `Pheat` 为负，这说明增益与加热字段在当前输出中可能带有截断、符号约定或特殊后处理；在没有补充字段定义前，不宜把某一个数单独拿来作强结论。

第五，几何字段未标记为测量权威值，意味着 `Vp`、`Sw`、`Pwall` 及相关比值的解释还受几何输入来源影响。

# 4. 下一步建议

1. 重新执行 `Ti0-ni0` 二维扫描，并输出完整 `x/y` 数组与 `nx/ny`。重点监控：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`nbar_o_Sudo`、`betaT`。目标是先把“单点成立”与“扫描证据”分开做实。

2. 在同一 `config/preset` 下做 `tauE` 敏感性扫描，至少比较当前 `use_tauE=1` 固定值与基于 `tau_ISS04` 的对照算例。重点监控：`tauE_used`、`tau_ISS04`、`H_ISS04`、`valid`、`ignited`、`Pfus`、`Pheat`、`Qfus`、`Qfus_raw`。

3. 做成分与辐射参数扫描，优先扫 `fimp`、`Zimp`、`fHe`。重点监控：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pwall`、`valid`。目标是确认当前点对杂质与辐射记账是否敏感。

4. 做几何一致性验证，检查 `Vp_override`、`Sw_override` 与当前默认几何的差异影响。重点监控：`Vp`、`Sw`、`aspect_vol`、`Pwall`、`betaT`、`valid`。这样可以把几何输入来源带来的不确定性单独拆开。
