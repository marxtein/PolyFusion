1. 核心结论
- `config=stellarator`（仿星器 Stellarator），`preset=HELIAS`；当前运行点 `valid=1.0`、`ignited=0.0`，`Pfus=25.494139909697324`、`Qfus=0.15831166139793929`、`Pheat=161.03766257378925`、`Pwall=0.11705842661195984`。
- 这说明本次 0-D 求解给出了一个“有效”的当前运行点，但未点火；同时 `Qfus` 较低，当前加热功率相对聚变输出更大。
- 当前运行点与 POPCON 扫描必须分开理解：`valid=1.0` 只说明这个单一运行点在当前模型与输入下可解且被接受，不等同于扫描已经确认出可用工作窗。
- POPCON 侧已提供 `best` 矩阵，且矩阵并非全 0，说明在当前最佳判据下存在被标记为最佳的离散网格点；但这不自动说明当前运行点本身就落在最佳区内。
- `use_tauE=1.0`，结果依赖固定约束时间假设；因此当前点的 `Pfus/Qfus/valid` 以及扫描中的最佳判据，都应在这一假设前提下解读。

2. 关键指标解读
- 当前运行点：`Pfus=25.4941`，`Pn=20.3953`，`Pheat=161.0377`，`Qfus=0.1583`，`ignited=0.0`。按这组数值看，系统并未进入点火状态，且外加/总加热在当前功率平衡中占主导。
- 约束与约束时间：`tauE_used=1.0`，`tau_ISS04=1.84881209036626`，`H_ISS04=0.5408878518324134`。由于 `use_tauE=1.0`，这里是用固定 `tauE` 直接参与计算，而不是完全由 ISS04 标度自行决定结果；`H_ISS04` 可视作当前固定约束时间与 ISS04 参考之间的相对量。
- 辐射与损失通道：`Pbrem=23.06670176623473`，`Pcycl=0.21822796883833398`，`P_line=0.0`。在已给出的辐射通道里，轫致辐射显著高于回旋辐射，谱线辐射当前为 0。
- 边界相关量：`betaT=0.00833195783005022`，对应给出的参考量有 `beta_soft_limit=0.05`、`beta_o_limit=0.1666391566010044`；密度侧 `nbar=1.0776784799189274e+20`，`n_Sudo=9.443851255810472e+19`，`nbar_o_Sudo=1.1411430048264148`。这表示当前线平均密度高于所给 Sudo 参考密度；但这里只能做相对比较，不能据此单独推出更完整的边界结论。
- 成分与几何：`Zeff=1.8672566371681416`，参数中 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；几何量 `Vp=1149.0700858727764`、`Sw=1593.4931630494737`，且 `geom_is_measured=0.0`、`Vp_override=0.0`、`Sw_override=0.0`，说明当前几何解释更应视作模型输入结果，而非已标记的实测几何。
- POPCON 扫描：`xkey=Ti0`，`ykey=ni0`，`n_invalid=0`。从已给 `best` 矩阵直接计数，可见共有 14 个网格点被标为 1，说明扫描中存在满足当前最佳判据的离散点；但由于缺少 `x/y` 坐标数组以及 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

3. 风险与不确定性
- 最大不确定性来自固定约束时间假设：`use_tauE=1.0` 使结果直接依赖 `tauE=1.0` 的设定，若改用约束时间标度或改变固定值，`valid`、`Pfus`、`Qfus` 以及 `best` 判据都可能变化。
- 扫描证据不完整：虽然 `best` 矩阵存在且非全 0，但未提供 `x/y` 数组和 `nx/ny`，因此不能确认最佳点对应的实际参数位置，也不能判断边界、连通性或敏感性方向。
- 几何解释存在保守空间：`geom_is_measured=0.0`，且未见 `geom_model` 字段，当前只能依据输出的 `Vp/Sw/Sp` 做结果解读，不能进一步确认几何修正细节对 `Pwall` 等量的影响范围。
- 边界信息并不完整：当前有 `betaT`、`nbar_o_Sudo`、`iota`，但缺少更多边界辅助量；因此只能说该运行点相对这些已给指标处在什么位置，不能把单个比值扩展成更强结论。
- 辐射通道覆盖需确认：`P_line=0.0` 这一结果本身是已给数据，但它是模型设置、成分设置还是该点确实无谱线辐射贡献，当前 JSON 不能单独区分。

4. 下一步建议
- 先补全并重跑 `Ti0`-`ni0` 的 POPCON 导出，至少保存 `x`、`y`、`nx`、`ny`、`best_fraction`；重点监控 `n_invalid`、`best` 中 1 的数量、`valid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`nbar_o_Sudo`、`betaT`。
- 做固定约束时间敏感性扫描：围绕当前 `tauE=1.0` 进行 1D 扫描，或联合 `H_fac=1.0` 做 2D 扫描；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做成分与辐射验证扫描：对 `fHe`、`fimp`、`Zimp` 分别或联合扫描，检查当前低 `Qfus` 是否对杂质与稀释敏感；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`、`valid`。
- 做几何输入一致性验证：对 `Vp_override`、`Sw_override` 进行替代值校验，或用设计/实测几何复算一次；重点监控 `Vp`、`Sw`、`Sp`、`Pwall`、`Pfus`、`Qfus`、`valid`。
