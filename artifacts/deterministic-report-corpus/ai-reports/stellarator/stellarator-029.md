1. 核心结论
- 本次报告对应 `config=stellarator`、`preset=HELIAS`；当前运行点 `valid=1`、`ignited=0`。
- 当前运行点给出的主要输出为：`Pfus=3148.17`、`Qfus=7.83`、`Pheat=402.22`、`Pwall=2.23`。
- 这表示 0-D 模型下当前点形成了有效解，并给出了聚变功率与增益估计；但 `ignited=0`，因此不能把该点解读为点火状态。
- 约束时间处理上，`use_tauE=1` 且 `tauE_used=1.0`，结果依赖固定约束时间假设，不是由约束时间标度自洽求出的结果。
- POPCON 扫描与当前运行点需要分开看：当前运行点“有效”仅说明这一组输入能收敛并满足模型内部判据；扫描中的 `best` 矩阵存在非零值，说明在离散扫描点中有部分点被当前“最佳”判据选中，但这不等同于当前运行点本身就是扫描中的最佳点。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势：`last_scan` 虽提供了 `best` 矩阵和 `xkey=Ti0`、`ykey=ni0`，但未提供 `x/y` 数组，也未明确 `nx/ny`。

2. 关键指标解读
- 运行点状态：`valid=1` 说明该 0-D 计算点是有效解；`ignited=0` 说明外加加热/维持仍然相关，不能按“点火”理解。
- 功率收支：`Pfus=3148.17`、`Pheat=402.22`、`Qfus=7.83`，对应当前模型下有较高聚变输出与有限外加加热；同时中子功率 `Pn=2518.53`，表明聚变输出中中子通道占比较大。
- 壁负荷与几何相关量：`Pwall=2.23`，并给出 `Sw=1593.49`、`Vp=1149.07`，可用于结合后续工程限值做壁面与体积尺度复核；但本报告不引入外部阈值判断。
- 能量损失构成：`Pbrem=115.10`、`Pcycl=9.23`、`P_line=0.0`；在已给输出中，轫致辐射明显高于回旋辐射，而线辐射项当前为零。
- 约束与经验标度：给出了 `H_ISS04=1.2836`、`tau_ISS04=0.7790`，但实际采用的是 `tauE_used=1.0` 且 `use_tauE=1`，因此报告结果对固定约束时间设定敏感；不能直接把 `H_ISS04` 单独当作最终运行充分性的结论。
- 密度与边界相关：`nbar=1.71e20`、`n_Sudo=2.35e20`、`nbar_o_Sudo=0.727`，说明当前线平均密度低于给出的 Sudo 参照值；这属于边界接近程度描述，不应外推为更强结论。
- 压强与稳定性相关表征：`betaT=0.0529`，同时给出 `beta_soft_limit=0.05`、`beta_o_limit=1.0586`；从字面看，`betaT` 与软限制值接近并略高于该数值，但这里仅能说明模型中存在这一比较关系，不能替代更高保真稳定性分析。
- 成分与辐射输入：`Zeff=1.867`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`，这些参数会直接影响辐射与稀释；尤其在 `P_line=0` 的结果下，更需要确认杂质辐射模型设定是否符合本轮扫描目的。

3. 风险与不确定性
- 最大的不确定性来自约束时间：`use_tauE=1` 表明结果依赖固定约束时间假设；若改用约束时间标度或改变 `tauE`，`Pfus`、`Qfus`、`Pheat` 可能明显变化。
- 当前点有效不代表存在宽裕扫描工作窗：运行点 `valid=1` 只说明单点成立，不能与 POPCON 扫描中是否存在连续可用区域混为一谈。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势：缺少 `x/y` 数组及明确 `nx/ny`，因此虽然 `best` 矩阵有非零元素，但无法可靠判读最佳区的真实参数位置、边界宽度或对 `Ti0`/`ni0` 的敏感方向。
- `best` 矩阵仅证明“有离散点入选”，不能证明区域连续性或鲁棒性；在没有坐标数组时，也不宜把矩阵行列直接解释成明确物理范围。
- 边界与损失模型仍较简化：例如 `P_line=0`、`P_ei=0` 以及当前给出的若干几何量多为直接输入/覆盖结果，意味着部分物理通道在本次 0-D 初筛中可能被简化处理。
- `geom_is_measured=0` 说明当前几何并非测量导入值；即使未显式给出 `geom_model`，也应把几何相关结论视为参数化估计而非装置定型结果。

4. 下一步建议
- 执行 `Ti0`-`ni0` 二维重扫，并显式输出 `x`、`y`、`nx`、`ny` 与 `best_fraction`；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`，以补足工作窗位置与连续性证据。
- 在当前点附近做 `tauE` 敏感性扫描，至少围绕 `tauE=1.0` 做上下扰动；重点监控 `tauE_used`、`Pfus`、`Qfus`、`Pheat`、`Pbrem`、`betaT`，用于识别固定约束时间假设对结论的影响幅度。
- 做成分参数扫描：联合扫描 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`，验证当前“低线辐射”结果是否对杂质设定敏感。
- 做边界接近度扫描：围绕密度与压强相关参数扫描 `ni0`、`B0`、`H_fac`；重点监控 `nbar_o_Sudo`、`betaT`、`beta_soft_limit` 对比、`Pfus`、`Pheat`、`Pwall`，用于识别输出对边界约束的接近程度。
- 做几何参数复核扫描：扫描 `R0`、`a`、`Rw`，必要时检查 `Sw_override`、`Vp_override` 的影响；重点监控 `Sw`、`Vp`、`Pwall`、`A_flux`、`iota_geom`，确认壁负荷和体积收益是否主要来自几何设定。
- 若后续要继续用 POPCON 作为筛选依据，建议每次保存完整扫描坐标与判据定义；至少同步记录 `xkey`、`ykey`、`x`、`y`、`best`、`n_invalid`，避免把“单点有效”误读为“存在明确工作窗”。
