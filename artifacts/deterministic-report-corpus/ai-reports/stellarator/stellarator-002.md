1. 核心结论
- 本次结果对应 `config=stellarator`，`preset=HELIAS`。
- 当前运行点给出 `valid=1`、`ignited=0`、`Pfus=12.703`、`Qfus=0.1155`、`Pheat=109.973`、`Pwall=0.0770`。
- 这表示当前单一运行点在该 0-D 计算中被判为有效，但未点火；“运行点有效”仅说明这一组参数可得到数值解，不等同于 POPCON 扫描已经证明存在清晰工作窗。
- POPCON 相关信息显示扫描轴为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵并非全 0，因此当前准则下不是“未找到最佳区”。
- 但本次 JSON 未提供 `x/y` 坐标数组，也未提供 `nx/ny`；扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 由于 `use_tauE=1` 且输入 `tauE=1.0`、输出 `tauE_used=1.0`，结果依赖固定约束时间假设；`H_ISS04=0.5147` 与 `tau_ISS04=1.9429` 只能作为与 ISS04 标度的对照，不应与固定 `tauE` 的运行结果混为一谈。

2. 关键指标解读
- 功率收支方面：`Pfus=12.703`，`Pheat=109.973`，`Qfus=0.1155`，说明当前运行点的聚变增益较低，外加/总加热功率在功率平衡中占主导。
- 损失项方面：`Ptrans=100.836`、`Pbrem=11.493`、`Pcycl=0.1833`、`P_line=0.0`；在已列出的辐射/电磁损失中，轫致辐射大于回旋辐射，线辐射在本次结果中为 0。
- 壁负荷方面：`Pwall=0.07699`，当前数值已直接给出，可与后续扫描中的 `Pfus`、辐射损失和几何量一起联动观察。
- 约束与经验标度方面：本次采用固定 `tauE_used=1.0` 秒；同时给出 `tau_ISS04=1.9429` 和 `H_ISS04=0.5147`，适合用于比较“固定约束时间假设”与“ISS04 经验标度参考”之间的差异。
- 密度与边界方面：`nbar=7.607e19`，`n_Sudo=7.772e19`，`nbar_o_Sudo=0.9788`；该比值说明当前平均密度与 Sudo 参考值接近，但这里只能作模型比值解读，不能外推出更强结论。
- 压强与构型方面：`betaT=0.005881`，并给出 `beta_o_limit=0.1176`、`beta_soft_limit=0.05`；仿星器相关几何/磁构型量包括 `iota=0.8221`、`N_fp=5`、`Vp=1149.07`、`Sw=1593.49`。
- 成分与辐射相关参数方面：`Zeff=1.8673`，输入包含 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；这些量会直接影响 `Pbrem`、可能的线辐射项以及有效加热分配。

3. 风险与不确定性
- 最大不确定性来自约束时间处理：`use_tauE=1` 使结果依赖固定约束时间假设，因此 `Pfus`、`Qfus`、`Pheat`、`ignited` 对 `tauE` 设定可能较敏感。
- 当前运行点 `valid=1` 仅代表这个点的数值解有效；它不能替代参数扫描结论，也不能证明参数邻域内仍保持同样结果。
- 虽然 `best` 矩阵存在若干 `1`，但缺少 `x/y` 数组以及 `nx/ny`，因此无法确认这些最佳点在 `Ti0-ni0` 平面中的实际位置、边界连续性或对参数扰动的敏感程度；扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `n_invalid=0` 只说明扫描中未记录无效点数量异常，不代表最佳区一定宽、连通或对其他物理参数不敏感。
- `P_line=0.0` 是本次结果的一部分，但是否来自当前参数下的物理结果、模型开关或数值截断，仅凭这份 JSON 不能区分。
- 几何方面虽给出了 `Vp_geom=Vp`、`Sw_geom=Sw`、`geom_is_measured=0`，但 JSON 未提供 `geom_model` 字段，因此不能进一步判断所用几何模型层级或其误差来源。

4. 下一步建议
- 执行一次完整的 `Ti0-ni0` 扫描导出任务，补齐 `x`、`y`、`nx`、`ny` 和 `best_fraction`；重点监控 `best`、`n_invalid`、`valid`、`Pfus`、`Qfus`、`Pwall`，以确认是否真的存在连续的候选区域。
- 执行 `tauE` 敏感性扫描（例如围绕当前 `tauE=1.0` 做上下扰动）；重点监控 `tauE_used`、`H_ISS04`、`Pfus`、`Qfus`、`Pheat`、`ignited`、`valid`，用于分离“固定约束时间假设”对结果的影响。
- 执行密度扫描（围绕当前 `ni0` 或 `nbar`）；重点监控 `nbar_o_Sudo`、`Pfus`、`Ptrans`、`Pbrem`、`valid`，检查结果是否对密度接近 Sudo 参考值较敏感。
- 执行杂质/氦灰参数扫描（`fimp`、`Zimp`、`fHe`）；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pheat`、`Qfus`、`Pwall`，识别辐射项和有效增益对成分设定的依赖。
- 执行几何一致性验证任务，对 `Vp`、`Sw`、`aspect`、`iota` 的输入来源和覆盖关系做核对；重点监控 `Vp_geom`、`Sw_geom`、`geom_is_measured`、`iota_geom_authoritative`，以避免后续把几何输入误差当成物理趋势。
