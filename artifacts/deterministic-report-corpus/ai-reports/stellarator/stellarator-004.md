1. 核心结论
- 本次算例为 `config=stellarator`，`preset=HELIAS`。当前运行点 `valid=1.0`、`ignited=0.0`，`Pfus=42.69606822246886`、`Qfus=0.19839580603288262`、`Pheat=215.20650600543595`、`Pwall=0.16184730515840792`。
- 就当前运行点而言：该点被模型判为有效，但并未点火；同时 `Qfus` 明显小于 1，表示当前聚变输出低于总加热输入，结果显示对外加热仍有依赖。
- 就 POPCON 扫描而言：扫描变量是 `Ti0` 与 `ni0`，`n_invalid=0`，且 `best` 矩阵存在非零单元，说明在当前判据下并非所有扫描点都失败；但这不等同于“当前运行点就在最佳区”。
- 由于缺少 `x/y` 扫描数组，且未见 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 本次结果使用 `use_tauE=1`，且 `tauE_used=1.0`，因此结论依赖固定约束时间假设。

2. 关键指标解读
- 能量收支：`Pfus=42.696...` 对比 `Pheat=215.206...`，对应 `Qfus=0.198...`；结合 `ignited=0.0`，当前点更像是外加热主导而非自持燃烧。
- 辐射与损失：`Pbrem=38.63073929016471`、`Pcycl=0.24825459185762208`、`P_line=0.0`；在已列出的辐射通道中，轫致辐射占主导，线辐射在本次输出里为 0。
- 约束时间与缩放：输出给出 `H_ISS04=0.5642974873920457`、`tau_ISS04=1.7721149258019822`，但本次实际采用的是 `tauE_used=1.0`；因此这些结果不是单纯按 ISS04 约束时间自洽得到，而是基于固定 `tauE` 假设。
- 边界相关量：`betaT=0.010782533662417932`，对应 `beta_soft_limit=0.05`、`beta_o_limit=0.21565067324835863`；密度侧给出 `nbar=1.3946427387186119e+20`、`n_Sudo=1.0959581988367904e+20`、`nbar_o_Sudo=1.2725327847347045`，说明当前平均密度与 Sudo 参考值之比需要单独跟踪。
- 成分与几何：`Zeff=1.8672566371681414`，输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；几何输出为 `Vp=1149.0700858727764`、`Sw=1593.4931630494737`，且 `geom_is_measured=0.0`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，表示本次未体现测量几何修正或覆盖值修正。
- POPCON 证据：`best` 矩阵中存在 1 值单元，说明当前准则下存在被标记为“best”的网格点；但因为缺少对应的 `x/y` 数组，不能据此说明这些点位于哪个 `Ti0/ni0` 区域，也不能描述边界或趋势。

3. 风险与不确定性
- 最大不确定性来自固定约束时间：`use_tauE=1` 会让 `Pfus`、`Qfus`、`Pheat`、`ignited` 等结果对 `tauE=1.0` 的设定较敏感，若改用缩放律自洽，结论可能变化。
- `valid=1.0` 只表示该 0-D 模型在当前判据下接受这个点，不等于更广义的运行结论；尤其不能把“当前点有效”直接等同为“存在可靠工作窗”。
- 扫描层面的证据不完整：虽然 `best` 矩阵存在非零单元且 `n_invalid=0`，但缺少 `x/y` 和 `nx/ny` 后，无法判断最佳区面积、连通性、边界陡峭程度或参数敏感性。
- `P_line=0.0` 的含义需要谨慎：仅凭当前 JSON，不能判断这代表线辐射物理上可忽略，还是该通道在本次设置中未被有效计入。
- `geom_is_measured=0.0` 说明几何并非测量输入；因此 `Vp`、`Sw`、`Pwall` 等量对几何假设的依赖仍需验证。
- `nbar_o_Sudo=1.2725327847347045` 值得重点复核；在当前输出里该点仍是 `valid=1.0`，但需要确认工具内部对这一约束的使用方式与敏感性。

4. 下一步建议
1. 做约束时间敏感性扫描：在保持其余参数不变的条件下，对 `tauE` 或 `H_fac` 进行 1D/2D 扫描；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`tauE_used`、`H_ISS04`。
2. 补全 POPCON 网格输出后重跑 `Ti0-ni0` 扫描：显式输出 `x`、`y`、`nx`、`ny`、`best_fraction`；重点监控 `n_invalid`、`best`、`Pfus`、`Qfus`、`Pwall`、`nbar_o_Sudo`，并把“最佳区”与“仅 valid 区”分开统计。
3. 做密度与成分联合扫描：围绕 `ni0`、`fHe`、`fimp`、`Zimp` 扫描，检验燃料稀释与辐射损失对结果的影响；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`nbar_o_Sudo`。
4. 做几何一致性验证：对 `R0`、`a`、`Rw` 以及必要时的 `Vp_override`、`Sw_override` 做一致性检查或参数扫描；重点监控 `Vp`、`Sw`、`Pwall`、`betaT`、`iota`、`Pfus`。
5. 做当前运行点邻域复扫：以当前 `Ti0=5.0`、`ni0=1.5714285714285714e+20` 为中心做小步长局部扫描，检验 `valid`、`ignited`、`Qfus` 是否对微小参数变化敏感；重点监控 `valid`、`ignited`、`Qfus`、`Pfus`、`Pheat`、`nbar_o_Sudo`、`betaT`。
