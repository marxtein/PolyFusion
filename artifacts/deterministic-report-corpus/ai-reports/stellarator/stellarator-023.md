1. 核心结论
- 本次结果的配置为 `config=stellarator`（`config_label=仿星器 Stellarator`），预设为 `preset=HELIAS`。
- 当前运行点的 0-D 求解结果为 `valid=1.0`、`ignited=0.0`；也就是该运行点在当前模型下给出了有效解，但不是点火态。
- 当前运行点的关键功率字段为 `Pfus=3344.6866936696947`、`Qfus=7.334654188940556`、`Pheat=456.0115047704537`、`Pwall=2.3851361816744405`。
- 这组结果依赖固定约束时间假设：`use_tauE=1.0`，且 `tauE=1.0`、`tauE_used=1.0`；对应输出里还有 `tau_ISS04=0.8761368839380301` 与 `H_ISS04=1.1413741600573122`。
- 需要把“当前运行点”和“POPCON 扫描”分开看：当前运行点 `valid=1.0` 只说明这个单点可求解，不等于扫描已经证明存在明确工作窗。
- 本次 POPCON 记录显示扫描轴键为 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵中存在非零单元，且 `n_invalid=0`；但同时，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- `运行点`：`ignited=0.0` 说明该点不是自持点火态；在此条件下，模型给出 `Pfus` 明显大于 `Pheat`，并对应 `Qfus=7.334654188940556`，但这仍然是“当前单点结果”，不能外推到邻近参数区。
- `约束时间与约束因子`：由于 `use_tauE=1.0`，结果直接依赖固定约束时间输入；当前 `tauE_used=1.0`，而参考量 `tau_ISS04=0.8761368839380301`，对应 `H_ISS04=1.1413741600573122`，可理解为该运行点相对 ISS04 标尺的对应关系。
- `边界与密度相关量`：输出给出了 `betaT=0.0544027834785632`、`beta_soft_limit=0.05`、`beta_o_limit=1.088055669571264`，以及 `nbar=2.345535515117665e+20`、`nbar_o_Sudo=0.9544627953878554`；这些字段提示模型在同时跟踪 beta 与密度相关约束，但仅凭当前 JSON 不能判断留有多少裕量。
- `成分与辐射`：输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`，输出 `Zeff=1.8672566371681416`；辐射相关字段中 `Pbrem=187.03125029292138`、`Pcycl=5.1809313824842835`、`P_line=0.0`，说明当前输出里已计入轫致辐射和回旋辐射，且提供的线辐射功率为 0。
- `几何量`：输出含 `Vp=1149.0700858727764`、`Sw=1593.4931630494737`，并且 `Vp=Vp_geom`、`Sw=Sw_geom`，同时 `geom_is_measured=0.0`；这说明本次记录里没有单独体现“测量几何”与“修正后几何”的差异。
- `POPCON 扫描`：`best` 矩阵有非零单元，表示在当前内部准则下，扫描过的某些网格单元被标记为 `best`；但这不等同于“当前运行点就在 best 区”，因为 JSON 未提供 `x/y` 数组来把当前 `Ti0=15.0`、`ni0=2.6428571428571428e+20` 精确映射到 `best` 网格位置。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：当前所有 `Pfus`、`Qfus`、`valid`、`ignited` 的判读都依赖固定约束时间假设；若改动 `tauE` 或改用别的约束时间处理方式，结果可能变化。
- 必须区分“单点有效”和“扫描找到窗口”：`valid=1.0` 只是当前运行点的求解状态；`best` 矩阵存在非零单元，只能说明扫描里有满足当前准则的网格单元，不能自动推出该单点已落在其中。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 虽然 `n_invalid=0`，但由于缺少 `nx/ny` 与 `x/y` 数组，仍然无法确认扫描分辨率、网格稠密度，以及 best 区对 `Ti0`、`ni0` 变化的敏感程度。
- 边界相关字段存在但解释空间有限：例如 `betaT`、`beta_soft_limit`、`beta_o_limit`、`nbar_o_Sudo` 都提供了约束线索，但 JSON 没有给出邻域扫描切片、误差条或判据细节，因此不能把这些值直接扩展成更强的运行结论。
- 几何信息也有来源不透明之处：输出虽给出 `Vp`、`Sw` 和 `geom_is_measured=0.0`，但没有 `geom_model` 字段，也没有更多几何对比数据，因此只能把几何处理视为本次 0-D 结果的一部分输入背景，不能进一步细分其误差来源。

4. 下一步建议
- 先补做一次完整的 `Ti0`-`ni0` POPCON 导出，要求同时保存 `x`、`y`、`nx`、`ny`、`best_fraction` 与当前点在网格中的索引；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕固定约束时间假设做敏感性扫描：在当前点附近扫描 `tauE` 或 `H_fac`，检查结果对约束时间设定的依赖；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`valid`、`ignited`、`Pfus`、`Qfus`。
- 以当前点为中心分别做 `Ti0` 单参数切片和 `ni0` 单参数切片，验证 `Pfus`、`Qfus` 与约束指标对温度、密度的局部敏感性；重点监控 `betaT`、`nbar_o_Sudo`、`Pbrem`、`Pcycl`、`P_line`、`Pwall`、`valid`。
- 做一次杂质/灰分参数扫描，例如扫描 `fimp`、`Zimp`、`fHe`，核查成分变化对辐射损失和聚变输出的影响；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Qfus`、`Pwall`、`valid`。
- 若后续工具链支持，也可补做几何敏感性验证，例如改变 `Vp_override`、`Sw_override` 或几何输入来源，对比 `Pwall` 与 `Pfus` 的变化；重点监控 `Vp`、`Sw`、`Pwall`、`Pfus`、`valid`。
