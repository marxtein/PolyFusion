1. 核心结论
- 本次报告的 `config` 为 `stellarator`，`preset` 为 `HELIAS`；当前运行点 `valid=1.0`、`ignited=0.0`、`Pfus=4309.735082274173`、`Qfus=9.720824966547248`、`Pheat=443.35075439640934`、`Pwall=2.982809055530922`。
- 就当前运行点而言，0-D 解是有效的，但未达到点火状态；这两点需要分开看：`valid=1.0` 说明该组方程在当前参数下有可接受解，`ignited=0.0` 说明不能据此认定为自持燃烧。
- 就 POPCON 扫描而言，`best` 矩阵并非全 0，说明在当前“best”判据下，扫描中存在部分满足条件的格点；但这不等同于“当前运行点有效”，二者代表的信息不同。
- `xkey=Ti0`、`ykey=ni0` 表明本次扫描围绕中心离子温度与中心离子密度展开；不过扫描数组中的 `x/y` 数值、`nx/ny` 未提供，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0`，说明本次结果依赖固定约束时间假设；对扫描区和当前点的解读都应把这一前提单独记住。

2. 关键指标解读
- 运行点功率面：聚变功率 `Pfus=4309.735082274173`，外加/总加热相关量 `Pheat=443.35075439640934`，对应 `Qfus=9.720824966547248`；从定义上看，这是较高聚变增益的计算结果，但仍未点火，因为 `ignited=0.0`。
- 壁负荷与中子功率：`Pwall=2.982809055530922`，中子功率 `Pn=3447.7880658193385`；这说明大量聚变能量以中子通道体现，壁负荷指标也需要与其他约束一起监控，而不能只看 `Qfus`。
- 辐射与能量损失：`Pbrem=240.99570892382278`、`Pcycl=5.519905256679197`、`P_line=0.0`；当前给出的辐射损失以轫致辐射为主，谱线辐射在本次模型输出中为 0。
- 约束与假设：`tauE_used=1.0`，而经验约束标度量给出 `tau_ISS04=0.856851946678724`、`H_ISS04=1.1670627625649186`；结合 `use_tauE=1.0`，可理解为本次解并不是纯粹由约束标度自由决定，而是依赖固定约束时间输入。
- 密度边界：`nbar=2.6624997739173498e+20`，`n_Sudo=2.647106535055811e+20`，`nbar_o_Sudo=1.0058151187561533`；该运行点的平均密度与 Sudo 指标非常接近，且略高于 1。
- beta 与几何相关量：`betaT=0.061754510975666324`，同时给出 `beta_soft_limit=0.05`、`beta_o_limit=1.2350902195133264`；这表明总 beta 需要与模型中的 beta 限值一并看待。几何上，`Vp=1149.0700858727764`、`Sw=1593.4931630494737`、`aspect_geom=10.0`，用于支撑体积、面积和壁负荷等 0-D 量的计算。
- 成分参数：`Zeff=1.8672566371681416`，输入中有 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；这些成分参数会直接影响辐射、稀释和功率平衡结果。
- 扫描判据证据：`best` 矩阵共有 64 个格点，其中 14 个为 1，可对应约 `21.9%` 的格点满足当前“best”判据；但由于缺少 `x/y` 数组及 `nx/ny`，这里只能确认“存在满足判据的格点”，不能据此描述最佳区位置、边界或趋势。

3. 风险与不确定性
- 最主要的不确定性来自 `use_tauE=1.0`：结果依赖固定约束时间假设，因此 `Pfus`、`Qfus`、是否接近某些边界以及扫描中的“best”分布，都可能对 `tauE` 取值敏感。
- 当前运行点 `valid=1.0` 但 `ignited=0.0`，说明“有解”不等于“点火”；不能把运行点有效与更高层次的性能状态混为一谈。
- 扫描层面虽然 `best` 矩阵存在非零值，但扫描网格证据不足，不能判断工作窗形状或敏感性趋势；因此不能仅凭这份输出判断最佳区的连续性、边界陡峭性或对 `Ti0/ni0` 的方向性敏感度。
- `nbar_o_Sudo=1.0058151187561533` 接近 1，提示密度相关边界判读需要谨慎；在 0-D 框架下，这只能说明该比值接近模型参考界限，不能外推更强结论。
- `betaT=0.061754510975666324` 高于输出中的 `beta_soft_limit=0.05`，而 `beta_o_limit=1.2350902195133264` 又显示离另一类限制还有距离；这意味着 beta 约束的具体主导项取决于你采用哪一套判据，存在模型解释差异。
- `P_line=0.0` 是本次输出的一部分，但不能据此泛化为谱线辐射一定可忽略；它可能反映的是当前模型设定、杂质参数或该工况下的计算结果。
- 几何方面未提供 `geom_model` 字段，不能据此判断是否采用了“较简化”的几何模型；只能确认 `geom_is_measured=0.0`，说明当前几何并非标记为实测权威输入。

4. 下一步建议
- 先做 `tauE` 敏感性扫描：围绕当前 `tauE=1.0` 与 `H_fac=1.0`，重复计算并监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`tauE_used`、`H_ISS04`，用来验证固定约束时间假设对结论的影响。
- 做 `Ti0`-`ni0` 二维加密扫描：沿当前 `xkey=Ti0`、`ykey=ni0` 提高网格分辨率，并在输出中保留明确的 `x/y` 数组与 `nx/ny`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`nbar_o_Sudo`、`betaT`，以补足工作窗形状证据。
- 做密度边界验证扫描：以当前 `nbar_o_Sudo` 接近 1 为重点，固定其他主参数，单独扫描 `ni0` 或平均密度相关输入，监控 `nbar`、`n_Sudo`、`nbar_o_Sudo`、`Pfus`、`Pwall`、`valid`，确认结果对密度边界的敏感性。
- 做 beta 约束核查：围绕当前点扫描 `B0`、压力相关输入或温度密度组合，监控 `betaT`、`beta_soft_limit`、`beta_o_limit`、`Pfus`、`Qfus`、`Pwall`，辨别当前结论更受哪类 beta 判据影响。
- 做成分/辐射敏感性扫描：围绕 `fHe=0.04`、`fimp=0.01`、`Zimp=10` 逐项变化，监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pheat`，验证辐射与稀释假设是否改变当前运行点评价。
- 补充几何一致性验证：在不改变物理目标的前提下，对 `Vp_override`、`Sw_override` 或几何输入做一致性检查，监控 `Vp`、`Sw`、`Pwall`、`aspect_geom`、`iota_geom`，确认壁负荷和体积相关结论对几何设定是否敏感。
