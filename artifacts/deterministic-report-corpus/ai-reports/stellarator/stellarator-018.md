## 1. 核心结论

- `config=stellarator`，`preset=HELIAS`。
- 当前运行点：`valid=1`，`ignited=0`，`Pfus=351.8151087570754`，`Qfus=1.3809137147382036`，`Pheat=254.76979843289718`，`Pwall=0.38066363964132033`。
- 这表示本次 **单一运行点** 在模型判定下是有效的，但**未点火**；“运行点有效”不等于“POPCON 扫描已经证明存在可操作工作窗”。
- POPCON 侧：已给出 `best` 矩阵，且矩阵中存在非零单元，说明在当前准则下**至少有部分扫描格点被标记为 best**。
- 但本次扫描仅明确了 `xkey=Ti0`、`ykey=ni0`，未提供 `x/y` 扫描数组，也未给出 `nx/ny`；因此 **扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1`，结果依赖**固定约束时间假设**。
- JSON 中未见 `geom_model` 字段，因此不能据此额外判断几何模型层级；仅可见 `geom_is_measured=0`，几何量主要表现为模型/参数化输入而非实测标定。

## 2. 关键指标解读

- 运行点功率收支：
  - `Pfus=351.8151087570754`，`Qfus=1.3809137147382036`，`Pheat=254.76979843289718`，`Pn=281.4520870056603`。
  - `ignited=0` 说明该点不能按本次 0-D 判据视为点火状态。
- 损失与辐射：
  - `Pbrem=19.673119095822265`，`Pcycl=2.9505134682780034`，`P_line=0.0`。
  - 可见本次辐射项中轫致辐射和回旋辐射有贡献，线辐射项为 0。
- 约束与约束时间：
  - `tauE_used=1.0`，`tau_ISS04=1.0170324331431195`，`H_ISS04=0.9832528122131945`。
  - 结合 `params.use_tauE=1.0`、`params.tauE=1.0`，本次结果是基于固定约束时间输入得到的，不应把它直接当成约束时间标度自由响应的结果。
- 边界与运行接近度：
  - `betaT=0.017644145993047526`，`beta_soft_limit=0.05`，`beta_o_limit=0.3528829198609505`。
  - 密度相关给出 `nbar=7.607142211192427e+19`，`n_Sudo=1.3211344809723398e+20`，`nbar_o_Sudo=0.5758037747673997`。
  - 这些数值可用于比较当前点与模型边界的相对位置，但不能单凭这份 0-D 输出外推更强结论。
- 成分与杂质：
  - `Zeff=1.8672566371681416`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`。
- 几何相关：
  - `Vp=1149.0700858727764`，`Sw=1593.4931630494737`。
  - `geom_is_measured=0`，且 `Vp_geom=Vp`、`Sw_geom=Sw`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明这里看到的是当前模型下采用的几何量，没有额外几何修正被显式体现。
- POPCON 扫描：
  - 扫描变量是 `Ti0` 和 `ni0`。
  - `n_invalid=0`，表示已评估格点中没有被记为无效点。
  - `best` 矩阵存在非零元素，说明扫描中有格点满足当前 best 判据；但由于缺少 `x/y` 数组和 `nx/ny`，不能描述最佳区的位置、边界或趋势。

## 3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1`：结果依赖固定约束时间假设，对约束时间变化的敏感性没有在这份结果里展开。
- 当前运行点 `valid=1` 仅说明该点通过了本次模型内判定；这与“扫描找到了怎样的工作窗”是两件事，不能混用。
- `best` 矩阵虽然非全零，但扫描数组缺失，因此 **扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `geom_is_measured=0` 表明几何输入不是实测标定几何；这会限制对壁负荷、体积和表面积相关结果的解释强度。
- 若涉及 L-H 比较，只能把 `Ptrans=302.509187620212` 视为与阈值模型相关的比较量；不能据此表述为保证进入或维持 H 模。

## 4. 下一步建议

1. 补齐 POPCON 扫描元数据并重跑：明确输出 `x`、`y` 数组以及 `nx`、`ny`，继续保留 `best` 和 `n_invalid`。  
需监控指标：`xkey/ykey`、`best`、`n_invalid`、每个格点的 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 做约束时间敏感性扫描：在当前 `Ti0`、`ni0` 附近，对 `tauE` 或等效约束时间设定做参数扫描。  
需监控指标：`tauE_used`、`tau_ISS04`、`H_ISS04`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`。

3. 做成分/辐射敏感性扫描：围绕 `fHe`、`fimp`、`Zimp` 扫描，检查辐射项和增益响应。  
需监控指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pheat`。

4. 做边界接近度验证：在当前点附近扫描 `ni0`、`Ti0`，并持续记录密度与 beta 相关量。  
需监控指标：`betaT`、`beta_o_limit`、`beta_soft_limit`、`nbar`、`n_Sudo`、`nbar_o_Sudo`、`valid`。
