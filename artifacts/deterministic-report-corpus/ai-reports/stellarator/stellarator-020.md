我先按约束逐项核对 JSON 字段，重点区分“单点运行结果”和“POPCON 扫描证据”，避免把两者混在一起。
# 1. 核心结论
- `config=stellarator`，`preset=HELIAS`。
- 当前运行点结果：`valid=1.0`，`ignited=0.0`，`Pfus=1182.4896711001697`，`Qfus=3.0459204712899557`，`Pheat=388.2207963884829`，`Pwall=0.9857026712827424`。
- 就单点 0-D 结果看，这次计算给出了一个“有效”运行点（`valid=1`），但不是点火态（`ignited=0`）。这只是当前运行点的结论，不等同于 POPCON 扫描已经证明存在可接受工作窗。
- POPCON 扫描与运行点需要分开看：本次扫描轴为 `Ti0` 和 `ni0`，且 `best` 矩阵并非全 0，说明当前准则下不是完全没有候选网格点；但 JSON 未提供 `x/y` 数组，且缺少 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

# 2. 关键指标解读
- 约束与约束时间：`use_tauE=1.0`，`tauE_used=1.0`，`tau_ISS04=0.9472720224030113`，`H_ISS04=1.055662973623173`。这表示本次结果**依赖固定约束时间假设**。
- 功率相关：当前点给出 `Pfus=1182.49`、`Pheat=388.22`、`Qfus=3.046`，同时 `Pn=945.99`。这是该单点的功率平衡输出。
- 辐射与损失：`Pbrem=66.1235`，`Pcycl=3.9950`，`P_line=0.0`，`Ptrans=554.6002`，`Eth=554.6002`。
- 边界相关量：`betaT=0.0323476`，`beta_soft_limit=0.05`，`beta_o_limit=0.646952`；密度相关给出 `nbar=1.3946427387186119e+20`、`n_Sudo=1.8312974291060294e+20`、`nbar_o_Sudo=0.7615599282523015`。这些是程序输出的边界/归一化量，但不能替代更高阶稳定性或输运判据。
- 成分与辐射输入：`Zeff=1.8672566371681414`，参数中 `fHe=0.04`、`fimp=0.01`、`Zimp=10`。
- 几何相关：`Vp=1149.0700858727764`，`Sw=1593.4931630494737`，`aspect_vol=10.009223084112334`，`N_fp=5`；同时 `geom_is_measured=0.0`，`geom_volume_ratio=1.0`，`geom_wall_ratio=1.0`。

# 3. 风险与不确定性
- `valid=1` 只说明该单点在当前 0-D 判据下可求得结果，不等于扫描已经证明存在稳定或宽裕的参数区。
- `ignited=0` 表明该点不是点火态，不能把当前 `Qfus` 直接解读为自持运行。
- 由于 `use_tauE=1`，结果对固定 `tauE` 假设敏感；`tauE_used` 与 `tau_ISS04` 的偏差会直接影响 `Pfus`、`Qfus`、`Pheat` 等输出解读。
- POPCON 部分虽然给出了非全零 `best` 矩阵，但缺少 `x/y` 数组且无 `nx/ny` 字段，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- JSON 未提供 `q/q95`、`betaN`、`betap`、`geom_model` 等字段；因此这份 0-D 结果对边界与几何模型层级的判读是不完整的。
- `P_line=0.0`、`P_ei=0.0` 这类输出在本 JSON 中仅能按结果字段记录，不能进一步判断其来源是物理状态还是模型设定。

# 4. 下一步建议
1. 重新做 `Ti0`-`ni0` 二维 POPCON 扫描，并显式导出 `x`、`y`、`nx`、`ny`、`best_fraction`。重点监控：`valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
2. 围绕当前 `use_tauE=1` 设置，做 `tauE` 或 `H_fac` 敏感性扫描，检查固定约束时间假设对结果的影响。重点监控：`tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
3. 在当前点附近做 `ni0`、`Ti0` 的局部细扫，专门观察边界量变化。重点监控：`betaT`、`beta_o_limit`、`nbar_o_Sudo`、`valid`、`ignited`。
4. 做成分与辐射参数扫描，至少覆盖 `fHe`、`fimp`、`Zimp`。重点监控：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`。
5. 若后续可提供更权威几何输入，再做一次几何一致性复算。重点监控：`Vp`、`Sw`、`aspect_vol`、`iota_geom`、`Pwall`。
