1. 核心结论
- 本次结果对应 `config=stellarator`、`preset=HELIAS`；当前运行点 `valid=1.0`、`ignited=0.0`、`Pfus=126.76336615994992`、`Qfus=0.6551692135793449`、`Pheat=193.48187236608933`、`Pwall=0.20097057580917685`。
- 就单个运行点而言，这是一组“可计算且有效”的 0-D 解，但不是点火解；同时 `Qfus<1`，说明按该输出口径，聚变增益未达到 1。
- 就 POPCON 扫描而言，`best` 矩阵并非全 0，且可直接数出有 `14/64` 个格点为 1，说明在当前扫描离散格点和当前判据下，存在被标记为“best”的候选格点；这不等同于“当前运行点 valid=1”，两者分别描述单点结果与扫描命中情况。
- `n_invalid=0` 表明这次扫描中没有被记为无效的格点，但由于缺少 `nx/ny` 和对应的 `x/y` 数组，不能把这 14 个命中格点进一步解释成明确的工作窗形状、边界或敏感性趋势。
- 本次结果依赖固定约束时间假设：`use_tauE=1.0`，且 `tauE_used=1.0`。

2. 关键指标解读
- 约束与能量闭合：`tauE_used=1.0`，对照程序同时给出的 `tau_ISS04=1.2948552137308322`，得到 `H_ISS04=0.7722871170427822`；也就是说，本次解是建立在固定 `tauE=1.0` 的前提下，而不是由 ISS04 标度自洽决定。
- 功率与损失：`Pfus=126.76336615994992`，`Pheat=193.48187236608933`，`Pbrem=16.107814886805983`，`Pcycl=1.0539389644653592`，`P_line=0.0`；在这组输出里，可见韧致辐射大于回旋辐射，线辐射被算为 0。
- 阈值模型比较：程序给出 `Ptrans=201.672791746808` 与 `PL_ISS04=218.83454559807933`。若把 `PL_ISS04` 作为阈值模型比较量，则当前 `Ptrans` 低于该数值；这只能说明模型比较结果，不能表述为保证进入或维持 H 模。
- 密度与边界量：`nbar=7.607142211192427e+19`，`n_Sudo=1.0838634288759864e+20`，因此 `nbar_o_Sudo=0.701854311947896`；`betaT=0.011762763995365017`，程序还给出 `beta_soft_limit=0.05` 与 `beta_o_limit=0.23525527990730033` 作为参考字段。
- 成分与快离子：`Zeff=1.8672566371681416`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`，`f_fast_ion=0.172818154030394`；这些参数共同影响辐射、燃料稀释与功率平衡。
- 几何量：`Vp=1149.0700858727764`，`Sw=1593.4931630494737`，`Pwall=0.20097057580917685`；同时 `geom_is_measured=0.0`、`Vp_override=0.0`、`Sw_override=0.0`，表示本次几何并非测量覆盖输入。

3. 风险与不确定性
- 固定约束时间假设是本次结果的核心不确定性来源：`use_tauE=1.0` 意味着 `Pfus`、`Qfus`、`Pheat`、`ignited` 等结论都依赖 `tauE=1.0` 这一先验设定。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。虽然给出了 `best` 矩阵和 `xkey=Ti0`、`ykey=ni0`，但未提供 `nx/ny` 与实际 `x/y` 取值数组，因此不能定位最佳区坐标，也不能可靠描述边界、连通性或斜率趋势。
- “运行点有效”和“扫描找到候选格点”必须分开看：`valid=1.0` 只说明当前单点求解有效；`best` 矩阵中存在 1 只说明扫描判据在部分格点被命中，不能据此反推当前运行点本身就是最佳点。
- 边界信息不完整：JSON 未提供 `q/q95`、`betaN/betap` 等字段，因此对 MHD 相关边界只能依赖当前已有的 `betaT`、`iota`、`nbar_o_Sudo` 做有限阅读。
- `P_line=0.0` 只是本次输出结果；JSON 没有说明其形成原因，因此不能据此外推邻近参数点的线辐射同样可忽略。

4. 下一步建议
- 重新执行 `Ti0-ni0` POPCON 扫描，并导出完整 `x/y` 数组以及 `nx/ny`；优先监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaT`、`nbar_o_Sudo`。
- 围绕固定约束时间做敏感性验证：在其他参数不变时扫描 `tauE`，或做一组 `use_tauE=1` 与基于 ISS04 的对照计算；重点监控 `tauE_used`、`H_ISS04`、`Pfus`、`Qfus`、`ignited`、`Pwall`、`best` 命中格点数。
- 以当前点 `Ti0=10.0`、`ni0=8.571428571428572e+19` 为中心做局部细网格扫描，验证当前点附近是否仍能保持 `valid=1`，并观察 `Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaT`、`nbar_o_Sudo`、`Ptrans` 与 `PL_ISS04` 的相对变化。
- 做成分/辐射参数验证扫描，优先扫 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`，以判断当前功率平衡对杂质与辐射设定的敏感度。
