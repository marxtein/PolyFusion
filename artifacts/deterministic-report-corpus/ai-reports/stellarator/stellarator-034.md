1. 核心结论
- 本次算例为 `config=stellarator`（仿星器），`preset=HELIAS`；当前运行点 `valid=1.0`、`ignited=0.0`，`Pfus=888.4471483724012`、`Qfus=2.4481429032401185`、`Pheat=362.90657183309884`、`Pwall=0.7852896700295718`。
- 就 `last_run` 而言，这是一个“当前模型求解有效、但未点火”的运行点；这里的 `valid=1.0` 只说明该单点结果有效，不能等同于 POPCON 扫描是否找到了可用区域。
- 就 `last_scan` 而言，扫描轴定义为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`；`best` 矩阵不是全 0，按矩阵可直接数出有 14/64 个格点标记为 1，说明扫描中存在满足当前 `best` 标记准则的格点。
- 但本次 JSON 未提供 `x`/`y` 扫描数组，也未给出 `nx`/`ny`。扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 约束时间与约束模型：`use_tauE=1.0`、`tauE=1.0`、`tauE_used=1.0`，结果依赖固定约束时间假设；同时给出了 `tau_ISS04=0.7458293534239022` 和 `H_ISS04=1.3407892776132617`，可理解为当前固定约束时间与 ISS04 标度的对照结果。
- 功率项：`Pfus=888.4471483724012`，`Qfus=2.4481429032401185`，`Pheat=362.90657183309884`，`Pn=710.7577186979211`，`Ptrans=504.18197936702006`，`PL_ISS04=540.5960015075791`；辐射/损失侧给出 `Pbrem=25.501849002352508`、`Pcycl=10.912173138206509`、`P_line=0.0`。这些值可用于看当前点的功率分配，但不应把单点功率关系外推为扫描趋势。
- 壁负荷与几何量：`Pwall=0.7852896700295718`，`Sw=1593.4931630494737`，`Vp=1149.0700858727764`，`aspect_vol=10.009223084112334`，`kappa_eff=2.5664096887270573`，`N_fp=5.0`。同时 `Vp=Vp_geom`、`Sw=Sw_geom`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明本次输出直接采用了当前几何量。
- 边界/密度相关：`betaT=0.029406909988412538`，并给出了 `beta_soft_limit=0.05` 与 `beta_o_limit=0.5881381997682508` 作为对照字段；密度侧给出 `nbar=7.607142211192427e+19`、`n_Sudo=1.7035427501807432e+20`、`nbar_o_Sudo=0.4465483599038135`。`q/q95` 与 `nbar_o_nGw` 在本 JSON 中未提供。
- 成分与辐射相关输入/输出：`Zeff=1.8672566371681416`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`，`f_fast_ion=0.35124454287167467`。结合 `Pbrem`、`Pcycl`、`P_line` 可做成分与辐射敏感性检查。
- 扫描证据本身：`best` 矩阵存在非零元素，且 `n_invalid=0`，说明扫描没有出现被标记为 invalid 的格点；但由于缺少 `x`/`y` 坐标数组，现阶段只能确认“有被标记的格点”，不能说明这些格点位于哪里、边界如何、朝哪个方向变敏感。

3. 风险与不确定性
- 结果依赖固定约束时间假设：`use_tauE=1.0` 是本次最直接的不确定性来源之一；如果改动 `tauE` 或改用标度主导，`Pfus`、`Qfus`、`valid`、`ignited` 都可能改变。
- 扫描与单点必须分开读：`valid=1.0` 只对应当前运行点，不能据此推出 POPCON 一定存在连续工作窗；反过来，`best` 矩阵有非零格点，也不能推出当前运行点一定落在最佳区内。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `best` 的判据细节没有在这份 JSON 中展开；因此可以确认“哪些格点被标记”，但不能从这份数据单独解释“为什么被标记”。
- 几何信息的权威性仍需留意：本次未提供 `geom_model` 字段，且 `geom_is_measured=0.0`；虽然当前几何量内部自洽，但是否对应更高保真几何输入，单靠这份 JSON 不能判断。
- 部分常见约束证据缺失：例如 `q/q95`、`nbar_o_nGw`、`LH_ratio` 未提供，因此不能对这些维度做进一步比较，也不能就 L-H 阈值做任何超出“模型对照”的表述。

4. 下一步建议
- 先补齐 POPCON 网格元数据后复扫：提供 `Ti0`/`ni0` 对应的 `x`、`y` 数组和 `nx`、`ny`，在相同 `best` 准则下重跑扫描；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做约束时间敏感性扫描：围绕当前 `tauE=1.0` 扫描 `tauE`，或配合扫描 `H_fac`；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`Ptrans`、`valid`、`ignited`。
- 做当前扫描轴的局部单点验证：以当前 `Ti0=25.0`、`ni0=8.571428571428572e+19` 为中心，对 `Ti0` 和 `ni0` 做更细的邻域扫描；重点监控 `nbar_o_Sudo`、`betaT`、`Pfus`、`Qfus`、`Pwall`、`Pbrem`、`Pcycl`、`valid`。
- 做成分/辐射敏感性扫描：分别扫描 `fHe`、`fimp`、`Zimp`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Pheat`、`Pwall`。
- 做几何一致性验证：若后续有更高保真几何来源，可对 `R0`、`a`、`Rw` 或 `Vp_override`、`Sw_override` 做一致性复核；重点监控 `Vp`、`Sw`、`aspect_vol`、`Pwall`、`nbar_o_Sudo`、`valid`。
