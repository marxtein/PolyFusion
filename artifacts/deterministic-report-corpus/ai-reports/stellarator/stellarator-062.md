1. 核心结论
- 本次数据对应 `config=stellarator`、`preset=HELIAS`。当前运行点 `valid=1`、`ignited=0`，说明该 0-D 解在模型内是有效解，但不是点火态；这两个结论只针对当前单个运行点。
- 当前运行点的主要功率指标为：`Pfus=10762.842`、`Qfus=36.784`、`Pheat=292.599`、`Pwall=6.938`。
- 运行点还给出较大的中子功率分量 `Pn=8610.274`，以及辐射/损失相关项 `Pbrem=233.660`、`Pcycl=60.331`、`P_line=0`、`Ptrans=2151.176`。
- 约束时间处理上，`use_tauE=1` 且 `tauE_used=1.0`，因此这组结果依赖固定约束时间假设；同时给出了 `tau_ISS04=0.504` 和 `H_ISS04=1.982`，可作为与 ISS04 标度的对照。
- POPCON 扫描是另一件事：`last_scan` 中存在 `best` 矩阵，且并非全 0，说明扫描中至少有部分网格点满足当前“best”判据；但这不等于“当前运行点有效”本身，也不等于已经完整确认工作窗形状。
- 由于扫描结果缺少 `x/y` 数组且未给出 `nx/ny`，必须明确：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点状态：`valid=1` 表示该参数组合下模型求得了一个有效结果；`ignited=0` 表示该点不是点火态，因此不能把较高 `Qfus` 直接理解为点火运行。
- 功率收支：`Pfus=10762.842` 明显高于 `Pheat=292.599`，对应 `Qfus=36.784`；同时存在 `Ptrans=2151.176`、`Pbrem=233.660`、`Pcycl=60.331`，而 `P_line=0`，说明在本次输出里线辐射项未体现为非零贡献。
- 壁负荷与几何量：`Pwall=6.938`，配套几何量为 `Vp=1149.070`、`Sw=1593.493`、`Sp=1521.056`。这些值可用于后续把功率水平与体积/受热面积一起审查，但仅凭本次 0-D 数据不能扩展成更强的工程判断。
- 约束与标度：本次直接使用 `tauE=1.0`，并得到 `tauE_used=1.0`；同时输出 `tau_ISS04=0.504`、`H_ISS04=1.982`、`PL_ISS04=2445.167`。这表示当前结果对固定约束时间设定敏感，ISS04 相关量更适合作为对照，而不是替代当前假设。
- 边界与归一化量：输出含 `betaT=0.125`、`beta_soft_limit=0.05`、`beta_o_limit=2.509`，可见当前 `betaT` 高于该输出中的 `beta_soft_limit`、低于 `beta_o_limit`；密度侧给出 `nbar=2.029e20`、`n_Sudo=3.623e20`、`nbar_o_Sudo=0.560`，说明当前平均密度约为 Sudo 指标的 56.0%。
- 成分与辐射相关：`Zeff=1.867`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`，快离子分数 `f_fast_ion=0.477`。这些输入/输出共同影响了 `Pbrem`、聚变功率和能量平衡，应在后续扫描中联动观察。
- POPCON 扫描信息：扫描轴键为 `xkey=Ti0`、`ykey=ni0`，并给出 `best` 8x8 矩阵、`n_invalid=0`。按矩阵直接计数，`best=1` 的网格点为 14/64，说明当前判据下并非完全找不到候选区；但由于缺少坐标数组，不能把这些 1 点进一步解释为具体位置边界或趋势。

3. 风险与不确定性
- 最大不确定性来自约束时间设定：`use_tauE=1` 说明结果依赖固定约束时间假设；如果改用标度或改变 `tauE`，`Pfus`、`Qfus`、`Pheat`、`Pwall` 以及扫描判据都可能变化。
- 当前运行点有效，不代表扫描意义上的工作窗已经被充分确认；反过来，扫描中存在 `best=1` 网格，也不代表当前名义运行点一定处在最优区内，这两者不能混为一谈。
- 扫描证据不完整：虽然有 `best` 矩阵，但缺少 `x/y` 数组，且未见 `nx/ny`，因此不能判断最佳区的真实坐标范围、边界斜率、连通性细节或对 `Ti0`/`ni0` 的敏感性趋势。
- 几何与模型层面的信息有限：本次可见 `geom_is_measured=0`，说明几何不是“实测权威输入”口径；但 JSON 未提供 `geom_model` 字段，因此不能进一步判断是否采用了更简化的几何模型。
- 本次没有 L-H 阈值比较字段、`LH_ratio` 字段，也没有 `q/q95`、`betaN`、`betap` 等更多约束输出；因此关于约束边界、模式切换或电流相关风险，当前证据是不完整的。
- `n_invalid=0` 只说明已记录的扫描网格都给出了可判定结果，不等于扫描范围足够大，也不等于已覆盖最敏感区域。

4. 下一步建议
- 执行 `Ti0-ni0` 二维复扫，并在输出中补齐 `x/y` 数组与 `nx/ny`；重点监控 `best`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`valid`、`ignited`，以确认候选区是否稳定存在以及坐标范围是否收缩/扩张。
- 执行约束时间敏感性扫描：在保持其余参数不变时，对 `tauE` 或 `use_tauE`/`tauE_scaling` 做对比；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`，验证当前高 `Qfus` 对固定约束时间假设的依赖程度。
- 执行成分参数扫描：对 `fHe`、`fimp`、`Zimp` 做小范围系统变化；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`nbar_o_Sudo`，检查辐射与聚变输出是否出现明显耦合变化。
- 执行边界量复核扫描：围绕当前点对 `betaT` 相关输入和密度相关输入做局部扰动；重点监控 `betaT`、`beta_soft_limit`、`beta_o_limit`、`nbar`、`nbar_o_Sudo`、`valid`、`best`，确认当前点附近是否容易越过已有判据。
- 执行几何口径一致性验证：若后续工具链支持，比较当前 `Vp/Sw/Sp` 与替代几何输入或覆盖值的结果；重点监控 `Vp`、`Sw`、`Sp`、`Pwall`、`Pfus`、`Qfus`，确认结论是否对几何口径敏感。
