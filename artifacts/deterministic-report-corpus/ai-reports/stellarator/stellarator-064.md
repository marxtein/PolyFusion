1. 核心结论
- 本次结果对应 `config=stellarator`、`preset=HELIAS`；当前运行点输入主坐标是 `Ti0=40.0`、`ni0=3e20`，输出标记为 `valid=1`、`ignited=1`，并给出 `Pfus=18540.68`、`Qfus=1000.0`、`Pheat=-413.08`、`Pwall=11.38`。
- 就“当前运行点”而言，这是一个 0-D 方程组求解成功的单点结果；`ignited=1` 且 `Pheat<0` 说明该单点在此模型设定下呈现点火态解。
- 就 “POPCON 扫描” 而言，扫描轴是 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵非全零；按矩阵直接计数，`8x8` 共 `64` 个格点里有 `14` 个格点被标为 `1`，说明当前判据下存在被选中的扫描格点。
- 这两件事不能混为一谈：`valid=1` 只说明当前这个单点可解；`best` 矩阵出现非零只说明扫描判据下有候选格点，并不自动等同于当前运行点就是完整工作窗中心。
- 由于未提供 `x/y` 数组，且未给出 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能把 `best` 矩阵映射到 `Ti0/ni0` 的具体数值边界。

2. 关键指标解读
- 约束时间与约束因子：`use_tauE=1`，`tauE=1.0`，实际使用的是 `tauE_used=1.0`；因此本次结果依赖固定约束时间假设。相对 `tau_ISS04=0.4871`，输出 `H_ISS04=2.0531`，应理解为对 ISS04 标度的比较结果，而不是在自由求解约束时间时独立得到的约束性能预测。
- 功率平衡：`Pfus=18540.68`，其中 `Pn=14832.54`；损失项给出 `Pbrem=402.52`、`Pcycl=69.12`、`P_line=0.0`，三项合计约 `471.63`。`Eth=2823.42` 且 `Ptrans=2823.42`，显示该解中能量含量与传输功率项是成套输出的一部分。
- 边界与密度相关量：`betaT=0.1647`，同时给出 `beta_soft_limit=0.05` 和 `beta_o_limit=3.2936`；从数值关系看，当前 `betaT` 相对该 soft limit 偏高，这一约束如何在求解器里处理需要单独核对。密度侧给出 `nbar=2.6625e20`、`n_Sudo=4.2058e20`、`nbar_o_Sudo=0.6331`，可直接理解为当前线平均密度约为该 Sudo 参照值的 `63.3%`。
- 成分与辐射：输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`，输出 `Zeff=1.8673`；当前结果里 `P_line=0.0`，说明线辐射在这次求解输出中没有贡献值，至少在该点上不是主导损失项。
- 几何量：`Vp=1149.07`、`Sw=1593.49`、`Sp=1521.06`，且 `Vp=Vp_geom`、`Sw=Sw_geom`、`Sp=Sp_geom`，同时 `geom_is_measured=0`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`；这表明本次结果没有体现额外的体积或壁面积修正量。

3. 风险与不确定性
- 最需要优先核对的是 `Qfus` 的定义：输出同时给出 `Qfus=1000.0` 和 `Qfus_raw=-44.8836`，二者符号与量级不一致，存在明显的截断、封顶或派生定义差异风险，不能把 `Qfus=1000` 直接当作连续物理裕度来解读。
- 结果对固定约束时间假设敏感：由于 `use_tauE=1`，当前 `valid/ignited/Pheat/Pfus` 都依赖于手动指定的 `tauE=1.0`；这意味着若改用约束标度自洽求解，结论可能发生变化。
- `beta` 约束需要谨慎：虽然当前点 `valid=1`，但输出同时给出 `betaT=0.1647` 与 `beta_soft_limit=0.05`、`beta_o_limit=3.2936`；这说明“求解成功”不等于所有软约束都留有充足余量。
- POPCON 证据仍不完整：虽然 `best` 矩阵非零且 `n_invalid=0`，但缺少 `x/y` 坐标数组和 `nx/ny`，因此不能判断最佳区的物理位置、连通边界、宽窄程度，也不能判断对 `Ti0` 或 `ni0` 的敏感方向。
- 物理细节证据有限：`P_line=0.0` 与 `geom_is_measured=0` 不一定表示这些效应不存在，只能说明当前输出里没有给出更丰富的线辐射或测量几何修正证据。

4. 下一步建议
- 先补全一次可追溯的 `Ti0-ni0` 二维扫描输出：要求同时导出 `x`、`y`、`nx`、`ny`、`best`，并保留 `n_invalid`；重点监控 `valid`、`ignited`、`best`、`Pfus`、`Pheat`、`Pwall`、`betaT`、`beta_o_limit`、`nbar_o_Sudo`，这样才能把候选格点映射回实际参数区间。
- 做约束时间敏感性验证：在当前点附近对 `tauE` 做分段扫描，或增加一组 `use_tauE=0` 的对照计算；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`valid`、`ignited`、`Pfus`、`Pheat`、`Qfus_raw`，确认当前点火结论是否主要由固定 `tauE` 驱动。
- 做约束边界核对扫描：围绕当前 `Ti0=40`、`ni0=3e20`，优先细扫 `Ti0` 与 `ni0`，必要时再加一维 `B0` 或 `H_fac` 扫描；重点监控 `betaT`、`beta_soft_limit`、`beta_o_limit`、`Pwall`、`Pfus`、`Pn`，检查哪些指标最先触发约束压力。
- 做成分/辐射验证：对 `fimp`、`Zimp`、`fHe` 进行小范围扫描，确认 `P_line=0` 是否仅是当前点结果；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pheat`、`valid`，避免把单一成分设定下的低线辐射结果外推到邻近区域。
