1. 核心结论
- 本次算例 `config=stellarator`（仿星器）、`preset=HELIAS`；当前运行点给出 `valid=1.0`、`ignited=1.0`、`Pfus=16386.79902593222`、`Qfus=1000.0`、`Pheat=-384.29673502986043`、`Pwall=10.04240411065104`。
- 就当前运行点本身看，模型把该点标记为“有效”且“点火”；但 `Pheat` 为负，说明按当前 0-D 记账下净加热项不是正值，这一点需要和 `valid/ignited` 分开理解，不能只看标签而忽略功率收支。
- POPCON 扫描与单点判定是两回事：本次扫描键为 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵存在非零单元，且 `n_invalid=0`，说明扫描判据下至少有部分网格点被标为 `best`；这不等同于“当前运行点一定落在最佳区”。
- 由于 `use_tauE=1.0`，结果依赖固定约束时间假设；当前求解使用 `tauE_used=1.0`，不能把这次结果直接当作对约束时间模型不敏感的结论。

2. 关键指标解读
- 约束时间与标度：输出给出 `tauE_used=1.0`、`tau_ISS04=0.5273030963565319`、`H_ISS04=1.896442495615193`；这表示本次结果是在固定约束时间前提下得到，同时提供了与 `ISS04` 标度的对照量。
- 功率收支：除 `Pfus` 外，输出还给出 `Pn=13109.439220745779`、`Ptrans=2470.4916988983978`、`Pbrem=373.7897185310073`、`Pcycl=48.781652727179086`、`P_line=0.0`；从已给数据看，轫致辐射和回旋辐射存在，线辐射在该结果中为零。
- 等离子体边界相关量：`betaT=0.1440938589432214`，同时输出给出 `beta_soft_limit=0.05` 和 `beta_o_limit=2.881877178864428`；密度侧给出 `nbar=2.6624997739173498e+20`、`n_Sudo=3.9409014337720235e+20`、`nbar_o_Sudo=0.6756067916595785`。这些量可用于后续逐项核查约束，但仅凭本 JSON 不能反推 `valid` 的完整判定逻辑。
- 成分与辐射背景：输入参数有 `fHe=0.04`、`fimp=0.01`、`Zimp=10`，结果给出 `Zeff=1.8672566371681416`；这说明当前点的辐射与功率平衡是在这一组杂质/氦灰设定下得到的。
- 几何量：结果给出 `Vp=1149.0700858727764`、`Sw=1593.4931630494737`、`aspect_vol=10.009223084112334`，且 `Vp_override=0.0`、`Sw_override=0.0`、`geom_is_measured=0.0`；说明本次使用的是模型/参数给出的几何量，而不是覆盖后的体积或壁面积。
- POPCON 证据：当前只明确给出 `xkey=Ti0`、`ykey=ni0` 和 `best` 矩阵，能支持“存在被标记为 `best` 的网格点”这一点；但不能据此直接声明最佳区的具体坐标边界。

3. 风险与不确定性
- `Pheat=-384.29673502986043` 与 `valid=1.0`、`ignited=1.0` 同时出现，提示功率平衡定义、符号约定或后处理逻辑需要复核；否则容易把“标签成立”和“功率收支健康”混为一谈。
- `Qfus=1000.0` 与 `Qfus_raw=-42.64100506776084` 明显不一致；仅凭这份 JSON 不能判断 `Qfus` 是否经过截断、上限处理或异常值替代，因此该指标当前可读性存在不确定性。
- 由于 `use_tauE=1.0`，结果依赖固定约束时间假设；如果 `tauE` 改变，`valid`、`ignited`、`Pfus`、`Pheat` 以及 POPCON 标记都可能变化，而本次 JSON 没有给出这类敏感性。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是当前未提供显式 `nx`、`ny`，也未提供对应的 `x/y` 数组坐标值；虽然 `best` 矩阵存在，但不足以严谨定位最佳区边界或描述变化方向。
- 输出给出了多组约束相关量，如 `betaT`、`beta_soft_limit`、`beta_o_limit`、`nbar_o_Sudo`，但没有直接说明哪些量参与了 `best` 或 `valid` 的最终判据，因此对“为什么某些网格点是 best”仍有判据透明度不足的问题。

4. 下一步建议
- 1）补做一版带显式 `x`、`y`、`nx`、`ny` 导出的 `Ti0`–`ni0` 扫描；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`nbar_o_Sudo`，用于确认扫描判据覆盖面与网格分辨率是否影响结论。
- 2）围绕约束时间做敏感性验证：在当前设定附近扫描 `tauE`，或在工具支持时对比固定 `tauE` 与标度驱动方案；重点监控 `tauE_used`、`tau_ISS04`、`H_ISS04`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`。
- 3）做一次功率平衡审计，逐项核对 `Pfus`、`Pn`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`、`Pheat`、`Qfus_raw`、`Qfus` 的计算和符号约定；目标是确认 `Pheat` 为负以及 `Qfus`/`Qfus_raw` 不一致是否来自定义或后处理。
- 4）补做成分扫描，例如对 `fimp`、`Zimp`、`fHe` 做小范围扰动；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Pwall`、`valid`，用于判断当前结果对杂质与氦灰设定的依赖程度。
- 5）若后续需要压实壁负荷与几何影响，可复跑带权威几何输入或覆盖量的案例；重点监控 `Vp`、`Sw`、`Pwall`、`aspect_vol`、`iota`，验证当前几何设定是否显著影响 0-D 输出。
