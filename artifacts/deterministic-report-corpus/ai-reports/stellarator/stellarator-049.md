1. 核心结论
- `config=stellarator`（`config_label=仿星器 Stellarator`），`preset=HELIAS`。
- 当前运行点的单点评估结果为：`valid=1.0`、`ignited=0.0`、`Pfus=455.18886183145054`、`Qfus=1.2968014151444907`、`Pheat=351.0089181856214`、`Pwall=0.5059311195752156`。
- 这表示该 0-D 单点解在当前求解条件下被标记为有效，但不是点火解；因此“运行点有效”不能等同于“已找到扫描工作窗”。
- POPCON 扫描需要单独看：本次扫描的坐标键为 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵并非全零，64 个网格中有 14 个格点被标记为 1，且 `n_invalid=0`。
- 但由于未提供 `x/y` 数组以及 `nx/ny`，必须保留限制性判断：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点层面：`valid=1.0` 说明该单点被模型接受，`ignited=0.0` 说明并非点火状态；同时 `Pfus=455.19` 与 `Qfus=1.2968` 对应的是当前假设下的聚变输出水平，`Pheat=351.01` 反映该点仍处在有显著加热功率参与的平衡中。
- 约束时间层面：`use_tauE=1.0`，且参数里 `tauE=1.0`、输出里 `tauE_used=1.0`；同时给出 `tau_ISS04=0.6303059382948054`、`H_ISS04=1.5865311418536596`。这必须解读为：结果依赖固定约束时间假设，而不是仅由 ISS04 标度自洽决定。
- 边界/裕度相关：输出给出 `betaT=0.02401564315720357`、`beta_soft_limit=0.05`、`beta_o_limit=0.48031286314407134`；密度侧给出 `nbar=4.4374996231955825e+19`、`n_Sudo=1.5404609505981776e+20`、`nbar_o_Sudo=0.2880631035452378`。这些字段说明当前点可以与该模型中的 beta 与 Sudo 密度参照做对照，但 JSON 未提供更完整的极限定义与扫描边界解释。
- 成分与辐射：`Zeff=1.8672566371681416`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`；辐射/损失项中给出 `Pbrem=10.383047736972426`、`Pcycl=19.915026331872703`、`P_line=0.0`。就这份结果看，轫致与回旋损失是已显式计入的，而线辐射在当前运行点输出为 0。
- 几何与装置量：`Vp=1149.0700858727764`、`Sw=1593.4931630494737`、`aspect_vol=10.009223084112334`、`kappa_eff=2.5664096887270573`、`N_fp=5.0`、`iota=0.8220570026198248`。同时 `geom_is_measured=0.0`，且 `Vp_override=0.0`、`Sw_override=0.0`，说明本次结果使用的是当前模型/参数生成的几何量，没有看到体积或壁面积覆盖值被启用。
- POPCON 层面：`n_invalid=0` 说明扫描结果中没有被标记为无效的网格点；`best` 矩阵存在值为 1 的格点，说明在当前“best”判据下，至少有部分采样点满足该判据。但这与 `last_run.outputs.valid=1.0` 是两件事：前者是扫描中的筛选标签，后者是当前运行点的单点有效性。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：本次结果对固定 `tauE=1.0` 的假设敏感，若约束时间设定改变，`Pfus`、`Qfus`、`valid`、`ignited` 都可能变化。
- 虽然 `best` 矩阵不是全零，但未提供 `x/y` 数组及 `nx/ny`；因此只能确认“有被标记为 best 的格点存在”，不能判断这些格点对应的物理位置、连通性、边界或趋势。扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `valid=1.0` 仅对应当前单个运行点，不代表其周围参数邻域也有效；反过来，扫描里存在 `best=1` 的格点，也不自动证明当前运行点就是最佳点。
- JSON 中未提供 `betaN`、`betap`、`q`、`q95` 等字段，因此无法按这些约束维度补充判断。
- `geom_model` 字段未出现，不能据此判断几何模型级别；仅能看到 `geom_is_measured=0.0`，因此几何输入的测量权威性与细节层级在这份数据里并不充分。
- `P_line=0.0` 只说明本次输出中线辐射项为 0，不能外推出更一般的杂质线辐射结论。

4. 下一步建议
1. 重新执行 `Ti0`-`ni0` 的 POPCON 扫描，并导出完整 `x`、`y`、`nx`、`ny`、`best`、`n_invalid`；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`nbar_o_Sudo`、`betaT`，先把“当前点有效”与“扫描中哪些点被标记为 best”明确对应起来。
2. 对固定约束时间做敏感性扫描：在 `use_tauE=1` 前提下扫描 `tauE`，并记录 `tauE_used`、`tau_ISS04`、`H_ISS04`、`Pfus`、`Qfus`、`valid`、`ignited`、`Ptrans`；若工具支持，再补做一次非固定 `tauE` 的对照运行，用来检验当前结果对固定约束时间假设的依赖程度。
3. 对成分与辐射做参数扫描，优先扫描 `fimp` 与 `Zimp`；监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`，确认当前 `P_line=0` 是否对这些输入敏感。
4. 对几何/壁负荷相关输入做一致性检查或参数扫描，例如 `Rw`、`Sw_override`、`Vp_override`；监控 `Sw`、`Vp`、`Pwall`、`aspect_vol`、`iota`，用于验证壁面积与体积设定变化对壁负荷和单点有效性的影响。
