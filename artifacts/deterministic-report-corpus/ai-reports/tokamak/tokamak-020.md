# 1. 核心结论
- `config=tokamak`，`preset=ITER`。本次单点结果为：`valid=1.0`，`ignited=1.0`，`Pfus=8789.29569445447`，`Qfus=1000.0`，`Pheat=-71.62511456149059`，`Pwall=11.853502450203893`。
- 以上只说明当前运行点在该 0-D 求解中被标记为有效且点火；这不等同于 POPCON 扫描已经证明存在工作窗。
- 当前结果依赖固定约束时间假设：`use_tauE=1.0`，`tauE_used=2.0`。
- 当前几何模型较简化：`geom_model=0.0`。
- POPCON 扫描轴为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，并提供了 `best` 标记矩阵；但缺少 `x/y` 数组以及显式 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

# 2. 关键指标解读
- 运行点功率相关输出中，`Pfus=8789.30`，`Pn=7031.44`，`Pth=Ptrans=1299.25`，而 `Pheat=-71.63`。就当前 JSON 而言，这是该单点的模型输出组合，需要与增益和功率平衡定义一起看。
- 约束时间与约束因子方面，`tauE_used=2.0`，`H98=3.1609`，`H_ITPA20=3.9920`，`tau_ITPA20=0.5010`。由于 `use_tauE=1.0`，这里首先反映的是固定约束时间设定下的结果。
- L-H 相关输出为 `P_LH=109.48`，`LH_ratio=11.8677`。这里只能解读为当前模型中传输功率与 L-H 阈值模型的比较结果，不能写成保证进入或维持 H 模。
- 边界/运行态指标包括：`betaN=12.3299`，`betaT=0.1745`，`betap=4.8572`，`q=2.12`，`q95=3.8661`，`nbar_o_nGw=1.4870`。这些量可用于后续扫描判读，但当前数据没有给出对应判据阈值。
- 成分与辐射方面，`Zeff=1.8673`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`；损失项为 `Pbrem=196.42`，`Pcycl=190.56`，`P_line=0.0`。
- 几何相关输出为 `Vp=888.33`，`Sw=735.45`，且 `Vp_geom=Vp`、`Sw_geom=Sw`，`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，与 `geom_model=0.0` 一起表明这里采用的是较简化的几何表示。
- POPCON 方面，`best` 矩阵并非全 0，说明在当前准则下至少有被标记为 `1` 的网格点；但由于没有 `x/y` 数组，不能据此描述最佳区的位置、边界或趋势。

# 3. 风险与不确定性
- 当前单点虽然 `valid=1.0` 且 `ignited=1.0`，但同时出现了 `Pheat<0`、`Qfus=1000.0` 与 `Qfus_raw=-122.7125` 的组合；这提示增益定义、截断/上限处理或功率平衡口径需要单独核对。
- `use_tauE=1.0` 使结果直接依赖固定约束时间假设，因此对 `Pfus`、`Qfus`、`Pheat` 的解读都带有明显模型设定依赖。
- `geom_model=0.0` 表明几何模型较简化，`Pwall`、`Vp`、`Sw` 等量对更细几何处理的敏感性在当前数据里没有被展开。
- 虽然提供了 `best` 矩阵，且其中有少量 `1` 标记，但缺少 `x/y` 数组和显式 `nx/ny`；因此不能判断最佳区的实际坐标范围、形状、边界连续性或敏感性趋势。
- `P_line=0.0` 需要确认这代表当前模型计算结果确为零，还是该项在本次设置下未起作用；这会影响对杂质辐射表征完整性的判断。
- `betaN`、`q95`、`nbar_o_nGw` 等边界量已给出，但当前 JSON 没有配套阈值或裕度定义，因此不能从这份 0-D 数据直接推出更强的工程结论。

# 4. 下一步建议
- 先补齐 POPCON 导出：对 `xkey=Ti0`、`ykey=ni0` 的扫描重新输出完整 `x`、`y`、`nx`、`ny`、`best`。监控指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`。
- 围绕当前运行点做局部二维扫描：继续扫 `Ti0` 与 `ni0`，检查单点结论对邻域参数的敏感性。监控指标：`valid/ignited` 的切换、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pwall`、`LH_ratio`、`Pbrem`、`Pcycl`。
- 做约束时间假设验证：扫描 `tauE`，并与 `tau_ITPA20`、`H98`、`H_ITPA20` 对照；必要时比较固定 `tauE` 与缩放关系驱动的结果。监控指标：`tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Qfus_raw`、`Pheat`、`betaN`、`nbar_o_nGw`。
- 做几何与辐射敏感性验证：改变 `geom_model` 或检查 `Vp/Sw` 覆盖设置，同时扫描 `fHe`、`fimp`、`Zimp`。监控指标：`Vp`、`Sw`、`Pwall`、`Pbrem`、`Pcycl`、`P_line`、`Zeff`、`Pfus`、`Pheat`。
