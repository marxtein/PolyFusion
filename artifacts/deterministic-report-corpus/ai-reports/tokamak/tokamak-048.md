1. 核心结论
- `config=tokamak`（托卡马克），`preset=ITER`。
- 当前运行点为 `valid=1`、`ignited=0`：这表示该 0-D 单点求解被模型接受，但未达到模型中的点火状态。
- 当前运行点的关键输出为：`Pfus=36255.58106658826`、`Qfus=23.239962637676854`、`Pheat=1560.0533284769726`、`Pwall=51.41829010972139`。
- 结果依赖固定约束时间假设：`use_tauE=1`，且本次使用 `tauE_used=2.0`。
- `geom_model=0`，几何模型较简化。
- 需要把单点与扫描分开看：单点 `valid=1` 只说明当前运行点可解；POPCON 扫描虽返回了 `best` 矩阵且 `n_invalid=0`，但缺少 `x/y` 数组与 `nx/ny`，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。

2. 关键指标解读
- **当前运行点**：`Pfus=36255.58`、`Qfus=23.24`、`Pheat=1560.05`、`Pwall=51.42`，同时 `Pn=29004.46`、`Pth=5404.89`、`Ptrans=5404.89`；这些值只对应当前这一个 `Ti0/ni0` 运行点。
- **约束与约束时间**：`tauE_used=2.0`，并给出等效约束倍数 `H98=7.4453`、`H_ITPA20=10.4354`、`tau_ITPA20=0.1917`；由于 `use_tauE=1`，这些结果应理解为“在固定 `tauE` 假设下反推得到的表现”，不是独立自洽地从约束时间缩放中求出。
- **L-H 阈值相关**：`P_LH=179.9554`、`LH_ratio=30.0346`；这里只能表述为当前 `Ptrans/P_LH` 的阈值模型比较结果，不能据此写成保证进入或维持 H 模。
- **边界类指标**：`betaN=51.2926`、`betaT=0.7258`、`betap=20.2057`、`q=2.12`、`q95=3.8661`、`nbar_o_nGw=2.9740`；JSON 未提供这些量的判据阈值，因此这里只能报告数值，不能扩展成超出 0-D 证据的结论。
- **成分与辐射**：`Zeff=1.8673`，输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；损失项中 `Pbrem=1379.4778`、`Pcycl=2026.8041`、`P_line=0.0`。这说明本次结果里韧致辐射与回旋辐射都被计入，而线辐射输出为 0。
- **几何与 POPCON 扫描**：几何量为 `Vp=888.3289`、`Sw=735.4510`，且 `Vp_geom`、`Sw_geom` 与其一致；扫描轴为 `xkey=Ti0`、`ykey=ni0`，返回了一个非全零 `best` 矩阵（可见有 2 个网格单元为 `1`）且 `n_invalid=0`。但由于没有 `x/y` 数组与 `nx/ny`，不能把这些 `1` 映射到具体坐标，也不能判断最佳区边界、形状或趋势。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1`：当前结论直接依赖固定 `tauE=2.0` 的设定，若改用约束时间缩放或改变 `tauE`，`Pfus`、`Qfus`、`valid`、`ignited` 可能发生明显变化。
- `geom_model=0` 表明几何模型较简化；`Vp`、`Sw`、`Pwall` 等量对更细化几何处理的敏感性，当前 JSON 不能回答。
- 单点 `valid=1` 不等于扫描中存在宽裕工作窗；同样，扫描 `n_invalid=0` 也不等于存在稳定或宽广的最佳区。
- 尽管 `best` 矩阵不是全零，但缺少 `x/y` 数组和 `nx/ny`，因此仍需明确写出：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 当前点 `ignited=0`，所以不能把较高的 `Pfus` 或 `Qfus` 直接等同为“点火”。
- 边界类指标、辐射分量和 `LH_ratio` 都只有数值，没有配套判据、坐标数组或多组对比结果；因此对“距离边界多远”“哪一侧更敏感”“最佳区是否连续”等问题，当前数据都不能下结论。

4. 下一步建议
- 重新执行 `Ti0-ni0` 的 POPCON 扫描，并强制输出完整 `x`、`y`、`nx`、`ny`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`。
- 做 `tauE` 敏感性验证：围绕当前 `tauE=2.0` 扫描，或切换到基于缩放的约束时间求解；重点监控 `tauE_used`、`H98`、`H_ITPA20`、`valid`、`ignited`、`Pfus`、`Qfus`、`LH_ratio`。
- 做几何模型对比：将 `geom_model=0` 与更细化几何设置、或 `Vp/Sw` 覆盖值做对照计算；重点监控 `Vp`、`Sw`、`Pwall`、`nbar_geom`、`Pfus`、`Qfus`。
- 做杂质/辐射扫描：围绕 `fimp`、`Zimp`、`fHe` 扫描，检查辐射与功率平衡的变化；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pheat`、`Pfus`、`Qfus`。
- 做边界参数扫描：以 `Ip`、`BT0`、`Sn` 为主轴，在当前点附近做局部扫描；重点监控 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`valid`、`Pwall`、`P_LH`、`LH_ratio`，其中 `LH_ratio` 仍只用于阈值模型比较。
