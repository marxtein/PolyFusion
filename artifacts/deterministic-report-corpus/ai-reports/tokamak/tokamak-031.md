## 1. 核心结论

- 本次结果对应 `config=tokamak`、`preset=ITER`；当前运行点 `valid=1`、`ignited=1`。
- 当前运行点给出的核心输出为：`Pfus=28432.40`、`Qfus=1000.0`、`Pheat=-1267.48`、`Pwall=36.94`。
- 运行点与扫描需要分开看：当前运行点被标记为有效且点火，但这只说明这一组输入参数下求解器给出了一个有效解；不能直接等同于 POPCON 扫描已经证明存在宽工作窗。
- 本次 POPCON 扫描提供了 `best` 矩阵，且并非全 0；按矩阵证据，`best` 中有 2 个网格点为 1，因此在当前准则下“存在被标记为 best 的网格点”，但不代表当前运行点本身就是扫描中的最佳区。
- 扫描轴标记为 `xkey=Ti0`、`ykey=ni0`，但未给出 `x/y` 数组以及 `nx/ny`；因此必须写明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 由于 `use_tauE=1` 且 `tauE=2.0`，本次结果依赖固定约束时间假设；同时 `geom_model=0`，几何模型较简化。

## 2. 关键指标解读

- 运行点状态：`valid=1`、`ignited=1` 说明模型在该输入点收敛并给出“点火”标记；但同时 `Pheat=-1267.48` 为负，表示按当前功率收支定义，净加热项呈负值，这与仅看 `ignited=1` 的直观理解并不完全一致，需要结合功率分项一起看。
- 聚变与增益：`Pfus=28432.40` 很高，但 `Qfus=1000.0` 需谨慎解读，因为同时存在 `Qfus_raw=-22.43`；从 JSON 看，报告值与原始值不一致，说明增益指标可能经过截断、钳位或特殊处理，不能只凭 `Qfus=1000.0` 单独下判断。
- 功率分项：`Pbrem=745.80`、`Pcycl=580.97`、`P_line=0.0`；可见当前辐射/辐射相关损失里，轫致和回旋项为主要已报告项，而线辐射在该组输入下为 0。`Ptrans=3092.22`、`Pth=3092.22` 也表明传输功率项在总热功率平衡中占据明显位置。
- 壁负荷：`Pwall=36.94`，对应本次运行点的第一壁功率负荷输出；这是应持续监控的工程量，但仅凭单点结果不能推断其对参数扰动的敏感性。
- 约束与模式相关：`use_tauE=1`、`tauE_used=2.0`、`tau_ITPA20=0.2930`、`H98=4.885`、`H_ITPA20=6.826`。这里的核心含义是：当前解依赖用户固定的约束时间，而不是完全由缩放律自行闭合求得；因此 `H98/H_ITPA20` 更适合作为“与缩放参照的偏离量”来看。
- L-H 阈值比较：`LH_ratio=18.91`、`P_LH=163.53`。这里只能表述为“当前功率与所用 L-H 阈值模型的比较结果较高”，不能把它写成保证进入或维持 H 模。
- 压力与安全因子：`betaN=29.35`、`betaT=0.415`、`betap=11.56`、`q=2.12`、`q95=3.866`。这些量说明该解对应的压强和磁约束状态需要重点核查，但 0-D 数据本身不足以判断 MHD 裕度。
- 密度边界：`nbar_o_nGw=2.602`，表明线平均密度相对 Greenwald 归一量较高；这是需要单独复核的边界类指标。
- 成分与辐射相关输入：`Zeff=1.867`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`。这些设定直接影响辐射和稀释，因而也会反馈到 `Pfus`、`Pheat` 与约束需求。
- 几何：`Vp=888.33`、`Sw=735.45`，且 `Vp_geom` 与 `Sw_geom` 与其相同，同时 `geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`；结合 `geom_model=0`，说明这里使用的是较简化且未额外修正的几何处理。
- POPCON 扫描：`best` 矩阵为 8x8，左上角附近有 2 个网格点为 1，其余为 0；因此可以说“在当前 best 判据下，只有少量网格点被标记为 best”。但因为缺少 `x/y` 实际数组与 `nx/ny`，不能可靠描述最佳区边界、连续性、宽度或趋势。

## 3. 风险与不确定性

- 最直接的不确定性是功率指标自洽性：`Qfus=1000.0` 与 `Qfus_raw=-22.43` 同时出现，且 `Pheat=-1267.48` 为负；这提示部分输出可能经过格式化或边界处理，需要先确认报告口径。
- 结果高度依赖固定约束时间假设：`use_tauE=1`、`tauE=2.0`。这意味着只要约束时间假设变化，`Pfus`、`Pheat`、`LH_ratio` 以及 best 判据都可能明显变化。
- 几何模型较简化：`geom_model=0`。因此 `Vp`、`Sw`、`Pwall` 等几何相关量的解释应保守，不宜延伸到更细的工程结论。
- 边界类指标存在进一步核查必要：`nbar_o_nGw=2.602`、`betaN=29.35`、`q=2.12`。这些值本身是重要信号，但 0-D 报告不能提供稳定性、剖面或控制层面的补充证据。
- POPCON 证据有限：虽然存在 `best` 矩阵且非全零，但缺少 `x/y` 数组以及 `nx/ny`，因此必须明确：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 当前运行点有效，不等于扫描证明存在可接受参数带宽；相反，本次 `best` 仅在极少数网格点出现，至少说明“扫描层面的证据目前比单点求解更弱”。

## 4. 下一步建议

- 执行一次“固定其余参数、复查单点口径”的验证任务：重点核对 `Qfus` 与 `Qfus_raw` 的生成规则，以及 `Pheat` 为负时 `ignited=1` 的判定逻辑；需监控 `Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Ptrans`、`Pbrem`、`Pcycl`。
- 执行一次“约束时间敏感性扫描”：在当前 `Ti0`、`ni0` 附近扫描 `tauE` 或切换 `use_tauE/tauE_scaling` 口径；需监控 `tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Pheat`、`LH_ratio`、`Pwall`、`valid`、`ignited`。
- 执行一次“补全 POPCON 网格证据”的扫描：输出完整 `x/y` 数组并显式记录 `nx/ny`，继续以 `xkey=Ti0`、`ykey=ni0` 做二维扫描；需监控 `best`、`n_invalid`、各网格点的 `valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`nbar_o_nGw`。
- 执行一次“边界类指标复核”扫描：围绕当前 `ni0`、`Ip`、`BT0` 小范围扰动，检查 `nbar_o_nGw`、`betaN`、`betaT`、`betap`、`q`、`q95` 的变化；目标是确认当前单点是否对这些边界量非常敏感。
- 执行一次“杂质/辐射参数扫描”：对 `fimp`、`Zimp`、`fHe` 做小范围扫描，查看辐射与净功率平衡如何变化；需监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Pheat`、`Pwall`。
- 执行一次“几何模型复核”任务：若工具支持，更换或提高几何模型等级，对比 `geom_model=0` 与更高保真设置下的 `Vp`、`Sw`、`Pwall`、`Pfus`、`valid`；以判断当前简化几何假设对结论的影响。
