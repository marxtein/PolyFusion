1. 核心结论
- `config=tokamak`，`preset=ITER`。
- 当前运行点与扫描结论需要分开看：当前运行点 `valid=1.0`，说明这次 0-D 求解给出了有效解；但 `ignited=0.0`，说明在当前设定下不是点火解。
- 当前运行点的关键结果为：`Pfus=654.36`，`Qfus=8.20`，`Pheat=79.77`，`Pwall=0.998`。
- 就当前单点而言，这是一个“有效但未点火”的运行点；这只说明该组输入下有解，不等同于扫描已经证明存在清晰工作窗。
- POPCON 扫描方面，扫描轴是 `Ti0` 与 `ni0`，`n_invalid=0`，且 `best` 矩阵并非全 0，说明按当前准则至少有被标记的候选单元。
- 但本次 JSON 未提供 `x/y` 网格数组，也未提供 `nx/ny`，因此必须写明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 约束时间与约束因子：`use_tauE=1`，`tauE_used=2.0`，这意味着结果依赖固定约束时间假设；`H98=0.9998`、`H_ITPA20=1.1603` 更适合作为对照量，不能替代对固定 `tauE` 假设本身的检验。
- 功率平衡：`Pheat=79.77`，`Pfus=654.36`，`Qfus=8.20`；同时 `Pth=Ptrans=181.90`，表明当前输出中热功率与传输功率平衡项是明确给出的。
- L-H 阈值比较：`P_LH=66.60`，`LH_ratio=2.73`，只能解读为当前点相对于所选 L-H 阈值模型的比较结果，不能写成保证进入或维持 H 模。
- 辐射与损失：当前给出的辐射项中，`Pbrem=23.92`，`Pcycl=4.83`，`P_line=0.0`；也就是说在已输出的辐射通道里，轫致辐射高于回旋辐射，而线辐射在本次结果中为 0。
- 边界与状态量：`betaN=1.726`，`betaT=0.0244`，`betap=0.6800`，`q=2.12`，`q95=3.8661`，`nbar_o_nGw=0.7435`；这些量可用于描述当前状态，但仅凭这份 0-D 数据不能推出更高维的稳定性结论。
- 成分与几何：`Zeff=1.8673`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`；`Vp=888.33`，`Sw=735.45`。同时 `geom_model=0`，需要明确说明几何模型较简化。

3. 风险与不确定性
- 最主要的不确定性来自 `use_tauE=1`：当前结果直接依赖固定 `tauE=2.0` 的设定，因此对 `Pfus`、`Qfus`、`ignited` 等结论应视为“对该假设成立时”的结果。
- `geom_model=0` 表明几何处理较简化；虽然本次 `Vp_geom=Vp`、`Sw_geom=Sw`、几何比值均为 1，但这不等于几何不敏感，只能说明当前使用的是简化几何输入。
- POPCON 方面不能把“当前单点 `valid=1`”与“扫描已证明存在明确工作窗”混为一谈；本次只有 `best` 矩阵和扫描轴键值，没有 `x/y` 数组与 `nx/ny`，因此仍应判定为扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `ignited=0.0` 是一个直接限制：即使当前有较高 `Pfus` 和 `Qfus`，这份结果仍未显示在该设定下达到点火。
- 辐射与成分结论依赖当前杂质设定：`Zeff=1.8673`、`fimp=0.01`、`Zimp=10`、`fHe=0.04`；其中 `P_line=0.0` 仅代表这次输出结果，后续若成分或模型处理变化，功率平衡解读也可能变化。

4. 下一步建议
- 1. 做约束时间敏感性扫描：在其他参数不变时扫描 `tauE`，并增加一组 `use_tauE=0` 的对照计算；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`tauE_used`、`H98`、`H_ITPA20`。
- 2. 重新做 `Ti0`-`ni0` 的 POPCON 扫描并导出完整 `x/y` 数组和 `nx/ny`；若可能，在当前 `best` 标记单元附近加密网格；重点监控 `best`、`n_invalid`、`Pfus`、`Qfus`、`Pwall`、`LH_ratio`、`nbar_o_nGw`、`betaN`、`q95`。
- 3. 做成分/辐射扫描：至少扫描 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。
- 4. 做几何假设校核：切换或补充更详细的几何模型，或对 `Sw`/`Vp` 覆盖值做对照；重点监控 `Vp`、`Sw`、`Pwall`、`betaN`、`q95`、`Pfus`。
- 5. 对当前单点补做边界量扫描：围绕 `Ip`、`BT0`、`Sn` 做小范围参数扰动；重点监控 `valid`、`ignited`、`nbar_o_nGw`、`betaN`、`q`、`q95`、`LH_ratio`。
