1. 核心结论
- 本次 0-D 结果对应 `config=tokamak`、`preset=ITER`；当前运行点 `valid=1.0`、`ignited=1.0`，`Pfus=13733.27`，`Qfus=1000.0`，`Pheat=-602.63`，`Pwall=17.85`。
- 就当前运行点而言，模型把该单点判为“有效”且“点火”；但这只是单点结论，不能直接等同于扫描已经确认出可扩展的工作区域。
- POPCON 扫描与单点判定需要分开看：扫描轴键为 `Ti0` 与 `ni0`，`n_invalid=0`，`best` 矩阵不是全 0，说明当前准则下至少出现了离散最佳单元。
- 由于缺少 `x/y` 数组且 `nx/ny` 未给出，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 约束时间方面，`use_tauE=1.0`、`tauE_used=2.0`，说明结果依赖固定约束时间假设；同时给出了 `H98=3.404`、`H_ITPA20=4.506`、`HST=0.0687`、`tau_ITPA20=0.444`，这些更适合作为对照量，而不是替代固定 `tauE` 的自洽结论。
- 功率与损失方面，除 `Pfus` 外，还给出 `Pn=10986.62`、`Pth=1624.06`、`Ptrans=1624.06`、`Pbrem=306.90`、`Pcycl=213.06`、`P_line=0.0`；在这组结果里，辐射损失主要体现为轫致与回旋，线辐射结果为 0。
- L-H 相关量为 `LH_ratio=12.64`、`P_LH=128.47`；这里只能表述为该运行点与所用 L-H 阈值模型的比较结果，不能写成保证进入或维持 H 模。
- 归一化与边界相关量为 `betaN=15.41`、`betaT=0.218`、`betap=6.071`、`q=2.12`、`q95=3.866`、`nbar_o_nGw=1.859`；这些数值描述了当前模型点所在状态，但单靠这组数不能推出额外边界结论。
- 成分与几何方面，`Zeff=1.867`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`、`Vp=888.33`、`Sw=735.45`；`geom_model=0.0`，说明几何模型较简化。
- POPCON 方面，`best` 矩阵中只有少量单元为 1；从矩阵索引看，1 仅出现在第一列前两行，其余位置为 0，但由于没有 `x/y` 数组，不能把这些索引可靠映射成 `Ti0/ni0` 的具体数值边界。

3. 风险与不确定性
- `Qfus=1000.0` 与 `Qfus_raw=-22.79` 明显不一致，需优先核对 `Qfus` 的定义、截断规则或后处理逻辑，否则聚变增益解读不稳定。
- `Pheat=-602.63` 与 `ignited=1.0` 同时出现，提示功率平衡相关定义需要复核；仅凭这组数，不能把“点火”直接解读为热平衡毫无疑点。
- `use_tauE=1.0` 表明结果对固定 `tauE=2.0` 的假设敏感，`Pfus`、`Qfus`、`ignited` 等指标都可能随该假设变化。
- `geom_model=0.0` 且几何比值为 `1.0`，说明 `Vp`、`Sw`、`Pwall` 等与几何相关的结果来自较简化处理。
- 虽然 `n_invalid=0`，但扫描仍缺少 `x/y` 数组及 `nx/ny`；因此只能确认“有离散最佳单元”，不能判断工作窗形状或敏感性趋势。

4. 下一步建议
- 重新执行 `Ti0`-`ni0` 的 POPCON 扫描，补齐 `x`、`y`、`nx`、`ny` 输出，并在 `best=1` 的离散单元周边加密网格；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pwall`。
- 在保持其余参数不变的前提下，执行 `tauE` 敏感性扫描，或切换到非固定约束时间的对照计算；重点监控 `tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`ignited`。
- 对当前单点做功率平衡核查，逐项复算 `Pth`、`Ptrans`、`Pn`、`Pbrem`、`Pcycl`、`P_line` 与 `Pheat` 的关系；同时监控 `Eth`、`Ecrit` 以确认定义链是否一致。
- 围绕成分与辐射参数做小范围扫描，如 `Zeff`、`fHe`、`fimp`、`Zimp`；重点监控 `Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Pwall`、`betaN`、`nbar_o_nGw`。
- 若工具支持，使用更高阶几何设置对照当前 `geom_model=0.0` 复跑同一点；重点监控 `Vp`、`Sw`、`Pwall`、`q95`、`betaN`、`Pfus`。
