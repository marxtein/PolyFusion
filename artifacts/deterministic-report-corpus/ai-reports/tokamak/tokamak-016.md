1. 核心结论
- 本次报告对应 `config=tokamak`、`preset=ITER`；当前运行点给出的状态为 `valid=1`、`ignited=1`。
- 当前运行点的主要输出为：`Pfus=28259.82236352749`、`Qfus=1000.0`、`Pheat=-3309.700672331713`、`Pwall=33.92492591789413`。
- 运行点解读与 POPCON 扫描需要分开看：就单点结果而言，求解器给出了 `valid=1` 且 `ignited=1`；但这不等同于“扫描已经证明存在清晰工作窗”。
- POPCON 方面，已提供 `best` 矩阵且并非全 0，因此不能写“当前准则下未找到最佳区”；按已给 `best` 矩阵统计，8×8 共 64 个格点里有 2 个格点被标为最佳区，占比约 3.1%。
- 但本次扫描缺少 `x`/`y` 数组，且未显式提供 `nx`、`ny`，因此仍应视为“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 由于 `use_tauE=1` 且 `tauE_used=2.0`，本次结果依赖固定约束时间假设；同时 `geom_model=0`，说明几何模型较简化。

2. 关键指标解读
- 运行点功率平衡：`Pfus=28259.8` 很高，但 `Pheat=-3309.7` 为负，说明在当前 0-D 记账下，外加/净加热项呈负值特征；这类结果需要结合模型定义审看，不能只凭 `Pfus` 高就直接延伸为更强工程判断。
- 点火与增益：`ignited=1`、`Qfus=1000.0` 显示当前单点被判为点火/高增益状态；但同时 `Qfus_raw=-8.538482830115933` 与 `Qfus=1000.0` 明显不一致，提示 `Qfus` 可能经过截断、钳位或特殊后处理，这会影响对增益结论的置信度。
- 约束与约束时间：`use_tauE=1`、`tauE_used=2.0`、`tau_ITPA20=0.44986099834092586`，说明结果不是完全由缩放律自洽求得，而是显式依赖固定约束时间假设；同时 `H98=2.9844224265922277`、`H_ITPA20=4.445817724532558` 可作为所需约束改善量的对照量。
- L-H 阈值比较：`LH_ratio=9.241415993130582`、`P_LH=179.95536121941507`。这只能表述为当前功率与所采用 L-H 阈值模型之间的比较结果，不能据此写成保证进入或维持 H 模。
- 边界与剖面相关量：`betaN=15.78232485751281`、`betaT=0.22333478571952092`、`betap=6.217152274219831`、`q=2.12`、`q95=3.866062097880496`、`nbar_o_nGw=2.974040792861593`。这些量表明当前单点处于较强约束、较高压力和较高线平均密度比的参数状态，但 0-D 数据本身不提供稳定性边界是否可接受的直接证据。
- 成分与辐射：`Zeff=1.8672566371681416`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`；辐射/损失项中 `Pbrem=596.9552710025991`、`Pcycl=82.2661761484927`、`P_line=0.0`。其中线辐射为 0 可能来自模型简化、输入设定或未启用相应损失项，解读时要谨慎。
- 几何：`Vp=888.3289066331332`、`Sw=735.4510294755139`，且 `Vp_geom=Vp`、`Sw_geom=Sw`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`；结合 `geom_model=0`，说明这里使用的是较简化几何表示，没有看到额外几何修正放大或缩小。
- POPCON 扫描：已知 `xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，说明扫描中没有被标记为无效的点；但因为没有提供 `x`/`y` 取值数组，也没有明确 `nx`/`ny`，只能确认“存在少量 best 标记”，不能可靠描述最佳区在 `Ti0` 或 `ni0` 上的具体位置、边界或趋势。

3. 风险与不确定性
- 最大不确定性来自增益输出的自洽性：`Qfus=1000.0` 与 `Qfus_raw=-8.538482830115933` 同时出现，提示结果中可能存在钳位值、异常态占位值或后处理逻辑，需先确认其定义，否则对“高增益”判断的可信度有限。
- `Pheat=-3309.700672331713` 为负，表明当前功率平衡的解释并不直观；若不了解 `Pheat` 的精确定义，容易误读为“加热裕量充足”或其他超出数据本意的结论。
- `use_tauE=1` 表示结果依赖固定约束时间假设；因此当前点火、增益和边界量对 `tauE` 的假设敏感，不能把本次输出直接当作纯缩放律自然得到的结果。
- `geom_model=0` 说明几何模型较简化；壁面积、体积、壁负荷等与几何强相关的量，可能对更细致几何描述敏感。
- 扫描层面的证据不足：虽然 `best` 矩阵显示有 2 个最佳格点，且 `n_invalid=0`，但因缺失 `x`/`y` 数组和明确的 `nx`/`ny`，仍然不能判断工作窗形状、连通性、边界陡峭程度或对 `Ti0`、`ni0` 的敏感性趋势。
- 辐射项中 `P_line=0.0` 以及 `P_ei=0.0` 这类结果，可能源于模型设定、关闭项或该工况下的简化处理；若据此直接评价损失结构，风险较高。

4. 下一步建议
- 执行一组约束时间敏感性扫描：围绕当前 `tauE=2.0` 对 `tauE` 或 `H_fac` 做离散扫描，重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`betaN`、`nbar_o_nGw`，先确认点火与高增益结论是否依赖固定约束时间假设。
- 执行 `Ti0`–`ni0` 二维扫描并补全网格元数据：输出明确的 `x`/`y` 数组以及 `nx`/`ny`，同时保留 `best`、`n_invalid`，监控 `best_fraction`、最佳格点数量、`valid` 分布、`ignited` 分布、`Pfus`、`Pwall`，以验证是否真的存在连续工作窗而不只是零散格点。
- 执行功率平衡核查任务：逐项复核 `Pheat`、`Pfus`、`Pn`、`Pbrem`、`Pcycl`、`P_line`、`Ptrans`、`Pth` 的定义和记账方式，特别检查 `Qfus` 与 `Qfus_raw` 的关系，确认是否存在钳位、异常值替代或符号约定问题。
- 执行边界量稳健性扫描：对 `Sn`、`fimp`、`fHe`、`Zimp` 做参数扰动，监控 `Zeff`、`Pbrem`、`P_line`、`LH_ratio`、`betaN`、`q95`、`nbar_o_nGw`、`Pwall`，查看当前单点是否对杂质与成分假设高度敏感。
- 执行几何模型对照：若工具支持，切换到更完整的几何模型或修改 `Sw_override`、`Vp_override` 进行对照，重点监控 `Sw`、`Vp`、`Pwall`、`Pfus`、`betaT`，以评估 `geom_model=0` 的简化对结果的影响。
