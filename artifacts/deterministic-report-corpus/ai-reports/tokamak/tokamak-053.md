1. 核心结论
- 本次报告对应 `config=tokamak`、`preset=ITER`；当前运行点输出为 `valid=1.0`、`ignited=0.0`、`Pfus=13698.190266588701`、`Qfus=3.1326737541696663`、`Pheat=4372.683318317482`、`Pwall=24.571144591086384`。
- 就“当前运行点”而言，模型给出了一个 `valid=1.0` 的解，但 `ignited=0.0`，因此不能把这次结果表述为点火状态。
- 就“POPCON 扫描”而言，`xkey=Ti0`、`ykey=ni0`，`best` 矩阵并非全零，说明当前准则下至少有少量网格点被标记为最佳；这与“当前运行点有效”是两件事，不能混为一谈。
- 本次扫描未提供 `x/y` 扫描数组，也未给出 `nx/ny`；因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 结果依赖固定约束时间假设：`use_tauE=1.0`，且 `tauE_used=2.0`；同时 `geom_model=0.0`，说明采用了较简化的几何模型。

2. 关键指标解读
- 运行点功率相关量显示：`Pfus=13698.19`，`Pheat=4372.68`，`Qfus=3.13`，`Pn=10958.55`，`Pwall=24.57`；这些量描述的是当前单个运行点的 0-D 平衡结果，不代表扫描整体表现。
- 约束时间与约束因子方面：`tauE_used=2.0`，同时给出 `H98=7.7873`、`H_ITPA20=9.7548`、`HST=0.1377`；由于 `use_tauE=1.0`，这些结果应理解为建立在固定约束时间设定上的对应量，而不是独立预测出的约束时间结论。
- L-H 阈值相关输出为 `LH_ratio=30.8448`、`P_LH=128.4727`；这只能解读为相对于所用阈值模型的比较结果，不能据此表述为保证进入或维持 H 模。
- 边界与运行参数方面：`betaN=37.6063`、`betaT=0.5322`、`betap=14.8143`、`q=2.12`、`q95=3.8661`、`nbar_o_nGw=1.8588`；这些是后续扫描和筛选时需要持续跟踪的边界量。
- 成分与辐射方面：`Zeff=1.8673`，输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；损失通道中给出 `Pbrem=625.8231`、`Pcycl=2523.7802`、`P_line=0.0`，本次输出里 `Pcycl` 大于 `Pbrem`，而 `P_line` 为零。
- 几何量方面：`Vp=888.3289`、`Sw=735.4510`，且 `Vp=Vp_geom`、`Sw=Sw_geom`，`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`；结合 `geom_model=0.0`，说明本次结果建立在简化几何表述上。
- POPCON 扫描方面：`n_invalid=0` 表明扫描记录中未标出无效点；`best` 矩阵中只有少量单元为 `1`，但由于缺少对应 `x/y` 数组和 `nx/ny`，目前只能确认“存在最佳点命中证据”，不能定位其具体 `Ti0/ni0` 数值位置，也不能判断边界或趋势。

3. 风险与不确定性
- 最大的不确定性来自扫描证据不完整：缺少 `x/y` 数组以及 `nx/ny`，因此不能判断最佳区的形状、连通性、边界陡峭程度，或 `Ti0`、`ni0` 的敏感性趋势。
- `valid=1.0` 只说明当前运行点给出了有效解，不等于 POPCON 扫描已经证明存在明确工作窗；两者必须分开解读。
- `use_tauE=1.0` 使结果依赖固定约束时间假设；因此 `Pfus`、`Qfus`、`H98`、`H_ITPA20` 等量对 `tauE` 设定敏感，当前结果不宜脱离该假设单独外推。
- `geom_model=0.0` 表明几何模型较简化，因而 `Pwall`、`Sw`、`Vp` 及相关负荷解读带有模型简化带来的不确定性。
- 辐射项中 `P_line=0.0`，而 `Pbrem`、`Pcycl` 非零；这意味着当前辐射分配的解读受本次模型设定影响，需要通过补充算例确认其稳定性。
- `ignited=0.0` 说明当前点并未表现为点火状态；因此不能把当前 `Pfus` 或 `Qfus` 直接解读为更强状态已经被 0-D 证据支持。

4. 下一步建议
1. 补做完整的 `Ti0-ni0` POPCON 导出，明确输出 `x`、`y`、`nx`、`ny` 以及 `best_fraction`；重点监控 `best`、`valid`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`。
2. 以当前运行点 `Ti0=174.28571428571428`、`ni0=2.5e+20` 为中心做加密局部扫描，验证当前点附近是否连续出现 `best=1` 或仅是离散命中；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`、`Pbrem`、`Pcycl`、`P_line`。
3. 针对固定约束时间假设做敏感性验证：在保持其余输入不变的前提下，扫描 `tauE`，或在工具支持时对比 `use_tauE=1` 与非固定约束时间模式；重点监控 `tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`valid`。
4. 做几何模型一致性检查：若工具支持，比较 `geom_model=0` 与更高保真几何设定，或核对 `Vp_override`、`Sw_override` 是否需要启用；重点监控 `Vp`、`Sw`、`Pwall`、`betaT`、`betap`、`Pfus`。
5. 单独复核阈值模型相关量，在扫描中同步记录 `LH_ratio` 与 `P_LH`，只做模型比较，不把它解读为 H 模保证；同时联动监控 `Pheat`、`Ptrans`、`valid`。
