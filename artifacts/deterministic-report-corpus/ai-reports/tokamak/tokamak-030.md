## 1. 核心结论
- 本次对象为 `config=tokamak`，`preset=ITER`；当前运行点 `valid=1.0`、`ignited=1.0`、`Pfus=20889.10911405574`、`Qfus=1000.0`、`Pheat=-441.5336595871736`、`Pwall=27.802769504654485`。
- 运行点层面：从 JSON 标记看，本次单点计算被判为有效，且点火标志为真；但这只说明该“当前运行点”的 0-D 求解结果，不等同于 POPCON 扫描已经证明存在明确、宽裕或稳定的工作窗。
- POPCON 扫描层面：`best` 矩阵并非全 0，因此不能写“当前准则下未找到最佳区”；但扫描结果仅显示少量网格被标记为最佳，且 `n_invalid=0`。
- 同时，`last_scan` 未提供 `nx`、`ny`，也未提供 `x`/`y` 数组坐标值，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 结果依赖 `use_tauE=1.0` 与 `tauE=2.0`，因此需要明确：本次结论依赖固定约束时间假设。
- `geom_model=0.0`，说明本次使用的是较简化的几何模型，几何相关量的解释应保持谨慎。

## 2. 关键指标解读
- 运行点功率平衡：`Pfus=20889.10911405574` 很高，`Pn=16711.287291244593` 为主要中子功率；但 `Pheat=-441.5336595871736` 为负，说明按当前 0-D 记账方式，净加热裕量的解释需要特别谨慎，不能只凭 `Qfus=1000.0` 做单向乐观判断。
- 聚变增益：`Qfus=1000.0` 与 `Qfus_raw=-47.31034352757319` 同时出现，表明报告中存在经过限幅或规则处理后的增益值；工程上更适合把它视为模型输出特征，而不是独立的性能定论。
- 辐射与损失：`Pbrem=547.9370797082423`、`Pcycl=537.8773330670649`、`P_line=0.0`。当前辐射项里轫致与回旋损失都不小，而线辐射为 0，是否由成分设定或模型简化导致，需要与杂质和辐射模型一起看。
- L-H 阈值比较：`LH_ratio=18.10255151099252`、`P_LH=146.41437417477889`。这里只能解读为“运行点功率与 L-H 阈值模型的比较结果较高”，不能据此写成保证进入或维持 H 模。
- 约束与经验标度：`tauE_used=2.0`，`H98=4.634686059601369`，`H_ITPA20=6.27044840261136`，且 `use_tauE=1.0`。这意味着结果不是由某个约束标度自行求出的闭环解主导，而是显著受固定 `tauE` 假设控制；因此这些 H 因子更适合当作“反推需求”看，而不是独立验证。
- 压力与稳定性相关指标：`betaN=25.153080241661037`、`betaT=0.3559398147404864`、`betap=9.908586437037853`，数值上都需要重点关注；但由于这里不能引入外部阈值，只能说这是当前运行点压力水平的模型输出，需要在后续扫描里检验其对结果的支配性。
- 边界与密度相关：`q=2.12`、`q95=3.866062097880496`、`nbar_o_nGw=2.230530594646195`。这些量说明当前点的安全因子与平均密度占比已被模型显式计算，后续应作为扫描中的关键监控项。
- 成分与辐射背景：`Zeff=1.8672566371681416`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`。这组参数直接影响辐射、燃料稀释和功率平衡，应视为高敏感输入。
- 几何量：`Vp=888.3289066331332`、`Sw=735.4510294755139`，且 `Vp_geom`、`Sw_geom` 与其相同，同时 `geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`。在 `geom_model=0.0` 下，这些结果体现的是简化几何描述，没有显示额外几何修正。
- POPCON 信息：扫描轴是 `xkey=Ti0`、`ykey=ni0`；`best` 矩阵存在且并非全 0，说明在该二维参数组合上至少有被当前准则接受的网格单元，但由于缺少 `x`/`y` 数组与 `nx`/`ny`，不能把这些单元进一步转写为明确坐标区间、边界斜率或敏感性趋势。

## 3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：结果依赖固定约束时间假设，若 `tauE` 改变，`Pfus`、`Qfus`、`ignited`、辐射分担和压力相关输出都可能明显变化。
- `geom_model=0.0` 带来几何简化风险；即使 `Vp`、`Sw` 已给出，也不能把这类 0-D 几何近似直接当作更高保真工程几何结论。
- `Pheat` 为负而 `ignited=1.0`、`Qfus=1000.0` 同时成立，说明不同输出字段之间存在需要人工复核的解释张力；至少应核对功率平衡定义、限幅逻辑和 `Qfus_raw` 的含义。
- `Qfus=1000.0` 与 `Qfus_raw=-47.31034352757319` 的反差提示：增益指标可能受后处理规则影响，不能只引用单一 `Qfus` 值做判断。
- 扫描方面，虽然 `best` 矩阵中有非零元素，但缺少 `x`、`y` 数组以及 `nx`、`ny` 元数据，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”；也不能可靠比较最佳区面积、边界连续性或离当前点的远近。
- 仅凭 `LH_ratio` 与 `P_LH`，只能说明和阈值模型的相对关系，不能外推成模式转换一定发生或可持续。
- `n_invalid=0` 只说明扫描网格没有被标记为无效点，不代表所有通过点在物理上同等可信；仍需结合 `betaN`、`q95`、`nbar_o_nGw`、辐射和功率平衡逐项筛查。

## 4. 下一步建议
- 先做 `tauE` 敏感性扫描：围绕当前 `tauE=2.0` 对 `tauE` 或等效约束假设做单参数与二维联扫；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pbrem`、`Pcycl`、`betaN`、`nbar_o_nGw`。
- 补全 POPCON 网格证据：重新输出包含 `x`、`y` 数组以及 `nx`、`ny` 的 `Ti0-ni0` 扫描；重点监控 `best` 分布、`n_invalid`、各网格点 `valid/ignited`、以及 `Pfus`、`Pheat`、`Pwall` 的二维变化。
- 做成分与辐射扫描：对 `fHe`、`fimp`、`Zimp` 做参数扫描，检查 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Pheat`、`ignited` 的响应，确认当前“线辐射为 0”的结果是否稳固依赖输入设定。
- 做边界相关扫描：围绕当前 `ni0`、`Ti0` 联合观察 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`，判断当前运行点是否对压力或密度边界量高度敏感。
- 做几何模型核对：在可用时切换到更完整的几何描述或引入 `Vp/Sw` 覆盖对比，监控 `Pwall`、`nbar_geom`、`Pfus`、`Pbrem`、`Pcycl`，评估 `geom_model=0.0` 简化对结论的影响。
- 单独复核功率平衡定义：重点检查 `Pheat`、`Pth`、`Ptrans`、`P_ei`、`Qfus_raw` 与 `Qfus` 的生成逻辑，确认当前运行点“有效/点火”判据与功率记账是否一致。
