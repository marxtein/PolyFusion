我先按报告约束把运行点和扫描证据拆开读，重点检查哪些结论能直接由 JSON 支撑，哪些不能越界。
# 1. 核心结论

- `config=tokamak`（托卡马克），`preset=ITER`。
- 当前运行点字段为：`valid=1.0`，`ignited=1.0`，`Pfus=11038.993110752925`，`Qfus=1000.0`，`Pheat=-870.1743758955477`，`Pwall=13.826642872618262`。
- 这只说明当前单点计算被模型标记为有效且 `ignited=1`；它不等同于 POPCON 扫描已经证明存在可用工作窗。
- POPCON 扫描的轴是 `xkey=Ti0`、`ykey=ni0`。给出的 `best` 矩阵是 8x8，其中有 2 个网格点为 1，`n_invalid=0`。
- 但本 JSON 没有给出扫描 `x/y` 数组，且 `nx/ny` 未提供，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**，也不能确认当前运行点是否落在 `best` 点内。
- `use_tauE=1`，结果依赖固定约束时间假设；同时 `geom_model=0`，几何模型较简化。

# 2. 关键指标解读

- 运行点功率相关：`Pfus=11038.99`，`Pn=8831.19`，`Pth=1039.40`，`Ptrans=1039.40`，`Pwall=13.83`。`Pheat=-870.17` 为负值，说明该字段需要结合模型定义一起看，不能单独当作“仍需外加热”的直观结论。
- 约束时间与约束因子：`tauE_used=2.0`，`tau_ITPA20=0.6014`，`H98=2.4585`，`H_ITPA20=3.3257`，`HST=0.0522`。由于 `use_tauE=1`，这些结果建立在固定 `tauE=2.0` 的假设上，不是由约束时间缩放自洽求得。
- L-H 相关：`P_LH=128.47`，`LH_ratio=8.0904`。这只能表述为当前点相对 L-H 阈值模型的比较结果，不能据此写成保证进入或维持 H 模。
- 边界/约束类指标：`betaN=9.864`，`betaT=0.1396`，`betap=3.8857`，`q=2.12`，`q95=3.8661`，`nbar_o_nGw=1.8588`。这些是后续扫描中应重点跟踪的约束量。
- 成分与辐射：`Zeff=1.8673`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`；辐射项为 `Pbrem=233.19`，`Pcycl=65.04`，`P_line=0.0`。当前这组输出里，线辐射没有贡献。
- 几何：`Vp=888.33`，`Sw=735.45`，且 `Vp_geom=Vp`、`Sw_geom=Sw`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明本次结果没有额外几何修正覆盖。

# 3. 风险与不确定性

- `valid=1`、`ignited=1` 是当前运行点的单点结论；它不能替代 POPCON 对参数空间的判断。
- 尽管 `best` 矩阵中有 2 个点被标记为 1，但由于缺少扫描 `x/y` 数组且 `nx/ny` 未提供，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `Qfus=1000.0` 与 `Qfus_raw=-12.685955156277783` 同时出现，口径明显不一致；在确认字段定义前，不能直接把 `Qfus` 当作稳定可信的增益结论。
- `Pheat` 为负、`P_line=0`、`P_ei=0` 这类结果都提示：当前报告依赖具体模型实现与开关设置，解释时需要先核对字段定义。
- `use_tauE=1` 使结果对固定约束时间假设敏感；`geom_model=0` 则意味着几何表征较简化，外推时不确定性更高。

# 4. 下一步建议

1. 重新导出 `Ti0-ni0` 的 POPCON 扫描原始网格，必须包含 `x`、`y`、`nx`、`ny`。监控：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pheat`、`Pwall`、`Qfus/Qfus_raw`。
2. 做约束时间敏感性扫描：在保留当前点附近参数的同时，比较固定 `tauE` 与缩放模型结果。监控：`tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`valid`、`ignited`、`Pfus`、`Pheat`。
3. 做边界量扫描，至少覆盖 `Ti0`、`ni0`、`Ip`、`BT0` 的邻域扰动。监控：`betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`Pwall`。
4. 做成分与辐射敏感性扫描，围绕 `Zeff`、`fHe`、`fimp`、`Zimp`。监控：`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`Pwall`。
5. 做几何模型复核，至少比较 `geom_model=0` 与更完整几何设置，或检查 `Sw/Vp` 覆盖值影响。监控：`Sw`、`Vp`、`Pwall`、`betaN`、`q95`。
