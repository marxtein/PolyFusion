1. 核心结论
- 本次计算的 `config=tokamak`，`preset=ITER`。当前运行点输出为：`valid=1.0`，`ignited=0.0`，`Pfus=19725.39398388773`，`Qfus=4.406894108885824`，`Pheat=4476.0308499617695`，`Pwall=32.90691543542841`。
- 运行点层面：该单点在模型内被判为有效（`valid=1.0`），但未点火（`ignited=0.0`）；这只说明当前参数组合得到了一组自洽输出，不等同于扫描已经给出明确工作窗。
- POPCON 扫描层面：`xkey=Ti0`、`ykey=ni0`，`best` 矩阵并非全 0，且 `n_invalid=0`；因此按当前判据，扫描中至少出现了少量被标记为 `best` 的网格单元。
- 但本数据未给出 `nx`、`ny`，也未给出 `x/y` 扫描数组，因此必须单独说明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 由于 `use_tauE=1.0` 且 `tauE_used=2.0`，本次结果依赖固定约束时间假设；同时 `geom_model=0.0`，几何模型较简化。

2. 关键指标解读
- 约束时间相关：当前采用固定 `tauE` 输入，表现为 `tauE_used=2.0`；同时输出了 `tau_ITPA20=0.1889927516215564`、`H98=8.11972466212545`、`H_ITPA20=10.582416430471618`。这表示当前结果不是直接由经验约束时间标度决定，而是在固定约束时间假设下再折算出对应的 H 因子。
- 功率与阈值模型比较：`Pheat=4476.0308499617695`，`P_LH=146.41437417477889`，`LH_ratio=32.47810712266304`。这里只能解读为当前功率量与所用 L-H 阈值模型值的比较结果，不能据此写成保证进入或维持 H 模。
- 功率收支与损失分量：`Ptrans=4755.261728746137`，`Pth=4755.261728746137`，`Pbrem=901.1852706849319`，`Pcycl=2764.662647308248`，`P_line=0.0`。本次输出中显式给出了轫致辐射和回旋辐射损失，而线辐射项为 0.0。
- 运行边界指标：`betaN=45.12758513945069`，`betaT=0.638597902916755`，`betap=17.777169784097328`，`q=2.12`，`q95=3.866062097880496`，`nbar_o_nGw=2.230530594646195`。这些量适合在后续扫描中作为边界类监控指标。
- 成分与辐射相关输入/输出：`Zeff=1.8672566371681416`，输入中 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；这些参数与 `Pbrem`、`P_line` 等辐射项直接相关。
- 几何相关：`Vp=888.3289066331332`，`Sw=735.4510294755139`，且 `Vp_geom=888.3289066331332`、`Sw_geom=735.4510294755139`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明本次结果中未体现额外几何修正或覆盖值影响。

3. 风险与不确定性
- 最大不确定性来自 `use_tauE=1.0`：结果依赖固定约束时间假设；若改用约束时间标度或改变 `tauE`，`Pfus`、`Qfus`、`ignited`、`Pwall` 可能明显变化。
- `geom_model=0.0` 表明几何模型较简化；因此 `Sw`、`Vp` 及派生量 `Pwall` 的解释需要谨慎，不能把当前数值直接当作更高保真几何结果。
- `valid=1.0` 仅代表当前单点在模型内有效，不等于 POPCON 扫描已经证明存在清晰、连续的最佳工作窗；运行点有效与扫描找到工作窗是两件事。
- 虽然 `best` 矩阵不是全 0，但缺少 `nx`、`ny` 和 `x/y` 坐标数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能把这些 `best` 标记还原为 `Ti0`/`ni0` 的具体区间。
- `P_line=0.0` 是当前模型输出事实，但仅凭这份 JSON 不能区分这是参数结果、模型分支选择，还是其他设置导致的零值。

4. 下一步建议
1. 补做完整的 `Ti0`-`ni0` 二维 POPCON 扫描，并显式导出 `x`、`y`、`nx`、`ny` 与 `best`；重点监控 `best`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`。
2. 做 `tauE` 假设验证：在其余参数不变时扫描 `tauE`，并对比固定 `tauE` 与经验标度结果；重点监控 `tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`ignited`。
3. 做边界敏感性扫描，例如围绕 `Ti0`、`ni0`、`Ip`、`BT0` 的单参或双参变化；重点监控 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`valid`。
4. 做成分与辐射验证扫描，改变 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`Pwall`。
5. 如工具支持，补做几何模型对照或 `Sw`/`Vp` 覆盖值验证；重点监控 `Sw`、`Vp`、`Pwall`、`betaN`、`q95`，评估简化几何设定对结果的影响。
