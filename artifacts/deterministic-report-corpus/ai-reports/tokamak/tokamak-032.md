1. 核心结论
- 本次单点结果对应 `config=tokamak`、`preset=ITER`；当前运行点 `valid=1.0`、`ignited=1.0`，`Pfus=37136.193980543525`、`Qfus=1000.0`、`Pheat=-2298.0761855101828`、`Pwall=47.369731496437105`。
- 仅就当前运行点看，模型把该点判为“有效且点火”；但这只是单点判定，不等同于 POPCON 扫描已经证明存在明确工作窗。
- POPCON 扫描与单点结论需要分开看：扫描侧给出 `xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，且 `best` 矩阵并非全 0，说明在离散扫描格点里存在满足当前“best”判据的点。
- 但本次扫描没有提供 `x/y` 坐标数组，也没有给出 `nx/ny`，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 结果明显依赖固定约束时间假设：`use_tauE=1.0`、`tauE_used=2.0`；同时 `geom_model=0.0`，说明这里采用的是较简化几何模型。

2. 关键指标解读
- 功率与能量侧：`Pfus=37136.19`，中子功率 `Pn=29708.96`，`Pwall=47.37`；同时 `Pheat=-2298.08` 为负值，`Pth=Ptrans=3533.97`，需要把它与功率平衡定义一起看，不能只凭单个量下结论。
- 约束与增益侧：`tauE_used=2.0`，对照标度有 `tau_ITPA20=0.27155`；`H98=5.13`、`H_ITPA20=7.37` 反映当前点相对这些标度的倍率，但由于 `use_tauE=1`，这些结果建立在固定约束时间输入之上。
- `Qfus=1000.0` 与 `Qfus_raw=-16.159687922747928` 同时出现，说明这里至少存在“显示值”和“原始值”两套口径；在没有额外定义说明前，应优先把它视为需要核对的输出一致性问题。
- 辐射与损失侧：`Pbrem=974.11`、`Pcycl=621.09`、`P_line=0.0`、`P_ei=0.0`；就当前 JSON 而言，可以确认轫致和回旋项存在，而线辐射项在该次结果中为 0。
- 边界与状态量：`betaN=33.54`、`betaT=0.4746`、`betap=13.21`、`q=2.12`、`q95=3.8661`、`nbar_o_nGw=2.9740`，这些是当前运行点的重要无量纲状态指标，应与功率和约束量一起联动观察。
- 成分与几何侧：`Zeff=1.8673`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`；`Vp=888.33`、`Sw=735.45`，且 `Vp_geom=Vp`、`Sw_geom=Sw`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，与 `geom_model=0` 一致，表明本次几何处理较简化。
- L-H 相关量只能按阈值模型比较来读：`LH_ratio=19.6380`、`P_LH=179.9554`，它说明当前计算点与所用 L-H 阈值模型之间的相对关系，不应表述为保证进入或维持 H 模。

3. 风险与不确定性
- 最大的不确定性来自建模假设：`use_tauE=1.0` 说明结果依赖固定约束时间假设，若改用约束标度或改变 `tauE`，`Pfus`、`Qfus`、`Pheat`、`ignited` 可能明显变化。
- `geom_model=0.0` 表明几何模型较简化；因此 `Vp`、`Sw`、`Pwall`、`Pcycl` 等几何相关结果应视为当前简化设定下的输出，不宜外推到更细几何结论。
- 单点与扫描不能混读：当前点 `valid=1`、`ignited=1` 只代表该运行点在当前模型下成立；扫描虽然 `best` 非全零、`n_invalid=0`，但并未自动证明存在可清晰描述的参数窗口。
- 扫描数据缺少 `x/y` 数组和 `nx/ny`，所以“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”；即使 `best` 矩阵里有少量 1，也无法映射成明确的 `Ti0`/`ni0` 区间边界。
- `Qfus=1000.0`、`Qfus_raw=-16.16`、`Pheat<0`、同时 `ignited=1` 的组合值得重点复核，至少说明输出口径、截断规则或后处理定义需要核实。
- `P_line=0.0`、`P_ei=0.0` 是当前结果的一部分，但 JSON 未提供这些量为何为 0 的上下文；因此对总功率收支和通道分解的解释仍有不确定性。

4. 下一步建议
- 执行一次同配置的 POPCON 复扫，并显式导出 `x`、`y`、`nx`、`ny`：继续用 `xkey=Ti0`、`ykey=ni0`，以当前点 `Ti0=97.14285714285714`、`ni0=4e20` 为中心做加密网格；重点监控 `best`、`valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pwall`。
- 做约束时间敏感性扫描：在保持其余参数不变时，扫描 `tauE`，并在工具支持时对比 `use_tauE=1` 与约束标度模式；重点监控 `tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`Pheat`、`ignited`。
- 做功率平衡核对任务：对当前运行点重新输出完整功率分解日志，逐项核对 `Pfus`、`Pn`、`Pheat`、`Pth`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`、`Qfus`、`Qfus_raw` 的定义和符号。
- 做几何模型对比验证：在工具支持时，用不同 `geom_model` 或更详细几何设置复算同一点；重点监控 `Vp`、`Sw`、`Pwall`、`Pcycl`、`betaN`、`q95`、`nbar_o_nGw` 是否显著变化。
- 做成分与辐射敏感性扫描：围绕当前 `fHe=0.04`、`fimp=0.01`、`Zimp=10` 做小范围参数扫描；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Pheat`、`Pwall`。
- 保留 L-H 阈值对比作为辅助检查，而不是状态保证：复扫时同步记录 `LH_ratio` 与 `P_LH`，并与 `Pfus`、`Pheat`、`tauE_used` 联动查看。
