1. 核心结论
- `config=tokamak`，`preset=ITER`。
- 当前运行点结果：`valid=1.0`，`ignited=0.0`，`Pfus=25856.98557621144`，`Qfus=3.671767830454554`，`Pheat=7042.107989984329`，`Pwall=44.733221176748806`。
- 对当前运行点的直接解读是：该单点计算通过了模型内部有效性判定（`valid=1.0`），但没有达到点火（`ignited=0.0`）；这只是“当前运行点”的结论，不等同于扫描已经确认了连续工作窗。
- POPCON 扫描信息与运行点应分开看：扫描维度键为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵存在非零单元，说明当前判据下至少有部分扫描单元被标记为最佳。
- 但本次扫描没有给出 `x/y` 数组，也未见 `nx/ny`；因此必须按数据原样说明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 结果还带有两个前提：`use_tauE=1.0`，所以结果依赖固定约束时间假设；`geom_model=0.0`，所以几何模型较简化。

2. 关键指标解读
- 运行点能量与输出：`Pfus=25856.98557621144`，`Pn=20685.588460969153`，`Pheat=7042.107989984329`，`Qfus=3.671767830454554`，`Pwall=44.733221176748806`；可直接据此判断该点有聚变功率输出与有限增益，但仍是非点火态（`ignited=0.0`）。
- 损失项：`Pbrem=1408.3859409408246`，`Pcycl=4438.785155855173`，`P_line=0.0`；在当前模型输出中，线辐射项为零，但这只能解读为本次计算结果为零，不能外推为线辐射在更一般条件下一定可忽略。
- 约束时间与缩放：`tauE_used=2.0`，`tau_ITPA20=0.1522298589729741`，`H98=9.851630310974382`，`H_ITPA20=13.138027017124589`；由于 `use_tauE=1.0`，这里首先应把它理解为“固定 `tauE` 假设下的结果”，而不是独立由缩放律自洽给出的约束时间结论。
- L-H 阈值比较：`Ptrans=6366.334008430621`，`P_LH=163.52515282240506`，`LH_ratio=38.9318334124703`；这只能表述为当前输运功率与 L-H 阈值模型的比较结果，不能据此写成保证进入或维持 H 模。
- 边界与运行状态量：`betaN=60.41671234516622`，`betaT=0.8549534765825408`，`betap=23.80003604974778`，`q=2.12`，`q95=3.866062097880496`，`nbar_o_nGw=2.602285693753894`；这些是当前点的状态描述，适合在后续扫描中与 `valid/ignited/Pwall` 联合监控，而不宜仅凭单点数值做更强结论。
- 成分与几何：`Zeff=1.8672566371681416`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`，`Vp=888.3289066331332`，`Sw=735.4510294755139`；同时 `geom_model=0.0`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明本次体积与受热面积采用了简化几何处理。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：当前结果依赖固定约束时间假设，`tauE_used=2.0` 与 `tau_ITPA20=0.1522298589729741` 的并列出现也提示，单点输出对约束时间设定较敏感。
- `geom_model=0.0` 表明几何模型较简化，因此 `Vp`、`Sw`、`Pwall` 以及与几何相关的功率密度判断，都应视为简化模型下的结果。
- 运行点与扫描不能混用：`valid=1.0` 仅说明当前单点有效；`best` 矩阵存在非零单元仅说明扫描判据在部分网格单元上被满足，不能自动推出当前点位于该区域，也不能推出区域连续、宽窄或对参数变化是否敏感。
- 扫描信息不完整：缺少 `x/y` 数组且未给 `nx/ny`，所以虽然有 `best` 矩阵和 `xkey/ykey`，仍然无法恢复最佳区对应的物理坐标、边界范围或趋势方向；必须保持“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”的表述。
- `ignited=0.0` 表明本次并非点火解；因此不能把当前输出延伸解读为自持燃烧能力。
- `P_line=0.0`、`P_ei=0.0` 等零值应按模型输出理解，不宜在缺少更多模型说明时赋予额外物理结论。

4. 下一步建议
1. 补全 `Ti0`-`ni0` 的 POPCON 导出，至少输出 `x`、`y`、`nx`、`ny`、`best_fraction` 与每个网格点的 `valid/ignited`；监控指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`。
2. 针对固定约束时间假设做敏感性扫描：在当前参数附近扫描 `tauE`，并与 `use_tauE=0` 的缩放律结果对照；监控指标：`tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`Pheat`、`ignited`。
3. 做几何模型验证：保持其余输入不变，切换或增强几何处理，再比较 `geom_model=0` 与更详细几何设定的差异；监控指标：`Vp`、`Sw`、`Pwall`、`betaN`、`q95`、`LH_ratio`。
4. 做成分与辐射扫描：围绕 `fimp=0.01`、`Zimp=10`、`fHe=0.04` 进行单参数或二维扫描，并检查是否需要补充线辐射模型输出；监控指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pwall`、`valid`。
5. 对边界相关参数做局部扫描，例如围绕 `Ip`、`BT0`、`Sn`、`Ti0`、`ni0` 做小步长扰动；监控指标：`betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`valid`、`ignited`。
