1. 核心结论
- 运行点信息：`config=tokamak`，`preset=ITER`；当前单点结果为 `valid=1.0`、`ignited=0.0`、`Pfus=2321.0121237839703`、`Qfus=2.9353357947917416`、`Pheat=790.7143461753899`、`Pwall=4.231045093754895`。
- 对当前运行点的直接解读是：该 0-D 单点被标记为有效，但未被标记为点火；`valid=1.0` 只说明这个单点计算成立，不能替代 POPCON 扫描结论。
- POPCON 扫描与运行点需要分开看：扫描键为 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵并非全零，按给出的矩阵直接计数仅有 2 个网格点标记为 1，`n_invalid=0`。
- 但本次扫描缺少 `x/y` 数组，且未提供 `nx/ny` 字段，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- `use_tauE=1.0` 且 `tauE_used=2.0`，结果依赖固定约束时间假设；`geom_model=0.0`，几何模型较简化。

2. 关键指标解读
- 功率与状态：该运行点给出 `Pfus=2321.0121`、`Qfus=2.9353`、`Pheat=790.7143`、`Pwall=4.2310`，同时有 `Pn=1856.8097`、`Pth=883.4913`、`Ptrans=883.4913`；这些都只对应当前单点。
- 约束时间与标度：`tauE_used=2.0`，对应 `H98=3.4253`、`H_ITPA20=3.6620`；由于 `use_tauE=1.0`，这里应把这些 H 因子理解为固定约束时间前提下的对应标度结果。
- L-H 阈值比较：`P_LH=66.6021`，`LH_ratio=13.2652`；这只能说明当前结果相对于所用 L-H 阈值模型的比值关系，不能表述为保证进入或维持 H 模。
- 边界相关量：`betaN=8.3844`、`betaT=0.1186`、`betap=3.3029`，`q=2.12`、`q95=3.8661`，`nbar_o_nGw=0.7435`；这些是后续边界与稳定性复核的关键输出。
- 成分与辐射：`Zeff=1.8673`，输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；辐射项为 `Pbrem=60.8819`、`Pcycl=310.5436`、`P_line=0.0`，本次结果中 `Pcycl` 数值高于 `Pbrem`。
- 几何量：`Vp=888.3289`、`Sw=735.4510`，且 `Vp_geom=888.3289`、`Sw_geom=735.4510`，`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`；但这些结果建立在 `geom_model=0.0` 的简化几何处理上。

3. 风险与不确定性
- 最大不确定性来自固定约束时间：`use_tauE=1.0` 使结果对 `tauE=2.0` 的设定敏感，若改动 `tauE` 或改用约束时间标度，`Pfus`、`Qfus`、`valid`、`ignited` 可能变化。
- 几何模型较简化：`geom_model=0.0` 意味着 `Vp`、`Sw`、`Pwall` 等量的解释要保留几何近似带来的不确定性。
- 运行点与扫描不可混读：`valid=1.0` 只说明当前 `Ti0=97.14285714285714`、`ni0=1e+20` 的单点有效，不代表周围参数区间一定存在连续可用区域。
- 扫描证据有限：虽然 `best` 矩阵里出现了 1，但缺少 `x/y` 数组和 `nx/ny`，因此只能确认“存在被标记的网格点”，不能确认最佳区边界、面积占比变化或参数敏感性；“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 辐射构成仍需复核：`P_line=0.0` 使当前辐射损失主要体现在 `Pbrem` 和 `Pcycl`，后续若线辐射设定变化，功率分配结果可能改变。
- `ignited=0.0` 表明该点未被标记为点火状态，因此不能把当前单点结果外推成更广泛的自持结论。

4. 下一步建议
- 重新执行 `Ti0`-`ni0` 的 POPCON 扫描，并显式输出 `x`、`y`、`nx`、`ny` 与完整 `best` 数据；重点监控 `valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`LH_ratio`、`betaN`、`q95`、`nbar_o_nGw`。
- 围绕当前 `tauE=2.0` 做约束时间敏感性扫描，或在工具支持时与约束时间标度结果对照；重点监控 `tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`valid`、`ignited`。
- 做几何一致性验证：若工具支持，比较不同 `geom_model`，并检查 `Vp_override`、`Sw_override` 对结果的影响；重点监控 `Vp`、`Sw`、`Pwall`、`betaN`、`nbar_o_nGw`、`Pbrem`、`Pcycl`。
- 围绕成分与辐射做局部扫描，优先检查 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`valid`。
- 对当前运行点周围再做局部参数扰动扫描，可从 `Ip`、`BT0`、`Sn`、`H_fac` 入手；重点监控 `betaN`、`betap`、`q`、`q95`、`nbar_o_nGw`、`LH_ratio`，并把“单点有效性”和“扫描 best 标记”分开记录。
