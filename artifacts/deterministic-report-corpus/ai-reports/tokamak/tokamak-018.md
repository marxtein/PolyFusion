1. 核心结论
- `config=tokamak`（托卡马克），`preset=ITER`。
- 当前运行点结果为 `valid=1.0`、`ignited=0.0`；这表示本次 0-D 运行点解是有效的，但未显示点火。
- 当前运行点的关键输出为 `Pfus=2197.3239236136174`、`Qfus=5.576749907130885`、`Pheat=394.01514505858347`、`Pwall=3.523469224756149`。
- 运行点与 POPCON 扫描需要分开理解：当前运行点有效，不等于扫描已经证明存在明确工作窗。
- 本次 POPCON 扫描轴为 `xkey=Ti0`、`ykey=ni0`；`best` 矩阵里有少量网格点被标记为 1，但由于缺少 `x/y` 数组且未给出 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- **运行点**：`Pfus=2197.3239` 对应 `Qfus=5.5767`，同时 `Pheat=394.0151`、`Pwall=3.5235`；`ignited=0.0` 说明该点不是点火态，需把“有聚变输出”和“已点火”区分开。
- **功率与损失**：结果中 `Pbrem=49.1043`、`Pcycl=134.7497`、`P_line=0.0`；按当前结果，辐射相关分量主要体现为轫致与回旋部分。另有 `Ptrans=649.6259`、`P_LH=66.6021`、`LH_ratio=9.7538`，这只能解读为在该 L-H 阈值模型比较下的比值关系，不能写成保证进入或维持 H 模。
- **约束与约束时间**：`use_tauE=1.0`，`tauE_used=2.0`，同时给出 `tau_ITPA20=0.7108`、`H98=2.5827`、`H_ITPA20=2.8137`；因此本次结果依赖固定约束时间假设，且相对经验标度需要较高约束增强因子。
- **边界与状态量**：本点的 `betaN=6.1650`、`betaT=0.08724`、`betap=2.4286`、`q=2.12`、`q95=3.8661`、`nbar_o_nGw=0.7435`；这些数值可用于描述运行点所在参数位置，但仅凭这份 0-D 数据不能进一步推出余量或边界远近。
- **成分与几何**：成分相关输入/输出包括 `Zeff=1.8673`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`。几何方面 `Vp=888.3289`、`Sw=735.4510`，且 `geom_model=0.0`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明这里使用的是较简化的几何模型。
- **POPCON 扫描**：`n_invalid=0` 表明扫描结果里未报告无效点，但这不等于已经找到了清晰工作窗。就已给出的 `best` 矩阵看，仅有 2 个网格点被标记为 1；不过因为缺少 `x/y` 数组且未给出 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

3. 风险与不确定性
- `use_tauE=1.0` 是本次解释中的首要不确定性来源；结果直接依赖固定 `tauE=2.0` 假设，若改用经验约束时间或做自洽更新，`Pfus`、`Qfus`、`Pwall` 及 `best` 判据都可能变化。
- `geom_model=0.0` 表明几何模型较简化；当前 `Vp`、`Sw`、壁负荷和密度相关量对几何细节的敏感性尚未被这份结果覆盖。
- 当前运行点 `valid=1.0` 仅说明这个单点可解；它不能替代 POPCON 对参数邻域的判断，更不能与“扫描找到工作窗”混为一谈。
- 虽然给出了 `best` 矩阵和 `n_invalid=0`，但缺少 `x/y` 网格数组及 `nx/ny` 信息，因此无法可靠判断最佳区的实际参数位置、边界宽度或趋势方向。
- `P_line=0.0` 使得本次辐射解读主要落在 `Pbrem` 与 `Pcycl` 上；对杂质线辐射敏感性的证据仍不足。
- `ignited=0.0` 说明该点不是点火态；若后续判据关注自持燃烧或外加加热依赖，这一点需要单独验证，而不能从当前 `Pfus` 或 `Qfus` 直接外推。

4. 下一步建议
- 1) 重新执行 `Ti0-ni0` 的 POPCON 扫描并补齐 `x`、`y`、`nx`、`ny` 输出；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`。
- 2) 围绕当前点做 `tauE`/`use_tauE` 敏感性扫描，分别比较固定 `tauE=2.0` 与经验标度约束；重点监控 `tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`Pwall`、`ignited`。
- 3) 做几何模型对比验证，至少比较 `geom_model=0` 与更完整几何设定，或引入 `Vp/Sw` 覆盖值做一致性检查；重点监控 `Vp`、`Sw`、`geom_volume_ratio`、`geom_wall_ratio`、`Pwall`、`nbar_geom`、`betaN`。
- 4) 做成分与辐射扫描，如扫 `fimp`、`Zimp`、`fHe` 或相关 `Zeff` 设置；重点监控 `Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`Qfus`、`nbar_o_nGw`。
- 5) 在当前点附近增加局部功率/密度扫描，专门复核 L-H 阈值模型比较结果；重点监控 `Ptrans`、`P_LH`、`LH_ratio`、`valid`、`ignited`，并把这一步明确限定为阈值模型比较，不把它表述为 H 模保证。
