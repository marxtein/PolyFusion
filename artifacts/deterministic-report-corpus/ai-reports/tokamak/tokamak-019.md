1. 核心结论
- `config=tokamak`（托卡马克），`preset=ITER`。
- 当前运行点给出：`valid=1`、`ignited=0`、`Pfus=4943.98`、`Qfus=18.93`、`Pheat=261.16`、`Pwall=7.08`。
- 这表示该 0-D 单点结果在模型中被判为有效，但不是点火态；“运行点有效”与“POPCON 扫描找到工作窗”不是同一件事。
- POPCON 扫描信息显示 `xkey=Ti0`、`ykey=ni0`、`n_invalid=0`，且 `best` 矩阵并非全 0，说明当前准则下至少存在命中单元。
- 但由于缺少 `x/y` 数组以及 `nx/ny` 元数据，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 本次结果还依赖 `use_tauE=1` 的固定约束时间假设，且 `geom_model=0`，几何模型较简化。

2. 关键指标解读
- 运行点方面：`Pfus=4943.98`、`Pn=3955.18`、`Pheat=261.16`、`Pth=Ptrans=974.44`、`Pwall=7.08`；其中 `valid=1` 说明该点被模型接受，`ignited=0` 说明该点并未达到点火判据。
- 约束时间方面：`tauE_used=2.0`，同时 `tau_ITPA20=0.582`、`H98=2.89`、`H_ITPA20=3.44`。由于 `use_tauE=1`，这些结果应理解为“在固定 `tauE=2.0` 假设下对应出的性能需求/等效约束”，不能脱离该假设单独解读。
- L-H 阈值比较方面：`P_LH=89.07`、`LH_ratio=10.94`。这只能表述为当前功率与所用 L-H 阈值模型之间的比值结果，不代表保证进入或维持 H 模。
- 边界与剖面相关输出：`betaN=9.25`、`betaT=0.131`、`betap=3.64`、`q=2.12`、`q95=3.87`、`nbar_o_nGw=1.12`。这些是当前单点的边界型指标，但 JSON 未提供相应裕度定义，因此这里只能作为需持续跟踪的状态量。
- 成分与辐射方面：`Zeff=1.87`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`；辐射/损失项为 `Pbrem=110.48`、`Pcycl=165.03`、`P_line=0.0`。就本次输出看，线辐射项没有贡献值，当前总辐射信息主要来自轫致与回旋部分。
- 几何方面：`Vp=888.33`、`Sw=735.45`，且 `Vp_geom=888.33`、`Sw_geom=735.45`、`geom_model=0`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`。这说明本次使用的是较简化几何表示，未看到额外几何修正放大或缩小。
- POPCON 扫描方面：`best` 矩阵中有命中单元，且 `n_invalid=0`，说明扫描中没有被标记为无效的网格点；但因为没有 `x/y` 数组，也没有 `nx/ny`，不能把这些命中单元进一步解释为某个确定坐标范围、边界位置或趋势方向。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1`：当前结果依赖固定约束时间假设，若改用约束时间标度或改变 `tauE`，`Pfus`、`Qfus`、`valid`、`ignited` 可能发生明显变化。
- `geom_model=0` 表明几何模型较简化，因此 `Vp`、`Sw` 以及进一步影响到的 `Pwall` 等结果，应视为简化几何下的 0-D 估计。
- POPCON 证据不完整：虽然 `best` 矩阵不是全 0，但缺少 `x/y` 数组和 `nx/ny`，所以不能判断工作窗的连续性、宽窄、边界朝向或对 `Ti0`/`ni0` 的敏感性。
- 单点结果与扫描结果不能混用：`valid=1` 只说明当前运行点有效，不等于已经证明存在可辨认的扫描工作窗；反过来，即便扫描有命中单元，也不自动等于当前点就在最佳区中心。
- `P_line=0.0`、`P_ei=0.0` 这类零值需要在后续验证中确认其来源，是该工况下确实为零，还是模型设置/输出口径使其未体现。
- `LH_ratio=10.94` 只能作为阈值模型比较结果使用，不能据此推出模式转换一定发生，也不能据此判断运行可维持性。

4. 下一步建议
1. 先补全 POPCON 扫描导出，重新输出 `x`、`y`、`nx`、`ny`，并保留 `best`、`n_invalid`；重点监控 `best` 分布、`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`，确认命中单元对应的实际参数坐标。
2. 围绕当前点做 `Ti0`-`ni0` 的局部二维细扫，检查 `valid=1` 是否只是一两个孤立网格点，还是存在连续命中区域；重点监控 `Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`、`LH_ratio`。
3. 做约束时间敏感性验证：在当前参数下扫描 `tauE`，或切换到 `tauE_scaling` 方案与 `use_tauE=1` 对比；重点监控 `tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`valid`、`ignited`、`Qfus`。
4. 做几何模型复核：如果工具支持，改用更完整的几何模型后复跑同一点与同一扫描；重点监控 `Vp`、`Sw`、`Pwall`、`nbar_geom`、`Pfus`，比较 `geom_model=0` 与更高保真几何下的差异。
5. 做成分与辐射敏感性扫描，优先扫描 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pheat`、`Pfus`、`Pwall`，确认当前辐射分量设置对结果的影响。
