我先按 JSON 逐项约束输出，重点把“单点运行结果”和“POPCON 扫描证据”拆开，不把两者混成一个结论。
1. 核心结论

本次报告的 `config=tokamak`，`preset=ITER`。当前运行点的单点结果为：`valid=1.0`、`ignited=0.0`、`Pfus=163.5888`、`Qfus=2.4191`、`Pheat=67.6227`、`Pwall=0.3144`。这表示该单点在当前 0-D 设定下被判为有效，但未点火。

需要把单点结果和 POPCON 扫描分开看：当前运行点 `valid=1.0` 只说明这个输入点通过了当前计算条件；它不等于“扫描已经找到明确工作窗”。本次 POPCON 扫描给出的 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵非全 0，且 `n_invalid=0`，说明扫描中存在满足当前 best 准则的点证据；但由于未提供 `x/y` 坐标数组以及 `nx/ny` 字段，不能据此判断最佳区的具体位置、形状或敏感性趋势。

此外，`use_tauE=1.0`，结果依赖固定约束时间假设；`geom_model=0.0`，几何模型较简化。

2. 关键指标解读

功率与增益方面：`Pfus=163.5888`、`Qfus=2.4191`、`Pheat=67.6227`，说明当前点有聚变功率输出，但 `ignited=0.0` 表明并非点火解。热功率相关量中，`Pth=90.9476`、`Ptrans=90.9476`，壁负荷 `Pwall=0.3144`。

损失与辐射方面：`Pbrem=5.9810`、`Pcycl=3.4118`、`P_line=0.0`。在这组输入下，报告中给出了轫致辐射和回旋辐射损失，线辐射项为 0。

约束时间与约束标度方面：`tauE_used=2.0`，同时给出了 `H98=0.7963`、`H_ITPA20=0.7986`、`tau_ITPA20=2.5044`。因为 `use_tauE=1.0`，当前结果是建立在固定 `tauE=2.0` 的假设上，不是由约束标度自洽决定。`HST=0.0331` 也被输出，但其物理解释仍应服从这一固定约束时间前提。

边界与运行区指标方面：`betaN=0.8631`、`betaT=0.01221`、`betap=0.3400`，`q=2.12`、`q95=3.8661`，`nbar_o_nGw=0.3718`。这些量可作为后续扫描时的边界监控指标，但仅凭当前单点不能外推趋势。

成分与几何方面：`Zeff=1.8673`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`。几何量给出 `Vp=888.3289`、`Sw=735.4510`，且 `geom_model=0.0`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明本次计算使用的是较简化几何。

L-H 阈值比较方面：`LH_ratio=2.2446`，对应 `P_LH=40.5181`。这只能表述为相对当前 L-H 阈值模型的比较结果，不能据此写成保证进入或维持 H 模。

3. 风险与不确定性

首要不确定性来自约束时间处理：`use_tauE=1.0` 使结果依赖固定 `tauE` 假设，因此 `Pfus`、`Qfus`、`valid`、`ignited` 对 `tauE` 的敏感性需要单独验证。

第二个不确定性来自几何：`geom_model=0.0` 表示几何模型较简化，因此 `Vp`、`Sw`、`Pwall` 等几何相关结果应视为该简化模型下的输出，不宜直接扩展解读。

第三个不确定性来自 POPCON 证据不完整：虽然 `best` 矩阵非全 0 且 `n_invalid=0`，但没有提供 `x/y` 数组以及 `nx/ny`，所以不能判断最佳区的精确坐标、边界形状或对 `Ti0`、`ni0` 的敏感性趋势。换言之，目前只能说“扫描里出现了 best 点证据”，不能把它解释成清晰的二维工作窗画像。

4. 下一步建议

建议先做 `Ti0-ni0` 二维加密扫描，并补充导出 `x/y` 数组与 `nx/ny`。重点监控：`best`、`valid`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`、`LH_ratio`。这样才能判断最佳区是否连续，以及结果对温度和密度的敏感性。

建议再做约束时间敏感性验证：围绕当前 `tauE=2.0` 扫描，或切换到标度驱动的比较算例。重点监控：`tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`P_LH`、`LH_ratio`。这一步是为了确认当前单点结论对固定约束时间假设的依赖程度。

建议补做几何与杂质参数验证扫描，例如对 `geom_model`、`Sw_override`、`Vp_override`，以及 `fimp/Zimp/fHe` 做参数扰动。重点监控：`Pwall`、`Pbrem`、`Pcycl`、`P_line`、`Zeff`、`Pfus`、`Qfus`、`valid`。这样可以区分结果是由简化几何假设驱动，还是对成分与辐射也高度敏感。
