1. 核心结论

- 本次数据对应 `config=tokamak`，`preset=ITER`。
- 当前运行点结果：`valid=1.0`，`ignited=0.0`，`Pfus=9063.895266647065`，`Qfus=3.397642686440931`，`Pheat=2667.7011396220705`，`Pwall=15.951567046735267`。
- 这表示当前单个运行点在模型判据下是“有效”的，但并未点火；这和 POPCON 扫描是否存在可用区域不是同一件事。
- POPCON 记录为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`。`best` 矩阵中有 2 个网格单元标记为 `1`，说明扫描记录里存在被当前准则标中的点。
- 但本次 JSON 未提供 `x/y` 扫描数组，也未给出 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

2. 关键指标解读

- 约束与约束时间：`use_tauE=1.0`，`tauE_used=2.0`，`tau_ITPA20=0.2670030884628022`，`H98=6.203657808049662`，`H_ITPA20=7.490550058856837`。由于 `use_tauE=1`，**结果依赖固定约束时间假设**。
- L-H 阈值比较：`LH_ratio=24.684844298737318`，`P_LH=109.47785577586617`。这里只能解读为当前运行点与所用 L-H 阈值模型的比较结果，不能把它表述成保证进入或维持 H 模。
- 功率与损失：`Pth=2702.4438239868764`，`Ptrans=2702.4438239868764`，`Pn=7251.116213317652`，`Pbrem=344.8694499523014`，`Pcycl=1433.1669190123057`，`P_line=0.0`。在给定结果中，回旋相关损失项大于轫致辐射项，线辐射项为 0。
- 边界与归一化量：`betaN=25.64627789345832`，`betaT=0.3629190267942215`，`betap=10.102872445607227`，`q=2.12`，`q95=3.866062097880496`，`nbar_o_nGw=1.4870203964307964`。这些量是后续扫描时应重点联动监控的边界指标。
- 成分与杂质：`Zeff=1.8672566371681416`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`。
- 几何：`Vp=888.3289066331332`，`Sw=735.4510294755139`，且 `geom_model=0.0`，因此**几何模型较简化**。

3. 风险与不确定性

- `valid=1.0` 只说明当前这个运行点通过了模型内部判据；它不能替代 POPCON 扫描对参数空间范围的说明。
- 虽然 `best` 矩阵里存在标记为 `1` 的网格单元，但缺少 `x/y` 数组与 `nx/ny`，所以**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1.0` 使结果对 `tauE=2.0` 的固定设定敏感；`H98`、`H_ITPA20` 和 `tau_ITPA20` 只能作为与该设定并列的参照。
- `geom_model=0.0` 表明几何采用简化处理，`Pwall`、`Vp`、`Sw` 及相关派生量的解释应保守。
- `P_line=0.0` 出现在本次结果中，但仅凭提供的 JSON，不能进一步区分这是当前参数点的结果还是模型设置带来的表现。

4. 下一步建议

- 重新执行 `Ti0` - `ni0` 的 POPCON 扫描，并导出完整 `x`、`y`、`best`、`nx`、`ny`。重点监控：`valid`、`ignited`、`best`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`。
- 做 `tauE` 敏感性扫描，至少比较当前固定 `tauE=2.0` 与其他 `tauE` 取值下的结果变化。重点监控：`tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`valid`、`Pfus`、`Qfus`。
- 做几何模型对比计算，比较 `geom_model=0` 与更细几何输入/覆盖值下的差异。重点监控：`Vp`、`Sw`、`Pwall`、`Pcycl`、`Pbrem`、`betaT`、`betap`。
- 围绕当前 `LH_ratio` 与 `P_LH` 做阈值比较扫描，例如联动扫描 `Ti0`、`ni0` 或辅助功率分配参数。重点监控：`LH_ratio`、`P_LH`、`Ptrans`、`valid`；该任务的目的只是核对阈值模型比较结果，不是宣称模式状态。
