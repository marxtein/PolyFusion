1. 核心结论
- 本次对象为 `config=tokamak`、`preset=ITER`。当前运行点给出的状态是 `valid=1.0`、`ignited=1.0`，并报告了 `Pfus=19775.915312522557`、`Qfus=1000.0`、`Pheat=-1330.9729290614323`、`Pwall=25.079769616496577`。
- 就“当前运行点”而言，0-D 结果显示该点被模型判为有效且点火；但同时 `Pheat` 为负值，说明在这组方程定义下，外加加热功率口径出现了负值，解读时不能只看 `valid/ignited`，还需要结合功率平衡口径一起核查。
- 就“POPCON 扫描”而言，`last_scan.best` 矩阵中存在少量 `1`，说明扫描结果里至少有被当前准则标记为“best”的网格点；这不等于“当前运行点有效”本身，也不能自动推出存在清晰的工作窗形状。
- 由于扫描结果未提供 `x/y` 数组，且未明确给出 `nx/ny`，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0`，说明结果依赖固定约束时间假设；`geom_model=0.0`，说明几何模型较简化。

2. 关键指标解读
- 运行点功率口径：`Pfus=19775.915312522557`、`Pn=15820.732250018045`、`Pth=1948.8777576828436`、`Ptrans=1948.8777576828436`、`Pwall=25.079769616496577`。这组值表明模型给出了较高聚变功率和对应壁负荷，但这些数值仍应与约束、辐射和功率平衡项一起看。
- 增益与点火判据：`Qfus=1000.0`，但同时存在 `Qfus_raw=-14.858240074400326`。这说明输出中至少包含一个经过处理的 `Qfus` 字段和一个原始口径字段，两者不一致，解读时应优先把它当作模型输出口径需要核对的信号，而不是直接把高 `Qfus` 当作单一结论。
- 约束时间与约束倍率：`tauE_used=2.0`，`tau_ITPA20=0.4004496218537907`，`H98=3.6319972420945903`，`H_ITPA20=4.994386037228487`。由于 `use_tauE=1.0` 且参数里 `tauE=2.0`，本次结果依赖固定约束时间假设，不是完全由缩放律自洽求出的约束时间。
- L-H 阈值比较：`LH_ratio=13.310699640435674`，`P_LH=146.41437417477889`。这只能说明模型中的相关功率与 L-H 阈值模型之间存在较高比值，不能写成保证进入或维持 H 模。
- 辐射与损失：`Pbrem=441.9391096044537`、`Pcycl=233.39326615578165`、`P_line=0.0`。在已给字段中，轫致辐射和回旋辐射有明确贡献，而线辐射被记为 0；这意味着当前成分设定下，模型并未给出线辐射损失贡献。
- 成分参数：`Zeff=1.8672566371681416`，参数中有 `fHe=0.04`、`fimp=0.01`、`Zimp=10`。这表明杂质与灰分已被纳入，但这里只能按给定输入理解，不能外推到更复杂杂质谱或辐射行为。
- 归一化边界类指标：`betaN=18.494911942397824`、`betaT=0.26172045201506355`、`betap=7.285725321351364`、`q=2.12`、`q95=3.866062097880496`、`nbar_o_nGw=2.230530594646195`。这些值可作为当前运行点的边界类参考，但仅凭这份 0-D 输出不能下更强的稳定性或可运行性结论。
- 几何口径：`Vp=888.3289066331332`、`Sw=735.4510294755139`，且 `geom_model=0.0`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`。这说明体积和受热面积采用了较简化几何处理，没有看到额外几何修正被启用。
- POPCON 扫描信息：扫描键为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵中有 `1`。这说明扫描过程中没有无效点计数，且存在被当前准则选中的网格单元；但因为缺少 `x/y` 数组与明确网格尺寸，不能定量说明这些点位于何处，也不能描述边界、宽度或趋势。

3. 风险与不确定性
- 最大的不确定性来自功率口径自洽性：`Pheat` 为负，而 `Qfus=1000.0` 与 `Qfus_raw=-14.858240074400326` 又明显不一致，这提示应先核查输出定义、截断规则或后处理逻辑。
- 结果依赖固定约束时间假设：`use_tauE=1.0` 且 `tauE_used=2.0`，所以对 `Pfus`、`ignited`、`Qfus` 的判断都受这个固定约束时间设定影响。
- 几何模型较简化：`geom_model=0.0`，因此 `Vp`、`Sw`、`Pwall` 等量的工程代表性受简化几何假设限制。
- 扫描证据不完整：虽然提供了 `best` 矩阵，但未提供 `x/y` 数组，也未明确 `nx/ny`；因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 不能把“运行点有效”与“扫描找到 best 点”混为一谈：前者是当前单点求解状态，后者是扫描准则下部分网格被标记；两者回答的是不同问题。
- 线辐射项为 `P_line=0.0`，在当前输入下这只是模型输出结果，不能据此推广为所有相关工况都可忽略线辐射。
- `LH_ratio` 只能作为阈值模型比较指标，不能把它解释成保证进入或维持某种约束模式。

4. 下一步建议
- 开展一组可执行的约束时间敏感性扫描：围绕 `tauE` 或切换 `use_tauE`/`tauE_scaling` 做对比，重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`tauE_used`、`H98`、`H_ITPA20`。
- 开展 `Ti0`–`ni0` 的完整 POPCON 重扫，并补齐 `x/y` 数组与明确 `nx/ny` 输出；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pwall`、`nbar_o_nGw`、`betaN`、`q95`，以便区分“单点成立”与“扫描中是否存在连续候选区域”。
- 做功率平衡核对任务：逐项复核 `Pheat`、`Pth`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`、`P_ei`、`Pn` 的定义和后处理规则，确认负 `Pheat` 与 `Qfus/Qfus_raw` 不一致是否来自口径差异、截断或符号约定。
- 做几何模型对比任务：在相同主参数下比较 `geom_model=0` 与更高保真几何口径，重点监控 `Vp`、`Sw`、`Pwall`、`nbar_geom`、`Te_line_geom`、`Ti_line_geom`，评估简化几何对壁负荷与平均量的影响。
- 做成分与辐射敏感性扫描：围绕 `fHe`、`fimp`、`Zimp` 做有限范围扫描，重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`valid`，检查当前“`P_line=0`”是否对输入成分高度敏感。
- 做边界类指标核查任务：在保持其余参数不变时，扫描 `Ip`、`BT0`、`Sn` 或相关密度设定，重点监控 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`LH_ratio`、`Pwall`，用于识别哪些指标最先偏离当前运行点。
