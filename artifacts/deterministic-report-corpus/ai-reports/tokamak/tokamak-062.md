1. 核心结论

- `config=tokamak`，`preset=ITER`。当前运行点 `valid=1.0`、`ignited=0.0`，`Pfus=18996.97`、`Qfus=2.793`、`Pheat=6801.71`、`Pwall=35.08`。
- 就单个运行点而言，这次 0-D 求解给出了“有效”结果，但 `ignited=0.0`，因此不能表述为点火状态。
- POPCON 扫描需要单独看待，不能把“运行点有效”与“扫描找到了工作窗”混为一谈。本次扫描的 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵不是全 0，说明当前判据下至少有部分网格点被标记为 `best`。
- 但本次未提供 `x/y` 数组，也未给出 `nx/ny`；因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能可靠描述最佳区的具体位置或边界。

2. 关键指标解读

- 运行点热功率与聚变输出：`Pfus=18996.97`，`Qfus=2.793`，`Pheat=6801.71`。同时 `Pth=Ptrans=5456.86`。
- L-H 相关量：`P_LH=146.41`，`LH_ratio=37.27`。这里仅能解读为当前功率与所用 L-H 阈值模型之间的比较结果，不能写成保证进入或维持 H 模。
- 约束时间与约束增益：`use_tauE=1.0`，`tauE_used=2.0`，结果依赖固定约束时间假设。对应输出还给出 `tau_ITPA20=0.163`、`H98=9.52`、`H_ITPA20=12.27`。
- 边界相关量：`betaN=51.79`、`betaT=0.733`、`betap=20.40`、`q=2.12`、`q95=3.87`、`nbar_o_nGw=2.23`。这些值可作为后续扫描的重点监控对象，但仅凭这份 0-D 输出不宜外推更多结论。
- 成分与辐射：`Zeff=1.867`，参数中 `fHe=0.04`、`fimp=0.01`、`Zimp=10`。辐射/损失项为 `Pbrem=1034.73`、`Pcycl=4109.52`、`P_line=0.0`。
- 几何：`Vp=888.33`、`Sw=735.45`，且 `geom_model=0.0`，说明几何模型较简化；本次 `Vp_override=0`、`Sw_override=0`，几何比值 `geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`。

3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1.0`：本次结果直接依赖固定 `tauE=2.0` s 的假设，而不是先由约束标度自洽决定。
- `geom_model=0.0` 表明几何处理较简化，`Pwall`、`Vp`、`Sw` 及相关派生量对几何细节的敏感性还没有被这份结果覆盖。
- POPCON 只给出了 `best` 矩阵、`xkey/ykey` 和 `n_invalid`，未给出 `x/y` 数组及 `nx/ny`；因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `P_line=0.0` 是本次输出的直接结果，但需要在后续验证中确认这是模型设定结果还是输入条件导致的结果。
- 当前运行点 `valid=1.0` 不等于扫描层面已经得到充分的参数空间证据；同时 `ignited=0.0`，说明“有效单点”和“点火状态”并不是同一件事。

4. 下一步建议

- 执行 `Ti0-ni0` 二维加密扫描，并完整导出 `x`、`y`、`best`、`nx`、`ny`。建议围绕当前点 `Ti0=200.0`、`ni0=3e20` 附近加密。需要监控：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best`。
- 执行约束时间敏感性扫描：在保持其他参数不变时扫描 `tauE`，并补做一次基于约束标度的对照计算。需要监控：`tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`valid`、`ignited`。
- 执行几何模型对照：在 `geom_model=0` 的基础上，与更高保真几何设定或 `Vp/Sw` 覆盖值做对照。需要监控：`Vp`、`Sw`、`Pwall`、`Pfus`、`betaN`、`q95`。
- 执行成分/辐射扫描：对 `fHe`、`fimp`、`Zimp`、`Zeff` 做参数扫描，并核对 `P_line` 的建模状态。需要监控：`Pbrem`、`Pcycl`、`P_line`、`Pheat`、`Pwall`、`valid`。
- 执行边界量跟踪扫描：围绕当前运行点记录 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw` 随输入变化的响应，用于判断该单点对输入扰动的敏感性。
