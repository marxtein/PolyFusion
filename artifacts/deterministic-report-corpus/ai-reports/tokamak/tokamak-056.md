1. 核心结论
- 本次对象为 `config=tokamak`、`preset=ITER`；当前运行点 `valid=1`、`ignited=0`，对应 `Pfus=35067.36708246707`、`Qfus=8.508729151998255`、`Pheat=4121.3401503362675`、`Pwall=53.2852707552136`。
- 就当前运行点而言，这是一组“已给出有效解但未点火”的 0-D 结果；`Qfus` 与 `Pfus` 为正，说明模型在该输入下给出了聚变输出，但 `ignited=0` 表明不能把它解读为点火态。
- 就 POPCON 扫描而言，`last_scan.best` 矩阵并非全 0，因此不能写“当前准则下未找到最佳区”；但仅凭现有数据，只能确认“最佳判据命中的网格点很少”。
- 需要单独强调：当前运行点 `valid=1` 只说明这一组参数下模型求解有效；这不等同于“扫描已经找到清晰工作窗”，两者不能混为一谈。
- 由于 `use_tauE=1` 且 `tauE_used=2.0`，结果依赖固定约束时间假设；同时 `geom_model=0`，几何模型较简化，这两点都会限制结论外推。
- 扫描层面缺少 `x/y` 数组且未提供 `nx/ny`，因此应明确写明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点功率面：`Pfus=35067.36708246707`，中子功率 `Pn=28053.893665973657`；`Pheat=4121.3401503362675`，热功率相关量中 `Pth=6340.348971661518`、`Ptrans=6340.348971661518`。这些值说明模型给出了显著聚变输出与有限外部/等效加热结果，但是否满足更严格工程目标，不能仅凭这份 0-D 数据外推。
- 墙负荷与几何：`Pwall=53.2852707552136`，`Sw=735.4510294755139`，`Vp=888.3289066331332`；且 `Sw_geom`、`Vp_geom` 与对应值相同，`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`。这表明本次结果直接建立在当前简化几何设定上；因 `geom_model=0`，几何描述较简化。
- 约束时间与约束标度：`use_tauE=1`、`tauE_used=2.0`，同时给出了 `H98=8.750297602180337`、`H_ITPA20=12.133050727261379`、`tau_ITPA20=0.16483900421732034`。由于本次采用固定 `tauE`，这些约束相关结果更适合作为与标度的对照，而不是独立证明运行点一定满足某一经验约束规律。
- 边界与平衡相关：`betaN=60.170113519267574`、`betaT=0.8514638705556733`、`betap=23.702893045463103`，安全因子有 `q=2.12`、`q95=3.866062097880496`。这些量对运行边界很关键，但本报告只应把它们视为当前模型输出，不应据此直接下“可运行边界已满足”的结论。
- 密度与成分：`nbar_o_nGw=2.974040792861593`，`Zeff=1.8672566371681416`，输入成分包括 `fHe=0.04`、`fimp=0.01`、`Zimp=10`。这些参数会共同影响辐射、约束和功率平衡，适合在后续扫描中作为敏感参数重点检查。
- 辐射与损失：`Pbrem=1602.1071478843232`、`Pcycl=3192.3574472838413`、`P_line=0.0`。当前结果中线辐射为 0，而轫致与回旋辐射占有明确份额，因此若后续改变杂质、密度或温度设定，应重点复核辐射分量是否显著变化。
- L-H 阈值比较：`LH_ratio=35.23289847381034`，`P_LH=179.95536121941507`。这里最多只能解读为“当前功率与所用 L-H 阈值模型之间的比较结果较高”，不能写成保证进入或维持 H 模。
- POPCON 扫描信息：扫描轴键为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`。`best` 矩阵中仅第一列前两行出现 `1`，其余为 `0`；这说明按当前判据，只有少量网格点被标记为最佳，但由于缺少 `x/y` 数组以及 `nx/ny`，还不能把它解释成明确的区域边界、宽度或趋势。

3. 风险与不确定性
- 最大的不确定性来自模型假设：`use_tauE=1` 表明结果依赖固定约束时间假设，若改用约束标度或让 `tauE` 随状态变化，`Pfus`、`Qfus`、`Pheat` 及最佳点判定都可能变化。
- `geom_model=0` 表明几何模型较简化；因此 `Pwall`、`Sw`、`Vp` 以及任何与表面积、体积、壁负荷相关的判断，都应视为简化模型下的结果。
- 当前运行点有效，不等于扫描结论充分。虽然 `n_invalid=0` 说明扫描没有无效点计数，但这不代表已经获得清晰工作窗；缺少 `x/y` 数组且未提供 `nx/ny`，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `best` 矩阵虽有命中点，但命中点很少；在没有坐标数组与网格尺寸说明时，不能可靠判断这些点是孤立解、边缘解，还是更大区域的一部分。
- `ignited=0` 是需要保留的限制条件。即使 `Pfus` 和 `Qfus` 为正，也不能据此把当前点描述为点火运行。
- `LH_ratio` 只能用于与阈值模型比较，不能写成保证进入或维持 H 模；同理，任何基于 `betaN`、`q95`、`nbar_o_nGw` 的“已满足工程边界”表述，都超出了这份 0-D 证据。
- `P_line=0.0` 可能意味着当前模型设置下未计入线辐射贡献或该项为零输出；对杂质辐射敏感性的判断因此仍不充分，需要额外扫描验证。

4. 下一步建议
- 执行 `Ti0`-`ni0` 二维加密扫描：保留 `xkey=Ti0`、`ykey=ni0`，补充导出完整 `x/y` 数组与明确 `nx/ny`，并记录 `best`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`，用于确认最佳点是否只是少数孤立网格命中。
- 执行约束时间假设验证：在相同基准参数下，对 `use_tauE` 与 `tauE` 做对比扫描，至少比较固定 `tauE=2.0` 与按标度结果的差异；重点监控 `tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`ignited`、`best` 判定是否变化。
- 执行成分/辐射敏感性扫描：围绕 `fHe=0.04`、`fimp=0.01`、`Zimp=10` 做参数扰动，重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pheat`、`Pfus`、`Qfus`，验证当前零线辐射结论对杂质设定是否敏感。
- 执行边界量复核扫描：围绕 `Ip`、`BT0`、`Sn` 或密度相关输入做小范围多点扫描，重点监控 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`valid`，用于识别当前结果是否高度依赖单点设定。
- 执行几何模型对照：如果工具支持，切换或扩展 `geom_model`，并对比 `Sw`、`Vp`、`Pwall`、`Pfus`；若暂不支持更复杂几何，也应至少保留一组“几何简化假设”标签，避免把当前壁负荷结果当作几何无关结论。
- 执行 L-H 阈值比较复核：继续输出 `LH_ratio` 与 `P_LH`，但把它作为阈值模型对比项单独监控，并与 `Pheat`、`Ptrans`、`valid`、`ignited` 同步记录，避免把阈值比较直接误读为模式进入结论。
