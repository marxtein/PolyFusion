1. 核心结论
- 当前运行点信息：`config=tokamak`，`preset=ITER`，`valid=1.0`，`ignited=0.0`，`Pfus=8766.841770616767`，`Qfus=2.151546175990757`，`Pheat=4074.6705176242663`，`Pwall=17.46073059058595`。
- 就单点结果看，`valid=1.0` 表示该参数组合在 0-D 模型中得到了有效数值解；`ignited=0.0` 表示该运行点未被模型标记为点火状态。这两个判断只针对当前运行点。
- 就 POPCON 扫描看，扫描变量名是 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵并非全 0，但只有少量单元被标记为 1；这说明扫描中存在满足当前“best”判据的离散网格点证据。
- 需要单独强调：当前运行点“有效”不等于 POPCON 扫描已经证明存在明确工作窗；本次数据缺少 `x/y` 数组以及 `nx/ny`，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 结果依赖 `use_tauE=1.0` 与 `tauE_used=2.0` 的固定约束时间假设；同时 `geom_model=0.0`，应视为几何模型较简化。

2. 关键指标解读
- 约束时间与约束因子：`use_tauE=1.0` 表明本次结果不是由传输模型自洽求得约束时间，而是依赖固定 `tauE=2.0`；对应输出 `tauE_used=2.0`、`tau_ITPA20=0.22540983002113762`、`H98=7.437797840726732`、`H_ITPA20=8.872727510652272`，可理解为在该固定约束时间假设下，对经验标度的对应需求水平。
- 加热与阈值比较：`Ptrans=3170.174485830759`，`P_LH=109.47785577586617`，`LH_ratio=28.9572211965957`。这里仅能解读为“模型中的功率与 L-H 阈值模型的比值较高”；这只是阈值模型比较，不能表述为保证进入或维持 H 模。
- 损失与辐射项：`Pbrem=400.5267869710808`，`Pcycl=2257.33759894578`，`P_line=0.0`。在当前列出的损失项里，`Pcycl` 大于 `Pbrem`，而 `P_line` 为 0；这反映的是当前输入成分与模型设定下的结果，不应外推到未扫描情形。
- 边界相关量：`betaN=30.085056759633787`，`betaT=0.42573193527783665`，`betap=11.851446522731552`，`q=2.12`，`q95=3.866062097880496`，`nbar_o_nGw=1.4870203964307964`。这些值给出了当前单点的压力、磁约束和平均密度相对量级，但仅凭这组 0-D 输出不能判断裕度或窗口宽度。
- 成分与杂质：`Zeff=1.8672566371681416`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`。这组参数与 `Pbrem`、`P_line`、`Pfus` 直接相关，后续若改动杂质或氦灰设定，应联动观察辐射损失和聚变性能变化。
- 几何量：`Vp=888.3289066331332`，`Sw=735.4510294755139`，且 `Vp_geom=Vp`、`Sw_geom=Sw`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`。结合 `geom_model=0.0`，可解读为本次几何处理未引入更复杂修正，`Pwall` 等结果对简化几何假设敏感。

3. 风险与不确定性
- 最大的不确定性来自约束时间处理：`use_tauE=1.0` 说明结果依赖固定约束时间假设；因此 `Pfus`、`Qfus`、`Pheat`、`LH_ratio` 等都应理解为“在 `tauE=2.0` 条件下”的结果，而不是更普适的自洽预测。
- 几何模型风险同样明显：`geom_model=0.0` 表明几何模型较简化，`Vp`、`Sw`、`Pwall` 以及部分体积/面积相关量可能随几何建模方式改变。
- 扫描证据有限：虽然 `best` 矩阵里有少量 1，且 `n_invalid=0`，但由于缺少 `x/y` 数组和 `nx/ny`，“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”；因此不能据此声称存在明确、连续或宽裕的工作窗。
- 单点有效不等于扫描稳健：`valid=1.0` 只说明当前这一组 `Ti0`、`ni0`、`tauE`、几何与成分参数能得到有效解，不代表邻近参数点也保持相同结论。
- 当前点未点火：`ignited=0.0` 是模型对当前单点的直接标记；在没有补充扫描或机理验证前，不应把较高 `Pfus` 或 `Qfus` 直接解读为点火或自持状态。
- 辐射与成分建模仍有不确定性：`P_line=0.0`、`Zeff=1.867...`、`fimp=0.01`、`Zimp=10` 说明当前结果对杂质/线辐射设定较敏感，尤其需要确认这是否为输入假设导致的输出特征。

4. 下一步建议
- 先补齐 POPCON 原始网格输出：重新导出 `Ti0`-`ni0` 扫描的 `x`、`y` 数组以及 `nx/ny`，并保留 `best`、`valid`、`ignited`、`n_invalid`。重点监控指标：`best` 矩阵、`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`、`n_invalid`。
- 围绕当前运行点做局部加密扫描：以当前 `Ti0=174.28571428571428`、`ni0=2e+20` 为中心，对 `Ti0` 和 `ni0` 做更细步长二维扫描。重点监控指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`、`LH_ratio`。
- 单独做约束时间敏感性验证：由于 `use_tauE=1.0`，建议扫描 `tauE` 或等效的 `H_fac`，检查结论对固定约束时间假设的依赖程度。重点监控指标：`tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`ignited`、`LH_ratio`。
- 做几何模型对比复算：在可用时切换更详细的几何模型，或对 `Vp_override`、`Sw_override` 做一致性验证。重点监控指标：`Vp`、`Sw`、`Pwall`、`Pfus`、`Qfus`、`nbar_o_nGw`。
- 做成分/辐射参数扫描：对 `fimp`、`Zimp`、`fHe` 做参数扫描，确认 `P_line=0.0` 与当前功率平衡是否对成分设定敏感。重点监控指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`。
