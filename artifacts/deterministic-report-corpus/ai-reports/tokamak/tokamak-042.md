1. 核心结论
- 配置与预设：`config=tokamak`，`preset=ITER`。当前运行点输出为 `valid=1.0`、`ignited=0.0`、`Pfus=2265.9738166617663`、`Qfus=1.1343216883944665`、`Pheat=1997.646558154993`、`Pwall=5.7972865682945`。
- 当前运行点：这次 0-D 求解把该点标记为 `valid=1.0`，但同时 `ignited=0.0`；因此“运行点有效”与“已点火”不是同一件事。
- POPCON 扫描：扫描键为 `xkey=Ti0`、`ykey=ni0`，记录到 `n_invalid=0`；`best` 矩阵不是全 0，说明当前准则下至少有部分网格单元被标记为 `best=1`，但这不等于当前运行点本身就在该区域内。
- 扫描证据：JSON 未给出 `nx`、`ny`，也未给出 `x/y` 扫描数组；扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 模型前提：`use_tauE=1.0` 且 `tauE_used=2.0`，结果依赖固定约束时间假设；`geom_model=0.0`，几何模型较简化。

2. 关键指标解读
- 当前运行点功率侧：`Pfus=2265.9738`、`Pheat=1997.6466`、`Qfus=1.1343`，同时有 `Pn=1812.7791`、`Pth=1351.2219`、`Ptrans=1351.2219`；这些数值共同描述了该单点的功率收支状态，但 `ignited=0.0` 表明它并未被本次模型判为点火。
- 约束时间与经验因子：`tauE_used=2.0`，`H98=5.4361`，`H_ITPA20=5.6355`，`HST=0.1346`；由于 `use_tauE=1.0`，这些结果都应放在“固定约束时间假设”下理解。
- L-H 阈值比较：`LH_ratio=20.2880`，`P_LH=66.6021`；这里只能把 `LH_ratio` 视为阈值模型比较结果，不能据此写成保证进入或维持 H 模。
- 边界与运行状态：`betaN=12.8231`、`betaT=0.18146`、`betap=5.0514`、`q=2.12`、`q95=3.8661`、`nbar_o_nGw=0.7435`；这些字段给出当前点的归一化压力、磁安全因子和平均密度占比信息。
- 成分与辐射/损失：`Zeff=1.8673`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`；已列出的辐射或相关损失项中，`Pcycl=1013.4020` 高于 `Pbrem=86.2174`，而 `P_line=0.0`。
- 几何：`Vp=888.3289`、`Sw=735.4510`，且 `geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`、`Vp_override=0.0`、`Sw_override=0.0`；结合 `geom_model=0.0`，说明本次结果建立在较简化的几何表示上。
- POPCON 扫描信息：扫描轴是 `Ti0` 与 `ni0`，`best` 矩阵中存在少量 `1` 值，说明扫描判据并非处处失败；但由于缺少 `x/y` 数组和 `nx/ny`，不能据此报告最佳区位置、边界或趋势。

3. 风险与不确定性
- 固定约束时间假设是首要不确定性来源：`use_tauE=1.0` 使结果直接依赖 `tauE_used=2.0`，若改用其他约束时间处理方式，`Pfus`、`Qfus`、`ignited` 及经验因子结果都可能变化。
- 几何模型较简化：`geom_model=0.0` 且几何修正比均为 `1.0`，因此壁面积、体积及相关负荷结果的解释要保留模型简化带来的不确定性。
- 扫描证据不足：虽然 `best` 矩阵不全为 0，且 `n_invalid=0`，但缺少 `x/y` 数组与 `nx/ny` 后，仍不能判断工作窗形状、连通性、边界位置或对 `Ti0`/`ni0` 的敏感性。
- 运行点与扫描不能混同：`valid=1.0` 只说明当前单点被模型接受，`best` 则是扫描判据对网格单元的标记；两者回答的是不同问题。
- `LH_ratio` 的解释范围有限：它只能用于阈值模型比较，不能单独作为运行状态已经进入某一约束模式的证据。

4. 下一步建议
1. 补做完整 POPCON 导出：保留 `x`、`y` 数组以及 `nx`、`ny`、`best_fraction` 等元数据；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
2. 在当前 `use_tauE=1`、`tauE=2.0` 前提下，对 `Ti0` 和 `ni0` 做更细的二维加密扫描，确认 `best=1` 单元是否持续出现；重点监控 `LH_ratio`、`P_LH`、`betaN`、`q95`、`nbar_o_nGw`、`Pbrem`、`Pcycl`、`P_line`。
3. 做约束时间假设对比复算：保持其余输入不变，比较固定 `tauE` 与可用约束时间标度/不同 `tauE` 取值的结果；重点监控 `tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`ignited`。
4. 做几何敏感性验证：若工具支持，比较不同 `geom_model` 或不同 `Vp/Sw` 覆盖设置；重点监控 `Vp`、`Sw`、`Pwall`、`Pcycl`、`Pfus`、`betaN`。
