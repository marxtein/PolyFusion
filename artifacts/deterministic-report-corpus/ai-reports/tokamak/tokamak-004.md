## 1. 核心结论

- 本次数据的 `config=tokamak`，`preset=ITER`。
- 当前运行点结果：`valid=1.0`，`ignited=1.0`，`Pfus=2617.4209994305675`，`Qfus=1000.0`，`Pheat=-57.174253723486515`，`Pwall=3.4811926873403363`。
- 这说明“当前这个单点”被模型标记为有效且点火；但这只对应 `last_run`，不能直接等同于扫描已经给出明确工作窗。
- POPCON 扫描是另一件事：本次扫描轴为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵并非全零，说明按当前准则至少有少量网格被标记为最佳候选。
- 但本次没有提供 `x/y` 扫描数组，也没有 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- 由于 `use_tauE=1.0`，结果依赖固定约束时间假设；由于 `geom_model=0.0`，几何模型较简化。

## 2. 关键指标解读

- 运行点功率收支：`Pfus=2617.4209994305675`，`Pwall=3.4811926873403363`；辐射/相关损失里有 `Pbrem=95.69580703161502`、`Pcycl=6.823624363547916`、`P_line=0.0`。同时 `Pheat=-57.174253723486515`，说明当前输出里净加热项为负值，需结合模型定义解读。
- 约束时间与约束表征：`tauE_used=2.0`，`H98=1.3020252754948103`，`H_ITPA20=1.7445651239131734`，`tau_ITPA20=1.1464175069107592`。由于 `use_tauE=1.0`，这些结果建立在固定 `tauE=2.0` 的前提上。
- L-H 阈值比较：`LH_ratio=3.3229598094454076`，`P_LH=109.47785577586617`。这里只能把它看成与 L-H 阈值模型的比较结果，不能写成保证进入或维持 H 模。
- 边界与平衡相关：`betaN=3.4523835625809265`，`betaT=0.048854484376145195`，`betap=1.3600020599855878`，`q=2.12`，`q95=3.866062097880496`，`nbar_o_nGw=1.4870203964307964`。
- 成分与辐射相关：`Zeff=1.8672566371681416`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`。
- 几何相关：`Vp=888.3289066331332`，`Sw=735.4510294755139`，且 `Vp_geom`、`Sw_geom` 与之相同；`geom_model=0.0`，说明这里采用的是较简化几何。

## 3. 风险与不确定性

- `last_run` 与 `last_scan` 不能混读：单点 `valid=1.0`、`ignited=1.0` 只说明当前输入点的模型判定，不代表扫描已经清楚给出可用区域。
- 扫描侧虽然有 `best` 矩阵，但缺少 `x/y` 数组以及 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。目前最多只能确认：`best` 不是全零。
- `Qfus=1000.0` 与 `Qfus_raw=-45.7797142764517` 同时出现，且符号不一致；这说明 `Qfus` 的定义、截断或后处理方式需要单独核查。
- `Pheat=-57.174253723486515` 与 `ignited=1.0` 并存，提示功率收支口径或点火判据需要复核，不能只看单个标签。
- 由于 `use_tauE=1.0`，结果对固定约束时间假设敏感；由于 `geom_model=0.0`，结果还受简化几何近似影响。

## 4. 下一步建议

1. 重新做 `Ti0-ni0` 的 POPCON 扫描，并补齐 `x`、`y`、`nx`、`ny` 输出。  
   重点监控：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 做约束时间敏感性扫描：在相同其余参数下扫描 `tauE`，或对比 `use_tauE=1` 与基于标度的结果。  
   重点监控：`tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`Pheat`、`ignited`。

3. 做几何模型/几何覆盖值校验：对比 `geom_model` 取值，或检查 `Vp_override`、`Sw_override` 对结果的影响。  
   重点监控：`Vp`、`Sw`、`Pwall`、`Pfus`、`betaN`、`q95`。

4. 做成分与辐射敏感性扫描：围绕 `Zeff`、`fHe`、`fimp`、`Zimp` 扫描。  
   重点监控：`Pbrem`、`P_line`、`Pcycl`、`Pheat`、`Pfus`。

5. 单独核查 L-H 阈值比较输出与功率口径。  
   重点监控：`LH_ratio`、`P_LH`、`Pheat`、`Ptrans`，并仅将其作为阈值模型比较结果使用。
