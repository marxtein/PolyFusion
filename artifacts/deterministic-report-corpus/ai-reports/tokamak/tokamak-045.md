1. 核心结论
- 本次记录来自 `config=tokamak`、`preset=ITER`；当前运行点给出 `valid=1.0`、`ignited=0.0`、`Pfus=14162.336354136038`、`Qfus=5.27112783921374`、`Pheat=2686.775351714585`、`Pwall=22.90990294468219`。
- 就当前运行点本身看，这是一个“有效但未点火”的 0-D 解；`valid=1` 仅说明该单点在本次模型与判据下被接受，`ignited=0` 说明该点不是自持点火状态。
- 该点还给出 `Ptrans=3378.054779983596`、`P_LH=128.47267357801456`、`LH_ratio=26.2939556397749`；这里的 `LH_ratio` 只能理解为与 L-H 阈值模型的比较量，不能据此表述为保证进入或维持 H 模。
- POPCON 扫描需要与当前运行点单独看待：`last_scan` 的 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵不是全 0，且 `n_invalid=0`，说明扫描中至少存在被当前准则标记为 `best` 的单元；但这不等于当前运行点就在最佳区内，也不等于已经确认了工作窗范围。
- `use_tauE=1.0` 且 `tauE_used=2.0`，因此结果依赖固定约束时间假设；`geom_model=0.0`，因此几何模型较简化。

2. 关键指标解读
- 约束时间与约束比较：当前直接使用 `tauE_used=2.0`；同时记录了 `tau_ITPA20=0.24139897171537206`、`H98=6.537288124753448`、`H_ITPA20=8.285039434045949`、`HST=0.1188517892446709`。在 `use_tauE=1` 的前提下，这些 `H` 因子更适合作为“相对缩放律的比较量”，而不是独立预测。
- 功率与损失：`Pfus=14162.34`、`Qfus=5.27`、`Pheat=2686.78`；损失相关项中 `Pbrem=538.86`、`Pcycl=1602.33`、`P_line=0.0`，按已给数据看，回旋辐射项大于轫致辐射项，线辐射项在本次结果中为 0。
- 边界与平衡指标：`betaN=32.05784736682289`、`betaT=0.4536487834927768`、`betap=12.628590557009026`；`q=2.12`、`q95=3.866062097880496`；密度相关量为 `nbar_geom=2.2187498115977914e+20`、`nbar_o_nGw=1.8587754955384956`。这些量说明当前点对应了一组明确的压力、磁安全因子和密度归一化状态，后续应与 `valid/best` 联动检查。
- 成分与燃料：`strcase=D-T`、`Zeff=1.8672566371681416`、`fHe=0.04`、`fimp=0.01`、`Zimp=10`、`fT_used=1.0`。这些输入/输出共同限定了本次辐射与功率平衡结果的成分背景。
- 几何量：`Vp=888.3289066331332`、`Sw=735.4510294755139`，且 `Vp_geom=888.3289066331332`、`Sw_geom=735.4510294755139`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明本次直接采用了当前简化几何模型结果，没有额外体积/壁面积修正。
- POPCON 证据：从已给 `best` 矩阵可直接看出，只有前两行第一列为 `1`，其余单元为 `0`；因此当前准则下并非“大面积 best 区”，但由于未提供 `x/y` 数组，不能把这些矩阵位置换算成明确的 `Ti0/ni0` 数值边界。

3. 风险与不确定性
- `last_scan` 未给出 `x/y` 数组，也未显式给出 `nx/ny`；扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 当前运行点 `valid=1` 与 POPCON 中存在 `best` 单元是两件事：前者是单点有效性，后者是扫描判据结果；仅凭这份 JSON，不能证明当前点位于扫描中的最佳区，也不能证明工作窗宽窄。
- `use_tauE=1` 表示结果建立在固定 `tauE=2.0` 的假设上，因此 `Pfus`、`Qfus`、`ignited`、以及各类 `H` 因子的解释都对这一假设敏感。
- `geom_model=0` 表示几何模型较简化，因此 `Vp`、`Sw`、`Pwall`、`Pcycl` 等与几何相关的量，存在模型层面的不确定性。
- `LH_ratio=26.29` 只能说明当前结果相对 L-H 阈值模型给出了较高的比较值；它不是“保证进入 H 模”或“保证维持 H 模”的证据。
- `P_line=0.0`、`P_ei=0.0` 在这份输出中没有更多上下文，无法仅凭 JSON 判断这是物理上可忽略，还是模型设置/通道关闭所致。

4. 下一步建议
- 优先补做一次带完整坐标输出的 POPCON 扫描：导出 `x`、`y`、`nx`、`ny`、`best` 或 `best_fraction`，继续围绕当前 `Ti0` 与 `ni0` 做加密扫描；重点监控 `best`、`n_invalid`、`valid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`、`LH_ratio`。
- 针对固定约束时间假设做敏感性验证：扫描 `tauE`，或在可用时对比固定 `tauE` 与缩放律模式；重点监控 `tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`valid`、`ignited`。
- 针对简化几何做模型对照：在可用时切换 `geom_model`，或测试 `Sw_override`、`Vp_override` 的影响；重点监控 `Sw`、`Vp`、`Pwall`、`Pcycl`、`Pfus`、`Qfus`、`valid`。
- 针对成分与辐射做参数扫描：围绕 `fimp`、`Zimp`、`fHe` 做小范围扰动，检查辐射与增益对杂质/氦灰的敏感性；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pheat`、`Qfus`、`valid`。
