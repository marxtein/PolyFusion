我先按 JSON 字段约束整理运行点和扫描信息，重点把“当前点成立”与“扫描是否有工作窗证据”分开。
1. 核心结论

- 本次结果对应 `config=tokamak`，`preset=ITER`。当前运行点输出为：`valid=1.0`，`ignited=1.0`，`Pfus=15896.150079484214`，`Qfus=1000.0`，`Pheat=-1524.916312624061`，`Pwall=19.5407079341625`。
- 这只说明“当前这一个 0-D 运行点”在模型内被标记为有效并点燃；不能据此替代 POPCON 扫描结论。
- POPCON 方面，`best` 矩阵并非全 0，且 `n_invalid=0`，说明按当前准则至少有少量网格单元被标记为满足条件；但由于未提供 `nx`、`ny` 以及对应 `x/y` 扫描数组，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1.0`，因此结果依赖固定约束时间假设；`tauE_used=2.0` 是当前点成立的重要前提。
- `geom_model=0.0`，因此几何模型较简化。

2. 关键指标解读

- 运行点功率与状态：`Pfus=15896.150079484214`，`Pn=12716.920063587371`，`Pwall=19.5407079341625`。同时 `Pheat=-1524.916312624061`，说明当前输出里的加热/功率平衡定义需要结合模型符号解释，不能只看单一字段下结论。
- 约束时间与约束提升：`tauE_used=2.0`，`H98=2.641695757768491`，`H_ITPA20=3.710538230274128`，`tau_ITPA20=0.5390053614545951`。这些值应理解为在固定 `tauE` 假设下，与不同标度/参考的对应关系。
- L-H 阈值比较：`LH_ratio=8.51884776987883`，`P_LH=146.41437417477889`，`Ptrans=1247.2817649170197`。这只能表明当前输出中“传输功率与所选 L-H 阈值模型”的比较结果，不能写成保证进入或维持 H 模。
- 边界相关输出：`betaN=11.836743643134607`，`betaT=0.16750108928964066`，`betap=4.662864205664874`，`q=2.12`，`q95=3.866062097880496`，`nbar_o_nGw=2.230530594646195`。这些是后续扫描和验证时需要持续监控的边界指标。
- 成分与辐射：`Zeff=1.8672566371681416`，输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；辐射相关输出为 `Pbrem=335.7873399389619`、`Pcycl=71.24459841680014`、`P_line=0.0`。
- 几何：`Vp=888.3289066331332`，`Sw=735.4510294755139`，且 `Vp_geom=Vp`、`Sw_geom=Sw`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明当前结果直接依赖这套简化几何参数。
- POPCON 扫描元信息：扫描键为 `xkey=Ti0`、`ykey=ni0`。`best` 矩阵存在且非全零，但缺少 `x/y/nx/ny`，所以只能确认“有命中单元”，不能可靠描述最佳区位置、边界或趋势。

3. 风险与不确定性

- 最大不确定性来自 `use_tauE=1.0`。当前结论依赖固定 `tauE=2.0`，不是独立从约束标度自洽推出。
- `geom_model=0.0` 表明几何处理较简化，`Pwall`、体积/面积相关量及部分功率分配对几何细节的敏感性尚未展开验证。
- `Qfus=1000.0` 与 `Qfus_raw=-10.424277022868406` 同时出现，且 `Pheat` 为负值；这提示 `Qfus`/`Pheat` 的定义、截断或符号规则需要核对，否则容易误读当前点的功率平衡。
- 虽然 `best` 矩阵非全零，但因为缺少完整扫描网格坐标与尺寸信息，**不能把“当前点 valid/ignited”理解为“已经明确找到了工作窗”**。
- `P_line=0.0`、`P_ei=0.0` 说明当前输出里部分通道未体现贡献；这会限制对辐射构成和能量交换分项的细化判断。

4. 下一步建议

- 做一次可复现的 `Ti0`-`ni0` 二维扫描补全：明确输出 `x`、`y`、`nx`、`ny`、`best_fraction`。重点监控 `valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`、`LH_ratio`。
- 做约束时间敏感性验证：在当前点附近扫描 `tauE`，或切换到非固定 `tauE` 的标度模式做对照。重点监控 `tauE_used`、`H98`、`H_ITPA20`、`tau_ITPA20`、`valid`、`ignited`、`Pfus`、`Pheat`。
- 做功率平衡核对任务：逐项检查 `Pheat`、`Qfus`、`Qfus_raw`、`Pfus`、`Pn`、`Pth`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line` 的定义和符号，确认当前运行点的能量收支解释。
- 做几何模型对照：在可用时比较 `geom_model=0` 与更高几何保真设置，或至少扫描 `Vp_override`、`Sw_override` 的影响。重点监控 `Vp`、`Sw`、`Pwall`、`Pcycl`、`Pfus`、`betaN`。
