1. 核心结论
- 本次 0-D 运行的直接结果为：`config=tokamak`，`preset=ITER`，`valid=1`，`ignited=1`，`Pfus=37111.70598719656`，`Qfus=1000.0`，`Pheat=-593.4012569254155`，`Pwall=49.65429820162756`。
- 就当前运行点而言，`valid=1` 和 `ignited=1` 仅说明这一组输入参数下求解返回了有效点且点火标志为开；这只代表“当前点”的状态，不能直接等同于扫描中存在稳定或连续的工作窗。
- 就 POPCON 扫描而言，扫描键为 `xkey=Ti0`、`ykey=ni0`，`n_invalid=0`，且 `best` 矩阵并非全 0；仅从矩阵可确认有 2 个离散网格点被标记为满足当前 `best` 准则。
- 但本次扫描未提供 `x/y` 数组，也未显式给出 `nx/ny`；因此必须单独说明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 结果还带有两个明确前提：`use_tauE=1`，所以结论依赖固定约束时间假设；`geom_model=0`，所以几何模型较简化。

2. 关键指标解读
- 运行点功率量级上，`Pfus=37111.71`，`Pn=29689.36`，`Pth=4469.43`，`Ptrans=4469.43`，`Pwall=49.65`；同时 `Pheat=-593.40`，说明报告中的加热相关量需要结合定义一起看，不能只按字面理解为“外加热功率充足/不足”。
- 约束时间与约束因子方面，当前实际使用的是 `tauE_used=2.0`，且参数中 `tauE=2.0`、`use_tauE=1`；对应给出 `H98=6.2448`、`H_ITPA20=8.8558`、`HST=0.0991`、`tau_ITPA20=0.2258`。这些 `H` 因子应理解为在固定 `tauE` 假设下相对标度的等效比较结果。
- L-H 阈值比较方面，`LH_ratio=24.8363`，`P_LH=179.9554`，`Ptrans=4469.4263`。这只能表述为“当前功率量与所用 L-H 阈值模型的比较结果”，不得解读为保证进入或维持 H 模。
- 边界/平衡相关量给出为 `betaN=42.4150`、`betaT=0.6002`、`betap=16.7086`、`q=2.12`、`q95=3.8661`、`nbar_o_nGw=2.9740`；这些都是后续扫描中应继续联动监控的关键约束量。
- 成分与辐射方面，`Zeff=1.8673`，参数中 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；辐射功率给出 `Pbrem=1170.7685`、`Pcycl=1188.7451`、`P_line=0.0`。就这份数据本身看，线辐射项为 0，但这不等于真实物理上可以忽略，只能说明本次模型输出如此。
- 几何方面，`Vp=888.3289`、`Sw=735.4510`，且 `Vp_geom=Vp`、`Sw_geom=Sw`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`、`Sw_override=0.0`、`Vp_override=0.0`；结合 `geom_model=0`，可理解为当前结果建立在较简化的几何处理之上。
- POPCON 结果需要与运行点分开看：当前点 `valid=1` 不代表扫描平面上存在宽裕区域；扫描只证明 `best` 矩阵里有少数离散点被选中，而由于缺少 `x/y` 数组与显式网格定义，不能据此描述最佳区位置、边界或趋势。

3. 风险与不确定性
- 最直接的不确定性来自扫描证据不完整：虽然有 `best` 矩阵，但缺少 `x/y` 数组以及显式 `nx/ny`，所以扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 结果依赖固定约束时间假设：`use_tauE=1` 且 `tauE_used=2.0`，这意味着 `Pfus`、`ignited`、`H98/H_ITPA20` 等结果都受该固定约束时间设定影响，不能直接当作约束标度自洽预测。
- 几何模型较简化：`geom_model=0`，且几何修正比均为 1.0；因此 `Vp`、`Sw`、`Pwall` 等与几何密切相关的量，对更细几何模型可能敏感。
- 报告内部存在需要核对定义的信号：`Qfus=1000.0`，但同时 `Qfus_raw=-62.5407`，且 `Pheat=-593.4013`。这提示至少部分派生量可能带有截断、占位上限或特殊符号约定，使用前应先确认计算口径。
- 边界与成分量虽然已给出，但缺少跨点比较证据：如 `betaN`、`q95`、`nbar_o_nGw`、`Zeff`、`Pbrem/Pcycl/P_line` 目前只有单点值，没有足够扫描证据支持“哪个指标最敏感”的判断。
- `n_invalid=0` 只说明本次扫描网格没有无效点，不等于物理判据宽松，也不等于 `best` 区域连续。

4. 下一步建议
- 1. 重新执行 `Ti0-ni0` 的 POPCON 扫描，并强制保存 `x`、`y` 数组及显式 `nx/ny`；重点监控 `best` 矩阵、`n_invalid`、`valid`、`ignited`、`Pfus`、`Pwall`、`betaN`、`q95`、`nbar_o_nGw`，先把工作窗的离散坐标证据补齐。
- 2. 做固定约束时间敏感性扫描：在保持其余参数不变时扫描 `tauE`，并与关闭固定约束时间假设的方案对照；重点监控 `tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`ignited`。
- 3. 做成分/辐射扫描：围绕 `fHe`、`fimp`、`Zimp` 或输出 `Zeff` 相关设置逐项扫描；重点监控 `Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pwall`、`Zeff`、`nbar_o_nGw`。
- 4. 做几何模型交叉验证：对比 `geom_model=0` 与更高保真几何设置，或引入 `Sw/Vp` 覆盖值测试；重点监控 `Vp`、`Sw`、`Pwall`、`betaN`、`q95`、`Pfus`。
- 5. 单独核对功率与增益量的定义和后处理：重点确认 `Pheat`、`Qfus`、`Qfus_raw`、`Ptrans`、`P_LH` 的公式与符号约定，避免后续把裁剪值或符号约定误当作物理趋势。
