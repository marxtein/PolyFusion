1. 核心结论

本次 0-D 结果的 `config=tokamak`，`preset=ITER`。当前运行点给出的核心状态是：`valid=1.0`、`ignited=1.0`、`Pfus=4089.72`、`Qfus=1000.0`、`Pheat=-206.05`、`Pwall=5.281`。

对当前运行点，应单独理解为“这个单点在本次 0-D 求解中得到了有效解”；这不等于 POPCON 扫描已经证明存在连续工作窗。`use_tauE=1.0` 且 `tauE_used=2.0`，说明结果依赖固定约束时间假设。`geom_model=0.0`，说明几何模型较简化。

POPCON 部分给出了 `xkey=Ti0`、`ykey=ni0`、`best` 矩阵和 `n_invalid=0`。`best` 矩阵并非全 0，说明在当前判据下至少出现了非零最佳标记点；但由于缺少 `x/y` 数组且 `nx/ny` 未给出，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读

当前运行点的功率与点火相关输出为：`Pfus=4089.72`、`Pn=3271.78`、`Pth=454.74`、`Ptrans=454.74`、`Pwall=5.281`。`ignited=1.0` 是该单点输出的一部分，但仍应与扫描结果分开看待。

约束与约束时间方面：`tauE_used=2.0`，而 `tau_ITPA20=0.995`；同时给出了 `H98=1.433`、`H_ITPA20=2.010`。由于 `use_tauE=1.0`，这些结果不是由约束时间标度自洽闭合出来，而是建立在固定 `tauE` 输入上。

边界相关量包括：`betaN=4.315`、`betaT=0.0611`、`betap=1.700`、`q=2.12`、`q95=3.866`、`nbar_o_nGw=1.859`。这些量适合在后续扫描里作为约束观察指标，而不是单凭这一次输出直接下运行裕量结论。

成分与辐射方面：`Zeff=1.867`，输入成分有 `fHe=0.04`、`fimp=0.01`、`Zimp=10`。辐射项中 `Pbrem=149.52`、`Pcycl=7.629`、`P_line=0.0`，就当前已列项看，轫致辐射高于回旋辐射。

L-H 相关只能按阈值模型比较来读：`P_LH=128.47`，`LH_ratio=3.540`，对应的是当前功率穿越与阈值模型的相对关系，不应表述为保证进入或维持 H 模。

几何量方面：`Vp=888.33`、`Sw=735.45`，且 `Vp_geom`、`Sw_geom` 与其一致，`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`，说明这里没有额外几何覆盖修正被显式体现出来。

3. 风险与不确定性

`Qfus=1000.0` 与 `Qfus_raw=-19.848` 同时出现，且 `Pheat=-206.05` 为负值，这组增益/加热相关输出存在明显需要复核的定义、符号或截断问题。在未核清前，不宜把 `Qfus` 单独当作稳定结论使用。

`use_tauE=1.0` 带来直接模型依赖：结果对固定 `tauE=2.0` 假设敏感，不能替代基于约束时间标度的自洽比较。

`geom_model=0.0` 表明几何模型较简化，因此 `Vp`、`Sw` 以及由其影响的 `Pwall` 等量，需要警惕模型简化带来的偏差。

POPCON 扫描虽然 `best` 矩阵有非零元素、`n_invalid=0`，但因为缺少 `x/y` 数组且 `nx/ny` 未给出，扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能据此描述最佳区位置或边界。

4. 下一步建议

1. 先做单点复核任务：固定当前输入参数，重复导出 `Pfus`、`Qfus`、`Qfus_raw`、`Pheat`、`Pn`、`Pth`、`Ptrans`，核对这些量的定义、符号和任何显示截断规则。重点监控指标：`Qfus`、`Qfus_raw`、`Pheat`、`Pfus`、`Pn`。

2. 做一轮完整的 `Ti0`-`ni0` POPCON 重扫，并要求同时输出 `x`、`y` 数组以及 `nx`、`ny`。重点监控指标：`best`、`valid`、`ignited`、`n_invalid`、`Pfus`、`Pwall`、`Ptrans`、`LH_ratio`。

3. 做约束时间敏感性验证：在当前点附近扫描 `tauE`，并与 `tau_ITPA20` 对照；如果工具支持，再比较固定 `use_tauE=1` 与标度驱动结果。重点监控指标：`tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`valid`、`ignited`。

4. 做几何敏感性验证：在可用条件下切换或细化 `geom_model`，并检查 `Vp`、`Sw` 变化对壁负荷和功率收支的影响。重点监控指标：`Vp`、`Sw`、`Pwall`、`Pfus`、`betaN`。
