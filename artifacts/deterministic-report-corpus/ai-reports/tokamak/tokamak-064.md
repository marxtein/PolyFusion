## 1. 核心结论

- `config=tokamak`，`preset=ITER`。
- 当前运行点结果：`valid=1.0`，`ignited=0.0`。这表示该 0-D 运行点在本次模型求解中有效，但未达到点火状态。
- 主要输出：`Pfus=33772.38932403127`，`Qfus=4.752579396802933`，`Pheat=7106.117858178236`，`Pwall=55.582908370339716`。
- 必须把“当前运行点”和“POPCON 扫描”分开看：  
  当前运行点有效，只说明这一组输入参数下模型给出了有效解；这不等于 POPCON 扫描已经证明存在明确工作窗。
- POPCON 方面，`last_scan` 给出了 `xkey=Ti0`、`ykey=ni0` 的 `best` 矩阵，且矩阵并非全 0，说明扫描中存在被当前准则标记的点；但这不能直接推出当前运行点本身就在最佳区内。
- `use_tauE=1.0`，结果依赖固定约束时间假设。`geom_model=0.0`，几何模型较简化。
- 由于扫描 `x/y` 数组缺失，且未提供 `nx/ny`，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

## 2. 关键指标解读

- 运行点功率项：`Pfus=33772.39`，`Pheat=7106.12`，`Qfus=4.75`，`Pwall=55.58`。就本次输出看，聚变产出、加热功率和壁负荷都已给出，可作为后续扫描对比基线。
- 点火与功率平衡相关：`ignited=0.0`；`Ptrans=7275.81`，`P_LH=179.96`，`LH_ratio=40.43`。这里的 `LH_ratio` 只能理解为与 L-H 阈值模型的比较结果，不能写成保证进入或维持 H 模。
- 约束时间相关：`tauE_used=2.0`，`tau_ITPA20=0.143158...`，`H98=10.1775`，`H_ITPA20=13.9706`。因为 `use_tauE=1`，当前结果是建立在固定 `tauE=2.0` 的前提上，不是由约束时间标度自洽求出的单一结论。
- 辐射与损失：`Pbrem=1839.52`，`Pcycl=4745.26`，`P_line=0.0`。本次非零辐射项主要是轫致和回旋辐射，线辐射输出为 0。
- 边界/约束表征：`betaN=69.05`，`betaT=0.9771`，`betap=27.2000`，`q=2.12`，`q95=3.8661`，`nbar_o_nGw=2.9740`。这些值可用于后续参数扫描时观察边界量如何联动变化，但仅凭这份 0-D 报告本身不能下更强结论。
- 成分与杂质：`Zeff=1.8673`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`。
- 几何：`Vp=888.33`，`Sw=735.45`，`geom_model=0`，`geom_volume_ratio=1.0`，`geom_wall_ratio=1.0`，且 `Vp_override=0`、`Sw_override=0`，说明本次没有额外体积/壁面积覆盖值。
- POPCON 扫描：`n_invalid=0`，`best` 为一个 8x8 矩阵，其中有 2 个非零单元。就矩阵计数而言，当前准则下扫描中确实出现了被标记点；但由于缺少扫描轴数组和网格定义，不能据此描述最佳区的位置、边界或趋势。

## 3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1`。当前结果依赖固定 `tauE=2.0` 假设，`tauE_used` 与 `tau_ITPA20`、`H98`、`H_ITPA20` 的组合说明，约束时间设定对结论影响需要单独复核。
- `geom_model=0` 表明几何模型较简化；因此 `Vp`、`Sw`、`Pwall` 及相关派生量对几何假设的敏感性，当前还没有被展开验证。
- 运行点 `valid=1` 与扫描找到工作窗不是一回事。前者只说明单点求解有效，后者需要完整扫描网格证据支持。
- 虽然 `best` 矩阵不是全 0，但当前没有 `x/y` 数组，也没有 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `LH_ratio` 只能作为阈值模型比较量使用，不能据此把模式转换写成已保证结果。

## 4. 下一步建议

1. 重新导出完整的 `Ti0-ni0` POPCON 扫描结果，补齐 `x`、`y`、`nx`、`ny`。  
   重点监控：`best` 非零单元数、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 做一组约束时间敏感性扫描：保持其余参数不变，对 `tauE` 取多个值，或对比 `use_tauE=1` 与标度驱动方案。  
   重点监控：`tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`valid`、`ignited`。

3. 做几何模型/几何参数敏感性验证，至少比较 `geom_model` 与 `Vp/Sw` 覆盖值变化对结果的影响。  
   重点监控：`Vp`、`Sw`、`Pwall`、`Pfus`、`betaN`、`q95`。

4. 做成分与辐射扫描，围绕 `fimp`、`Zimp`、`fHe` 展开。  
   重点监控：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`Pwall`。

5. 在运行点附近补做 L-H 阈值比较扫描，而不是只看单点 `LH_ratio`。  
   重点监控：`Ptrans`、`P_LH`、`LH_ratio`，并把它们仅作为阈值模型比较结果记录。
