1. 核心结论
- `config=tokamak`（托卡马克），`preset=ITER`。
- 当前运行点：`valid=1.0`，`ignited=0.0`；这表示该单点计算在当前模型下有效，但未点火。
- 主要输出：`Pfus=579.8704060499463`，`Qfus=0.6579848221506472`，`Pheat=881.282343496342`，`Pwall=1.986743768090593`。
- 运行点还给出 `Ptrans=558.6782905357486`、`P_LH=40.518118381780944`、`LH_ratio=13.788357230007982`；这里只能解读为当前 L-H 阈值模型下的比值比较，不能据此表述为保证进入或维持 H 模。
- POPCON 扫描需要单独看：`last_scan` 以 `Ti0` 为横轴、`ni0` 为纵轴，提供了 `best` 矩阵且 `n_invalid=0`；这说明扫描里存在被当前准则标记为 `best=1` 的网格单元，但这与“当前运行点 `valid=1.0`”不是一回事，不能混同为已经证明存在完整工作窗。

2. 关键指标解读
- 运行点状态：`valid=1.0` 仅说明当前这一个参数点通过了模型内部有效性检查；`ignited=0.0` 则说明该点不在点火状态。按当前输出定义，`Qfus=0.6579848221506472`，`Pfus` 小于 `Pheat`。
- 约束与约束时间：`use_tauE=1.0`，`tauE_used=2.0`，说明结果依赖固定约束时间假设；同时给出了 `H98=3.8837852199594396`、`H_ITPA20=3.5043001598820878`、`HST=0.12541633173853056`、`tau_ITPA20=0.5707273660220065`，这些更适合作为与经验标度的对照量，而不是本次求解的独立约束来源。
- 边界与负荷相关量：当前点给出 `betaN=5.30187475682071`、`betaT=0.07502652957765156`、`betap=2.0885745921207244`、`q=2.12`、`q95=3.866062097880496`、`nbar_o_nGw=0.3717550991076991`、`Pwall=1.986743768090593`；这些量描述了该运行点的压力、磁约束和平均壁负荷状态，但仅凭这一组数值不能外推更广参数区的变化趋势。
- 成分与辐射/损失：`Zeff=1.8672566371681416`，`fHe=0.04`，`fimp=0.01`，`Zimp=10`；损失项中 `Pbrem=18.2932573517649`、`Pcycl=420.28487681881774`、`P_line=0.0`，按当前输出，`Pcycl` 明显大于 `Pbrem` 和 `P_line`。
- 几何：`Vp=888.3289066331332`，`Sw=735.4510294755139`，且 `Vp_geom=Vp`、`Sw_geom=Sw`、`geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`、`Vp_override=0.0`、`Sw_override=0.0`；同时 `geom_model=0.0`，说明几何模型较简化。
- POPCON 扫描：已给出 `best` 矩阵，矩阵中共有 2 个单元为 `1`，其余为 `0`，并且从矩阵索引上看，这 2 个 `1` 位于第一列的前两行；但由于未提供 `x/y` 数组以及明确的 `nx/ny`，这些只能视为索引层面的证据，不能可靠映射为实际 `Ti0`/`ni0` 数值位置、边界或趋势。

3. 风险与不确定性
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；原因是虽然提供了 `best` 矩阵，但缺少 `x/y` 扫描数组，且未显式给出 `nx/ny`。
- 必须区分单点与扫描：当前运行点 `valid=1.0` 只说明这一个点有效，不等于 POPCON 已经证明存在可用工作窗；反过来，扫描中存在 `best=1` 的网格单元，也不等于当前运行点本身就是最佳点。
- `use_tauE=1.0` 带来明显模型依赖性：本次结果直接依赖固定 `tauE=2.0` 的设定，因此 `Pfus`、`Qfus`、`ignited` 以及相关约束量对 `tauE` 假设的敏感性需要单独验证。
- `geom_model=0.0` 表明几何模型较简化；因此 `Vp`、`Sw`、`Pwall` 以及与平均量相关的状态量，可能对更详细几何处理敏感。
- `LH_ratio=13.788357230007982` 只能视为 `Ptrans` 与 `P_LH` 的阈值模型比较，不能据此写成保证进入或维持 H 模。
- `P_line=0.0` 是本次模型输出的一部分，但这不自动说明所有杂质线辐射影响都可忽略；若成分、辐射模型或扫描变量变化，损失分配可能改变。

4. 下一步建议
- 重新执行 `Ti0`–`ni0` 的 POPCON 扫描，并务必导出完整 `x/y` 数组和明确的 `nx/ny`；建议在当前 `Ti0=122.85714285714286`、`ni0=5e19` 附近加密网格，重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`。
- 做约束时间敏感性验证：在其余参数不变时扫描 `tauE`，或对比 `use_tauE=1` 与经验标度驱动的求解结果；重点监控 `tauE_used`、`H98`、`H_ITPA20`、`Pfus`、`Qfus`、`ignited`。
- 做几何模型对比：若工具支持，比较 `geom_model=0` 与更详细几何模型，或检查 `Vp_override`、`Sw_override` 的影响；重点监控 `Vp`、`Sw`、`Pwall`、`betaN`、`nbar_o_nGw`。
- 做成分/辐射敏感性扫描：围绕当前设定扫描 `fimp`、`fHe`、`Zimp`，必要时联动 `Ti0`、`ni0`；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`LH_ratio`。
