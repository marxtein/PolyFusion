1. 核心结论
- `config=tokamak`，`preset=ITER`；本次运行点结果为 `valid=1.0`、`ignited=0.0`、`Pfus=27758.17925410664`、`Qfus=13.033813218400464`、`Pheat=2129.7051591102345`、`Pwall=40.638850467761785`。
- 当前运行点可解且被模型判为有效，但未被标记为点火；因此“运行点有效”和“点火成立”在这组结果里不是一回事。
- POPCON 扫描需要单独看：扫描轴是 `Ti0` 与 `ni0`，`best` 矩阵非全 0，且 `n_invalid=0`；这只说明扫描中存在满足当前 `best` 判据的网格点，不等同于当前运行点本身就是最佳点，也不等同于已经完整确定工作窗。
- `use_tauE=1.0` 且 `tauE_used=2.0`，说明结果依赖固定约束时间假设；`geom_model=0.0`，说明几何模型较简化。

2. 关键指标解读
- 运行点：`ignited=0.0`，但 `Pfus` 和 `Qfus` 数值较高；在当前模型输出里，应理解为“有聚变输出与增益结果”，但尚未被标记为点火状态。
- 约束与约束时间：`tauE_used=2.0`，对比 `tau_ITPA20=0.20521237775314488`；同时给出 `H98=7.153850305981627`、`H_ITPA20=9.74600081095425`。由于 `use_tauE=1.0`，这些更适合解读为与缩放关系的比较指标，而不是自洽闭合后的独立预测。
- L-H 阈值比较：`P_LH=163.52515282240506`，`LH_ratio=28.920790534977947`。这里只能表述为当前功率与所用 L-H 阈值模型之间的比较结果，不能据此写成保证进入或维持 H 模。
- 边界相关指标：`betaN=44.88098631355205`、`betaT=0.6351082968898876`、`betap=17.680026779812643`、`q=2.12`、`q95=3.866062097880496`、`nbar_o_nGw=2.602285693753894`。这些是当前 0-D 结果里与压力、磁安全因子、平均密度相关的边界指标。
- 成分与辐射：`Zeff=1.8672566371681416`，输入成分为 `fHe=0.04`、`fimp=0.01`、`Zimp=10`；辐射/损失项中 `Pbrem=1056.162690478923`、`Pcycl=1895.9016274756043`、`P_line=0.0`，说明本次结果里线辐射项未体现。
- 几何与 POPCON：`Vp=888.3289066331332`、`Sw=735.4510294755139`，且 `geom_volume_ratio=1.0`、`geom_wall_ratio=1.0`、`Sw_override=0.0`、`Vp_override=0.0`；扫描维度键为 `xkey=Ti0`、`ykey=ni0`，`best` 矩阵给出了最佳判据的网格标记，但未给出完整坐标数组。

3. 风险与不确定性
- `use_tauE=1.0` 使结果直接依赖固定 `tauE=2.0` 假设；若改用约束时间缩放或改变 `tauE`，`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat` 可能明显变化。
- `geom_model=0.0` 表明几何模型较简化，因此 `Vp`、`Sw`、`Pwall` 等几何相关结果带有模型简化不确定性。
- `last_scan` 缺少 `x`/`y` 数组且未给出 `nx`/`ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 虽然 `best` 矩阵非全 0 且 `n_invalid=0`，但在缺少 `x`/`y` 数组的情况下，不能确认最佳区相对当前运行点的准确位置，也不能判断其边界是否连续。
- `P_line=0.0` 使当前结果未体现线辐射通道；结合 `fimp`、`Zimp`、`Zeff` 来看，杂质辐射敏感性仍需进一步验证。
- `betaN`、`q/q95`、`nbar_o_nGw` 这些边界指标虽然已给出，但仅凭 0-D 数据不能单独判断稳定性裕度、控制难度或更高维物理响应。

4. 下一步建议
1. 重新执行 `Ti0`-`ni0` 的 POPCON 扫描，并导出完整 `x`/`y` 数组与明确的 `nx`/`ny`；重点监控 `best`、`valid`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`LH_ratio`、`betaN`、`q95`、`nbar_o_nGw`。
2. 做约束时间敏感性扫描：围绕当前 `tauE=2.0` 进行多点扫描，或在工具支持时对比关闭 `use_tauE` 的缩放闭合结果；重点监控 `tauE_used`、`tau_ITPA20`、`H98`、`H_ITPA20`、`valid`、`ignited`、`Pfus`、`Qfus`。
3. 做几何模型验证：对比更详细的几何模型，或扫描 `Sw_override`、`Vp_override`；重点监控 `Sw`、`Vp`、`Pwall`、`nbar_geom`、`betaT`、`betap`、`valid`。
4. 做成分/辐射扫描：扫描 `fimp`、`Zimp`、`fHe`；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Pheat`、`LH_ratio`、`valid`。
5. 做边界相关参数扫描：围绕当前点扫描 `ni0`，并结合 `Ip`、`BT0` 或 `H_fac` 做交叉检查；重点监控 `betaN`、`q`、`q95`、`nbar_o_nGw`、`Pfus`、`Qfus`、`Pwall`、`ignited`。
