1. 核心结论
- 本次结果对应 `config=frc`、`preset=C-2W`，当前运行点 `valid=1.0`、`ignited=0.0`，`Pfus=1.3442823440549443`、`Qfus=0.003390527459518115`、`Pheat=396.4817746221713`、`Pwall=29.31295530413558`。
- 就当前运行点而言，这是一个“数值上有效但未点火”的解：`valid=1.0` 说明该点被模型接受，`ignited=0.0` 说明不能把它解读为点火状态。
- 本次约束时间采用固定输入：`use_tauE=1.0`，`tau_E=0.0015`；因此本结果依赖固定约束时间假设，不应把结论外推为对其他约束时间模型同样成立。
- POPCON 扫描需要与当前运行点分开看：当前运行点有效，不等于扫描中已经确认了明确工作窗。
- 扫描结果里 `best` 矩阵并非全零，且 `n_invalid=0`；但缺少 `x/y` 扫描数组与显式 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 功率平衡上，外加/加热功率很高：`Pheat=396.4817746221713`，而聚变功率 `Pfus=1.3442823440549443`、增益 `Qfus=0.003390527459518115` 很低，说明当前点主要不是由聚变功率主导。
- 输运与损失方面，`Ptrans=396.6666666666668` 明显高于辐射相关损失项；辐射中 `Pbrem=0.0839643103297082`、`Pcycl=1.1398579112255588e-07`、`P_line=0.0`，至少在该点上，主导项更接近输运功率而非线辐射。
- 中子与壁负荷方面，`Pn=1.0754258752439554`、`Pwall=29.31295530413558`；壁负荷字段已给出，可直接作为后续扫参与校核的监控量之一。
- 约束与时间尺度方面，`tau_E=0.0015`、`tau_Bohm=0.0006400000000000002`、`tau_classical=1.798115523221014`、`tau_eq_ie=0.0034591836994968305`。其中真正用于本次结果的是固定给定的 `tau_E`，不是由缩放律自动求出的约束时间。
- 等离子体与成分方面，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；在这组输入下杂质份额取零，`P_line=0.0` 与此一致，但这也意味着线辐射结论依赖当前成分设定。
- 几何与构型方面，标签为“场反位形 FRC”，并给出了 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`sep_model=superellipse`、`elongation=3.75`、`f_shape_calc=0.850000000000001`；这些量说明结果受当前简化几何参数化输入约束。

3. 风险与不确定性
- 最核心的不确定性来自约束时间处理：`use_tauE=1.0` 表明结果依赖固定约束时间假设；若改用其他 `tauE` 模型或扫描 `tauE`，`Pfus`、`Qfus`、`Pheat` 与有效性判断都可能变化。
- 当前运行点“有效”不代表扫描已清楚显示可用区域。这里必须区分：`valid=1.0` 只说明当前单点成立；POPCON 证据则还不足以支持对工作窗范围、形状或敏感性的判断。
- 扫描方面虽然 `best` 矩阵里存在一个命中单元、且 `n_invalid=0`，但缺少 `x/y` 数组与显式 `nx/ny`，因此无法把这个命中单元可靠映射到具体参数坐标，也不能描述边界、斜率或趋势。
- 成分相关结论有条件性：`fimp=0.0`、`fHe=0.0` 导致当前辐射结构较“干净”，因此 `P_line=0.0` 不能直接代表含杂质或含氦积累情形。
- 本次 JSON 未提供诸如 `betaN`、`q95`、`nbar_o_nGw`、`LH_ratio` 等更多边界比较量，因此不能补充这些约束上的余量或风险判断。

4. 下一步建议
- 先补齐一次可追溯的 POPCON 扫描输出：围绕当前 `xkey=Ti`、`ykey=B_e` 重新导出包含 `x`、`y` 数组以及显式 `nx/ny` 的网格结果；监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做 `tauE` 敏感性验证：在保持其余输入不变的条件下，对 `tauE` 进行扫描，专门检查固定约束时间假设对 `Pfus`、`Qfus`、`Ptrans`、`Pwall`、`valid`、`ignited` 的影响。
- 做成分与辐射验证：扫描 `fimp`、`fHe`，必要时联动 `Zimp`；重点监控 `P_line`、`Pbrem`、`Pcycl`、`Pheat`、`Pfus`、`Qfus`、`Pwall`，确认当前“低线辐射”结论是否仅由零杂质设定造成。
- 做当前运行点邻域复核：以 `Ti=5.0`、`B_e=1.0` 为中心做局部细网格，检查单点 `valid=1.0` 是否只是在孤立网格命中；监控 `best` 命中数量、`n_invalid`、以及 `Pfus/Qfus/Pwall` 的局部变化。
- 做几何输入一致性复核：针对 `Rw`、`r_w`、`r_s`、`l_s`、`f_shape` 的输入组合重复计算，监控 `Vp`、`Sw`、`Sp`、`Pwall`、`Pfus`、`Pheat`，确认结果是否对当前几何设定敏感。
