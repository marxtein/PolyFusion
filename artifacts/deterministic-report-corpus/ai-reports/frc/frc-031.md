# 1. 核心结论

本次 `config=frc`，`preset=C-2W`。当前运行点 `valid=1.0`、`ignited=0.0`，对应 `Pfus=8058.84`、`Qfus=0.1940`、`Pheat=41535.45`、`Pwall=3654.25`。按这组 0-D 输出，当前点被模型判为有效，但未达到点火状态。

当前运行点与 POPCON 扫描需要分开看：  
当前运行点说明的是这一个参数点在模型下可计算且有效；POPCON 扫描说明的是参数平面上是否出现满足判据的离散区域。两者不能等同。

就 POPCON 而言，`best` 矩阵不是全 0，说明当前判据下至少出现了一个离散“最佳”网格点；但由于未提供 `nx/ny` 以及 `x/y` 坐标数组，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

另外，`use_tauE=1.0`，说明结果依赖固定约束时间假设；这会直接影响对功率平衡和可达性的解读。

# 2. 关键指标解读

当前运行点方面：

- 功率指标：`Pfus=8058.84`，但 `Qfus=0.1940` 明显低于 1，表示聚变输出相对加热功率不高；同时 `Pheat=41535.45`，表明该点对外加热依赖较强。
- 点火状态：`ignited=0.0`，表示按本模型结果，该点不是自持点火状态。
- 壁负荷：`Pwall=3654.25`，可作为后续壁面热负荷核查的直接监控量。
- 辐射与损失：`Pbrem=7.699`，`Pcycl=0.000124`，`P_line=0.0`。在已给数据里，线辐射未体现，制动辐射有非零贡献，回旋辐射很小。
- 传输相关：`Ptrans=43139.52`，与 `tau_E=0.0015` 一起看，说明该解对能量约束时间设定比较敏感。由于 `use_tauE=1.0`，这里不是由标度律自由求出的约束时间，而是固定输入的 `tauE=0.0015`。
- 成分与杂质：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。从输入上看，当前点采用了很理想化的低杂质设定，辐射结论因此要按该前提理解。
- 几何量：`Vp=1.2818`、`Sw=13.5717`。这些量决定体积与受热面积的换算关系，也会影响 `Pwall` 的解读。

POPCON 扫描方面：

- 扫描轴为 `xkey=Ti`、`ykey=B_e`。
- `n_invalid=0`，说明扫描中没有被标记为无效的网格点。
- `best` 矩阵中仅见单个 1，其余为 0，说明在离散网格上只出现了非常有限的“最佳”判据命中点。
- 但因为缺少 `x/y` 数组以及 `nx/ny`，目前只能确认“有离散命中点”，不能可靠说明该点对应的精确参数位置、区域边界、面积占比或趋势方向。

# 3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1.0`。结果依赖固定约束时间假设，若 `tauE` 改变，`Ptrans`、`Pheat`、`Qfus`、是否有效等结论都可能变化。
- 当前点 `valid=1.0` 只说明该单点在模型内成立，不代表扫描平面上存在连续可用区域。
- POPCON 里虽有一个 `best=1` 的离散点，但没有 `x/y` 数组、`nx/ny` 和最佳区占比，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `ignited=0.0` 且 `Qfus=0.1940`，说明当前解不是点火解，对外部加热与模型设定更敏感。
- 杂质和辐射前提偏理想化：`Zeff=1.0`、`fimp=0.0`、`fHe=0.0`。因此辐射损失和功率平衡结论仅适用于这组输入，不宜外推到更高杂质或灰分条件。
- `geom_model` 字段未提供，因此无法判断是否采用了更简化或更完整的几何模型，只能依据现有 `Vp`、`Sw`、形状参数做有限解读。

# 4. 下一步建议

1. 先做 `Ti-B_e` 加密扫描：围绕当前点 `Ti=67.14`、`B_e=10.43` 提高网格分辨率，并输出完整 `x/y` 数组、`nx/ny`、`best_fraction`。  
需要监控：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best` 分布。

2. 做 `tauE` 敏感性扫描：在当前其余参数不变时，对固定 `tauE` 取多组值，检验 `use_tauE=1` 假设对结论的影响。  
需要监控：`Qfus`、`Ptrans`、`Pheat`、`valid`、`ignited`。

3. 做成分/辐射验证扫描：改变 `fimp`、`fHe`、必要时联动 `Zeff`，检查理想化低杂质设定是否显著改变功率平衡。  
需要监控：`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

4. 做壁负荷与几何一致性复核：结合 `Sw`、`Vp`、当前 `Pwall`，检查几何输入变化对壁负荷与功率密度的影响。  
需要监控：`Pwall`、`Sw`、`Vp`、`Pfus`、`Pheat`。
