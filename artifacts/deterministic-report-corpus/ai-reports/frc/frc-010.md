1. 核心结论
- 本次结果对应 `config=frc`（场反位形 FRC），`preset=C-2W`；当前运行点输出为 `valid=1.0`、`ignited=0.0`、`Pfus=127.02401371406664`、`Qfus=0.04889982238444877`、`Pheat=2597.6375275028217`、`Pwall=200.76081135974525`。
- 当前运行点位于参数 `Ti=25.714285714285715`、`B_e=2.571428571428571`；`valid=1.0` 说明该 0-D 点在模型内被接受，但 `ignited=0.0` 表明它不是点火态。
- 从功率量级看，当前点 `Pfus` 明显小于 `Pheat`，并反映在较低的 `Qfus=0.04889982238444877`；就这组数据而言，结果更依赖外加加热而非自持燃烧。
- POPCON 扫描与当前运行点要分开看：扫描轴为 `xkey=Ti`、`ykey=B_e`，且 `best` 矩阵并非全 0，说明在当前“best”判据下至少存在一个被标记的网格点；这不等同于“当前运行点有效”或“当前运行点就在最佳区”。
- 由于未提供 `nx`、`ny`，也未提供 `x/y` 数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0` 且 `tauE=0.0015`，说明结果依赖固定约束时间假设。

2. 关键指标解读
- 运行点功率平衡：`Pheat=2597.6375275028217`、`Pfus=127.02401371406664`、`Qfus=0.04889982238444877`、`Ptrans=2622.8571428571427`、`P_ei=1276.6010902301193`；从这些数值关系看，当前点的聚变输出对总加热输入贡献有限。
- 点火与中子相关输出：`ignited=0.0`、`Pn=101.61921097125332`；因此这组结果不能按点火态来解读。
- 约束时间：`tau_E=0.0015`，且 `use_tauE=1.0`，表示这里没有展示基于约束时间标度律的自洽变化；JSON 中也未提供 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling`，因此不能比较不同约束标度下的结果。
- 边界与等离子体状态：提供了 `beta=0.7777777777777777`、`beta_null=1.0`、`nbar=4.347705870363372e+20`、`ne0=6.14679566858152e+20`、`ni0=6.14679566858152e+20`、`ntau=7.171261613345107e+17`；但未提供 `q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能对安全因子或密度极限相对位置做判断。
- 成分与辐射：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`，辐射输出为 `Pbrem=0.18518591561982414`、`Pcycl=1.4728726153796224e-06`、`P_line=0.0`；这表明当前结果对应一个无 He 灰分、无杂质分数的设定，线辐射在本次输入下未体现。
- 几何与形状：`Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`elongation=3.75`、`sep_model=superellipse`、`f_shape=0.85`、`geom_weighted=0.0`；JSON 未提供 `geom_model`，因此不能据此判断几何模型是否为某一特定简化等级。
- POPCON 扫描本身：`n_invalid=0` 说明扫描结果里没有被标记为 invalid 的网格点；但因为缺少网格坐标与尺寸信息，不能据此判断最佳区范围、边界连续性或对 `Ti/B_e` 的单调敏感性。

3. 风险与不确定性
- 最大的不确定性来自固定约束时间设定：`use_tauE=1.0` 会使 `Pfus`、`Qfus`、`ignited`、`Ptrans` 等结果对 `tau_E=0.0015` 的取值敏感，当前报告不能说明换用其他约束假设后是否仍保持相同结论。
- 扫描证据不完整：虽然 `best` 矩阵非全 0，但缺少 `nx/ny` 与 `x/y` 数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能确认那个被标记点是孤立点、窄带还是更大区域的一部分。
- `valid=1.0` 只代表当前 0-D 运行点在模型中被接受，不能替代对扫描连续性、参数裕量或最佳区归属的判断；尤其不能把“当前点有效”和“扫描存在 best 点”视为同一结论。
- 成分设定偏理想化：当前输入为 `fHe=0.0`、`fimp=0.0`，对应 `P_line=0.0`；因此这组结果没有覆盖 He 灰分和杂质线辐射对功率平衡的影响。
- 边界判读信息不全：虽然给出了 `beta`、密度与 `ntau`，但没有 `q/q95`、密度极限比值、L-H 相关指标或更多约束量，无法判断当前点距离哪些运行边界更近。
- 几何层面的不确定性仍在：JSON 只给出 `Vp/Sw/Sp`、形状因子和 `geom_weighted=0.0`，未给出 `geom_model` 或更详细几何修正来源，因此几何相关结论仍需补充验证。

4. 下一步建议
- 先补全 `Ti-B_e` POPCON 扫描输出：重新导出 `x`、`y`、`nx`、`ny` 以及完整 `best`，并在当前点 `Ti=25.714285714285715`、`B_e=2.571428571428571` 附近加密扫描；重点监控 `best`、`valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 针对固定约束时间做敏感性扫描：在保持其他参数不变的条件下，对 `tauE` 做多点扫描；若工具支持，再增加一组 `use_tauE=0` 的对照运行；重点监控 `tau_E`、`Pfus`、`Qfus`、`ignited`、`Ptrans`、`ntau`、`valid`。
- 做成分与辐射验证扫描：对 `fHe`、`fimp`、`Zimp` 分别做参数扫描，检查当前 `P_line=0.0` 是否只是理想化输入的结果；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做形状/几何相关扫描：围绕 `f_shape=0.85`、`l_s=3.0`、`r_s=0.4`、`Rw=0.8` 或 `r_w=0.6` 做对照扫描，检查几何量变化对壁相关输出和功率平衡的影响；重点监控 `Vp`、`Sw`、`Sp`、`B_int`、`flux_p`、`beta`、`Pwall`、`valid`。
