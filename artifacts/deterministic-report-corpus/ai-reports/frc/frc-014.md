1. 核心结论
- 本次报告的 `config=frc`，`preset=C-2W`。当前运行点 `valid=1.0`、`ignited=0.0`，对应 `Pfus=17879.79639829731`、`Qfus=0.6485659076302617`、`Pheat=27568.202688338544`、`Pwall=3348.737827904649`。
- 就“当前运行点”本身看：该点被标记为有效，但未点火；`Qfus` 小于 1，说明聚变输出相对加热输入未达到更高增益状态。
- 就“POPCON 扫描”单独看：`xkey=Ti`、`ykey=B_e`，`n_invalid=0`，`best` 矩阵中存在 1 个取值为 1 的网格点，说明在这次扫描准则下至少出现了候选点；这不等同于“当前运行点有效”，两者是不同证据。
- 由于 JSON 未给出 `nx`、`ny`，且未提供独立的 `x/y` 扫描数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 本次结果采用 `use_tauE=1.0` 且 `tauE=0.0015`，因此结论依赖固定约束时间假设。

2. 关键指标解读
- 运行点能量与损失：`Pheat=27568.202688338544`，`Pfus=17879.79639829731`，`Qfus=0.6485659076302617`；损失相关字段中，`Ptrans=31118.09523809524` 明显高于 `Pbrem=26.066618195265313`、`Pcycl=0.00011170750012174558`、`P_line=0.0`，当前功率平衡更受输运/传输项影响。
- 点火与中子相关：`ignited=0.0`，`Pn=14303.837118637848`；这表明当前点不是自持点火状态，同时存在较高的中子功率输出字段，需要与壁负荷一起跟踪。
- 壁负荷与几何相关输出：`Pwall=3348.737827904649`，几何量给出 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`；这些字段可作为后续按体积、表面积归一化复核的基础。
- 约束时间假设：`use_tauE=1.0`、`tau_E=0.0015`、`tauE=0.0015`，且同时给出 `tau_Bohm=0.00566857142857143`、`tau_classical=1.7981155232210142`；本次主结果并非来自这些替代约束时间字段的自洽扫描，而是依赖固定 `tauE` 输入。
- 成分与辐射：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；在该输入下，线辐射 `P_line=0.0`，轫致辐射 `Pbrem=26.066618195265313`，回旋辐射 `Pcycl=0.00011170750012174558`，说明当前辐射项以轫致为主、线辐射未体现。
- 边界/状态量：`beta=0.7777777777777777`、`nbar=5.158204125208891e+21`、`B_int=3.7025083008706448`、`Ecrit=33.02535887072036`、`Eth=46.67714285714286`；这些是当前点状态的直接输出，但 JSON 未提供对应判据阈值，因此只能作为监控量，不能单独据此下更强结论。
- POPCON 扫描证据：`best` 矩阵不是全 0，因此不能写“当前准则下未找到最佳区”；但由于缺少 `nx/ny` 和 `x/y` 数组，只能确认“至少有候选网格点”，不能可靠描述最佳区的位置、边界或趋势。

3. 风险与不确定性
- 最大不确定性来自固定约束时间：`use_tauE=1.0` 使结果直接依赖 `tauE=0.0015` 假设；如果 `tauE` 变化，`valid`、`Pfus`、`Qfus`、`Pwall` 都可能随之改变。
- 当前运行点“有效”不代表扫描中存在可观工作窗；虽然 `best` 矩阵里有 1 个候选点，但缺少 `nx/ny` 与 `x/y` 数组，无法判断这是一处孤立点还是连续区域。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；因此不能从本次 0-D 结果推出参数裕度、边界斜率或趋势稳定性。
- `ignited=0.0` 且 `Qfus=0.6485659076302617`，说明当前点对外加热仍有依赖；若后续目标包含更高增益或更少外加热，本点与该目标之间仍有距离。
- 成分设定较理想化：`fHe=0.0`、`fimp=0.0`、`Zeff=1.0`，使辐射与稀释效应体现有限；这会限制本次结果对杂质、灰分积累等情形的代表性。
- `P_line=0.0` 是当前输入/模型下的输出结果，但不应外推为线辐射在更广参数空间都可忽略。

4. 下一步建议
- 扫描 `tauE`：在保持 `config=frc` 与当前 `preset=C-2W` 的前提下，对 `tauE` 做一维或二维扫描，验证固定约束时间假设对结论的影响；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`。
- 补全 POPCON 网格定义后重扫：明确输出 `nx`、`ny` 以及对应 `x/y` 数组，并继续围绕 `Ti` 与 `B_e` 扫描；重点监控 `n_invalid`、`best` 矩阵占比、候选点是否连续，以及 `Pfus`、`Qfus`、`Pwall` 在候选点附近的变化。
- 做成分敏感性验证：对 `fHe`、`fimp`、`Zimp` 做参数扫描，检查理想化成分设定被放宽后结果是否明显变化；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Qfus`、`valid`。
- 做功率损失分解复核：围绕当前点分别扫描 `Te`、`Ti`、`B_e`，识别 `Ptrans`、`Pbrem`、`Pcycl` 对总功率平衡的主导区间；重点监控 `Pheat`、`Ptrans`、`Pbrem`、`Pcycl`、`Pn`、`Pwall`。
- 做壁负荷相关验证：结合 `Sw`、`Vp` 与 `Pwall`，对几何输入 `r_s`、`r_w`、`l_s`、`f_shape` 做扫描，观察壁负荷与聚变输出是否同步变化；重点监控 `Pwall`、`Pfus`、`Pn`、`Sw`、`Vp`。
