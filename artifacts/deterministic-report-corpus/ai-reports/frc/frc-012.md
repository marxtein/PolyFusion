# 1. 核心结论

- `config=frc`（`config_label=场反位形 FRC`），`preset=C-2W`。
- 当前运行点结果：`valid=1.0`，`ignited=0.0`，`Pfus=3097.6744694788413`，`Qfus=0.25108077710559`，`Pheat=12337.362123808223`，`Pwall=1137.2973938082987`。
- 就当前运行点而言，这是一个模型内有效解，但不是点火解。
- POPCON 扫描需要单独看：扫描轴为 `xkey=Ti`、`ykey=B_e`；`best` 矩阵不是全 0，说明当前判据下至少存在被标记的扫描点。但这不等于“当前运行点有效”就代表“扫描已经确认了工作窗”。

# 2. 关键指标解读

- 约束与能量学：`use_tauE=1.0`，`tau_E=0.0015`，结果依赖固定约束时间假设。当前还有 `Ptrans=12952.380952380954`、`Pn=2478.139575583073`。
- 辐射与成分：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。辐射项为 `Pbrem=4.516041228345052`、`Pcycl=2.409469256652393e-05`、`P_line=0.0`。
- 边界相关：已给出 `beta=0.7777777777777777`、`beta_null=1.0`、`nbar=2.147015244623888e+21`。但未见 `q/q95`、`nbar_o_nGw`、`nbar_o_Sudo` 等字段，边界判读不完整。
- 几何相关：`Vp=1.2817698026646358`，`Sw=13.571680263507906`，`Sp=6.974335690969341`，`sep_model=superellipse`，`f_shape_calc=0.850000000000001`，`elongation=3.75`。JSON 中记录了 `geom_weighted=0.0`，但未给出 `geom_model` 字段。
- POPCON 信息：`n_invalid=0`，且 `best` 矩阵存在非零元素，说明扫描结果里不是所有点都被排除；但现有数据不足以进一步描述最佳区的参数位置、边界或趋势。

# 3. 风险与不确定性

- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是未给出 `x/y` 数组，也未给出 `nx/ny`。
- `use_tauE=1` 使结果直接依赖固定 `tau_E=0.0015` 的假设；因此当前 `valid`、`Pfus`、`Qfus`、`Pwall` 的解释都带有这一前提。
- `valid=1.0` 只说明当前运行点通过了模型内部有效性检查；它不能替代 POPCON 对参数空间的扫描证据。
- 未见 `LH_ratio` 或 L-H 阈值比较结果，不能做 H 模相关判断；即便后续补充该类量，也只能表述为阈值模型比较。
- 未见 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling` 相关输出，当前无法比较不同约束或经验标度下的一致性。
- `geom_model` 未提供，因此不能判断几何描述是否属于更简化或更细化的模型层级。

# 4. 下一步建议

- 重新导出并执行完整的 `Ti-B_e` POPCON 扫描，补齐 `x`、`y`、`nx`、`ny`，并保留 `best` 与 `n_invalid`。需要监控：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕当前点做 `tauE` 敏感性扫描，因为 `use_tauE=1`。需要监控：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`。
- 做成分与辐射敏感性扫描，优先扫 `fimp`、`fHe`，必要时联动 `Zimp`。需要监控：`Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。
- 做几何/形状参数验证扫描，例如 `f_shape`、`Rw`、`l_s`、`r_s`。需要监控：`Vp`、`Sw`、`Sp`、`beta`、`Pfus`、`Pwall`。
- 若目标包含 L-H 阈值比较，应在后续计算中显式输出 `LH_ratio` 或对应阈值比较量，并监控其与 `Pheat`、`Ptrans` 的对应关系。
