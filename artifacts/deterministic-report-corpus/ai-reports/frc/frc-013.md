1. 核心结论
- 当前运行点来自 `config=frc`（场反位形 FRC）、`preset=C-2W`；本次求解 `valid=1.0`，但 `ignited=0.0`，说明该点在模型中可解/有效，不等于实现点火自持。
- 该运行点的主要输出为：`Pfus=8186.090`，`Qfus=0.4213`，`Pheat=19430.431`，`Pwall=2034.864`。
- 当前点参数为 `B_e=7.2857`、`Ti=25.7143`、`Te=1.0`；从功率项看，`Ptrans=21055.714` 高于 `Pheat=19430.431`，与 `ignited=0.0` 相一致。
- POPCON 扫描与当前运行点需分开看：当前点 `valid=1.0` 只说明这个单点可解；扫描侧给出 `xkey=Ti`、`ykey=B_e`、`n_invalid=0`，且 `best` 矩阵中至少有 1 个格点被标记为最佳，但这不等于已经证明存在清晰、可描述的工作窗。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点状态：`valid=1.0` 表示该组输入在 0-D 模型中得到有效输出；`ignited=0.0` 表示该点并非点火自持状态，不能把“有效”理解成“已点火”。
- 功率平衡：`Pfus=8186.090`、`Qfus=0.4213`、`Pheat=19430.431`；在当前点上，聚变功率低于加热功率，且 `Ptrans=21055.714` 为主要损失通道之一。
- 辐射与损失：`Pbrem=11.934`，`Pcycl=5.639e-05`，`P_line=0.0`；在本组输入下，线辐射项为零，当前辐射损失主要体现为轫致辐射，回旋辐射很小。
- 成分与辐射假设：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`，对应的是非常干净的成分设定；这与 `P_line=0.0` 是一致的。
- 约束时间：`use_tauE=1.0`，`tauE=0.0015`，输出中 `tau_E=0.0015`；结果依赖固定约束时间假设，而不是由约束时间标度自行决定。
- 几何相关：已给出 `Vp=1.2818`、`Sw=13.5717`、`Sp=6.9743`、`elongation=3.75`、`sep_model=superellipse`、`f_shape=0.85`；但 JSON 未提供 `geom_model`，因此不能进一步判断是否采用了更简化的几何模型。
- 边界/等离子体量：`beta=0.7778`、`beta_null=1.0`、`nbar=3.490e21`、`ntau=5.757e18`；这些量可用于后续做约束敏感性核对，但当前 JSON 未提供 `betaN/q95/nbar_o_nGw` 等更直接的比较量。
- POPCON 证据：扫描轴名是 `Ti-B_e`，`n_invalid=0` 说明扫描中未记录无效点；`best` 矩阵并非全零，说明当前准则下至少出现了一个“最佳”标记格点，但由于缺少 `x/y` 数组及 `nx/ny`，不能把它解释成可定位的最佳区边界或趋势。

3. 风险与不确定性
- 最大不确定性来自 `use_tauE=1`：结果依赖固定约束时间假设，`tauE=0.0015` 的取值会直接影响 `Pheat`、`Qfus`、`ignited` 等结论。
- 成分假设较理想化：`fimp=0.0`、`fHe=0.0`、`Zeff=1.0` 使当前点对应 `P_line=0.0`；如果后续引入杂质或氦灰，功率平衡与辐射分配可能改变。
- 扫描证据不完整：虽然有 `best` 矩阵和 `xkey/ykey`，但缺少 `x/y` 数组以及 `nx/ny`。扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 约束信息不全：JSON 未提供 `H98/H_ITPA20/H_ISS04`、`q/q95`、`nbar_o_nGw/nbar_o_Sudo` 等字段，因此不能对约束裕度做更细的工程判读。
- 几何判读有限：已有 `superellipse` 形状与体积/表面积输出，但缺少 `geom_model` 字段，无法确认几何简化程度或不同几何修正对结果的影响。

4. 下一步建议
1. 重新导出完整 `Ti-B_e` POPCON 扫描数据，必须包含 `x`、`y` 数组和 `nx/ny`；同时保留 `best`、`valid`、`ignited`。重点监控：`best`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`。
2. 对 `tauE` 做显式敏感性扫描，因为当前 `use_tauE=1`。重点监控：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`ntau`。
3. 对成分参数做扫描，优先检查 `fimp`、`fHe`、`Zimp` 对结果的影响。重点监控：`Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`。
4. 在当前运行点附近做更细的 `Ti` 与 `B_e` 局部扫描，并与 POPCON 的 `best` 标记交叉核对。重点监控：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`beta`、`nbar`。
