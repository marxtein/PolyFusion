1. 核心结论
- 本次报告对应 `config=frc`（`config_label=场反位形 FRC`）、`preset=C-2W`。
- 当前运行点结果为：`valid=1.0`、`ignited=0.0`、`Pfus=27711.2041`、`Qfus=0.53698`、`Pheat=51605.6233`、`Pwall=5844.2894`。
- 就单点而言，这是一个“有效解”，但不是点火解；`valid=1` 仅说明当前运行点通过了模型内部有效性判定，不等同于 POPCON 扫描已经证明存在可用工作窗。
- POPCON 扫描记录了 `xkey=Ti`、`ykey=B_e`，且 `n_invalid=0`；从现有 `best` 矩阵看，64 个网格点里有 1 个点被标记为 `best`，说明当前准则下至少出现了单个候选最优格点。
- 同时，JSON 未提供 `x/y` 扫描数组，也未提供 `nx/ny`；因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。

2. 关键指标解读
- 当前运行点：`Pfus=27711.2041`，`Qfus=0.53698`，`Pheat=51605.6233`，`Pwall=5844.2894`，`ignited=0.0`；这说明该点有聚变功率输出，但在当前模型判定下并未达到点火。
- 功率与损失项：`Pn=22168.9633`，`Ptrans=57120.0`，`Pbrem=27.8639`，`Pcycl=0.0002427`，`P_line=0.0`；就显式给出的辐射项看，轫致辐射和回旋辐射都很小，线辐射为 0。
- 约束时间与模型依赖：`use_tauE=1.0`，`params.tauE=0.0015`，`outputs.tau_E=0.0015`；结果依赖固定约束时间假设，不是从本 JSON 中可见的约束时间标度律自动推得。JSON 也未给出 `H98`、`H_ITPA20`、`H_ISS04` 等标度系数，无法据此比较经验约束提升需求。
- 等离子体与边界相关量：`beta=0.7777778`，`beta_null=1.0`，`nbar=5.3331e+21`，`ne0=7.5399e+21`，`ni0=7.5399e+21`，`ntau=8.7966e+18`；这些值可用于跟踪当前点的压强与密度状态，但 JSON 未给出 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，所以不能进一步判断相对边界裕量。
- 成分与辐射：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`；当前输入下没有杂质分数，且 `P_line=0.0`，因此这份结果基本没有体现杂质线辐射敏感性。
- 几何信息：`sep_model=superellipse`，`elongation=3.75`，`f_shape_calc=0.85`，`Vp=1.28177`，`Sw=13.57168`，`Sp=6.97434`；这些量说明当前 0-D 计算已使用一套给定形状参数，但 JSON 未提供 `geom_model`，无法确认几何模型是否还有其他修正层级。
- POPCON 扫描：这里只能确认扫描轴名是 `Ti` 与 `B_e`，`n_invalid=0`，且 `best` 矩阵中仅 1 个格点为 1；这与“当前运行点 valid=1”是两件事，前者是单点是否有效，后者是扫描准则下是否出现候选最优格点。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1`：结果直接依赖固定 `tauE=0.0015` 的假设，若约束时间假设变化，`Pfus`、`Qfus`、`ignited` 与 `Pwall` 都可能明显变化。
- 扫描证据不完整：扫描网格证据不足，不能判断工作窗形状或敏感性趋势；虽然 `best` 矩阵里有 1 个被标记格点，但没有 `x/y` 数组与 `nx/ny`，不能定位到具体变量值，也不能判断该点周围是否连续。
- 运行点有效不等于扫描窗口成立：`valid=1` 只说明当前单点可解；扫描里出现 1 个 `best` 格点，也不代表当前运行点本身就是最佳点，更不代表存在宽的工作区。
- 边界信息不全：JSON 没有给出 `betaN/betaT/betap`、`q/q95`、Greenwald/Sudo 归一化密度等常见边界指标，因此无法从这份 0-D 输出单独判断磁流体或密度相关边界接近程度。
- 辐射与成分覆盖有限：当前 `fimp=0`、`fHe=0`、`P_line=0`，使得结果对氦灰或杂质累积的敏感性没有被实际扫描验证。
- 几何模型层级未明：虽然给出了 `Vp/Sw/Sp` 和形状参数，但 `geom_model` 未出现，无法确认几何处理是否为更细化版本还是仅使用了当前已见的形状覆盖。

4. 下一步建议
- 补做完整的 `Ti-B_e` POPCON 导出任务：要求同时输出 `x`、`y`、`nx`、`ny`、`best_fraction` 或等价统计；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid` 与 `best` 覆盖率。
- 围绕当前点做 `tauE` 敏感性扫描：以 `tauE=0.0015` 为中心做上下扰动，验证固定约束时间假设对结果的影响；重点监控 `Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pwall`、`Pbrem`。
- 做成分/辐射验证扫描：扫描 `fHe`、`fimp`，必要时联动 `Zimp`，检查当前“`P_line=0`、`Zeff=1`”结论对成分假设是否敏感；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`。
- 做形状与尺寸参数扫描：针对 `f_shape`、`elongation`、`r_s`、`l_s`、`Rw` 或 `r_w` 做局部扫描，确认几何设定变化对单点解和墙负荷的影响；重点监控 `Vp`、`Sw`、`Sp`、`beta`、`Pfus`、`Pwall`、`valid`。
