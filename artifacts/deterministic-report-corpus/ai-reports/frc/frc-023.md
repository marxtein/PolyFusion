1. 核心结论
- `config=frc`（场反位形 FRC），`preset=C-2W`。
- 当前运行点结果为：`valid=1.0`，`ignited=0.0`，`Pfus=15806.280819649042`，`Qfus=0.3952147101836888`，`Pheat=39994.16117963464`，`Pwall=4111.535264304908`。
- 这表示当前运行点在模型内部判据下是有效解，但不是点火解；同时 `Qfus<1`，且当前给出的 `Pfus` 小于 `Pheat`。
- 必须将“当前运行点”和“POPCON 扫描”分开理解：当前运行点 `valid=1.0` 只说明该点可计算且满足内部有效性判据，不等同于扫描已经找到稳定工作窗，也不等同于当前点位于最佳区。
- POPCON 扫描给出 `xkey=Ti`、`ykey=B_e`，且 `best` 矩阵并非全零，说明至少有一个离散扫描格点满足当前 `best` 判据；但这不等同于当前运行点本身就是最佳点。
- `use_tauE=1`，结果依赖固定约束时间假设，当前使用 `tauE=0.0015`；同时由于缺少 `x/y` 扫描数组以及 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 功率相关：除 `Pfus`、`Pheat` 外，输出还给出 `Pn=12645.024655719235`、`Ptrans=43139.52380952382`、`Pwall=4111.535264304908`；在当前结果里，`Ptrans` 数值高于 `Pheat`，说明传输相关损失在账目中占比不小。
- 辐射相关：`Pbrem=15.8933855520014`、`Pcycl=0.00014848862698905684`、`P_line=0.0`；结合 `fimp=0.0`、`fHe=0.0`、`Zeff=1.0`，可见当前辐射结果建立在非常“干净”的成分假设上，尤其线辐射在当前输入下被算为零。
- 约束时间相关：输出给出 `tau_E=0.0015`、`tau_Bohm=0.006674285714285715`、`tau_classical=1.7981155232210142`、`tauC_eff=9188.262005676052`；其中本次结果直接受固定 `tauE` 设定控制，而不是由约束时间标度自行决定。
- 等离子体与边界量：`beta=0.7777777777777777`、`beta_null=1.0`、`nbar=4.027767455983303e+21`、`ntau=6.643543746016945e+18`；这些量可作为当前运行点的状态刻画，但本 JSON 未提供 `q/q95`、`nbar_o_nGw`、`nbar_o_Sudo` 等字段，因此不能补充这部分边界判读。
- 几何相关：`Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`，并使用 `sep_model=superellipse`、`elongation=3.75`、`f_shape=0.85`；这些是当前 0-D 结果所依赖的几何输入与派生量。
- POPCON 相关：`n_invalid=0` 表明扫描结果里没有被计为无效的网格点；但由于没有提供 `x/y` 数组、`nx/ny` 和 `best_fraction`，只能确认“存在至少一个 best 格点”，不能定量描述最佳区占比、位置、边界或连通性。

3. 风险与不确定性
- 最大不确定性来自 `use_tauE=1`：当前结论依赖固定 `tauE=0.0015` 假设，因此对 `Pfus`、`Qfus`、`ignited` 的判断不应直接外推到其他约束时间设定。
- 扫描证据不完整：虽然 `best` 矩阵不是全零，但缺少 `x/y` 数组以及 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能确认最佳区是否连续、狭窄或孤立。
- 当前成分假设较理想化：`fimp=0.0`、`fHe=0.0`、`Zeff=1.0`、`P_line=0.0`，意味着辐射与稀释相关结论对输入假设非常敏感；一旦成分设定变化，功率平衡结果可能明显改变。
- 几何简化程度无法完整核实：本 JSON 给出了 `geom_weighted=0.0`，但未提供 `geom_model` 字段，因此不能判断是否启用了更简化的几何模型标志，也不能据此补充相关偏差方向。
- 约束/经验标度信息不全：建议框架中的 `H98`、`H_ITPA20`、`H_ISS04`、`tauE_scaling` 等字段未提供，因此无法判断当前固定 `tauE` 与任何经验标度之间的一致性或偏离程度。

4. 下一步建议
- 对 `Ti-B_e` 做加密 POPCON 扫描，并在导出中补齐 `x`、`y`、`nx`、`ny`、`best_fraction`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，以验证扫描中“单个 best 格点”是否会扩展、移动或消失。
- 对固定约束时间做敏感性扫描：在当前点附近改变 `tauE`，或在可用时对比固定 `tauE` 与 `tauE_scaling` 模式；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`ntau`，以判断当前结论对约束时间假设的依赖程度。
- 对成分参数做验证扫描：改变 `fimp`、`Zimp`、`fHe`、`Zeff`；重点监控 `Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，以检验当前“低辐射、零线辐射”结果是否主要由输入假设决定。
- 对几何与形状参数做局部扰动扫描：改变 `f_shape`、`l_s`、`r_s`、`r_w`，并尽量补充 `geom_model` 导出；重点监控 `Vp`、`Sw`、`Sp`、`Pwall`、`Pfus`、`valid`，以确认几何设定对壁负荷和聚变输出的影响。
- 做一次“运行点 vs 扫描点”交叉核验：将当前参数点明确映射到扫描网格中，确认当前运行点是否正好对应某个 `best` 格点；重点监控该格点的 `valid`、`ignited`、`Pfus`、`Qfus` 以及 `best` 标记，避免把“当前点有效”误读为“扫描已找到可接受工作窗”。
