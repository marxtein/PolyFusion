## 1. 核心结论
- 本次对象为 `config=frc`（场反位形 FRC），`preset=C-2W`。
- 当前运行点 `valid=1.0`，表示该组输入下 0-D 求解得到一个有效解；同时 `ignited=0.0`，表示并非点火状态。
- 当前运行点的主要功率量级为：`Pfus=7.080781854746941`、`Qfus=0.002701094878483995`、`Pheat=2621.448772921694`、`Pwall=193.677533197134`。
- 运行点与 POPCON 扫描需要分开看：当前运行点“有效”不等于“扫描已经明确找到工作窗”。
- POPCON 扫描方面，已提供 `best` 矩阵，且矩阵中至少有一个 `1`，因此不能写“当前准则下未找到最佳区”；但由于缺少 `nx`、`ny` 以及对应的 `x/y` 坐标数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 由于 `use_tauE=1.0` 且 `tauE=0.0015`，本次结果依赖固定约束时间假设。

## 2. 关键指标解读
- 运行点功率平衡上，`Pheat=2621.448772921694` 明显高于 `Pfus=7.080781854746941`，对应 `Qfus=0.002701094878483995` 很低；这说明当前解虽然有效，但聚变输出相对外加/总加热贡献很小。
- 壁负荷方面，`Pwall=193.677533197134`；同时中子功率为 `Pn=5.664625483797553`，可作为后续与壁面相关约束联动检查的直接监控量。
- 损失项中，`Pbrem=0.00778576855776493`、`Pcycl=6.669427741767859e-07`、`P_line=0.0`；从已给数据看，线辐射未体现为主要项，但这只是当前输入成分下的结果，不代表更广参数区。
- 约束时间相关指标显示：`tau_E=0.0015` 为固定输入，`tau_Bohm=0.001645714285714286`，`tau_classical=1.7981155232210142`；由于采用固定 `tauE`，本次不能把结果直接解读为某个经验缩放自动支持的性能结论。
- 等离子体与成分方面，`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；在当前输入里杂质份额为零，因此辐射与性能对杂质的敏感性尚未被展开检验。
- 几何量已给出 `Vp=1.2817698026646358`、`Sw=13.571680263507906`，形状相关输入有 `f_shape=0.85`，分离面模型为 `sep_model=superellipse`；这些量会影响体积、表面积及相应功率密度解释。
- 边界/状态量里，当前可直接读到 `beta=0.7777777777777777`、`nbar=8.914703922784546e+19`；但未提供用户要求框架中的完整 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能扩展到这些未给出的边界判读。
- POPCON 扫描已知 `xkey=Ti`、`ykey=B_e`，`n_invalid=0`，说明扫描结果里没有被标记为无效的网格点；但因为未给出 `nx`、`ny`、`x`、`y` 数组，最多只能确认“存在最佳区单元证据”，不能说明其物理位置、边界宽窄或随 `Ti/B_e` 的变化趋势。

## 3. 风险与不确定性
- 最大的不确定性来自固定约束时间假设：`use_tauE=1.0` 且 `tauE=0.0015`，因此性能判断对 `tauE` 取值高度敏感。
- 当前运行点虽 `valid=1.0`，但 `ignited=0.0` 且 `Qfus` 很低；这意味着“求解可行”与“高聚变增益”并不是一回事，不能混同解读。
- 扫描证据不完整：虽有 `best` 矩阵和 `n_invalid=0`，但缺少 `nx`、`ny` 以及 `x/y` 数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `best` 矩阵只表明至少存在一个满足当前准则的网格单元，但在没有坐标轴数组时，无法确认它对应的 `Ti`、`B_e` 精确区间，也无法判断是否孤立、狭窄或接近边界。
- 当前成分输入为 `fHe=0.0`、`fimp=0.0`，使得杂质和灰分累积对 `Pbrem/P_line/Pwall` 的影响尚未被充分覆盖；因此对辐射与功率分配的外推应保持谨慎。
- JSON 中未提供 `geom_model` 字段，因此不能判断是否使用了更简化或更完整的几何模型，也不能据此追加几何模型层面的确定性结论。

## 4. 下一步建议
- 执行 `Ti-B_e` 二维加密扫描：在当前 `xkey=Ti`、`ykey=B_e` 基础上补齐并输出 `nx`、`ny`、`x`、`y` 数组，同时记录 `best`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`。
- 执行 `tauE` 敏感性扫描：围绕当前 `tauE=0.0015` 做单参数或二维联扫，验证固定约束时间假设对 `valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`、`tau_Bohm`、`tau_classical` 的影响。
- 执行成分扫描：改变 `fimp`、`fHe`，必要时联动 `Zimp`，重点监控 `Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`、`Zeff`，检查辐射与壁负荷响应。
- 执行几何/形状扫描：围绕 `f_shape`、`r_s`、`l_s`、`Rw` 或相关几何输入做参数扫描，监控 `Vp`、`Sw`、`Pwall`、`Pfus`、`Pheat` 以及 `valid` 的变化。
- 执行约束一致性核查：在后处理中同时输出 `beta`、`nbar`、`tau_E`、`tau_Bohm`、`tau_classical`、`Ptrans`、`P_ei`，用于区分“运行点有效”与“对固定假设敏感”的情形。
- 若要判断 POPCON 工作窗是否连续、是否贴边、是否对 `Ti` 或 `B_e` 敏感，下一次扫描必须显式保存坐标轴数组与网格尺寸；在此之前，不建议对最佳区位置或趋势做进一步工程外推。
