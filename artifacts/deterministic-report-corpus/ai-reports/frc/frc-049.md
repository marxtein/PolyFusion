# 1. 核心结论

- `config=frc`（场反位形 FRC），`preset=C-2W`。
- 当前运行点：`valid=1.0`，`ignited=0.0`，`Pfus=0.16195089575948227`，`Qfus=0.0004083127269616857`，`Pheat=396.63445458725323`，`Pwall=29.237087654498854`。
- 就当前运行点本身看，这是一个模型内可求解的非点火解；聚变输出相对外加/总加热很小，不能把 `valid=1` 解读为已经获得高增益或点火。
- POPCON 扫描与单点运行需要分开看：本次扫描的 `xkey=Ti`、`ykey=B_e`，`n_invalid=0`，且 `best` 矩阵并非全 0，说明在当前判据下至少有一个网格点被标记为 `best`。
- 但本 JSON 未提供 `nx`、`ny` 和 `x/y` 数组，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。单点 `valid=1` 也不等于“扫描已经找到可推广的工作窗”。

# 2. 关键指标解读

- 运行点功率平衡：`Pfus=0.16195089575948227`，`Qfus=0.0004083127269616857`，而 `Pheat=396.63445458725323`、`Ptrans=396.66666666666663`。就这组数值看，当前解主要受外部/总加热与输运损失主导，尚未表现为点火态（`ignited=0.0`）。
- 壁负荷与中子功率：`Pwall=29.237087654498854`，`Pn=0.1295607166075858`。这里只能确认模型给出了壁负荷和中子相关输出，不能据此单独判断设计裕量，因为 JSON 未给出限值。
- 约束与约束时间：`use_tauE=1.0`，`tauE=0.0015`，输出中 `tau_E=0.0015`。因此**结果依赖固定约束时间假设**。`H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling` 未提供，当前不能用标度律交叉解释。
- 边界/整体状态：可见 `beta=0.7777777777777777`；但 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 未提供，所以边界约束解读不完整。
- 成分与辐射：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`；辐射项为 `Pbrem=0.00017807527727474473`、`Pcycl=2.4461210437844314e-08`、`P_line=0.0`。当前结果对应的是低杂质、无线辐射贡献的设定，辐射损失主要体现在轫致辐射项。
- 几何：`Vp=1.2817698026646358`，`Sw=13.571680263507906`，`Sp=6.974335690969341`，`sep_model="superellipse"`，`f_shape=0.85`。可确认本次 0-D 结果包含了体积/表面积与形状参数；但 `geom_model` 字段未提供，不能额外判断几何模型层级。
- POPCON 证据：`best` 矩阵中仅有极少数格点被标记，且 `n_invalid=0` 说明扫描点并未大面积失效；但由于缺少 `x/y` 轴数组和 `nx/ny`，不能可靠说明最佳区具体位置、边界扩展或随 `Ti`、`B_e` 的变化趋势。

# 3. 风险与不确定性

- `valid=1.0` 只说明当前单点在模型内可解，不等于点火，也不等于扫描上存在宽工作窗。
- `use_tauE=1.0` 使结果直接依赖固定 `tauE=0.0015` 假设；若约束时间改动，`Pfus`、`Qfus`、`ignited` 和功率平衡都可能变化。
- 扫描端虽然 `best` 非全 0，但缺少 `nx`、`ny`、`x/y` 数组，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- 当前成分设定为 `fimp=0.0`、`Zeff=1.0`、`P_line=0.0`，对杂质和线辐射的敏感性没有被这份结果覆盖。
- 边界约束指标缺失较多，尤其是 `betaN/q95/密度比` 一类未给出，无法完成更完整的约束交叉核对。
- `best` 矩阵里存在被标记格点，但这不应被解读为“已经证明存在稳定或宽裕的运行窗口”；目前只能说在当前判据下，扫描结果里出现了有限的候选格点。

# 4. 下一步建议

1. 重新执行 `Ti-B_e` 二维扫描，并导出完整 `x/y` 数组以及 `nx/ny`、`best_fraction`。  
监控指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best` 覆盖率。

2. 做 `tauE` 敏感性扫描（单独扫 `tauE`，或做 `tauE-Ti` / `tauE-B_e` 二维扫描），因为当前结果依赖固定约束时间假设。  
监控指标：`tau_E`、`Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pheat`、`ntau`、`valid`。

3. 在当前运行点及 `best` 标记附近，增加成分/辐射扫描，如 `fimp`、`Zimp`、`fHe`。  
监控指标：`Zeff`、`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`、`valid`。

4. 若模型支持，补充导出边界约束量并做约束核对扫描。  
监控指标：`betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，并同时记录 `valid`、`ignited`、`Pfus`、`Qfus`。
