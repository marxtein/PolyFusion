1. 核心结论

- 本次报告对应 `config=frc`（场反位形 FRC），`preset=C-2W`。
- 当前运行点结果：`valid=1.0`，`ignited=0.0`，`Pfus=2398.5409627737977`，`Qfus=0.07827946435185078`，`Pheat=30640.743171067552`，`Pwall=2434.4284194992965`。
- 对当前运行点的直接含义是：该点在本次 0-D 判据下被记为有效，但未点火；这只说明单个运行点状态，不等同于扫描已找到可用工作窗。
- POPCON 扫描方面：`xkey=Ti`，`ykey=B_e`，`n_invalid=0`；`best` 矩阵中存在 1 个被标记的点，不属于“当前准则下未找到最佳区”。
- 但本次 JSON 未提供扫描坐标数组 `x/y`，也未给出 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

2. 关键指标解读

- 运行点功率相关：`Pfus=2398.54`，`Qfus=0.0783`，`Pheat=30640.74`，`Pwall=2434.43`；同时有 `Pn=1918.83`，`Ptrans=31118.10`。
- 辐射与损失：`Pbrem=2.3561`，`Pcycl=6.125e-05`，`P_line=0.0`。在当前输入下，线辐射项为 0，已记录的辐射损失主要来自轫致辐射，回旋辐射很小。
- 约束与约束时间：`tau_E=0.0015`，且 `use_tauE=1.0`，所以结果依赖固定约束时间假设；这会直接影响对 `Pfus/Qfus/Ptrans` 的判读。
- 成分与杂质：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。当前输入接近“无杂质、无氦灰”的简化成分设定。
- 几何相关：`Vp=1.2818`，`Sw=13.5717`，`sep_model=superellipse`，`elongation=3.75`，`f_shape_calc=0.85`。这些量说明本次 0-D 结果包含给定几何形状参数的影响。
- 边界类指标中，报告给出了 `beta=0.7778`，但未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能进一步做归一化边界裕度解读。

3. 风险与不确定性

- `valid=1.0` 只说明当前运行点通过了这次 0-D 判据；它不代表 POPCON 扫描已经确认了参数空间中的连续工作窗。
- 虽然 `best` 矩阵里有 1 个点被标记，但由于缺失 `x/y` 数组以及 `nx/ny`，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1.0` 表明结果对固定 `tau_E` 假设敏感；如果 `tau_E` 改动，`Pfus`、`Qfus`、`Ptrans`、`ignited` 都可能变化。
- 当前成分设定较理想化：`fHe=0`、`fimp=0`、`P_line=0`。这意味着对杂质、氦灰、线辐射的敏感性还没有在这份结果里体现出来。
- 未提供 `geom_model` 字段，几何模型层级无法直接核验；只能确认本次结果使用了给定的几何参数与 `superellipse` 形状设定。

4. 下一步建议

- 补充一次完整 POPCON 导出：保留 `xkey=Ti`、`ykey=B_e`，同时输出 `x`、`y`、`nx`、`ny`。  
  需要监控：`best` 矩阵、`n_invalid`、各网格点的 `valid/ignited`、以及是否仍只出现单个最佳标记点。
- 围绕当前运行点做 `Ti` 和 `B_e` 的局部加密扫描，检查单点最佳标记是否稳定。  
  需要监控：`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`、`valid`、`ignited`。
- 单独做 `tau_E` 敏感性扫描，或在可用时对比不同 `tauE_scaling` 设定，因为当前结果依赖固定约束时间假设。  
  需要监控：`tau_E`、`Pfus`、`Qfus`、`Ptrans`、`ignited`、`valid`。
- 增加成分/辐射验证扫描，例如扫描 `fimp`、`fHe`、必要时联动 `Zimp`。  
  需要监控：`Zeff`、`P_line`、`Pbrem`、`Pfus`、`Pwall`、`valid`。
