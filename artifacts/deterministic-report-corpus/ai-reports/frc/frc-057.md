## 1. 核心结论

`config=frc`（场反位形 FRC），`preset=C-2W`。当前运行点结果为：`valid=1.0`，`ignited=0.0`，`Pfus=0.1133846027`，`Qfus=0.0002858598`，`Pheat=396.6441223383`，`Pwall=29.2342214993`。

就当前运行点而言，本次 0-D 求解给出了有效解（`valid=1`），但不是点火态（`ignited=0`）；同时 `Pfus` 和 `Qfus` 都明显小于 `Pheat`，说明该点的功率平衡主要依赖外加加热，而不是聚变自持。

POPCON 扫描需要单独看待，不能把“运行点有效”和“扫描找到工作窗”混为一谈。本次扫描的 `xkey=Ti`、`ykey=B_e`，且 `best` 矩阵并非全零，说明至少有 1 个格点被标记为 `best`；但由于未提供 `nx/ny`，且缺少 `x/y` 扫描数组，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

## 2. 关键指标解读

功率与增益方面：`Pheat=396.6441223383`，`Ptrans=396.6666666667`，`Pfus=0.1133846027`，`Pn=0.0907076822`，`Qfus=0.0002858598`。这些数值对应的是“有效但非点火”的运行点，且聚变输出相对外加加热很小。

约束方面：`use_tauE=1.0`，`tauE=0.0015`，输出中 `tau_E=0.0015`。这意味着本次结果**依赖固定约束时间假设**，不是由约束时间标度自洽求出。JSON 中未给出 `H98/H_ITPA20/H_ISS04` 或 `tauE_scaling` 结果，无法进一步比较不同约束标度。

成分与辐射方面：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`；辐射功率为 `P_line=0.0`，`Pbrem=0.0001325694`，`Pcycl=2.272e-08`。在这组输入下，线辐射为 0，辐射损失主要体现为很小的轫致辐射项。

边界与几何方面：`beta=0.7777777778`，`nbar=1.1632628126e19`，`ne0=ni0=1.6446234016e19`；几何量为 `Vp=1.2817698027`，`Sw=13.5716802635`，`sep_model=superellipse`，`elongation=3.75`，`f_shape_calc=0.85`，`geom_weighted=0.0`。但 JSON 中未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能对这些边界约束做进一步判读。

## 3. 风险与不确定性

首先，`valid=1` 只说明当前这一个运行点在本次 0-D 求解下成立；它不等于 POPCON 扫描已经明确给出可辨识的工作窗。

其次，由于 `use_tauE=1`，结果对固定 `tauE=0.0015` 的假设敏感。若 `tauE` 改变，`Pfus`、`Qfus`、`Pheat` 和是否仍保持 `valid` 都可能变化；当前 JSON 不能说明这种敏感性大小。

再次，扫描侧虽然给出了 `best` 矩阵和 `n_invalid=0`，但没有 `nx/ny`，也没有 `x/y` 数组。因此只能确认“存在被标记的 best 格点”，不能确认其对应参数坐标、区域连通性、边界位置或趋势方向；**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

最后，当前输入里 `fHe=0`、`fimp=0`、`P_line=0`，说明这次结果没有展示杂质和氦灰变化下的辐射敏感性；而 `geom_model` 字段未提供，也无法判断几何模型是否进一步简化或替代。

## 4. 下一步建议

1. 重新导出一次完整的 `Ti-B_e` POPCON 扫描，补齐 `x/y` 数组和 `nx/ny`。  
监控指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 围绕当前运行点 `Ti=150.0`、`B_e=1.0` 做更密的局部扫描，检查当前点附近是否连续保持 `valid=1`，以及 `best` 标记是否扩展。  
监控指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`、`Ptrans`、`Pbrem`、`Pcycl`、`P_line`。

3. 针对固定约束时间假设做敏感性验证：在保持其余参数不变时扫描 `tauE`，或对比固定 `tauE` 与可用约束时间模型结果。  
监控指标：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

4. 对成分项做补充验证，至少扫描 `fHe` 和 `fimp`，检查当前“低辐射、低杂质”结果是否稳定。  
监控指标：`Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`。
