1. 核心结论
- 本次结果对应 `config=frc`（场反位形 FRC），`preset=C-2W`。当前运行点给出的核心输出为：`valid=1.0`、`ignited=0.0`、`Pfus=1424.8797627395845`、`Qfus=0.11247122986916439`、`Pheat=12668.837749859405`、`Pwall=1038.4651891995097`。
- 就当前运行点而言，模型将该点标记为有效（`valid=1.0`），但未标记为点火（`ignited=0.0`）。这只能说明该单点在本次 0-D 判据下可解，不能等同于“扫描已经证明存在工作窗”。
- 就 POPCON 扫描而言，扫描轴是 `xkey=Ti`、`ykey=B_e`，且 `n_invalid=0`。`best` 矩阵并非全 0，说明按当前准则至少有一个网格单元被标记为 best；这与“当前运行点是否有效”是两件不同的事。
- 由于 JSON 未提供 `x/y` 数组，且也未给出 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 当前运行点的功率关系为 `Pfus=1424.8797627395845`、`Pheat=12668.837749859405`、`Qfus=0.11247122986916439`、`Pwall=1038.4651891995097`，同时还有 `Pn=1139.9038101916676` 和 `Ptrans=12952.38095238095`；按这些原始数值看，当前点更像是依赖外部加热的运行状态，而不是由 `ignited=1` 标记的点火状态。
- 辐射与相关损失项中，`Pbrem=1.4327319432609782`、`Pcycl=1.808311110869817e-05`、`P_line=0.0`；在本次设定下，线辐射没有被体现出来。
- 约束时间方面，`use_tauE=1.0`，参数给定 `tauE=0.0015`，输出中 `tau_E=0.0015`；因此结果依赖固定约束时间假设。可对照的时间量还包括 `tau_Bohm=0.003657142857142858`、`tau_classical=1.7981155232210144`、`tauC_eff=22653.101656817613`。
- 当前点的状态与成分设定包括 `Te=1.0`、`Ti=46.42857142857143`、`B_e=5.714285714285714`、`nbar=1.2093128034477922e+21`、`beta=0.7777777777777777`、`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；这说明本次结果是在较“干净”的成分假设下得到的。
- 几何相关输出给出了 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`f_shape=0.85`、`sep_model=superellipse`；但 JSON 中未提供 `geom_model` 字段，因此这里不能进一步判断几何模型是否被简化。
- POPCON 方面，已知 `best` 是一个 `8x8` 矩阵，且只有一个单元为 `1`，`n_invalid=0`。这只能说明在已扫网格里至少存在一个满足当前 best 判据的格点；由于缺少 `x/y/nx/ny`，不能把这个信息扩展成位置、边界或趋势判断。

3. 风险与不确定性
- `use_tauE=1.0` 意味着本次结论对固定 `tau_E=0.0015` 的假设敏感；一旦改动约束时间，`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat` 等结果都可能改变。
- 当前运行点有效（`valid=1.0`）并不等于 POPCON 扫描已经找到明确且可描述的工作窗；扫描里出现 best 单元，也不等于当前运行点本身就是 best 点。
- 虽然 `best` 矩阵不是全 0，但由于缺少 `x/y` 数组与 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势；也无法判断那个 best 单元是否孤立、连续，或是否紧邻当前运行点。
- 成分与辐射假设较理想化：`fHe=0.0`、`fimp=0.0`、`Zeff=1.0`，且 `P_line=0.0`。因此，本次结果没有覆盖氦灰或杂质变化对辐射、增益和壁负荷的影响范围。
- JSON 未提供 `betaN`、`betaT`、`betap`、`q`/`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo` 等归一化边界指标，所以对边界裕度的解读只能停留在 `beta` 和绝对参数层面，不能扩展为更完整的约束判断。

4. 下一步建议
- 先重跑一次 `Ti-B_e` 的 POPCON 扫描，并确保导出 `x`、`y`、`nx`、`ny`、`best_fraction` 以及逐点 `valid/ignited` 结果；重点监控 `n_invalid`、`best` 分布、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕当前点 `Ti=46.42857142857143`、`B_e=5.714285714285714` 做更细网格扫描，专门检查当前点附近是否连续出现 best 单元；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`、`beta`。
- 针对固定约束时间假设，执行 `tauE` 灵敏度扫描，至少围绕 `tauE=0.0015` 做多点比较；若工具支持，再与非固定 `tauE` 的模式对照。重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`。
- 对成分与辐射设定做验证扫描，例如改变 `fHe`、`fimp`、`Zeff` 或 `Zimp`，检查 `P_line=0.0` 是否仅由当前假设导致；重点监控 `Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。
