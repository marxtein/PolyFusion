1. 核心结论
- 本次结果对应 `config=frc`（场反位形 FRC），`preset=C-2W`；当前运行点 `valid=1.0`、`ignited=0.0`、`Pfus=8081.603255041705`、`Qfus=0.14558399745589018`、`Pheat=55511.61800932353`、`Pwall=4685.729403407573`。JSON 未单列单位，以下均按原始输出值解读。
- 就单个运行点而言，这是一组“可求解/有效”的 0-D 结果，但不是点火状态；`Qfus` 也明显小于 1。
- 当前运行点和 POPCON 扫描不能混为一谈：`valid=1` 只说明这个输入点在模型中有效，不等同于扫描已经找到连续工作窗。
- POPCON 扫描给出 `xkey=Ti`、`ykey=B_e`、`n_invalid=0`，且 `best` 矩阵中至少存在一个被标记点；但由于缺少 `x/y` 网格数组且未给出 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 由于 `use_tauE=1.0`，结果依赖固定约束时间假设；这里固定为 `tauE=0.0015`，输出中的 `tau_E=0.0015` 与之对应。

2. 关键指标解读
- 功率与点火状态：当前点 `Pfus=8081.603255041705`，`Pheat=55511.61800932353`，`Qfus=0.14558399745589018`，`ignited=0.0`；按这组输出，聚变产出存在，但尚未表现为点火运行点。
- 功率平衡相关量：`Ptrans=57119.99999999999`，与 `Pheat=55511.61800932353` 数值接近且略高；同时 `Pn=6465.282604033364`，可作为能量去向核对的一部分。
- 约束时间假设：`use_tauE=1.0`、`tauE=0.0015`、`tau_E=0.0015`，说明本次不是在比较不同约束时间标度，而是直接采用固定约束时间输入；`tauC_eff=5438.417958059028` 给出了粒子相关时间量，但 `use_tauC=0.0`。
- 辐射与成分：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；已列出的辐射项中 `Pbrem=7.93848302951377`、`Pcycl=0.0001773023663180335`、`P_line=0.0`，就当前输出而言，辐射项主要来自 `Pbrem`。
- 几何与体表参数：`Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`，形状相关输入/输出包括 `sep_model=superellipse`、`f_shape=0.85`、`elongation=3.75`、`geom_weighted=0.0`；但 `geom_model` 未提供，因此不能据此判断几何模型层级。
- 运行点背景量：输入侧 `B_e=12.0`、`Ti=87.85714285714286`、`Te=1.0`；输出侧 `B_int=5.016301568921518`、`nbar=2.8465901314855005e+21`、`beta=0.7777777777777777`，这些值可作为后续扫描对比基线。

3. 风险与不确定性
- 最重要的不确定性来自扫描证据不完整：虽然 `best` 矩阵中有被标记点，且 `n_invalid=0`，但缺少 `x/y` 网格数组和 `nx/ny`，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 单点有效不代表扫描有窗：当前点 `valid=1.0` 且 `ignited=0.0`，只能说明这个运行点被模型接受且未点火，不能外推出“扫描已经找到可推广的最佳区”。
- 结果依赖固定约束时间假设：`use_tauE=1.0` 使结论对 `tauE=0.0015` 的设定敏感；如果 `tauE` 改变，`Pfus`、`Qfus`、`Pheat` 和 `ignited` 都可能变化。
- 约束边界证据不全：JSON 中未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，也没有 H 因子类标度量，因此无法就这些边界接近程度做进一步判断。
- 几何与阈值信息不全：`geom_model`、`LH_ratio` 等字段未给出，因此不能对几何简化程度或 L-H 阈值比较结果做额外解读。

4. 下一步建议
- 1. 补全 POPCON 导出内容：重新执行 `Ti-B_e` 扫描，并同时保存 `x`、`y`、`nx`、`ny`、`best_fraction`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`。
- 2. 围绕当前点做加密二维扫描：以当前 `Ti=87.85714285714286`、`B_e=12.0` 为中心细化 `Ti-B_e` 网格，验证 `best` 标记是孤立点还是能连成区域；重点监控 `best`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`。
- 3. 做约束时间敏感性验证：在保持其余输入不变的条件下，对 `tauE` 做单参数扫描，或对比固定 `tauE` 与其他约束时间设定；重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`。
- 4. 做成分与辐射校核扫描：对 `fHe`、`fimp`、`Zimp` 做有限范围扰动，检查 `P_line=0.0` 是否仅由当前成分假设给出；重点监控 `Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`。
