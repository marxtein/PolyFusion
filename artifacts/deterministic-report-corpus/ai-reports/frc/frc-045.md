## 1. 核心结论

- `config=frc`，`config_label=场反位形 FRC`，`preset=C-2W`。
- 当前运行点（`last_run`）结果：`valid=1.0`，`ignited=0.0`，`Pfus=685.2071`，`Qfus=0.0327547`，`Pheat=20919.3823`，`Pwall=1591.8876`。
- 这表示本次 0-D 求解在当前参数下是有效的，但未达到点火状态。
- 必须单独区分：`valid=1.0` 只说明当前这一组输入参数的运行点可解，**不等同于** POPCON 扫描已经证明存在明确工作窗。
- POPCON 扫描信息显示：`xkey=Ti`，`ykey=B_e`，`n_invalid=0`，且 `best` 矩阵非全 0，说明按当前准则至少有一个网格单元被标记为 `best`。
- 但由于未提供 `x/y` 扫描数组，且 `nx/ny` 也未给出，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1`，因此结果**依赖固定约束时间假设**；本次使用的是 `tau_E=0.0015`。

## 2. 关键指标解读

- 功率量级上，当前运行点的 `Pheat=20919.3823` 明显高于 `Pfus=685.2071`，对应 `Qfus=0.0327547`。从这组数值看，当前解主要依赖外加加热，不是由聚变功率主导。
- 中子功率给出为 `Pn=548.1657`；壁负荷相关输出为 `Pwall=1591.8876`。
- 损失项中：`Pbrem=0.7094`，`Pcycl=2.784e-05`，`P_line=0.0`。就本次设定而言，线辐射项为 0，轫致辐射存在，回旋辐射很小。
- 约束时间方面：`use_tauE=1`，`tau_E=0.0015`，因此这不是由约束时间标度自行闭合出来的结果，而是建立在固定 `tau_E` 输入上的结果。
- 成分设定为：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。这说明本次计算没有引入非零氦灰分数和非零杂质分数；`P_line=0.0` 也应放在这一设定背景下理解。
- 几何相关输出包括：`Vp=1.2818`，`Sw=13.5717`，`Sp=6.9743`，`elongation=3.75`，`sep_model=superellipse`，`f_shape=0.85`。报告里没有给出 `geom_model` 字段，因此这里只能引用现有几何输出，不能补充几何模型级别判断。
- 其他状态量中，可直接引用的有：`beta=0.7778`，`nbar=8.509e20`，`B_int=3.0456`；参数侧当前点为 `Ti=108.5714`，`Te=1.0`，`B_e=7.2857`。

## 3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1`。由于结果依赖固定约束时间假设，`Pfus`、`Qfus`、`Pheat`、`Pwall` 对 `tau_E` 设定的敏感性需要单独验证。
- 虽然 `best` 矩阵非全 0，但当前只知道“有命中单元”，不知道对应的 `Ti` 和 `B_e` 坐标数组，也没有 `nx/ny`。因此，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- 同样因为缺少完整扫描轴信息，不能据此描述最佳区的位置、边界、宽窄或连续性。
- `n_invalid=0` 只能说明本次扫描里没有被记为无效的网格点；它不等于“运行窗口宽”或“参数鲁棒”。
- 组分设定中 `fHe=0.0`、`fimp=0.0`，使得当前结果对氦灰和杂质引起的辐射/稀释影响覆盖有限；`P_line=0.0` 也仅反映这次输入，不应外推。
- 报告未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo` 等归一化边界量，因此无法从这份 JSON 直接判断这些边界约束的接近程度。

## 4. 下一步建议

1. 重新输出一次完整的 `Ti-B_e` POPCON 扫描，补齐 `x`、`y`、`nx`、`ny`，必要时同时导出 `best_fraction`。  
   需要监控的指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 围绕当前固定值 `tau_E=0.0015` 做约束时间敏感性扫描，或与可用的 `tauE_scaling` 模式做对照。  
   需要监控的指标：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`。

3. 对成分与辐射假设做扫描，至少覆盖 `fHe`、`fimp`、`Zimp`。  
   需要监控的指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`、`valid`。

4. 在当前运行点附近做局部参数扫描，优先围绕 `Ti=108.5714` 和 `B_e=7.2857` 展开。  
   需要监控的指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`beta`、`nbar`。
