# 1. 核心结论

当前运行点：`config=frc`（场反位形 FRC），`preset=C-2W`，`valid=1.0`，`ignited=0.0`，`Pfus=0.6813548911760671`，`Qfus=0.001718288887770908`，`Pheat=396.5310466856195`，`Pwall=29.267739429791657`。这表示本次单点计算得到了模型内有效解，但该点并未点火；同时从 `Pfus`、`Qfus` 与 `Pheat` 的组合看，当前点主要依赖外加加热而不是聚变自持。

POPCON 扫描：`xkey=Ti`，`ykey=B_e`，`n_invalid=0`。`best` 矩阵不是全零，按给出的 8x8 矩阵可见共有 1 个单元被标记为最佳，说明在当前离散准则下至少出现过候选最佳单元。但这与“当前运行点 valid=1.0”不是一回事，也不能直接等同于已经确认了明确工作窗。由于缺少 `x/y` 扫描数组，且 `nx/ny` 未提供，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

此外，`use_tauE=1.0`，且输出 `tau_E=0.0015`，因此本次结果依赖固定约束时间假设。

# 2. 关键指标解读

功率与状态方面：`valid=1.0` 说明该运行点在模型中可求解；`ignited=0.0` 说明该点不是点火态。`Pfus=0.6813548911760671`、`Qfus=0.001718288887770908`、`Pheat=396.5310466856195` 一起表明，当前点的聚变输出相对加热输入很低。壁负荷 `Pwall=29.267739429791657` 需要与功率平衡一起跟踪。中子功率 `Pn=0.5450839129408537` 已给出，可作为聚变输出侧的辅助观测量。

约束与约束时间方面：输出给出 `tau_E=0.0015`，输入给出 `use_tauE=1.0`、`tauE=0.0015`，因此这里的能量约束时间是固定输入，不是从缩放律自洽求出的结果。`H98`、`H_ITPA20`、`H_ISS04`、`tauE_scaling` 未提供，不能据此比较不同约束时间模型。

边界与等离子体状态方面：已给出 `beta=0.7777777777777777`、`beta_null=1.0`，以及 `nbar=2.5777123540996706e+19`。但 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 都未提供，因此这份数据不足以判断常见边界约束的接近程度。

成分与辐射方面：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。已报告的辐射通道中，`Pbrem=0.0006509633647649542`，`Pcycl=3.382331525223589e-08`，`P_line=0.0`；按这组结果，已报告辐射里以轫致辐射为主，线辐射未体现。

几何方面：已给出 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`elongation=3.75`、`sep_model=superellipse`、`f_shape_calc=0.850000000000001`、`geom_weighted=0.0`。`geom_model` 字段未提供，因此不能直接判断几何模型复杂度。

# 3. 风险与不确定性

首先，`valid=1.0` 只说明当前单点可求解，不代表存在宽的扫描可行区；`ignited=0.0` 也明确表明当前点不是点火态。单点有效与扫描找到候选最佳区必须分开看。

其次，POPCON 侧虽然 `best` 矩阵中有 1 个标记单元，但缺少 `x/y` 扫描数组且 `nx/ny` 未提供，所以**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。在这种证据条件下，不宜把这个结果扩展成位置、边界或趋势判断。

再次，`use_tauE=1.0` 使结果直接依赖固定 `tau_E=0.0015` 假设；如果约束时间设定变化，`Pfus`、`Qfus`、`ignited` 和功率平衡都可能随之改变。

另外，这组结果对应 `fHe=0.0`、`fimp=0.0`、`Zeff=1.0` 的成分设定；若后续加入氦灰或杂质，`Pbrem/P_line/Pcycl` 以及总功率平衡可能变化。边界相关字段缺失也意味着无法从这份 JSON 单独判断更完整的运行约束。

# 4. 下一步建议

1. 对 `Ti-B_e` 做加密 POPCON 扫描，并明确输出完整 `x`、`y` 数组以及 `nx`、`ny`、`best` 或 `best_fraction`。重点监控：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best` 标记分布。

2. 针对固定约束时间假设，做 `tauE` 灵敏度扫描。重点监控：`tau_E`、`ntau`、`Pfus`、`Qfus`、`ignited`、`Pheat`、`Ptrans`、`Pwall`。

3. 做成分与辐射验证扫描，至少覆盖 `fHe`、`fimp`、`Zimp`。重点监控：`Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。

4. 做几何输入复核或形状参数扫描，优先检查 `f_shape`、`r_s`、`l_s`、`Rw` 对 `Vp/Sw/Sp` 和功率结果的影响；若工具支持，应同时输出 `geom_model`。重点监控：`Vp`、`Sw`、`Sp`、`elongation`、`Pfus`、`Qfus`、`Pwall`。
