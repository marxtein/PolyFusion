1. 核心结论
- 本次结果对应 `config=frc`（场反位形 FRC）、`preset=C-2W`。当前运行点 `valid=1.0`、`ignited=0.0`，即该 0-D 求解点被判为有效，但未达到点火。
- 当前运行点的主要功率量为：`Pfus=33.4006`、`Qfus=0.0049108`、`Pheat=6801.4542`、`Pwall=503.6115`。从数值关系看，聚变输出相对外加/总加热很小，`Qfus` 很低。
- 当前结果明确依赖固定约束时间假设：`use_tauE=1.0`，且本次使用 `tau_E=0.0015`。因此，运行点评价对该固定约束时间设定敏感。
- 运行点有效，不等于 POPCON 扫描已经找到清晰工作窗。此次扫描给出 `xkey=Ti`、`ykey=B_e`，`n_invalid=0`，且 `best` 矩阵中至少有 1 个网格点被标记为最佳；但由于缺少 `x/y` 数组以及 `nx/ny` 信息，仍应写明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

2. 关键指标解读
- 运行点状态：`valid=1.0` 表示该组输入下模型成功得到有效解；`ignited=0.0` 表示没有达到点火。两者含义不同，不能把“可求得有效运行点”理解为“已形成自持燃烧”。
- 功率平衡：`Pfus=33.4006`，其中中子功率 `Pn=26.7205`；对应 `Pheat=6801.4542`、`Ptrans=6808.0952`、`Pwall=503.6115`。就当前点看，系统处于高加热/高输运功率通量背景下，而聚变增益 `Qfus=0.0049108` 很低。
- 辐射与损失：`Pbrem=0.03905`、`Pcycl=3.288e-06`、`P_line=0.0`。在已给数据里，线辐射被设为 0，回旋辐射很小，轫致辐射非零但相对 `Pheat` 也较小；这说明当前总功率平衡主要不是由这几项辐射损失主导。
- 约束与标度：本次直接使用固定 `tau_E=0.0015`，并给出 `tau_Bohm=0.002651`、`tau_classical=1.7981`、`tauC_eff=20566.1127`。由于 `use_tauE=1.0`，结果首先反映的是“在该固定约束时间下”的 0-D 平衡，而不是由某个约束标度自洽推出的唯一结论。
- 边界与等离子体状态：`beta=0.7778`，`beta_null=1.0`，`nbar=1.9965e+20`，`ntau=3.2932e+17`。这些量可用于后续扫描约束边界，但仅凭当前 JSON，不能进一步外推稳定工作范围。
- 成分与杂质：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。这表示当前点采用了非常理想化的低有效电荷、无氦灰分、无杂质分数设定；因此辐射与稀释效应在本次结果中被弱化。
- 几何信息：`Vp=1.2818`、`Sw=13.5717`、`Sp=6.9743`，形状相关参数包括 `elongation=3.75`、`f_shape=0.85`、`sep_model=superellipse`。这些几何输入会直接影响体积、表面积和壁负荷估计。
- POPCON 扫描：扫描轴为 `Ti` 与 `B_e`，`n_invalid=0` 说明已计算网格中没有被标为无效的点；`best` 矩阵中仅见单个网格点为 1，说明在当前判据下至少存在一个被标记的最佳点。但因为缺少 `x/y` 数组及 `nx/ny`，不能据此判断最佳区的实际参数位置、边界宽度或趋势方向。

3. 风险与不确定性
- 最大不确定性来自固定约束时间假设：`use_tauE=1.0` 且 `tau_E=0.0015`。若约束时间改动，`Pfus`、`Qfus`、`Pheat`、`Pwall` 等结果都可能明显变化。
- 扫描证据不完整：虽然提供了 `best` 矩阵，但没有 `x/y` 数组，也没有 `nx/ny`。因此必须保留判断：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 最佳区证据很弱：`best` 矩阵中只有一个网格点被标记，不足以支持“存在宽裕区域”之类表述；它更像是“在当前判据下出现单点命中”，而非已证明有连续区域。
- 运行点与扫描结论不能混同：`valid=1.0` 只说明当前参数点可解；扫描中出现最佳点，只说明某些网格点满足当前“best”判据。两者不是同一层面的结论。
- 成分设定偏理想化：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0` 会降低辐射和燃料稀释影响。若后续引入氦灰或杂质，当前功率平衡与壁负荷判断可能变化。
- 几何模型不确定性仍存在：虽然给出了 `Vp/Sw/Sp` 和形状参数，但 JSON 未提供更完整的几何模型对比信息；因此壁负荷和表面积相关结果应视为当前几何设定下的模型输出，而非独立验证值。

4. 下一步建议
- 开展 `Ti-B_e` 二维补充扫描，明确补齐 `x`、`y`、`nx`、`ny` 输出；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best` 分布。
- 做约束时间敏感性扫描：围绕当前 `tau_E=0.0015` 进行多点扫描，因为 `use_tauE=1.0`；重点监控 `Qfus`、`Pfus`、`Ptrans`、`Pwall`、`ntau` 对 `tau_E` 的响应。
- 做成分/辐射扰动扫描：提高 `fHe`、`fimp`，并测试不同 `Zimp`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Qfus`、`Pwall` 的变化。
- 做几何参数扫描：对 `f_shape`、`r_s`、`l_s`、`r_w` 进行系统扰动；重点监控 `Vp`、`Sw`、`Sp`、`Pwall`、`beta` 以及最佳点是否仍存在。
- 对当前单个最佳点做局部加密扫描：以其所在网格邻域增加分辨率，而不是直接外推连续工作窗；重点监控 `best` 是否从单点扩展为多点，以及 `n_invalid` 是否上升。
