## 1. 核心结论

- `config=frc`，`preset=C-2W`。
- 当前运行点 `valid=1.0`，表示该 0-D 求解点在模型内被判为有效；但 `ignited=0.0`，表示未点火。
- 当前运行点的主要功率指标为：`Pfus=393.6681185430529`、`Qfus=0.05849662800070105`、`Pheat=6729.757457786029`、`Pwall=524.8742556574106`。
- 约束上，`use_tauE=1.0` 且 `tauE=0.0015`，因此本次结果依赖固定约束时间假设。
- 当前运行点与 POPCON 扫描需要分开看：当前运行点是一个单点结果；POPCON 扫描则是参数面搜索证据，二者不能互相替代。
- POPCON 方面，已给出 `best` 矩阵，且其中不是全 0，说明在当前准则下至少存在被标记的候选点；但由于未提供 `nx`、`ny` 以及 `x/y` 扫描数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

## 2. 关键指标解读

- 当前运行点：
  - `valid=1.0` 说明该点在当前 0-D 模型下可求得有效结果；`ignited=0.0` 说明不属于点火状态。
  - `Pfus=393.6681185430529`，同时 `Qfus=0.05849662800070105`，对应的是较低的聚变增益水平；这里仅报告模型结果，不外推运行能力。
  - `Pheat=6729.757457786029` 明显高于 `Pfus`，与 `Qfus` 偏低这一点一致。
  - `Pwall=524.8742556574106` 给出了对应壁负荷水平；`Pn=314.9344948344424` 也表明中子功率在总聚变功率中占有明显部分。
  - 辐射与损失项中，`Pbrem=0.39583753185998727`、`Pcycl=5.867541551246619e-06`、`P_line=0.0`。从已给字段看，线辐射未体现，制动辐射和回旋辐射量级相对较小。
- 约束与输运：
  - `use_tauE=1.0`、`tau_E=0.0015`，说明不是由缩放律自洽求出的约束时间，而是直接采用固定值，因此结果对该假设敏感。
  - 未提供 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling`，所以不能从本报告判断与这些经验约束的偏离程度。
- 成分与几何：
  - `Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。按输入看，模型中未引入非零杂质份额，灰分也为 0。
  - `Vp=1.2817698026646358`、`Sw=13.571680263507906`，可作为体积和表面积的几何背景量。
  - 未提供 `geom_model` 字段，因此这里不能进一步判断是否采用了更简化或更完整的几何模型；仅能看到 `geom_weighted=0.0`。
- POPCON 扫描：
  - 扫描轴键为 `xkey="Ti"`、`ykey="B_e"`，说明搜索是在离子温度与外加磁场参数面上进行。
  - `n_invalid=0`，表示扫描结果里没有被记录为无效的点。
  - `best` 矩阵已提供，且可见并非全 0，说明在当前准则下至少有一个网格单元被标记为最佳候选。
  - 但未提供 `x/y` 数组，也未提供 `nx/ny`，因此不能把这个候选点映射成具体坐标，也不能描述最佳区边界、面积占比或趋势。

## 3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1.0`：结果依赖固定约束时间 `tauE=0.0015`，不是对约束性能的独立预测。
- 当前运行点 `valid=1.0` 不等于“扫描找到了明确工作窗”。单点有效，只能说明该输入点在模型下可解；是否存在连续可用区域，需要完整扫描网格证据支持。
- 虽然 `best` 矩阵不是全 0，但由于缺少 `nx`、`ny` 与 `x/y` 扫描数组，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo` 等边界化指标，因此无法基于本 JSON 判断更具体的稳定性或密度边界余量。
- `P_line=0.0`、`fimp=0.0`、`fHe=0.0` 反映的是当前输入/模型设定下的结果，不应外推为真实辐射环境或杂质行为已经被充分覆盖。
- 未提供 `LH_ratio` 等字段，因此不能对 L-H 阈值比较做任何判断。

## 4. 下一步建议

1. 重新导出完整 POPCON 扫描数据：至少补充 `x` 数组、`y` 数组、`nx`、`ny`，并保留 `best`、`n_invalid`。  
   重点监控：`best` 分布、`n_invalid`、对应点的 `valid` 与 `ignited`。

2. 围绕当前运行点做 `tauE` 敏感性扫描，而不是只看固定 `tauE=0.0015` 的单值结果。  
   重点监控：`Pfus`、`Qfus`、`Pheat`、`Pwall`、`ignited`、`Ptrans`。

3. 在 `Ti` 与 `B_e` 上做加密扫描，并保持其他输入不变，确认当前 `best` 单元是孤立点还是连续区域。  
   重点监控：`best` 矩阵变化、`valid`、`n_invalid`、`Pfus`、`Qfus`。

4. 增加成分与辐射相关的验证扫描，例如对 `fHe`、`fimp`、`Zimp` 做参数扰动。  
   重点监控：`Pbrem`、`P_line`、`Pfus`、`Qfus`、`Pwall`、`Zeff`。

5. 若后续工具链支持，补充边界化输出并复算。  
   重点监控：`beta` 相关量、密度边界相关量、以及它们与 `valid/ignited`、`best` 判据的对应关系。
