1. 核心结论
- 本次案例为 `config=frc`（场反位形 FRC），`preset=C-2W`；当前运行点结果为 `valid=1.0`、`ignited=0.0`、`Pfus=8224.414896031403`、`Qfus=0.2789688413081298`、`Pheat=29481.48208045672`、`Pwall=2778.277725704554`。
- 就当前运行点而言，模型将其判为有效，但未点火；`Qfus` 也表明本次输出中的聚变功率低于加热功率。
- POPCON 扫描需要单独解读：扫描轴为 `xkey=Ti`、`ykey=B_e`，`best` 矩阵并非全 0，说明扫描记录中至少有 1 个被当前准则标记的单元；这不等同于“当前运行点有效”，两者不能混为一谈。
- 由于 `use_tauE=1.0`，且输出 `tau_E=0.0015`，本次结果依赖固定约束时间假设。

2. 关键指标解读
- **运行点** `Pfus=8224.414896031403`，中子功率 `Pn=6579.531916825123`；损失项中 `Ptrans=31118.09523809524`，而辐射相关输出为 `Pbrem=8.269737731077013`、`Pcycl=8.383668439841243e-05`、`P_line=0.0`。
- **约束与边界** 当前直接给出的约束时间为 `tau_E=0.0015`；边界相关输出有 `beta=0.7777777777777777`、`beta_null=1.0`，以及 `nbar=2.905374010283321e+21`。本 JSON 未提供 `q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此这些边界不能进一步判读。
- **成分与辐射** `Zeff=1.0`，参数中 `fHe=0.0`、`fimp=0.0`、`Zimp=10`；对应输出里 `P_line=0.0`，辐射项主要体现为 `Pbrem`，`Pcycl` 很小。
- **几何** 给出了 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`elongation=3.75`，截面模型为 `sep_model=superellipse`；`Pwall` 应结合这类几何量一起看。
- **POPCON 扫描** `n_invalid=0`，说明记录到的扫描单元中没有被标成无效的点；`best` 矩阵里只有 1 个单元为 1，其余为 0，表示当前准则下仅出现很有限的被标记证据。

3. 风险与不确定性
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；原因是本 JSON 未提供 `x`/`y` 数组，也未明确给出 `nx`/`ny`，因此无法把 `best` 单元映射到具体的 `Ti`、`B_e` 数值坐标。
- 虽然 `best` 矩阵中存在 1 个被标记单元，但现有证据不足以判断它是孤立点、窄带，还是更大区域的一部分。
- `use_tauE=1.0` 使 `Pfus`、`Qfus`、`Pheat`、`Pwall` 等结果对固定 `tau_E=0.0015` 假设敏感；当前输出不能把这种敏感性与其他物理参数变化完全区分开。
- 本 JSON 未提供 `H98`、`H_ITPA20`、`H_ISS04`、`tauE_scaling` 等约束信息，也未提供更完整的边界指标，因此不能进一步拆分约束来源。
- `valid=1.0` 只表示当前运行点通过模型内部有效性检查；`ignited=0.0` 则说明不能把该点表述为点火态，这与扫描中是否出现被标记单元是两个不同判断。

4. 下一步建议
1. 补做并导出完整 `Ti-B_e` POPCON 网格，至少包含 `x`、`y`、`nx`、`ny`、`best`、`n_invalid`；同时监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，用于确认被标记单元对应的实际参数坐标。
2. 围绕当前点 `Ti=46.42857142857143`、`B_e=8.857142857142858` 做局部加密二维扫描；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pwall`、`beta`、`nbar`、`Ptrans`、`Pbrem`，检查当前点附近是否连续出现被标记单元。
3. 针对固定约束时间假设做敏感性验证：在 `use_tauE=1` 条件下扫描 `tauE`，或增加一组 `tauE_scaling` 对照计算；重点监控 `tau_E`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`ignited`、`best` 的变化。
4. 做成分与辐射对照扫描，如改变 `fimp`、`fHe`、`Zimp` 或相关 `Zeff` 输入；重点监控 `Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall` 与 `valid`，验证当前低线辐射结果对成分设定的依赖。
