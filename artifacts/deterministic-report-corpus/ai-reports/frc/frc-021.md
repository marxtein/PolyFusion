# 1. 核心结论

- 本次结果对应 `config=frc`（场反位形 FRC）、`preset=C-2W`。当前运行点 `valid=1`、`ignited=0`；`Pfus=3765.4679670959385`，`Qfus=0.18543250786680734`，`Pheat=20306.406953200476`，`Pwall=1773.684205118055`。
- 运行点层面，只能说明这个单点在模型中给出了有效解，且未点火；这不等同于 POPCON 扫描已经证明存在明确工作窗。
- POPCON 扫描层面，已给出 `xkey=Ti`、`ykey=B_e`、`best` 矩阵和 `n_invalid=0`。`best` 矩阵并非全 0，说明在当前判据下至少有一个网格点被标记为 best。
- 由于 `use_tauE=1`，本次结果依赖固定约束时间假设，具体为 `tauE=0.0015`。

# 2. 关键指标解读

- 当前运行点的功率项中，`Pfus=3765.47`，`Qfus=0.1854`，`Pheat=20306.41`，`Pwall=1773.68`；同时 `Pn=3012.37`，`Ptrans=21055.71`，`ignited=0`。这些量说明该点的聚变输出、加热功率、输运功率和壁负荷都已被模型显式计算。
- 约束时间方面，输出给出 `tau_E=0.0015`，参数中 `use_tauE=1`、`tauE=0.0015`，因此这里采用的是固定 `tauE` 输入，而不是从缩放律自动求得的约束时间结果。
- 成分与辐射方面，`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`；辐射项为 `Pbrem=3.7862`、`Pcycl=4.2321280298536575e-05`、`P_line=0.0`。就这组输入而言，线辐射在结果中为 0，轫致辐射和回旋辐射已被单独列出。
- 几何相关量包括 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`elongation=3.75`、`sep_model=superellipse`、`f_shape_calc=0.85`。
- POPCON 扫描与当前运行点需要分开看：当前点参数里有 `Ti=46.42857142857143`、`B_e=7.285714285714286`；扫描则是在 `Ti-B_e` 平面上做了网格判定，且 `n_invalid=0` 表明已计算网格点中没有被标成无效点。

# 3. 风险与不确定性

- `valid=1` 只说明当前单个运行点在该模型设置下可计算且有效，不能直接推出扫描范围内存在可接受的连续区域。
- `ignited=0` 说明该点不是点火态；不能把这一点与 `best` 矩阵中的标记混为一谈。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是虽然提供了 `best` 矩阵，但未提供 `x/y` 数组，也未给出 `nx/ny`。
- 由于缺少 `x/y` 坐标数组，不能可靠描述最佳区的物理位置、边界或随 `Ti`、`B_e` 的变化趋势；目前只能确认 `best` 矩阵中存在至少一个被标记点。
- 本次结果依赖固定约束时间假设（`use_tauE=1`，`tauE=0.0015`），因此对 `Pfus`、`Qfus`、`ignited`、`Pwall` 的判读都带有该假设依赖性。
- 输入和输出里未提供 `geom_model`，因此不能据此判断几何模型是否采用更简化或更完整的处理。

# 4. 下一步建议

- 在 `Ti-B_e` 平面重新导出完整扫描数据，至少补齐 `x`、`y`、`nx`、`ny`。监控指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕当前点 `Ti=46.42857142857143`、`B_e=7.285714285714286` 做局部加密扫描，检查单点有效解附近是否连续。监控指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pheat`、`Pwall`。
- 对固定约束时间做敏感性验证，在 `use_tauE=1` 前提下扫描 `tauE` 取值，或补做采用约束时间缩放的对照算例。监控指标：`tau_E`、`Pfus`、`Qfus`、`ignited`、`Pwall`、`n_invalid`。
- 补做成分与辐射敏感性扫描，至少检查 `Zeff`、`fHe`、`fimp` 对 `Pbrem`、`Pcycl`、`P_line` 以及 `Pfus/Qfus` 的影响。监控指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`。
