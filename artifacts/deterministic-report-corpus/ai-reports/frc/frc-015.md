## 1. 核心结论
- 本次对象为 `config=frc`（场反位形 FRC），`preset=C-2W`。
- 当前运行点给出的状态是：`valid=1.0`、`ignited=0.0`；对应 `Pfus=34362.697704612205`、`Qfus=0.9461855571303611`、`Pheat=36317.08119581651`、`Pwall=5207.887124372905`。
- 这表示当前运行点在模型内被判为“有效”，但未被判为“点火”；两者不能等同。
- 本次结果明确依赖固定约束时间假设：`use_tauE=1.0`，且使用 `tau_E=0.0015`。
- POPCON 扫描与当前运行点需分开看：当前运行点 `valid=1.0` 只说明这一组参数可被模型接受；扫描部分虽然给出了 `best` 矩阵，且矩阵中存在 1，但由于缺少 `x/y` 数组且未提供 `nx/ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

## 2. 关键指标解读
- 运行点性能上，`Pfus=34362.697704612205` 与 `Qfus=0.9461855571303611` 表明该点有聚变功率输出，但 `ignited=0.0` 显示并未达到模型所标记的点火状态。
- 加热与损失相关量中，`Pheat=36317.08119581651`、`Ptrans=43139.52380952382`、`Pn=27490.158163689764`、`Pwall=5207.887124372905`，说明需要同时关注外加/总加热、输运功率通道与壁面负荷。
- 辐射与相关损失方面，`Pbrem=50.09672936268703`、`Pcycl=0.00019785244891879343`、`P_line=0.0`。按当前输入，杂质相关设置为 `fimp=0.0`、`Zimp=10`、`Zeff=1.0`、`fHe=0.0`，因此这一组结果对应的是非常洁净的成分设定。
- 约束时间与约束假设方面，输出给出 `tau_E=0.0015`、`tau_Bohm=0.006674285714285715`、`tau_classical=1.7981155232210142`，而参数中 `use_tauE=1.0`、`tauE=0.0015`，说明本次判读应以固定 `tau_E` 假设为前提，而不是由某个缩放律自洽求得。
- 等离子体与几何量方面，`beta=0.7777777777777777`、`nbar=7.150902649125436e+21`、`Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`，配合 `sep_model=superellipse`、`f_shape=0.85`、`elongation=3.75`，说明结果对当前几何和形状设定敏感。
- POPCON 扫描信息显示 `xkey=Ti`、`ykey=B_e`、`n_invalid=0`。这说明扫描维度名已知，且已记录网格中无无效点计数；但因未提供 `x`、`y`、`nx`、`ny`，不能据此判断最佳区面积、边界、连通性或趋势。

## 3. 风险与不确定性
- 最大的不确定性来自约束时间处理：`use_tauE=1.0` 使结果直接依赖固定约束时间假设，因此 `valid`、`ignited`、`Pfus`、`Qfus` 对 `tau_E` 设定可能敏感。
- 当前运行点“有效”不等于扫描已充分证明存在稳定工作窗；这里必须区分单点判定与参数扫描证据。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是虽然存在 `best` 矩阵，但缺少 `x/y` 数组，且未给出 `nx/ny`。
- 扫描中并非“当前准则下未找到最佳区”，因为 `best` 矩阵并非全 0；但由于缺少横纵坐标数组，仍无法说明最佳区对应的物理位置、范围或边界。
- 成分设定较理想化：`Zeff=1.0`、`fimp=0.0`、`fHe=0.0`、`P_line=0.0`。这会限制对杂质辐射、氦灰分累积和成分扰动影响的判断。
- 本次 JSON 未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo` 等归一化边界量，因此无法据此补充边界裕度解释。

## 4. 下一步建议
- 重新导出一份完整 POPCON 扫描结果，至少补齐 `x`、`y`、`nx`、`ny` 以及 `best_fraction`（若工具支持）；监控指标：`xkey/ykey`、`n_invalid`、`best`、`best_fraction`、`valid` 分布。
- 继续沿当前扫描维度 `Ti-B_e` 做加密扫描，重点检查当前点附近是否仍保持 `valid=1`，以及 `ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall` 如何变化；监控指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`。
- 针对固定约束时间假设做敏感性验证，围绕 `tauE=0.0015` 扫描 `tauE`；监控指标：`valid`、`ignited`、`Pfus`、`Qfus`、`tau_E`、`ntau`、`Ptrans`。
- 做成分与辐射扰动扫描，逐步改变 `fimp`、`Zimp`、`fHe`，检查当前“洁净成分”假设是否显著影响结论；监控指标：`Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。
- 做几何参数一致性验证，围绕 `f_shape=0.85`、`elongation=3.75`、`r_s=0.4`、`r_w=0.6`、`Rw=0.8` 进行参数扫描；监控指标：`Vp`、`Sw`、`Sp`、`beta`、`Pfus`、`Pwall`。
