1. 核心结论
- 本次对象为 `config=frc`（`config_label=场反位形 FRC`），`preset=C-2W`。
- 当前运行点结果：`valid=1.0`、`ignited=0.0`、`Pfus=47.7071184938202`、`Qfus=0.007017190960106788`、`Pheat=6798.606274937997`、`Pwall=504.45584190783416`。
- 这表示当前运行点在求解器标记下是有效点，但并未点火；同时 `Qfus` 明显小于 1，聚变输出相对加热输入占比很小。
- POPCON 扫描需要与当前运行点分开看：扫描轴为 `xkey=Ti`、`ykey=B_e`，`n_invalid=0`；提供的 `best` 矩阵并非全 0，说明当前准则下至少有一个离散网格点被标记为最佳。
- 但“当前运行点有效”不等于“扫描已经确认存在连续工作窗”；后者还需要完整扫描网格证据支撑。
- 由于 `use_tauE=1.0`，本次结果依赖固定约束时间假设。

2. 关键指标解读
- 运行点状态：`valid=1.0` 说明该单点计算成功并被模型接受；`ignited=0.0` 说明该点未达到点火状态，这两个标记不能混为一谈。
- 功率相关：当前点给出 `Pfus=47.7071184938202`、`Pheat=6798.606274937997`、`Qfus=0.007017190960106788`、`Pwall=504.45584190783416`，另有 `Pn=38.16569479505616`、`Ptrans=6808.095238095235`。
- 辐射相关：`Pbrem=0.05245700132701316`、`Pcycl=3.5401999894044433e-06`、`P_line=0.0`；在本次输入下，线辐射输出为 0，同时参数中 `fimp=0.0`、`fHe=0.0`、`Zeff=1.0`。
- 约束与假设：输出给出 `tau_E=0.0015`，参数中 `use_tauE=1.0`、`tauE=0.0015`，因此本次 `Pfus`、`Qfus` 与扫描判读都建立在固定 `tau_E` 的前提上；`H98`、`H_ITPA20`、`H_ISS04` 未提供，不能据此评价经验约束裕量。
- 边界与几何：可见 `beta=0.7777777777777777`、`nbar=2.3139709873647537e+20`、`Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`elongation=3.75`、`sep_model=superellipse`、`f_shape_calc=0.850000000000001`；`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 未提供，不能扩展到这些边界判读。
- POPCON 扫描：当前只知道扫描变量是 `Ti` 与 `B_e`，且 `best` 矩阵中仅有一个单元被标为 1、`n_invalid=0`；这说明扫描中至少有一个离散点满足当前“best”准则，但不能据此认定当前运行点本身就在该点上。

3. 风险与不确定性
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是未提供 `x`/`y` 扫描数组，也未给出 `nx`、`ny`，因此无法把 `best` 标记映射到明确的 `Ti`、`B_e` 数值位置。
- `valid=1.0` 只说明当前单点求解有效，不等于 POPCON 扫描已经找到可连续使用的参数区域；同样，`n_invalid=0` 也只说明扫描未报无效点，不等于“最佳区”范围清晰。
- 结果依赖固定约束时间假设：`use_tauE=1.0` 会使 `tau_E=0.0015` 成为核心前提，若该前提变化，`Pfus`、`Qfus`、`ignited` 与 `best` 判定都可能变化。
- 成分与辐射覆盖有限：当前参数中 `fHe=0.0`、`fimp=0.0`、`P_line=0.0`，因此这份结果对灰杂质、氦灰和线辐射敏感性的覆盖较弱。
- 缺少若干常见边界信息：未提供 `q/q95`、Greenwald/Sudo 比值或经验约束因子，0-D 证据无法补足这些方面的边界判断。

4. 下一步建议
- 重新执行 `Ti-B_e` 的 POPCON 扫描，并导出完整 `x`/`y` 数组以及 `nx`、`ny`、`best_fraction`；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`n_invalid`、`best`，先确认当前运行点是否落在被标记的最佳单元上。
- 在固定 `tau_E=0.0015` 附近做约束时间敏感性扫描，或在程序支持时对比固定 `tau_E` 与 `tauE_scaling` 模式；重点监控 `Pfus`、`Qfus`、`ignited`、`Pheat`、`Ptrans`、`Pwall`。
- 对成分参数做独立扫描，如 `fHe`、`fimp`、`Zimp`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`valid`，验证当前“低辐射”结果是否仅由零杂质输入造成。
- 对几何/形状输入做验证性扫描，如 `f_shape`、`r_s`、`l_s`、`r_w` 或其他可暴露的形状参数；重点监控 `Vp`、`Sw`、`beta`、`nbar`、`Pwall`、`valid`，检查最佳标记是否对几何设定敏感。
