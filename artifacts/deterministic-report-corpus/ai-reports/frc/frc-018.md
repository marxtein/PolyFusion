1. 核心结论
- `config=frc`（`config_label=场反位形 FRC`），`preset=C-2W`。
- 当前运行点输出为：`valid=1.0`，`ignited=0.0`，`Pfus=58.42897577084008`，`Qfus=0.022376034871813107`，`Pheat=2611.2300997726165`，`Pwall=196.70807326059295`。
- 就当前运行点而言，这是一个“计算有效但未标记为点火”的 0-D 解；`valid=1.0` 只说明该点求解成立，不等同于 POPCON 扫描已经找到可观的工作区。
- POPCON 扫描的键为 `xkey=Ti`、`ykey=B_e`；`best` 矩阵不是全 0，矩阵证据上有 1 个网格点被标记为 `1`，且 `n_invalid=0`。
- 由于 `use_tauE=1.0`，本次结果依赖固定约束时间假设，当前采用 `tauE=0.0015`。

2. 关键指标解读
- 运行点状态：`valid=1.0` 且 `ignited=0.0`，表示该参数点有有效输出，但输出未标记为点火状态。
- 功率相关输出：`Pfus=58.42897577084008`，`Qfus=0.022376034871813107`，`Pheat=2611.2300997726165`，`Pn=46.74318061667207`，`Ptrans=2622.8571428571427`，`Pwall=196.70807326059295`。
- 辐射与损失项：`Pbrem=0.058750964248345486`，`Pcycl=1.1053936081289878e-06`，`P_line=0.0`；当前 JSON 给出了这些损失通道，但没有额外图像或阈值信息可用于扩展判读。
- 约束时间与约束模型：输出中 `tau_E=0.0015`，参数中 `use_tauE=1.0`、`tauE=0.0015`；同时给出了 `tau_Bohm=0.001645714285714286`、`tau_classical=1.7981155232210142`，但本次结果实际依赖固定 `tauE` 设定，而不是从这些标度自动闭合得到。
- 成分与几何：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；几何相关输出有 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`sep_model=superellipse`、`elongation=3.75`、`f_shape=0.85`。
- POPCON 扫描：`best` 矩阵中仅见单个网格标记为 `1`，其余为 `0`，`n_invalid=0` 说明已记录到的扫描点未出现无效点；但未提供 `x`/`y` 扫描数组及 `nx`/`ny`，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：结果依赖固定约束时间假设，当前 `tauE=0.0015` 的设定若改变，`Pfus`、`Qfus`、`ignited` 和 `Pwall` 都可能随之变化。
- 单点 `valid=1.0` 与扫描结论必须分开看：前者只代表当前运行点有解，不能推出周边参数区同样有效，也不能推出存在宽的 POPCON 最佳区。
- 虽然 `best` 矩阵不是全 0，但由于缺少 `x`/`y` 数组以及 `nx`/`ny`，只能确认“至少有一个网格点被标记为最佳”，不能判断该标记点对应的实际坐标、区域边界或趋势方向。
- 成分设定较理想化于当前这组输入：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`P_line=0.0`；现有 JSON 没有给出杂质、氦灰或线辐射扰动后的敏感性结果。
- 常见的补充判据在这份 JSON 中未给出，例如 `H98/H_ITPA20/H_ISS04`、`betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`；因此不能据此进一步判断约束裕度或边界接近程度。

4. 下一步建议
1. 重新执行 `Ti-B_e` 的 POPCON 扫描，并显式导出 `x`、`y`、`nx`、`ny`；优先在当前运行点附近做更细网格。需要监控的指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
2. 针对固定约束时间假设做 `tauE` 敏感性扫描，至少围绕 `tauE=0.0015` 做局部扰动；如果工具支持，再对比固定 `tauE` 与标度闭合两种模式。需要监控的指标：`tau_E`、`Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pwall`。
3. 做成分与辐射复核扫描，优先扰动 `fHe`、`fimp`、`Zimp`，检查当前 `Zeff=1.0`、`P_line=0.0` 设定对结果的影响。需要监控的指标：`Zeff`、`Pbrem`、`Pcycl`、`P_line`、`Pfus`、`Qfus`、`Pwall`。
4. 若后续要继续收敛到更明确的候选点，可在保持 `config=frc`、`preset=C-2W` 不变的前提下，对当前主扫描变量 `Ti`、`B_e` 做局部加密，并同时记录几何相关输出。需要监控的指标：`Vp`、`Sw`、`beta`、`Pfus`、`Qfus`、`Pwall`。
