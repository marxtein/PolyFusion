# 1. 核心结论

- `config=frc`（场反位形 FRC），`preset=C-2W`。当前运行点结果为：`valid=1.0`、`ignited=0.0`、`Pfus=996.6850692391025`、`Qfus=0.03223446860420885`、`Pheat=30919.85419325217`、`Pwall=2351.701384264834`。
- 当前运行点与 POPCON 扫描需要分开解读：`valid=1.0` 只说明这一次 0-D 求解得到有效点，不等同于扫描已经识别出明确工作窗。
- POPCON 扫描给出 `xkey=Ti`、`ykey=B_e`、`n_invalid=0`；`best` 矩阵不是全 0，说明按当前准则至少有 1 个网格点被标记。
- 但扫描结果未提供 `x/y` 数组，且未给出 `nx/ny`，因此扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

# 2. 关键指标解读

- 约束与约简：`use_tauE=1.0`，`tau_E=0.0015`，因此本次结果依赖固定约束时间假设；未提供 `H98/H_ITPA20/H_ISS04`，不能用这些约束量补充判断。
- 运行点状态：`ignited=0.0` 且 `Qfus=0.03223446860420885`，说明该运行点在当前 0-D 结果中不是点火状态。
- 功率项：除 `Pfus`、`Pheat`、`Pwall` 外，还给出了 `Pn=797.3480553912821`、`Ptrans=31118.09523809524`、`P_ei=39215.315962261564`。
- 成分与辐射：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；辐射项为 `Pbrem=1.0959184216179891`、`Pcycl=5.05831320710299e-05`、`P_line=0.0`。这表明当前输入下线辐射未体现，辐射解读明显受成分设定影响。
- 边界与几何：`beta=0.7777777777777777`、`beta_null=1.0`、`nbar=1.0576580826908582e+21`；几何相关量包括 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`elongation=3.75`、`sep_model=superellipse`、`f_shape_calc=0.850000000000001`。
- POPCON 证据：`best` 矩阵中仅见单个标记点，其余为 0；这只能说明“在当前扫描判据下出现过被标记网格点”，不能据此推出最佳区边界、宽度或趋势。

# 3. 风险与不确定性

- 最大的不确定性来自 `use_tauE=1.0`：结果直接依赖固定 `tau_E=0.0015`，对约束时间假设敏感。
- 扫描证据不完整：缺少 `x/y` 数组且未给出 `nx/ny`，所以不能判断 `Ti-B_e` 扫描中的工作窗形状、连续性或参数敏感性。
- 成分设定较理想化：`fimp=0.0`、`fHe=0.0`、`Zeff=1.0` 且 `P_line=0.0`，使得辐射与稀释相关影响在当前结果中体现有限。
- `geom_model` 字段未提供，因此不能确认几何模型是否为简化模型；当前只能基于 `superellipse`、`Vp/Sw` 和 `geom_weighted=0.0` 做有限解读。
- 未提供 `q/q95`、`nbar_o_nGw`、`nbar_o_Sudo` 等边界比值，相关约束余量无法从这份 JSON 直接判断。

# 4. 下一步建议

- 执行一次更完整的 `Ti-B_e` 二维扫描，中心可放在当前参数 `Ti=129.28571428571428`、`B_e=8.857142857142858` 附近，并显式导出 `x`、`y`、`nx`、`ny`、`best` 或 `best_fraction`。需监控：`valid`、`ignited`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做 `tau_E` 敏感性扫描，或在工具支持时与约束时间标度结果对照。需监控：`tau_E`、`ntau`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`。
- 做成分与辐射验证扫描，重点检查 `fimp`、`Zimp`、`fHe` 对结果的影响。需监控：`Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`。
- 做几何输入一致性复核或替代几何设定对照。需监控：`Vp`、`Sw`、`elongation`、`f_shape_calc`、`beta`、`Pwall`。
