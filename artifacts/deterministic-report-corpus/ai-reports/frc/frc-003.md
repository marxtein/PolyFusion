1. 核心结论
- `config=frc`（`config_label=场反位形 FRC`），`preset=C-2W`。
- 当前运行点与扫描结论要分开看：当前运行点 `valid=1.0`，说明该点在本次 0-D 计算中可得到有效结果；但 `ignited=0.0`，说明当前点未点火。
- 当前运行点的核心输出为：`Pfus=395.9955687569865`，`Qfus=0.058634476545259856`，`Pheat=6753.6301522418835`，`Pwall=526.8047568305215`。
- 仅就当前运行点看，`Qfus` 很低且 `ignited=0.0`，对应的是“可计算但未点火、聚变增益偏低”的状态；这不等同于 POPCON 扫描已经找到可接受的参数区。
- POPCON 扫描给出 `xkey=Ti`、`ykey=B_e`、`n_invalid=0`，且 `best` 矩阵不是全 0：从给出的 8x8 `best` 矩阵看，仅有 1 个网格单元为 1，说明按当前 `best` 判据至少命中了一个扫描单元。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是 JSON 未提供 `x/y` 数组，且未见 `nx/ny`。

2. 关键指标解读
- 约束时间假设：`use_tauE=1.0`，`params.tauE=0.0015`，且输出 `tau_E=0.0015`；因此本次结果依赖固定约束时间假设，而不是从本 JSON 中可见的约束时间标度自洽推导。
- 功率相关：`Pfus=395.9955687569865`，`Pn=316.7964550055892`，`Pheat=6753.6301522418835`，`Ptrans=6808.095238095238`，`Pwall=526.8047568305215`。从这些数值关系看，当前点的外部加热/功率通量负担明显高于聚变输出，和 `Qfus=0.058634476545259856`、`ignited=0.0` 一致。
- 辐射与损失：`Pbrem=24.73401140121046`，`Pcycl=1.649683271192574e-05`，`P_line=0.0`。在已列出的辐射项中，轫致辐射是主要项，回旋辐射很小，谱线辐射在本次输入/输出下为 0。
- 成分设定：输入为 `fHe=0.0`、`fimp=0.0`、`Zimp=10`，输出 `Zeff=1.0`。就这组数据本身看，本次算例相当于没有 He 稀释、没有杂质分数的理想化成分设定，且对应的有效电荷数为 1。
- 运行点状态量：`B_e=4.142857142857142`，`Ti=5.0`，`Te=1.0`，`nbar=5.024622715420609e+21`，`beta=0.7777777777777777`，`beta_null=1.0`。JSON 未提供 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此无法按这些常见边界指标继续判读。
- 几何相关：输出有 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`elongation=3.75`、`sep_model=superellipse`、`f_shape_calc=0.850000000000001`。这些说明本次 0-D 结果使用了给定形状参数，但 JSON 未给出 `geom_model` 字段，不能据此判断是否采用了更简化或更完整的几何模型。
- POPCON 证据：`n_invalid=0` 说明扫描点并非因大量无效解而失真；但由于缺少 `x/y/nx/ny`，目前只能确认“有 1 个 `best` 命中单元”，不能把它映射成具体 `Ti-B_e` 坐标位置，也不能据此描述边界、连通性或趋势。

3. 风险与不确定性
- 最大不确定性来自 `use_tauE=1`：结果直接依赖固定 `tauE=0.0015` 的设定，若约束时间假设变化，`valid`、`ignited`、`Pfus`、`Qfus`、`Pwall` 都可能明显变化。
- 当前运行点有效，不代表扫描已找到宽裕区域；反过来，扫描中存在 1 个 `best` 单元，也不代表当前运行点就在该单元内。这两件事不能混为一谈。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。缺失的关键信息是 `x/y` 数组以及 `nx/ny`，因此无法判断最佳区在 `Ti-B_e` 平面上的实际位置、尺寸和连续性。
- `best` 的判据定义未在 JSON 中给出，因此只能说“按当前 `best` 标记规则命中 1 个单元”，不能进一步解释该单元满足了哪些具体工程条件。
- 成分与辐射设定较理想化：`fHe=0`、`fimp=0`、`P_line=0`。这意味着本次结果对 He 灰分、杂质稀释、线辐射增强的敏感性尚未从现有数据中体现出来。
- 关键边界类指标未齐全：未提供 `betaN/q95/nbar_o_nGw/LH_ratio` 等数据，因此不能对这些边界约束做进一步比较或风险排序。

4. 下一步建议
1. 补做完整的 `Ti-B_e` POPCON 导出：在下次扫描中同时输出 `x`、`y`、`nx`、`ny`、`best` 判据说明；重点监控 `best` 命中单元数、`n_invalid`、当前运行点是否落在 `best` 区、以及 `Pfus/Qfus/Pheat/Pwall/ignited/valid`。
2. 做 `tauE` 敏感性扫描：围绕当前固定值 `tauE=0.0015` 做多点扫描，直接验证结果对固定约束时间假设的依赖；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`、`Ptrans`、`Pbrem`。
3. 做局部加密扫描：围绕当前 `Ti=5.0`、`B_e=4.142857142857142` 进行更细网格扫描，检验单个 `best` 命中是否连续、是否只是离散孤点；重点监控 `best` 连通性、`n_invalid`、`beta`、`nbar`、`Pfus`、`Qfus`、`Pwall`。
4. 做成分/辐射验证任务：扫描 `fHe`、`fimp` 和必要时的 `Zimp`，检查理想化成分假设对结果的影响；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`、`ignited`。
