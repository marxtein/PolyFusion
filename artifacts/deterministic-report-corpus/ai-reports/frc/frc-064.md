我先按运行点和扫描两条线分开读，避免把“这个点算出来有效”与“扫描里存在候选区”混在一起。接下来会严格只引用 JSON 里已有字段。
1. 核心结论

- `config=frc`（`config_label=场反位形 FRC`），`preset=C-2W`。
- 当前运行点结果：`valid=1.0`，`ignited=0.0`，`Pfus=2351.1431`，`Qfus=0.041501`，`Pheat=56652.5205`，`Pwall=4347.5577`。
- 这表示本次**单个运行点**在 0-D 判据下被标记为有效，但**未点燃**。`valid=1` 只说明这个运行点通过了当前模型判据，不能替代 POPCON 扫描结论。
- POPCON 扫描信息显示：`xkey=Ti`，`ykey=B_e`，`n_invalid=0`。`best` 矩阵并非全 0，说明当前准则下至少有被标记的网格单元；但由于缺少 `x/y` 数组，且未给出 `nx/ny`，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

2. 关键指标解读

- 运行点功率关系：`Pfus=2351.1431`，`Pheat=56652.5205`，对应 `Qfus=0.041501`；从数值上看，当前点的聚变输出相对加热功率较低，且 `ignited=0.0`。
- 损失项中，`Ptrans=57120.0` 明显高于显式辐射项；辐射相关输出为 `Pbrem=2.74896`、`Pcycl=0.000136`、`P_line=0.0`。
- 约束方面：`use_tauE=1.0`，`tauE=0.0015`，输出 `tau_E=0.0015`。因此，**结果依赖固定约束时间假设**。本次数据未提供 `H98`、`H_ITPA20`、`H_ISS04` 等约束因子，不能进一步比较不同约束标度。
- 边界/等离子体状态方面：输出给出 `beta=0.7777778`、`beta_null=1.0`；但未提供 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此边界判读信息不完整。
- 成分与辐射方面：`Zeff=1.0`，输入 `fHe=0.0`、`fimp=0.0`、`Zimp=10`；本次结果中 `P_line=0.0`，对应当前这组输入下未体现线辐射贡献。
- 几何方面：输出给出 `Vp=1.28177`、`Sw=13.57168`、`Sp=6.97434`，形状模型为 `sep_model=superellipse`，`elongation=3.75`，`f_shape_calc=0.85`。数据中未见 `geom_model` 字段，因此这里只能按已给几何量解读，不额外判断几何模型简化程度。

3. 风险与不确定性

- 最主要的不确定性来自 `use_tauE=1`：当前结果绑定在固定 `tauE=0.0015` 假设上，对约束时间变化的敏感性尚未展开。
- `valid=1.0` 与 `ignited=0.0` 同时出现，说明“当前运行点有效”不等于“达到点火”，也不等于扫描里已经明确找到可接受工作窗。
- 虽然 `best` 矩阵里存在非零单元，但缺少 `x/y` 数组及 `nx/ny`，因此不能说明最佳区的位置、边界、连通性或趋势；只能确认扫描里存在被标记单元、且 `n_invalid=0`。
- 本次未提供更完整的边界指标与约束标度因子，导致对 beta、密度和安全因子相关限制的解释空间有限。
- 输入取 `fHe=0`、`fimp=0`，会使当前成分/辐射结论更依赖这组理想化输入；若后续引入氦灰或杂质，`Pbrem/P_line` 与整体功率平衡可能变化。

4. 下一步建议

- 先补齐同一组 `Ti-B_e` POPCON 扫描输出：至少导出 `x`、`y`、`nx`、`ny`、`best_fraction`（若有）以及现有 `best`、`n_invalid`。监控指标：`valid`、`ignited`、`best`/`best_fraction`、`n_invalid`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 做 `tauE` 敏感性扫描，因为 `use_tauE=1`。可用 `tauE-B_e` 或 `tauE-Ti` 二维扫描验证当前点对固定约束时间假设的依赖。监控指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`、`beta`。
- 做成分与辐射敏感性扫描：以 `fHe`、`fimp` 为扫描轴，检查当前 `P_line=0` 是否只由输入设定导致。监控指标：`Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`valid`。
- 做几何参数校核/扫描：围绕 `r_s`、`l_s`、`f_shape` 复算 `Vp`、`Sw`、`Sp` 与 `Pwall` 的变化，确认壁负荷结果对几何输入的敏感性。监控指标：`Vp`、`Sw`、`Sp`、`Pwall`、`valid`、`Pfus`。
