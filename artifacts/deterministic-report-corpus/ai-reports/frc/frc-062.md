# 1. 核心结论

- 本次数据的 `config=frc`（场反位形 FRC），`preset=C-2W`。当前运行点输出为：`valid=1.0`，`ignited=0.0`，`Pfus=697.7963`，`Qfus=0.0225246`，`Pheat=30979.3519`，`Pwall=2334.0624`。
- 就当前运行点而言，这是一组被模型标记为有效的 0-D 解，但未被标记为点火；同时 `Qfus` 数值较低，说明该点的聚变增益在当前设定下不高。
- POPCON 扫描需要单独看：本次扫描的坐标键为 `xkey=Ti`、`ykey=B_e`，`n_invalid=0`。`best` 矩阵中存在 1 个被标记为最佳的网格单元，因此不能把“运行点 `valid=1`”直接等同于“扫描上已经找到明显工作窗”。
- 由于本 JSON 未提供 `nx/ny`，也未提供对应的 `x/y` 坐标数组，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。

# 2. 关键指标解读

- 功率与状态：`Pfus=697.7963`，其中 `Pn=558.2371`；外加/总加热相关输出为 `Pheat=30979.3519`，对应 `Qfus=0.0225246`。`ignited=0.0` 表明该点未被模型标记为点火状态。
- 损失与辐射：`Ptrans=31118.0952`，`Pbrem=0.815864`，`Pcycl=4.6986e-05`，`P_line=0.0`。按这组输入，线辐射未出现，辐射项主要体现为韧致辐射，回旋辐射很小。
- 约束时间：`use_tauE=1.0`，且 `tauE=0.0015`、运行输出 `tau_E=0.0015`。这意味着**结果依赖固定约束时间假设**，不是由本次结果中可见的约束时间标度自行决定。
- 等离子体与边界相关量：可见 `beta=0.7777778`，`nbar=9.1257e20`，`ne0=1.2902e21`，`ni0=1.2902e21`。但本 JSON 没有给出 `betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此这里只能报告已有量，不能扩展成边界裕度判断。
- 成分与杂质：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。这组结果对应的是无 He 灰、无杂质分数的输入假设。
- 几何：`Vp=1.28177`，`Sw=13.57168`，`Sp=6.97434`，`sep_model=superellipse`，`f_shape=0.85`。这些量说明本次 0-D 计算使用了给定形状参数和超椭圆分离面模型。

# 3. 风险与不确定性

- 当前结果对约束时间非常敏感，因为 `use_tauE=1` 且 `tauE` 被固定为 `0.0015`；本 JSON 也未给出 `H98/H_ITPA20/H_ISS04` 或 `tauE_scaling` 的对照证据。
- `valid=1` 只说明当前运行点在模型内成立，`ignited=0` 说明未点火；这两者都不能替代 POPCON 扫描对参数空间的判断。
- 虽然 `best` 矩阵中有 1 个最佳单元，但没有 `x/y` 数组和 `nx/ny`，因此**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**；也不能可靠描述最佳区的位置、边界或延展方向。
- 本次成分假设为 `fHe=0`、`fimp=0`、`Zeff=1.0`，且 `P_line=0.0`；因此该结果主要反映当前理想化成分输入下的表现，不能替代杂质或 He 灰敏感性验证。
- JSON 中未提供 `LH_ratio` 或 L-H 阈值比较结果，因此不能对 L-H 阈值模型比较作任何判断。

# 4. 下一步建议

1. 重新导出完整 POPCON 扫描坐标，明确给出 `x/y/nx/ny`，并继续以 `Ti-B_e` 为主轴做加密扫描。  
监控指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 以当前点为中心做 `tauE` 敏感性扫描，或补充可用的 `tauE_scaling/H` 类输出，与固定 `tauE=0.0015` 的结果对照。  
监控指标：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`。

3. 增加成分与辐射验证扫描，至少扫 `fHe`、`fimp`、`Zimp`。  
监控指标：`Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`valid`。

4. 补充边界与几何相关输出后再复判当前点，尤其是归一化 beta、磁安全因子和密度比值类量。  
监控指标：`beta/betaN/betaT/betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`、`Vp`、`Sw`、`Pwall`。
