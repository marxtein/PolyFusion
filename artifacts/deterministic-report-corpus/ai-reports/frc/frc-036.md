1. 核心结论
- 本次算例 `config=frc`（场反位形 FRC），`preset=C-2W`。
- 当前运行点结果为 `valid=1.0`、`ignited=0.0`、`Pfus=415.54718738805883`、`Qfus=0.032288852290895446`、`Pheat=12869.679716216842`、`Pwall=978.8933017621089`。
- 就单个运行点看，模型给出了有效解，但不是点火态；同时 `Qfus` 很低，说明该点的功率平衡仍明显依赖外加加热。
- POPCON 扫描需要与单点运行分开理解：`last_scan` 的扫描轴是 `xkey=Ti`、`ykey=B_e`，`n_invalid=0`，且 `best` 矩阵并非全零，说明扫描结果里至少出现了被标记为 `best` 的网格点；这不等同于“当前运行点有效”。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0` 且 `tauE=0.0015`，因此本次结果依赖固定约束时间假设。

2. 关键指标解读
- **当前运行点** `valid=1.0` 只能说明这组输入下模型返回了有效解，`ignited=0.0` 则表明当前点不是点火态；这两个标记只对应当前运行点，不代表扫描中存在连续可用区域。
- **功率与输运** `Pfus=415.54718738805883`，但 `Qfus=0.032288852290895446` 且 `Pheat=12869.679716216842`，当前点更像是强外加加热支撑的状态；`Ptrans=12952.380952380954` 与 `Pheat` 接近，建议把输运损失与加热需求一起看。
- **壁负荷与中子功率** `Pwall=978.8933017621089`、`Pn=332.4377499104471`，说明当前解不仅要看聚变功率，还要同时关注壁面相关负担与中子通道。
- **辐射与成分** 当前输出中 `Pbrem=0.40818810215464607`、`Pcycl=1.3211343978617513e-05`、`P_line=0.0`；结合输入 `Zeff=1.0`、`fimp=0.0`、`fHe=0.0` 看，当前模型设定下显式线辐射没有贡献，轫致辐射大于回旋辐射。
- **约束与几何量** `tau_E=0.0015` 为直接指定值，`beta=0.7777777777777777`，几何相关量给出了 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`elongation=3.75`、`f_shape=0.85`；这些量会共同影响功率密度、壁负荷和约束结果。
- **POPCON 扫描** 扫描对象是 `Ti` 与 `B_e`，`n_invalid=0` 表明记录到的扫描点没有被标成无效点；但未提供 `x/y` 数组和 `nx/ny`，因此不能把 `best` 的非零结果进一步解释成最佳区的位置、边界或趋势。

3. 风险与不确定性
- `use_tauE=1.0` 使结果对固定 `tauE=0.0015` 假设敏感；如果约束时间设定变化，`valid`、`ignited`、`Pfus`、`Qfus` 和 `Pheat` 都可能随之改变。
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势；虽然 `best` 矩阵里有非零元素，但缺少 `x/y` 和 `nx/ny` 后，无法确认这是孤立点、狭窄带还是更连续的区域。
- 当前边界信息不完整：JSON 中没有 `q`/`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 等归一化边界字段，因此对稳定性和密度裕量的判断不能展开。
- `P_line=0.0` 是当前成分设定下的模型结果，不代表成分变化后仍可忽略线辐射；若后续引入 `fimp` 或 `fHe`，辐射分配可能变化。
- `n_invalid=0` 只说明扫描点没有被判为无效，并不等于扫描已经找到了宽广或不敏感的工作窗；这一点不能与单点 `valid=1.0` 混同。

4. 下一步建议
- 重新导出一次完整 POPCON 数据，至少补齐 `x`、`y`、`nx`、`ny`，并保持 `Ti`/`B_e` 为扫描轴；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕当前点 `Ti=87.85714285714286`、`B_e=5.714285714285714` 做更细局部扫描，确认 `best` 非零结果是否只出现在很少数网格；重点监控 `Pfus`、`Qfus`、`Ptrans`、`Pwall`、`beta`、`nbar`。
- 由于本次使用固定约束时间，单独做 `tauE` 敏感性扫描，比较 `tauE=0.0015` 附近变化对结果的影响；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`。
- 做成分/辐射验证扫描，逐步改变 `fHe`、`fimp`，必要时联动检查 `Zeff` 设定；重点监控 `Pbrem`、`P_line`、`Pcycl`、`Pwall`、`Qfus`、`valid`。
- 若后处理可输出更多边界指标，补充 `q`/`q95` 与归一化密度类字段，再复核当前点与扫描点；重点监控 `beta`、密度归一化指标、`valid`、`best` 标记的一致性。
