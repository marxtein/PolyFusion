1. 核心结论
- `config=frc（场反位形 FRC）`，`preset=C-2W`；当前运行点 `valid=1.0`、`ignited=0.0`，`Pfus=319.4792`、`Qfus=0.015219`、`Pheat=20992.1920`、`Pwall=1570.3045`。
- 这表示本次单点计算在模型内是有效的，但该点未点火；`valid=1.0` 不能替代对扫描工作窗的判断，`ignited=0.0` 也只对应当前输入点。
- 当前运行点对应的主要输入是 `Ti=150.0`、`B_e=7.285714285714286`、`Te=1.0`、`tauE=0.0015`，因此上述输出只说明这一组参数下的 0-D 结果。
- POPCON 扫描单独看：`xkey=Ti`、`ykey=B_e`，`n_invalid=0`；返回的 `best` 矩阵并非全零，说明至少有一个网格单元被当前判据标记为最佳，但这不等同于已经确认了连续或宽裕的工作窗。

2. 关键指标解读
- 功率侧：`Qfus=0.015219` 明显小于 1，说明当前点的聚变增益在模型输出中偏低；同时 `Pheat=20992.1920`、`Pfus=319.4792`，表明外加加热量级远大于聚变功率输出。
- 损失侧：`Ptrans=21055.7143` 与 `Pheat=20992.1920` 接近；辐射相关输出中 `Pbrem=0.3735`、`Pcycl=0.0000237`、`P_line=0.0`，当前设定下线辐射项未体现为非零输出。
- 约束侧：`use_tauE=1.0`、`tau_E=0.0015`，结果依赖固定约束时间假设；本次数据未提供 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling`，因此不能从经验约束标度角度补充判断。
- 边界与等离子体状态：可见 `beta=0.7778`、`beta_null=1.0`、`nbar=6.1748e+20`、`ntau=1.0185e+18`；但未提供 `q`/`q95`、`betaN`/`betaT`/`betap`、`nbar_o_nGw` 或 `nbar_o_Sudo`，边界约束证据不完整。
- 成分与辐射：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`；在这组输入下，杂质分数为零且 `P_line=0.0`，说明本次结果对线辐射的体现有限。
- 几何侧：当前输出给出 `Vp=1.2818`、`Sw=13.5717`、`Sp=6.9743`、`elongation=3.75`、`sep_model=superellipse`；这些量可用于后续交叉检查壁负荷与体积/表面积缩放关系。

3. 风险与不确定性
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- 之所以存在上述不确定性，是因为虽然提供了 `best` 矩阵，但未提供 `x`/`y` 数组和显式 `nx`/`ny`；因此无法给出最佳区坐标、面积占比、边界或随 `Ti`/`B_e` 的趋势。
- `best_fraction` 未提供，所以不能量化最佳区占比；但由于 `best` 矩阵并非全零，本次也不能写成“当前准则下未找到最佳区”。
- `use_tauE=1.0` 使结果对固定 `tau_E=0.0015` 的设定敏感；这意味着 `Pfus`、`Qfus`、`ignited` 等关键结论都带有该假设依赖性。
- 缺少 `q`/`q95`、归一化密度比、更多边界判据和完整扫描坐标，使得对约束主导项的识别仍不充分；当前只能做单点和有限扫描回执层面的解释。

4. 下一步建议
- 重新导出一次完整 POPCON 扫描，明确保存 `x`、`y`、`nx`、`ny`、`best_fraction`，并保持 `xkey=Ti`、`ykey=B_e`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 在当前运行点附近做 `tauE` 离散敏感性扫描，因为 `use_tauE=1.0`；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`，确认结论对固定约束时间的依赖程度。
- 增加成分/辐射验证扫描，例如对 `fimp` 与 `Zimp` 做离散变化；重点监控 `Zeff`、`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`，核对当前“零线辐射输出”是否仅由输入设定导致。
- 若模型支持，补充导出边界相关量后再扫一次 `Ti-B_e` 平面；重点监控 `beta`、`q`/`q95`、归一化密度比、`valid`、`ignited`，把“单点有效”与“扫描中哪些约束先触发”区分开。
