## 1. 核心结论

- `config=frc`，`preset=C-2W`。
- 当前运行点：`valid=1.0`，`ignited=0.0`，`Pfus=200.71194035231133`，`Qfus=0.029655385898721992`，`Pheat=6768.144614195058`，`Pwall=513.4851705345225`。
- 这表示本次 **单点计算结果有效**，但 **未点火**；同时 `Qfus` 数值较低，当前运行点仍表现为强外部加热主导。
- POPCON 扫描与单点有效性需要分开看：扫描维度标识为 `xkey=Ti`、`ykey=B_e`，`n_invalid=0`，且 `best` 矩阵并非全 0，说明在该扫描判据下至少出现了被标记为“best”的网格单元；但由于未提供 `x/y` 扫描数组，**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- `use_tauE=1.0`，因此本次结果 **依赖固定约束时间假设**（`tauE=0.0015`）。

## 2. 关键指标解读

- 运行点功率结构：`Pfus=200.71194035231133`，其中 `Pn=160.56955228184907`；而 `Pheat=6768.144614195058`、`Ptrans=6808.095238095238`，说明当前点的功率平衡主要受外部加热与传输功率约束影响。
- 辐射与损失：`Pbrem=0.1917592751329951`，`Pcycl=4.895150246217375e-06`，`P_line=0.0`。就已给数据看，线辐射未体现，轫致与回旋辐射数值相对较小。
- 约束时间与约束假设：`tau_E=0.0015`，`tau_Bohm=0.0026514285714285715`，`tau_classical=1.798115523221014`，`tauC_eff=30614.735834102034`。由于 `use_tauE=1.0`，这里应优先把结果理解为在给定 `tauE` 下的 0-D 平衡输出，而不是自洽预测出的约束性能。
- 等离子体与边界相关量：`beta=0.7777777777777777`，`beta_null=1.0`，`nbar=4.424196101628209e+20`，`ne0=6.254937717801653e+20`，`ni0=6.254937717801653e+20`。这些量给出了当前点的高压强/高密度背景，但本 JSON 未给出额外稳定性阈值字段，不能据此外推更强结论。
- 成分与辐射设定：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。当前输入中杂质分数为 0，且 `P_line=0.0`，因此该点对杂质线辐射的体现有限。
- 几何量：`Vp=1.2817698026646358`，`Sw=13.571680263507906`，`Sp=6.974335690969341`，分离面模型为 `superellipse`。`geom_weighted=0.0` 已给出，但 JSON 中没有显式 `geom_model` 字段，因此这里只能说明采用了当前输入对应的几何设定，不能替代为明确的几何模型编号解释。
- POPCON 扫描：`best` 矩阵中仅见少量标记，且 `n_invalid=0` 说明扫描中没有被记录为无效的网格点；但缺少 `nx/ny` 与 `x/y` 数组，不能据此描述最佳区边界、面积占比、位置漂移或趋势。

## 3. 风险与不确定性

- **单点有效不等于扫描存在清晰工作窗**：`valid=1.0` 只说明当前运行点算例有效；是否存在可用参数区域，需要看扫描坐标数组和判据分布，本数据不足以给出工作窗形状结论。
- **扫描证据不完整**：虽然有 `best` 矩阵和 `xkey/ykey`，但没有 `x/y` 数组，也没有 `nx/ny`。因此必须保持结论克制：**扫描网格证据不足，不能判断工作窗形状或敏感性趋势**。
- **结果依赖固定约束时间假设**：`use_tauE=1.0` 使当前 `Pfus`、`Qfus`、`Pwall` 等输出对 `tauE=0.0015` 敏感；若约束时间假设变化，结果可能随之明显变化。
- **未点火**：`ignited=0.0`，因此不能把当前点解读为自持燃烧状态。
- **杂质与辐射表征有限**：`fimp=0.0`、`P_line=0.0`，意味着该结果对杂质线辐射与成分扰动的覆盖有限。
- **几何解释边界**：没有显式 `geom_model` 字段，不能把当前几何处理进一步解释为某个已知简化/修正模型等级。

## 4. 下一步建议

1. 做一轮可复现的 `Ti-B_e` 加密扫描，补齐并导出 `x/y` 数组以及 `nx/ny`。  
   重点监控：`best` 分布、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 在当前点附近对 `tauE` 做参数扫描，而不是只固定 `tauE=0.0015`。  
   重点监控：`Qfus`、`ignited`、`Ptrans`、`Pfus`、`Pwall` 对 `tauE` 的响应。

3. 做成分敏感性验证，至少扫描 `fimp`、`fHe`，并记录 `Zeff` 变化。  
   重点监控：`P_line`、`Pbrem`、`Pfus`、`Qfus`、`Pwall`、`valid`。

4. 对当前高功率负荷点补做壁负荷与功率平衡核查。  
   重点监控：`Pwall`、`Pheat`、`Ptrans`、`Pn`、`Pbrem`、`Pcycl`，并与 `Ti`、`B_e` 扫描结果联动检查。
