1. 核心结论
- 本次报告对应 `config=frc`（场反位形 FRC），`preset=C-2W`。
- 当前运行点结果为：`valid=1.0`，`ignited=0.0`，`Pfus=1919.8262353766015`，`Qfus=0.09286373874152451`，`Pheat=20673.583267202026`，`Pwall=1664.7466683494392`。
- 这表示当前参数点得到了一个有效的 0-D 求解结果，但没有点火；这里的 `valid=1.0` 只说明“当前运行点可解且通过模型内部有效性判定”，不等同于 POPCON 扫描已经证明存在可靠工作窗。
- POPCON 扫描需要单独看：扫描轴是 `xkey=Ti`、`ykey=B_e`，返回 `n_invalid=0`，且 `best` 矩阵不是全零，说明当前准则下至少有被标记的候选格点。
- 但本次扫描未提供 `nx/ny` 和 `x/y` 数组，因此必须写明：扫描网格证据不足，不能判断工作窗形状或敏感性趋势；也不能把 `best` 标记精确对应到物理坐标。

2. 关键指标解读
- 当前运行点输入参数中，`Ti=67.14285714285714`、`B_e=7.285714285714286`；对应输出里 `Pfus` 明显小于 `Pheat`，`Qfus=0.09286373874152451`，说明该点的融合输出相对加热输入仍偏低。
- 功率损失结构里，`Ptrans=21055.714285714286` 显著大于辐射项；辐射相关量为 `Pbrem=1.8341932554232538`、`Pcycl=3.530763674431939e-05`、`P_line=0.0`，可见本次返回结果中损失主要体现为输运项，辐射里以轫致辐射为主。
- 约束时间方面，`use_tauE=1.0`，参数里 `tauE=0.0015`，输出里 `tau_E=0.0015`；因此本次结果依赖固定约束时间假设，而不是由本次报告中可见的约束时间标度自洽给出。
- 成分与辐射假设较“干净”：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`，并且 `P_line=0.0`；在这个设定下，杂质线辐射未进入当前运行点结果。
- 几何相关输出包括 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`elongation=3.75`、`f_shape_calc=0.85`、`sep_model=superellipse`；这些量说明模型包含了形状与表面积/体积效应，但本 JSON 未给出 `geom_model`，无法进一步判断几何模型层级。
- POPCON 部分只能做有限解读：`best` 矩阵中仅见很少量被标记格点，且 `n_invalid=0`；这说明扫描结果里没有被标记为无效的点，但“当前运行点有效”与“扫描中存在 best 标记”是两件事，不能混为一谈。

3. 风险与不确定性
- 最大不确定性来自 `use_tauE=1.0`：由于结果直接依赖固定 `tauE=0.0015`，任何对 `Pfus`、`Qfus`、`ignited` 的判断都对该假设敏感。
- 扫描证据不完整：缺少 `nx/ny` 与 `x/y` 数组，即使 `best` 矩阵存在标记，也不能判断候选区是否连续、是否狭窄、是否对 `Ti` 或 `B_e` 更敏感。
- 本报告未提供常见归一化边界量，如 `betaN`、`betaT`、`betap`、`q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`；因此不能把当前点放到更完整的稳定性/密度边界框架下解读。
- 辐射与成分假设偏理想化：`fHe=0.0`、`fimp=0.0`、`Zeff=1.0` 使得当前 `P_line=0.0`，因此对辐射负担和壁负荷的判断依赖这组输入，不宜外推到含灰分或杂质更高的情况。
- 几何信息不完整：虽然有 `Vp/Sw/Sp` 和形状参数，但没有 `geom_model` 字段，不能确认是否使用了更简化或更完整的几何模型；这会影响对 `Pwall` 与体表比相关结果的解释边界。

4. 下一步建议
1. 重新执行同一组 `Ti-B_e` 的 POPCON 扫描，并完整导出 `x`、`y`、`nx`、`ny`、`best`、`valid` 矩阵；重点监控 `n_invalid`、`best` 标记数量、`Pfus`、`Qfus`、`Pwall`、`ignited`，先把“当前点有效”与“扫描候选区范围”分开证实。
2. 围绕当前 `tauE=0.0015` 做约束时间敏感性扫描；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`ntau`、`Pwall`，用来验证本次固定约束时间假设对结论的影响。
3. 在保持主配置不变的前提下，对 `fimp`、`fHe`、`Zeff`、`Zimp` 做成分/辐射验证扫描；重点监控 `Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`、`valid`，确认当前“零线辐射”结论是否只由输入假设造成。
4. 做一组几何/形状验证任务，优先扫描 `f_shape`、`elongation`、`r_s`、`l_s`，并在输出中补充 `geom_model`（若程序支持）；重点监控 `Vp`、`Sw`、`Sp`、`Pwall`、`Pfus`、`beta`、`valid`，检查壁负荷与体表比对结果的影响。
