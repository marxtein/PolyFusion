1. 核心结论
- 本次结果对应 `config=frc`（场反位形 FRC），`preset=C-2W`；当前运行点 `valid=1.0`，说明该 0-D 计算点被模型判为有效；但 `ignited=0.0`，说明未达到点火状态。
- 当前运行点的关键功率量级为：`Pfus=259.287`、`Qfus=0.0201`、`Pheat=12900.792`、`Pwall=969.672`。按这些结果看，聚变输出已非零，但增益很低，且壁负荷数值较高。
- 运行点与 POPCON 扫描需要分开看：当前运行点“有效”只说明这一个参数点可算且满足模型内部判据；不等于 POPCON 扫描已经证明存在明确、宽裕或稳定的工作窗。
- POPCON 方面，扫描轴键为 `xkey=Ti`、`ykey=B_e`，`n_invalid=0`；`best` 矩阵中存在 1 个标记点，因此不能写“当前准则下未找到最佳区”。但由于缺少 `x/y` 扫描数组且未给出 `nx/ny`，仍应认定：扫描网格证据不足，不能判断工作窗形状或敏感性趋势。
- `use_tauE=1.0`，说明结果依赖固定约束时间假设；因此对功率平衡、增益和扫描判据的解读，都建立在 `tauE=0.0015` 被直接指定的前提下。

2. 关键指标解读
- 运行点功率平衡：`Pfus=259.287`，其中中子功率 `Pn=207.430`；但 `Pheat=12900.792`、`Ptrans=12952.381`，同时 `Qfus=0.0201` 很低，表明当前点对外加/总加热的依赖很强，聚变输出相对加热输入占比较小。
- 约束与点火相关：`ignited=0.0` 与低 `Qfus` 一致；`tau_E=0.0015` 且 `use_tauE=1.0`，表示没有通过缩放律自洽求得能量约束，而是采用固定值。`tauC_eff=14903.869`、`tau_Bohm=0.003657`、`tau_classical=1.798` 已给出，但当前判读应以实际使用的 `tau_E` 为主。
- 辐射与损失：`Pbrem=0.2684`、`Pcycl=1.19e-05`、`P_line=0.0`。在已给数据里，线辐射被设为零，回旋辐射很小，轫致辐射非零但相对 `Pheat` 很小；不能据此外推未提供杂质辐射情形。
- 等离子体成分：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。这表示当前计算采用了非常理想化的低杂质设定；其中 `Zimp` 给出，但由于 `fimp=0.0`，杂质分数本次并未实际参与成分占比。
- 几何与体表参数：`Vp=1.282`、`Sw=13.572`、`Sp=6.974`，壁负荷结果 `Pwall=969.672` 与几何表面积直接相关。`sep_model=superellipse`，`elongation=3.75`，`f_shape_calc=0.85`，说明该点对形状参数有显式依赖。
- 扫描信息：`best` 矩阵并非全零，说明在当前判据下至少有一个网格点被标记为“最佳”；`n_invalid=0` 说明扫描中没有被判无效的点。但因为未提供 `x/y` 数组与 `nx/ny`，不能可靠说明该点位于何处，也不能描述最佳区边界、延展方向或趋势。

3. 风险与不确定性
- 最大的不确定性来自固定约束时间：`use_tauE=1.0` 且 `tauE=0.0015`，这意味着 `Pfus`、`Qfus`、`ignited` 以及扫描判据都明显依赖这一假设；若 `tauE` 改变，结论可能显著变化。
- 当前运行点有效，不代表扫描已充分证明存在清晰工作窗。这里虽然 `best` 矩阵有 1 个标记点，但扫描数组缺失、`nx/ny` 未给出，因此仍属于“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 成分设定较理想化：`Zeff=1.0`、`fimp=0.0`、`fHe=0.0` 会弱化辐射和稀释效应；因此当前结果对更真实杂质/灰分条件的代表性有限。
- `Pwall=969.672` 数值本身提示需要重点跟踪壁负荷，但仅凭这份 0-D 结果，不能推导壁材料适用性、热工裕量或工程可实施性。
- `P_line=0.0` 是当前输入/模型输出的一部分，不应被理解为现实中线辐射必然可忽略；它只说明这次计算没有体现该通道的贡献。
- 本次未提供 betaN、q95、nbar_o_nGw、LH_ratio 等常见边界/阈值指标，因此不能补充这些方向的边界判断，也不能对模式进入或维持作出保证式表述。

4. 下一步建议
- 做 `tauE` 敏感性扫描：围绕当前 `tauE=0.0015` 做多点扫描，并同步监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`、`Pbrem`，用于验证当前结论对固定约束时间假设的依赖程度。
- 补全 POPCON 网格输出：在 `Ti-B_e` 扫描中明确导出 `x`、`y`、`nx`、`ny` 以及 `best_fraction` 或等价统计，并监控 `n_invalid`、`best` 标记数量、`Pfus`、`Qfus`、`Pwall`，以便判断是否真的存在连续工作窗，而不是孤立网格点。
- 做成分与辐射扰动扫描：对 `fimp`、`Zimp`、`fHe` 做参数扫描，并监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Qfus`、`Pheat`、`valid`，检查当前“低杂质、零线辐射”设定对结果的影响。
- 做几何参数复核扫描：围绕 `f_shape`、`elongation`、`r_s`、`l_s`、`r_w` 扫描，并监控 `Vp`、`Sw`、`Sp`、`Pwall`、`Pfus`、`Ptrans`，确认壁负荷和功率平衡对几何设定的敏感性。
- 若后续要比较不同运行点，建议固定一组统一监控指标：`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`、`Pwall`、`tau_E`、`Pbrem`、`P_line`、`Zeff`、`n_invalid` 与 `best` 标记数量。
