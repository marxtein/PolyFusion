1. 核心结论
- `config=frc`（`config_label=场反位形 FRC`），`preset=C-2W`。
- 当前运行点：`valid=1.0`、`ignited=0.0`、`Pfus=855.8305845497899`、`Qfus=0.12892554788588506`、`Pheat=6638.1768282831345`、`Pwall=552.1797793146602`。这说明该单点在模型内可解，但不是点火态。
- 运行点和 POPCON 扫描需要分开解释：`valid=1.0` 只说明当前这一个运行点有效；是否存在扫描意义上的“最佳”点，要看 `last_scan.best` 等扫描证据，不能把两者混为一谈。
- POPCON 扫描给出 `xkey=Ti`、`ykey=B_e`、`n_invalid=0`，且 `best` 矩阵并非全 0，说明在当前扫描判据下至少有被标记的点。
- 但本次扫描未提供 `x/y` 数组和 `nx/ny`，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- 由于 `use_tauE=1.0`，结果依赖固定约束时间假设；当前直接采用 `tauE=0.0015`。

2. 关键指标解读
- 功率状态：当前点 `Pfus=855.8305845497899`，`Pheat=6638.1768282831345`，`Qfus=0.12892554788588506`，并且 `ignited=0.0`；从这组结果看，当前点不是点火态，且聚变输出相对加热输入偏低。
- 损失与辐射：`Ptrans=6808.095238095238`，`Pbrem=1.2476992796973099`，`Pcycl=7.818157448061494e-06`，`P_line=0.0`。在给定输入下，线辐射未体现，轫致辐射有非零贡献，回旋辐射很小。
- 成分设定：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。也就是这次结果对应“无氦灰、无杂质分数输入”的设定，辐射与功率平衡结论直接依赖这个成分前提。
- 约束与状态量：`tau_E=0.0015`，`tau_Bohm=0.0026514285714285715`，`tau_classical=1.798115523221014`，`ntau=1.8614293261800108e+18`，`beta=0.7777777777777777`，`nbar=1.1285248879554306e+21`。这些是当前点的重要状态描述，但本次 JSON 未提供相应外部边界判据，不能据此单独下边界结论。
- 几何相关：`Vp=1.2817698026646358`，`Sw=13.571680263507906`，`Sp=6.974335690969341`，`elongation=3.75`，`sep_model=superellipse`，`f_shape=0.85`。这些说明本次 0-D 结果绑定在特定几何输入上。
- 扫描轴信息：当前参数点是 `Ti=25.714285714285715`、`B_e=4.142857142857142`、`Te=1.0`；扫描轴也正是 `Ti` 和 `B_e`，因此后续若做复扫，最直接的就是围绕这两个量加密。

3. 风险与不确定性
- 最大的不确定性来自扫描证据不完整：虽然有 `best` 矩阵且不是全 0，但缺少 `x/y` 数组和 `nx/ny`，所以“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- `use_tauE=1.0` 意味着结果依赖固定约束时间假设；当前 `tauE=0.0015` 不是由本 JSON 中可见的约束标度自洽展开出来，因此对 `Pfus`、`Qfus`、`ignited` 的判断都应视为对该假设敏感。
- `valid=1.0` 不能外推成“扫描存在明确工作窗”；它只说明当前运行点有有效解。相反，扫描层面的判断仍然受限于缺失的网格坐标信息。
- 成分与辐射前提较理想化：`fHe=0.0`、`fimp=0.0`、`Zeff=1.0`、`P_line=0.0`，因此当前功率平衡并未覆盖非零氦灰或非零杂质分数下的变化。
- 本次 JSON 未提供 `q/q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`LH_ratio`、`geom_model`、`best_fraction` 等字段，因此相应的边界、阈值比较、几何复杂度和最佳区占比都不能展开判断。

4. 下一步建议
- 先补做一次完整的 `Ti-B_e` POPCON 导出，要求同时输出 `x`、`y`、`nx`、`ny` 和 `best`；重点监控 `best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 围绕当前点 `Ti=25.714285714285715`、`B_e=4.142857142857142` 做局部加密扫描，确认 `best` 标记是否连续出现；重点监控 `Pfus`、`Qfus`、`Ptrans`、`Pwall`、`beta`、`nbar`、`ntau`。
- 针对固定约束时间假设，做 `tauE` 敏感性扫描（保持 `use_tauE=1`，改变 `tauE` 取值）；重点监控 `valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Ptrans`，用来判断结论对 `tauE=0.0015` 的依赖程度。
- 做成分与辐射验证扫描，逐步引入非零 `fHe` 或 `fimp`；重点监控 `Zeff`、`Pbrem`、`P_line`、`Pfus`、`Qfus`、`Pwall`，检查当前“零氦灰/零杂质”前提是否显著影响结果。
