1. 核心结论
- 工况标识：`config=frc`（`config_label=场反位形 FRC`），`preset=C-2W`。
- 当前运行点：`valid=1.0`，`ignited=0.0`，`Pfus=1433.3039570098533`，`Qfus=0.11236977219071569`，`Pheat=12755.244840909956`，`Pwall=1045.4526280044015`。
- 运行点含义：该运行点在本次 0-D 求解中被判为有效（`valid=1`），但未达到点火（`ignited=0`）；同时 `Qfus` 数值较低，说明该点仍明显依赖外加加热，对应 `Pheat=12755.244840909956`。
- POPCON 扫描含义：扫描维度标记为 `xkey=Ti`、`ykey=B_e`；`best` 矩阵存在非零元素，说明在当前内部判据下，扫描网格中至少有一个点被标记为 `best`。这件事与“当前运行点 `valid=1`”不是同一结论，不能互相替代。
- 扫描有效性补充：`n_invalid=0` 表明已记录的扫描结果里没有被标记为无效的点，但这并不自动说明工作窗范围、形状或参数敏感性。

2. 关键指标解读
- 约束与输运：`use_tauE=1.0`，`tauE=0.0015`，输出中 `tau_E=0.0015`；因此本次结果依赖固定约束时间假设，而不是由某个约束时间标度律自动给出。JSON 中未提供 `H98`、`H_ITPA20`、`H_ISS04` 等指标，无法进一步比较标度裕量。
- 功率相关：除 `Pfus`、`Pheat`、`Pwall` 外，输出还给出 `Pn=1146.6431656078828`、`Ptrans=12952.380952380954`、`P_ei=99885.33174964577`、`Pbrem=89.52462908956808`、`Pcycl=5.0841405427794516e-05`、`P_line=0.0`。按这些字段看，本次案例中线辐射被设为 0，轫致辐射存在但数值远小于 `Pheat`，回旋辐射更小。
- 成分与辐射：`Zeff=1.0`，`fHe=0.0`，`fimp=0.0`，`Zimp=10`。这表示本组输入采用了非常干净的等离子体成分设定，且没有给出 He 灰和杂质分数；因此当前辐射结果主要对应 `Pbrem`，而不是杂质线辐射主导的情形。
- 边界与状态量：输出提供了 `beta=0.7777777777777777`，但未提供 `betaN`、`betaT`、`betap`、`q`、`q95`、`nbar_o_nGw`、`nbar_o_Sudo` 等字段。可直接引用的密度相关量包括 `nbar=9.55932977963493e+21`、`ne0=1.3515000470602525e+22`、`ni0=1.3515000470602525e+22`、`ntau=1.576750054903628e+19`。
- 几何与体量：输出给出 `Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`、`elongation=3.75`，输入包含 `f_shape=0.85`，分离面模型为 `sep_model=superellipse`。JSON 未提供 `geom_model` 字段，因此不能直接判断是否采用了简化几何模型；可确认的是，本次结果对这些给定几何参数敏感。

3. 风险与不确定性
- 扫描网格证据不足，不能判断工作窗形状或敏感性趋势。原因是 JSON 里没有提供 `nx`、`ny`，也没有提供扫描 `x/y` 数组；虽然有 `best` 矩阵，但无法把其映射回明确的 `Ti`/`B_e` 数值坐标，也不能可靠描述最佳区边界。
- 结果依赖固定约束时间假设。`use_tauE=1` 且 `tauE=0.0015`，因此 `valid`、`ignited`、`Pfus`、`Qfus` 等结论都建立在这个固定 `tau_E` 设定上；若改用其他 `tau_E` 设定或标度律，结果可能变化。
- 当前运行点“有效”不等于“扫描工作窗明确存在且范围清楚”。这里最多只能说：单个运行点 `valid=1`，且 `best` 矩阵并非全零；但由于扫描网格信息不完整，不能把这两点外推成清晰的参数窗口判断。
- 辐射与成分表征偏简化。当前输入中 `fHe=0`、`fimp=0`、`Zeff=1`、`P_line=0`，因此本次 0-D 结果没有覆盖 He 灰积累、非零杂质分数或线辐射增强下的变化。
- 几何模型信息不完整。虽然给出了 `Vp`、`Sw`、`Sp`、`elongation`、`f_shape` 等量，但未给出 `geom_model`，因此不能判断几何描述的复杂程度，也不能单凭这份 JSON 评价几何近似对结果的影响大小。

4. 下一步建议
- 开展一次可复核的 `Ti-B_e` 扫描导出补全：补充 `x` 数组、`y` 数组以及 `nx/ny`，并保留 `best`、`n_invalid`。重点监控 `best` 分布、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`，先把“当前点有效”与“扫描是否存在连续可用区域”明确分开。
- 在相同 `config/preset` 下做 `tauE` 敏感性扫描：围绕 `tauE=0.0015` 做多点扫描，或在工具支持时切换到 `tauE_scaling` 方案对比。重点监控 `tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`Pwall`，验证当前结论对固定约束时间假设的依赖程度。
- 做成分与辐射参数扫描：至少对 `fimp`、`Zimp`、`fHe` 做独立或联合扫描，并检查是否允许非零 `P_line` 情形。重点监控 `Zeff`、`Pbrem`、`P_line`、`Pcycl`、`Pfus`、`Qfus`、`Pwall`，确认当前“低辐射”结论是否仅由输入设定决定。
- 做几何参数一致性验证：围绕 `f_shape`、`r_s`、`l_s`、`r_w` 做几何敏感性扫描，并对比 `Vp`、`Sw`、`Sp`、`B_int`、`beta` 的响应。重点监控 `valid`、`Pfus`、`Qfus`、`Pwall` 与几何量联动，避免把单一几何设定下的结果误读为普遍结论。
