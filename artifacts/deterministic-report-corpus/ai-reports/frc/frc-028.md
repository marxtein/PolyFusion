# 1. 核心结论
`config=frc`（场反位形 FRC），`preset=C-2W`。当前运行点给出的核心结果是：`valid=1.0`，`ignited=0.0`，`Pfus=726.4758523160064`，`Qfus=0.05672145053723874`，`Pheat=12807.779868729922`，`Pwall=997.2424532751037`。

这表示该单点在当前 0-D 模型下被判为有效，但并未点火；同时 `Qfus` 数值较低，说明该点的聚变输出相对总加热输入不高。这里的“运行点有效”只针对当前这一个计算点，不等同于 POPCON 扫描已经证明存在明确工作窗。

约束方面，`use_tauE=1.0` 且 `tau_E=0.0015`，结果依赖固定约束时间假设。

POPCON 扫描方面，扫描轴是 `xkey=Ti`、`ykey=B_e`，`n_invalid=0`，且 `best` 矩阵并非全 0，说明当前判据下至少存在被标记为 `best` 的网格点。但由于缺少 `x/y` 数组以及 `nx/ny` 信息，扫描网格证据不足，不能判断工作窗形状或敏感性趋势。

# 2. 关键指标解读
先看当前运行点。功率相关结果中，`Pfus=726.4758523160064`，`Pheat=12807.779868729922`，`Qfus=0.05672145053723874`，`Pn=581.1806818528052`，`Ptrans=12952.38095238095`，`P_ei=12805.203916008146`。这些数值放在一起看，当前点更像是高加热/高输运平衡下的有效解，而不是接近点火的状态，这与 `ignited=0.0` 一致。

辐射与成分方面，`Pbrem=0.694071725863508`，`Pcycl=1.5086309150603825e-05`，`P_line=0.0`；同时 `Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。就这组输入而言，线辐射通道没有贡献，成分设定偏理想化。

约束与等离子体状态方面，`beta=0.7777777777777777`，`beta_null=1.0`，`nbar=8.417019931754025e+20`，`ne0=1.1900000414367002e+21`，`ni0=1.1900000414367002e+21`，`ntau=1.3883333816761503e+18`。JSON 未提供 `q/q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`H98/H_ITPA20/H_ISS04` 等字段，因此不能据此展开相应边界判读。

几何相关结果中，`Vp=1.2817698026646358`，`Sw=13.571680263507906`，`Sp=6.974335690969341`，`elongation=3.75`，`f_shape_calc=0.850000000000001`，`geom_weighted=0.0`。这些值说明当前解对几何量有明确依赖，但未提供 `geom_model`，因此无法直接判断几何模型是否做了进一步简化。

再看 POPCON 扫描。已知 `best` 矩阵存在非零元素，且 `n_invalid=0`，说明扫描点并未出现无效点统计，且至少有网格点满足当前 `best` 判据。但当前数据没有 `x`、`y` 数组，也没有 `nx/ny`，因此不能把这个结果解读成已经明确找到了可描述边界、面积占比或趋势的工作窗。

# 3. 风险与不确定性
第一，结果依赖固定约束时间假设。`use_tauE=1.0` 表明这次不是从约束标度自洽推出来的 `tau_E`，而是直接采用 `tau_E=0.0015`；因此对 `Pfus`、`Qfus`、`ignited` 的判断都对该假设敏感。

第二，扫描证据不完整。虽然有 `best` 矩阵，但缺少 `x/y` 数组和 `nx/ny`，所以扫描网格证据不足，不能判断工作窗形状或敏感性趋势，也不能给出最佳区占比。

第三，成分与辐射设定偏理想化。`fimp=0.0`、`fHe=0.0`、`Zeff=1.0`、`P_line=0.0` 使当前结果没有覆盖杂质线辐射和氦灰积累的影响；这会限制对辐射损失敏感性的判断。

第四，部分常见边界字段未提供。没有 `q/q95`、密度归一化指标、约束增益标度、`geom_model` 等信息，因此不能对边界裕量、几何简化程度或标度一致性做进一步解读。

# 4. 下一步建议
1. 重新导出一次 `Ti-B_e` POPCON 扫描，补齐 `x`、`y` 数组和 `nx/ny`，最好同时输出 `best_fraction`。需要监控的指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。

2. 围绕当前运行点做更细的局部扫描，至少覆盖 `Ti` 与 `B_e` 的邻域，而不是只保留单个 `best` 矩阵。需要监控：`Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pwall`、`beta`、`nbar`。

3. 对固定约束时间做敏感性验证：以当前 `tau_E=0.0015` 为中心做参数扫描，或在模型支持时补做一次 `tauE_scaling` 对照计算。需要监控：`tau_E`、`valid`、`ignited`、`Pfus`、`Qfus`、`Ptrans`、`tauC_eff`、`Pwall`。

4. 补做成分与辐射扫描，至少扫描 `fimp`、`fHe`，必要时联动 `Zeff` 或 `Zimp`。需要监控：`P_line`、`Pbrem`、`Pcycl`、`Pfus`、`Qfus`、`valid`、`Pwall`。
