1. 核心结论
- 本次报告对应 `config=frc`（`config_label=场反位形 FRC`），`preset=C-2W`。当前运行点结果为：`valid=1.0`、`ignited=0.0`、`Pfus=27875.038686323318`、`Qfus=0.5231204906861597`、`Pheat=53286.07688404742`、`Pwall=5980.181819387544`。
- 就当前运行点而言，这是一个在给定输入下“计算有效”的 0-D 解，但并未点火；`ignited=0.0` 与 `Qfus<1` 一致，说明不能把当前点解读为自持燃烧状态。
- POPCON 扫描与单一运行点需要分开看：扫描轴标记为 `xkey=Ti`、`ykey=B_e`，`best` 矩阵并非全 0，说明按当前判据至少有 1 个被标记的候选格点；这不等同于“当前运行点有效”。
- 同时，本 JSON 未给出 `x/y` 数组，且 `nx/ny` 缺失，因此“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。

2. 关键指标解读
- 运行点功率结构：`Pheat=53286.07688404742` 高于 `Pfus=27875.038686323318`，`Qfus=0.5231204906861597` 低于 1；输出还给出 `Pn=22300.030949058655`、`Ptrans=57120.0`，其中 `Ptrans` 高于 `Pheat`，与 `ignited=0.0` 一起表明当前点对外加加热/约束假设仍有依赖。
- 辐射与损失：`Pbrem=1741.083938996829`、`Pcycl=0.000682315251489332`、`P_line=0.0`。按本次输入，线辐射未体现出来，且回旋辐射数值很小；已提供的辐射项里，轫致辐射是主要非零项。
- 约束时间假设：`use_tauE=1.0`，输入 `tauE=0.0015`，输出 `tau_E=0.0015`。这意味着结果依赖固定约束时间假设，而不是基于本 JSON 中可见的约束时间标度比较。报告未给出 `H98`、`H_ITPA20`、`H_ISS04` 或 `tauE_scaling`，因此无法做标度律横向解读。
- 边界与等离子体状态：输出给出 `beta=0.7777777777777777`、`beta_null=1.0`，以及 `nbar=4.215664432819004e+22`、`ne0=5.960115207535713e+22`、`ni0=5.960115207535713e+22`。但本 JSON 未提供 `q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo`，因此不能判断与这些边界的相对余量。
- 成分与辐射设定：`Zeff=1.0`、`fHe=0.0`、`fimp=0.0`、`Zimp=10`。在这组输入下，灰分与杂质分数都取 0，因此当前结果更接近“理想洁净成分”假设下的计算点；这也与 `P_line=0.0` 相一致。
- 几何与形状：给出了 `sep_model=superellipse`、`elongation=3.75`、`f_shape_calc=0.85`、`Vp=1.2817698026646358`、`Sw=13.571680263507906`、`Sp=6.974335690969341`。这些是本次 0-D 结果使用的几何/形状信息；`geom_model` 字段未提供，因此不能额外判断几何模型层级。
- POPCON 扫描证据：`n_invalid=0`，说明扫描结果里没有被标记为无效的格点；`best` 矩阵是一个 8x8 矩阵，且仅看到 1 个被标记格点。由于缺少 `x/y` 数组和 `nx/ny`，只能确认“存在候选格点”，不能把它映射成具体 `Ti`、`B_e` 数值位置，也不能判断边界走向或趋势。

3. 风险与不确定性
- 最大的不确定性来自 `use_tauE=1.0`：当前结论直接依赖固定 `tauE=0.0015` 的假设，若约束时间改动，`Pfus`、`Qfus`、`ignited`、`Pwall` 都可能明显变化。
- 扫描证据不完整：虽然 `best` 矩阵里有标记点，但因为缺少 `x/y` 数组且 `nx/ny` 未给出，所以“扫描网格证据不足，不能判断工作窗形状或敏感性趋势”。
- `valid=1.0` 只说明当前点在模型里可计算，`n_invalid=0` 只说明扫描格点未被判为无效；这两者都不能替代对参数敏感性、边界余量或多约束一致性的判断。
- 成分假设偏理想化：`fHe=0.0`、`fimp=0.0`、`Zeff=1.0` 使当前结果缺少灰分与杂质敏感性信息，因此对辐射损失和性能退化的评估仍不充分。
- 关键边界量缺失：没有 `q/q95`、`nbar_o_nGw`、`nbar_o_Sudo` 以及约束增强因子输出，因此边界贴近程度与约束标度一致性都无法在本报告中完成闭环判断。

4. 下一步建议
- 1. 重新导出完整 POPCON 扫描网格：保留 `xkey=Ti`、`ykey=B_e`，同时输出 `x`、`y` 数组及 `nx/ny`。需要监控的指标：`best`、`n_invalid`、`valid`、`ignited`、`Pfus`、`Qfus`、`Pheat`、`Pwall`。
- 2. 围绕当前点对固定约束时间做参数扫描：在当前模型内对 `tauE` 进行上/下扰动扫描，检查结果对 `use_tauE=1` 假设的敏感性。需要监控的指标：`tau_E`、`Pfus`、`Qfus`、`ignited`、`Ptrans`、`Pheat`、`Pwall`。
- 3. 做成分与辐射敏感性扫描：从当前 `fHe=0.0`、`fimp=0.0` 出发，逐步扫描 `fHe`、`fimp`，必要时联动 `Zimp`。需要监控的指标：`Pbrem`、`P_line`、`Pcycl`、`Zeff`、`Pfus`、`Qfus`、`Pwall`、`valid`。
- 4. 补充边界相关输出或验证任务：在后续运行中增加 `q/q95`、`nbar_o_nGw` 或 `nbar_o_Sudo` 等字段的导出，再与当前 `beta`、`nbar` 同步检查。需要监控的指标：`beta`、`nbar`、`q/q95`、`nbar_o_nGw`、`nbar_o_Sudo`、`valid`、`ignited`。
- 5. 对当前扫描中被标记的候选格点做单点复算：在拿到完整 `x/y` 网格后，单独抽取 `best=1` 的格点复核其 `Pfus`、`Qfus`、`Pheat`、`Pwall`、辐射项和约束时间输出，确认该候选点不是由网格稀疏或固定假设偶然产生。
